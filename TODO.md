# TODO

跨 session 待辦清單。規則本身的變更歷史見 `common/auto-script-learning/CHANGELOG.md`／`RULES.md`；這裡只放「還沒做、下次要接著做」的項目。

## 🔥🔥 最優先：掃帶排程改用 Windows 工作排程器，`CronCreate` 撐不住（2026-08-05 實錯，使用者列為最高優先）

**症狀**：0805 排程掃帶**一直沒有順利觸發**，22:00 那輪到 22:10 都還沒跑。工作 agent 回報 cron（`69109260`）還在排程清單裡但沒被觸發。

### 根因：`CronCreate` 有三個結構性限制，今天至少中了兩個

工具說明白紙黑字寫著（不是猜測）：
> **Jobs only fire while the REPL is idle (not mid-query).**
> **Jobs live only in this Claude session — nothing is written to disk, and the job is gone when Claude exits.**
> Recurring tasks auto-expire after 7 days.

| # | 限制 | 0805 的實際傷害 |
|---|---|---|
| ① | **只在 REPL idle 時觸發，busy 就錯過且不補跑** | **主因**。實測每輪要跑 **20 分鐘左右**（16:30輪→16:47完成、18:00輪→18:23、19:00輪→19:22、20:00輪→20:23），加上人機對話，REPL 大部分時間都是 busy |
| ② | **session-only，關掉就消失** | 使用者當天關過一個 session 換 model。cron 清單是**各 session 各一份**，工作 agent 看到「還在清單裡」不代表真正該觸發的環境還在 |
| ③ | 7 天自動過期 | 尚未咬到，但長期必然 |

⚠️ **另一個隱性風險**：cron 觸發是把 prompt 丟進佇列，若剛好插在人機對話中間，執行順序會亂。

### 這件事 2026-07-31 就記過，當時裁示先不做

見下方「S2b 側錄轉譯摘要」節的「排程機制待評估」——當時是為側錄監看提的，**但對 S2 掃帶完全適用而且更嚴重**：側錄漏一次沒關係，**掃帶漏一輪就是素材空窗**。

- [x] **✅ 環境已查證（2026-08-05，實測非憑印象）——Task Scheduler 方案技術上成立**

  | 項目 | 結果 |
  |---|---|
  | CLI 路徑 | `C:\Users\User\.local\bin\claude.exe` |
  | 版本 | 2.1.222 |
  | **非互動執行** | ✅ `-p` / `--print`，**實測離開碼 0、耗時 11 秒**、輸出正確 |
  | 權限模式 | ✅ `--permission-mode`（含 `bypassPermissions`／`manual`） |
  | 指定 model | ✅ `--model` |
  | 追加系統提示 | ✅ `--append-system-prompt`（可用來塞「開工前重讀規則」） |
  | 輸出格式 | ✅ `--output-format`（含 `stream-json`，要解析執行結果時用） |

  指令雛形（**尚未實際建立排程**）：
  ```
  claude -p "<開工 prompt>" --permission-mode bypassPermissions --model <model> > 掃帶log_%date%.txt 2>&1
  ```

- [ ] **改用 Windows 工作排程器（Task Scheduler）呼叫 `claude` CLI**。它不管 REPL 忙不忙、不管 session 有沒有關、重開機也還在。
- [ ] **先查環境再提方案**：`claude` CLI 的實際路徑、能否非互動執行（`-p`／`--print` 之類）、怎麼把 prompt 帶進去、輸出往哪去、失敗如何得知。⚠️ **不要憑印象寫排程指令**，先實測跑得起來。
- [ ] **要處理「上一輪還沒跑完，下一輪就到了」**：實測每輪 20 分鐘、最短間隔 1 小時，目前還有餘裕，但補漏或素材暴增時可能撞上。需要簡單的互斥（例如鎖檔）或「跑不動就跳過並記錄」，**不要兩輪並行**——Playwright 只有一個 persistent profile，並行必互鎖（0803 空窗八小時的坑）。
- [ ] **cron 不是完全沒用**：它適合「REPL 閒著時順手做點事」（例如 NS 58 分鐘保活），不適合「準時執行 20 分鐘的長工作」。改用 Task Scheduler 之後，可評估保活是否仍留在 cron。
- [ ] 🔴 **順帶抓到的獨立 bug：`settings.json` 有一條 `Write(...)` 規則實際上沒生效**（2026-08-05 實測 `claude -p` 時，輸出前兩行就是這個警告）：
  ```
  Permission allow rule (.claude\settings.json):
  Write(G:/我的雲端硬碟/Claude共用/自動掃帶系統/**) is not matched by file permission
  checks — only Edit(path) rules are.
  Use Edit(G:/我的雲端硬碟/Claude共用/自動掃帶系統/**) instead
  ```
  **檔案權限檢查只認 `Edit(...)`**（`Edit` 規則涵蓋所有寫檔工具），所以那條 `Write(...)` 等於沒設——工作 agent 每次要寫掃帶系統資料夾可能都在跳權限提示，**這可能是它常卡住的原因之一**。修法：把 `Write(` 改成 `Edit(`。
  ⚠️ **只有使用者能改 `permissions`**（自我授權被擋）。**這件事獨立於排程，值得先修。**
- [ ] **動手前要先決定的三件事**：①**權限模式**——排程執行時沒人能回應提示，得用 `bypassPermissions` 或把 allow 規則設完整（安全取捨，使用者決定）②**互斥**（見上一條）③先修好上面那條 `Write(`→`Edit(`。
- 📌 **在改好之前**：排程輪次要靠使用者手動觸發，或接受會漏。**漏掉的輪次要事後補掃**（0805 RT 漏 36 則、NS 漏 5 則都是這樣補回來的）。

## ~~AP(SNTV) 直式／橫式重複主題：只收橫式~~ ✅ 規則已訂（2026-08-03）

同一體育主題若同時有**直式與橫式兩種版本**（不同素材代碼），**只收錄橫式版本，放棄直式版本**——電視新聞用橫式，直式（多半是社群／手機直式素材）不適用。規則已寫進 `common/13-S2-定時掃帶.md`「SNTV 例外」節（判準、配對依據、只有直式時仍收的例外）與 `common/13b-S2-定時掃帶-v2省token.md` §4。

判準是**標題／描述人工判斷**，不是機械欄位——AP 清單沒有結構化 Aspect Ratio 可讀，靠標題或縮圖看是否為直式拍攝。

- [ ] **待評估**：`s2_validate.py check` 要不要加「同標題關鍵字出現兩個 `AP5` 代碼」的軟性提醒——標題相似度比對容易誤判，且會破壞現有「`check` 0 命中＝可上線」的假設（多處腳本／流程拿這個當硬性通過條件）。**先不做**，等實測遇到幾次真的重複再評估，別為了想像中的頻率預先蓋機制。

## ~~資安：帳密外洩後續~~ ✅ 換密碼已處理，不用再提（2026-08-04 使用者確認）

**2026-08-04 使用者確認：外洩的那批帳密都已是舊密碼，已經換過，不用再追這件事。**以下保留原始記錄供查證，但「更換帳密」這項不再是待辦，之後回報 TODO 不必再列。

清除語料時發現**歐新社（EPA）與 TVBS 數位帳號的帳號＋密碼明文**被當頁尾樣板貼進 401 篇稿件，並隨語料進了版控（commit `b4b278d`）。

> 🔴 **2026-07-27 更新（B7 精讀時發現）：上次的清除只清掉一半，工作區至今仍有明文密碼。**
> `redact_secrets.py` 錨定的是「標籤＋值」的行（`帳號 …`／`密碼 …`），但**沒有標籤、單獨成行的密碼字串整批倖存**：
> `Tvbsnews@@451`、`Tvbs@451`、`Tvbs@@ruiguang451`、`X+3Ch8*gMuHN^L9c`。
> 實測波及：`raw/` 401 檔、`cleaned/` 401 檔、`draft_corpus.jsonl` 336 筆、`sample_B`～`sample_B7` 26 檔，全部在版控裡。
> 原本寫在這裡的「工作區已清乾淨（`ffda02f`）」**當時就不成立**，已刪除該敘述。

- [x] **補做第二輪清除（裸 token）**（2026-07-27 完成）——四個裸 token 已加進 gitignored 的 `.secret-tokens.txt`。發現腳本**本來就有** stage 3 裸 token 階段，第一輪失效的真正原因有兩個：①`.secret-tokens.txt` 裡沒有這四個密碼 ②`DIRS` 硬編到 `sample_B4`，B5–B7 三輪的樣本從來沒被掃過（現已改為 glob，未來輪次自動涵蓋）。清除 1164 個交付物、4760 行，複驗零殘留，31 行提到「帳號／密碼／分機」的新聞內文未誤刪。同時補上 `test_redact_secrets.py`（19 項，含當初漏掉的裸 token 案例）。
- [x] **✅ 更換帳密（2026-08-04 使用者確認已完成）**——外洩那批已是舊密碼，已換過，不再是待辦。
- [ ] **（可選、低優先）改寫 git 歷史徹底移除舊憑證**——需 `git filter-repo`，代價：全部 commit hash 改變、必須 force-push、Mac 那台的自動 pull hook 失效。密碼已換，舊憑證已失效，改不改寫歷史不影響安全性，純粹是「git 歷史留著洩漏過的字串好不好看」的問題——**建議與下方「`.git` 肥大清理」合併成同一次歷史改寫**，只付一次代價，但不急。
- [ ] **源頭防治**：這批帳密是 Notion 稿件範本的頁尾，日後再從 Notion 匯出語料仍會帶進來。要嘛請同事把範本裡的帳密拿掉，要嘛每次匯入後固定跑一次 `python style-corpus/redact_secrets.py`（需先備妥 gitignored 的 `.secret-tokens.txt`）。

## 自動寫稿 SOT：2026-07-31 裁定但未完成的兩項

來自 [`俄炸朝彈2200` 案例](common/auto-script-learning/cases/2026-07-31-俄炸朝彈2200.md)，使用者已裁定方向、但明確要求「這次先不要改」或需要另外建表。

- [ ] **建立「TVBS 常用譯名對照表」**（`P-035` 的完成條件）。優先序已定案：**TVBS 公司慣用譯名 ＞ 中央社慣用譯名**，表內沒有的才用中央社。目前規則是暫行版，以稿單用字與同 SLUG 既有稿件為準。建表時一併釐清一個張力：`2026-07-28-朝增兵俄2200` 案例記載「稿單譯名非最終權威」（使用者當場改掉稿單譯名），`P-035` 卻是稿單優先——暫時解讀為「使用者當下的明確指示 ＞ 稿單 ＞ 中央社」，建表時確認。
- [ ] **網路標併入完成稿、並容許全形**。使用者裁定「不需要另外開檔案放，可一起放在完成稿；『網路標』可容許全形」，但同時明確說**這次先不要改**。要動之前必須先想清楚：現行 [`scripts/validate_sot.py`](scripts/validate_sot.py) 對主標／次標一律套 17.5~18.5 全形字＋半形空格＋六個半形標點白名單，**沒有區分「上鏡標」與「網路標」**。若直接把網路標寫進完成稿，現有檢查會把網路標當上鏡標一起驗、整批 `[FAIL]`。需要的改法是讓驗證器認得網路標區塊（例如獨立的 `【網路標】` 段）並套一組寬鬆規則，同時補反例測試證明上鏡標的嚴格檢查沒有被一起放寬（`P-006` 精神）。人工稿的實際寫法可參考案例檔：網路標寫在完成稿裡、上方壓一行 24 格字數標尺 `１２３４５６７８９０…`，長版含 `！` 與全形空格，一次產 13 條。

## 寫稿「人味」風格研究（2026-07-24 啟動，**待使用者說「開始」再動手**）

語料庫已就緒：[`style-corpus/draft_corpus.jsonl`](style-corpus/draft_corpus.jsonl)，1701 篇許岱軒過去 TVBS 完成稿的 `Draft` 區塊，共約 481 萬字、平均每篇 2829 字、中位數 1916 字。

### 已選定的做法：A+B 搭配（2026-07-24 評估後裁定）

評估過三種規模（純統計 A／抽樣質性 B／全量窮舉 C）。全量 C 換算約 800 萬–1200 萬 token、2–5 小時以上，CP 值低，**不採用**。採 **A+B**：用統計數據當骨架、用抽樣質性讀當血肉。

> 🔍 **B1／B2 遺漏稽核（2026-07-27）**：B1、B2 跟 B3–B10 不同，**沒有逐條裁定紀錄**，是直接被合成進指南初版的，所以回頭重看了一次。**分兩次查**：第一次用關鍵詞 grep 只撈到 4 處；使用者追問「真的沒遺漏嗎」後改成**逐節逐條**走完 B1 八節與 B2 六項新發現，又找到 6 處，合計 **10 處**。教訓：稽核既有文件要逐條走，不要用關鍵詞抽查——抽查只找得到你已經想到的東西。
>
> 第一批 4 處：①對仗的內部規則「**動詞對動詞、名詞對名詞**」（第 3 節）②「一則新聞產 **5–10 條標題**、不同切角輪替」（第 7 節）③回扣型收尾的**畫面回扣**（稿內註記 `#回到<開場畫面>`，第 8 節）④快訊 BS 的**內部四段結構**「導言含最新數字→經過重述→官方回應→下一步」（第 0 節語體表）。
>
> 第二批 6 處：⑤**單位系統換算**與「參照物比較」是兩件事——**溫度華氏→攝氏、距離英里→公里**一律要做（第 11 節；這條與 14b 的可比單位參照物**不同**，14b 是比喻性參照，這是原文單位一個都不留）⑥人性註腳的**示範句式**「真的人沒事就好」「沒有習慣成自然這回事」，並註明註腳講普遍人性、寫成事件總結就變成收尾句（第 5 節）⑦懸置展望詞庫補「**還沒結束**」（第 8 節）⑧連線體的固定套語「**在為您重複一次**」，並註明重複是規範動作不是贅語（第 0 節）⑨檢核表補「**無改稿歷程殘留**」（每條標題只出現一次、不留劃掉的舊版標題、不留 `(21)(23)` 字數註記）⑩檢核表補「**無未完成斷句**」（缺句號的殘句是沒寫完，不是節奏技巧）。
>
> ❌ **已評估並否決，不要再重提**：B2 §四-5 建議「共同署名稿（如『TVBS 許岱軒 黃語暄』）語感可能混入他人筆跡，抽樣時權重應低」。實測語料有 63 篇多人署名稿、已讀 14 篇，八輪抽樣都沒處理過。**2026-07-27 使用者裁定：署名一律不處理，不是問題點**——電視新聞的共同署名多半是攝影／剪輯或小段落協作，文字仍出自本人。故不加 byline 過濾、不回溯已讀的 14 篇。日後任何一輪都不必再為這件事開工。

> 📐 **各輪報告的條數規矩（2026-07-27 立，因為 B7 與 B8 都算錯過）**：「入指南建議」一節**先列分級總表、再由表加總**，不要在正文另外估一個數。加總前先扣掉兩類東西：①驗證通過「維持原樣」與純方法論的條目（不進寫稿指南）②同一條內容因為要改兩處而重複出現的編號。若「依動筆處計」與「依不重複規則計」不同，兩個數字都寫出來並標明適用場合。

- [x] **A 步：純統計分析（2026-07-24 完成）**——[`style-corpus/analyze_style_A.py`](style-corpus/analyze_style_A.py) → [`style-corpus/analysis_A_report.md`](style-corpus/analysis_A_report.md)。已量出：乾淨斷行中位數 10.5 全形字、p90 19.5、僅 26.9% 超過 14 字上限；標題中位數 19.5（比現行 BAR 16.5–17.5 明顯長）；高頻連接詞 而/更/不過/這是/卻/甚至；語料屬「草稿散文語體」（44.7% 行帶句讀、成句），非最終字幕格式。
- [x] **B 步第十一輪（2026-07-27，Opus，改用篇幅軸）**——抽 6 篇最長未讀長稿（≥9000 字，客觀欄位、無關鍵詞），**6 篇全數精讀**（分兩次完成）→ [`style-corpus/analysis_B11.md`](style-corpus/analysis_B11.md)。9 條新招（U55–U63）。**本輪主產出是推翻兩項舊主張，兩者都是全語料統計、不依賴精讀篇數**：
  1. ❌ **B8 的「一檔多稿切分訊號＝署名計數」推翻，永久結案**。這條 to-do 被 B8→B9→B10 帶了三輪沒人驗。實測：全語料 1655/1701 篇署名恰好 1 次，**B7／B8 認定的 11 篇複合檔署名數全部是 1**，訊號召回 0/11。失敗原因是署名出現在整個檔案最末、不是每則稿一個。替代訊號也全測過（第 2 個 `網路標:`／`==BS==`／「國際最新消息」／`##` 分隔），最好的只有 5/11 召回。**結構層根本沒有標記可用，不是還沒找到對的訊號**。處置：接受 ~25% 是無法消除的雜訊底線、讀稿時人工辨識；要真解只能上 LLM 分類，不建議現在做。**此條不要再進任何一輪的建議清單。**
  2. ⚠️ **B2 的「長專題必落台灣錨點」過度推論，需降級**。抽出的 6 篇最長長稿**沒有任何一篇出現「台灣／臺灣」**（只有 3 篇有台幣換算，而台幣屬第 11 節單位在地化、每篇都該做，不能充當台灣錨點）；全池 38 篇未讀長稿有 **16 篇（42%）連台幣都沒有**。B2 的長專題樣本只有 10 篇且本來就是有台灣角度的題目，把樣本特性當成了規則。**待改**：第 9 節第 6 條由「必答題」降為「優先項」——有自然連結一定要寫、沒有不要硬掰。與 B7-M4（外國選舉稿對台立場前置）不衝突，那是選舉稿的硬規則。
  3. ⚠️ **B2 的「四層推軌」也不成立**（第三項推翻）。B2 說長專題「開場永遠是一個具體現場、禁止綜觀全局式開場」，但 6 篇沒有一篇是「單一具體現場」——0514-1 開在人民大會堂排排站的官員、0505-2 開在「說好的停火呢？」的設問、0703-2 開在爆炸 NS。**長專題的開場是「最強畫面」不是「最小現場」**。與台灣錨點一樣，是 B2 從 10 篇樣本過度推論。
  ⏳ **9 條新招與兩項規則修正（台灣錨點降級、四層推軌改寫）尚未裁定入指南**。
- [x] **B 步第十輪（2026-07-27 完成，Opus，開新子類）——子類抽樣法在此告終**——開了六個從沒碰過的子類（農業糧食／時尚服飾／考古文史／美食旅遊／寵物動物／競賽選拔），全部未讀語料掃過，**只有 2 篇進得了高段**，四個子類依 B9 規格判定挖不動、直接放棄（考古文史全語料 0 篇、時尚服飾 1 篇）→ [`style-corpus/analysis_B10.md`](style-corpus/analysis_B10.md)。4 條新招（U51–U54）。**這輪的價值在診斷不在招式**：剩下 984 篇未讀可用稿裡，**戰爭＋政治佔 59.5%**（346＋239），而這兩類已經讀了 184 篇；各題材已讀率全部落在 20–42%，**沒有任何題材被抽乾**。結論是「不是語料挖完了，是**找沒讀過的小眾子類這個方法沒有小眾可找了**」。B9 訂的「不足額寧可少讀不要往下補」在本輪第一次真正生效——沒有它我會很自然地把美食旅遊那 6 篇低段拉來湊數。**誠實但書**：本輪 2 篇高段有 1 篇是半（NBA 稿），失手原因不在排序器而在我的關鍵詞表把「冠軍賽／奪冠」放進「競賽選拔」——那正是體育稿的核心詞彙，而體育是 B6 涵蓋過的大題材；**檢查關鍵詞時要連「已涵蓋的大題材」一起排除**。年代軸實測走不通（`date_prefix` 只有 MMDD 無年份）。**已決定（2026-07-27）：B9＋B10 共 11 條全收**——U50 諧音改字擴及政策批判稿就地改在第 6 節（補三項條件：原名須為官方自取的宣傳性名稱／改一字可還原／改後的字要有事實撐）；其餘 10 條新招**合併寫入單一 14i 節「B9–B10 增補」**（兩輪各 6、4 條，分兩節太碎）。無新題材禁忌。
- [x] **B 步第九輪（2026-07-27 完成，Opus，重抽三子類＋排序器假設驗證）**——16 篇分層樣本（高段 10＋低段 6）→ [`style-corpus/analysis_B9.md`](style-corpus/analysis_B9.md)。7 條新招（U44–U50）。**本輪真正的產出是方法論定案**：B8 的「命中數當排序器」主張經過**預先登記的可證偽測試**（假設寫在 `sample_for_B9.py` 的 `HYPOTHESIS` 常數裡、讀任何一篇之前就固定；抽樣刻意分層而非只取前段），三條假設全部通過且是壓倒性的——**高段（hits ≥10）10／10 ＝ 100%，低段（hits <10）0／6 ＝ 0%，差距 100 個百分點**。全部 3 篇誤標與 3 篇半無一例外落在低段。**收緊關鍵詞與排序是兩件事**：收緊把池子從 148 篇縮到 35 篇（必要的清理），但沒有消滅誤標、只是把誤標壓到尾巴；真正有效的那一刀是排序。**B10 抽樣規格已定案**（見報告 §1-4）：入池 ≥3、依命中數排序、**只讀 hits ≥10、不足額時寧可少讀不要往下補**。教育校園整池 6 篇無一進高段，正確處置是承認這個子類挖不動、不再單獨開輪次。**已決定（2026-07-27）：與 B10 合併裁定全收**，見 B10 條目。
- [x] **B 步第八輪（2026-07-27 完成，Opus，抽乾法庭＋勞資）**——25 篇（法庭審判 15＋勞資爭議 10，兩池全數讀完）→ [`style-corpus/analysis_B8.md`](style-corpus/analysis_B8.md)。15 條新招（U29–U43）＋5 條修正（M8–M12）＋1 組禁忌（G5）。**方法論最重要的一條**：關鍵詞命中數當**過濾器**弱、當**排序器**強——命中 ≥10 次的 7 篇 **100% 命中**，<10 次的 18 篇只有 44%。B7 之所以命中率漂亮（法庭 5/5），正是因為誤打誤撞取了密度最高的前 5 篇。B9 應改為「入池 ≥3、依命中數排序、只讀前段」。專屬動作詞原則再次成立（誤標率 25%→**12%**）。另：一檔多稿比例升到 **28%**（法庭快訊特別嚴重），候選切分訊號從 `網路標:` 改為 `TVBS 許岱軒` 署名計數（未實測）。發現一枚體例異常標本（`0602-4`，粗體 SB＋帶時間碼的斜體場景註記＋句號前空白 16 處，斜體訊號全語料 1/1701），**依 B6 撤銷第五語體的同一標準，只登記標本、不升格為「形態四」、不加偵測器**。最強新招：法庭素描當唯一視覺（U29）、用最不可信的來源反證（U30，「連北韓也罵尹錫悅說謊，這次北韓說對了」）、台灣參照物換算推廣到任何可比單位（U35）。**已決定（2026-07-27）：全收，唯 U39 改存檔**——實際寫進指南 **16 條新內容 ＋ 2 處既有規則改寫**。新增 **14h 節**（法庭 6／勞資民生 5／結構通用 3，共 14 條）；A 級改寫就地改在 **14b**（面積換算 → **任何可比單位**，含速度／佔比／單價）與 **14g**（出庭稿整理成**視覺三型**：有畫面→外觀＋沉默、只有素描→須明講「審理沒有轉播」、有戒護→規格白描）；U40 諺語翻轉補標題版寫進第 1 節；第 15 節新增 **G5 法庭家屬說情**（說情 SB 要放、OS 不評論說情內容）；檢核表加三項。**U39 情色雙關經裁定存檔不入指南**——四項使用條件都要靠寫稿者判斷「什麼叫符合條件」，誤判一次（把性醜聞受害者寫進黃色雙關）的代價遠高於少用一招，原句與條件留在 B8 報告 §2。
  > 條數在此處誤植過兩次（20 → 18 → 正確 17 條新內容＋2 處改寫）：20 是直接相加沒扣不進指南的 M8／M12，18 是漏了 M9（標題掛「驗證通過」但內文結論是「建議在 14g 補變體」，其實是新內容）。詳見 B8 報告 §6-2。
- [x] **B 步第七輪（2026-07-27 完成，Opus，未讀子類抽樣）**——40 篇（8 個從沒讀過的子類 × 5）→ [`style-corpus/analysis_B7.md`](style-corpus/analysis_B7.md)。28 條新招（U1–U28）＋7 條既有規則修正（M1–M7）＋4 組題材禁忌（G1–G4）。**三項硬修正**：①比喻死亡開關再切一刀，判準改為**「意外奪走／壽終儀典」**——訃聞與國葬稿的比喻是**開**的（鳥山明「生涯就像他筆下的悟空」、女王「靈柩如皇冠鑽石般耀眼」），只有意外／暴力奪走的死亡才關；②法庭稿吐槽開關看**案件有無死者**（川普性侵民事案「臉很腫」「過街老鼠」全開，柯克命案 OS 零吐槽、比喻全外包）；③外國選舉稿的**對台立場必須放在第一次提到當選人的那一句裡**（台灣錨點升級為位置硬規則）。最反直覺的發現是 **G3：宗教場所受襲稿的價值序是「古蹟＞人命」**（耶路撒冷稿零傷亡描述、奧德薩稿 2 死只用一句帶過）。**方法論**：B6 §8 三張處方全部實作——AI 產出前置偵測**完全成立**（攔截 4 篇，四個已知陽性回測全中、286 篇已讀零誤報）；題材標籤前驗**有效但不夠**（誤標率 43.8%→25%，但嚴格命中率降到 52.5%）。關鍵發現是**專屬「動作」詞遠比場景「名詞」準**：法庭／選舉／宗教誤標 0%，太空／教育／移民 40–60%。另發現**一檔多稿佔 12.5%**，是誤標的第二個來源（唯一測過的切分訊號召回率只有 1/5，尚未找到可靠訊號）。**⚠️ 精讀時發現語料裡的裸 token 密碼從未被清除，見上方資安節。** **已決定（2026-07-27）：38 條全數入指南**（U1–U28 招式、M1–M6 修正、G1–G4 禁忌，含原本列為存檔的 U12／U21／U27）——A 級四項硬修正就地改在第 1／4／6／9 節，「題材語彙開場」升格為第 1 節通用變體，其餘新招寫入 **14g 節**，第 15 節新增 4 組題材禁忌，第 11 節新增三個轉折標記，檢核表同步更新。M7 一檔多稿寫入 `extract_corpus.py` 維護註記。
- [x] **B 步第六輪（2026-07-26 完成，Opus，改用題材覆蓋抽樣）**——43 篇補抽低覆蓋題材 → [`style-corpus/analysis_B6.md`](style-corpus/analysis_B6.md)。23 條題材專屬招（T1–T23）＋14 條既有招跨題材檢驗（C1–C14）＋7 組題材禁忌（F1–F7）。**四項硬修正**：①比喻死亡開關由**全篇級改段落級**，並訂「隱喻箭頭指向加害者可存活、指向受害者一律禁止」；②稿頭四型補為五型（觀感驚嘆前置型）；③台語配額由「配額模型」改為**「觸發＋用途」模型**，取消戰爭稿絕對禁令（新增體恤弱勢用法）；④**第五語體撤銷**——降為個案存檔，標記語法拆成「短影音強調標記」附加交付件，語體清單改回四分法。**⚠️ 抽樣方法檢討**：題材標籤錯誤率偏高（醫療健康 6 篇僅 1 篇正確），有效樣本 43→38 篇；另發現**第三種 AI/流程產出形態**（CNN 自動寫稿的 BAR 分段成品，佔掉環境生態配額一半）。**已決定（2026-07-27）**：31 條全數入指南——A 級四項硬修正就地改（第 1／4／5／6 節：稿頭補第五型、比喻開關改段落級、情緒外包對象對照表、台語改觸發模型）；其餘新招寫入 **14f 節**；**新增第 15 節「題材禁忌檢核」**（F1–F7 七組）；**第五語體正式撤銷**（語體表改回四分法，標記語法降為第 16b 節附加交付件）；第三種 AI 產出形態偵測寫入維護註記；檢核表同步更新。4 條「太個案」留 B6 報告 §7-2。
- [x] **B 步第五輪（2026-07-26 完成，改由 Opus 執行，篩選器 v2）**——44 篇（40 異常分前段＋4 篇 ⚑ 旗標）→ [`style-corpus/analysis_B5.md`](style-corpus/analysis_B5.md)。12 條新招（P1–P12）＋24 條既有招變體補證（V1–V24）。**三項重要修正：**①**推翻 B4「第五語體」判定**——那 4 篇 ⚑ 稿件不是影音變色強調體，而是給 AI 生圖工具的「AICG 資訊圖卡規格書」（標籤式 DSL、獨立區塊、不影響 OS 文體）；B4-N1 標本數仍是 2、維持暫定。②**推翻 B4「ellipsis 是最佳訊號」**——B4 樣本偏娛樂稿故成立，B5 偏硬新聞就翻車（刪節號多來自英文 in/out cue 行）。③**指出命中率指標本身有問題**：v2 帳面 81.8%／63.6%（優於 v1 的 70%／50%），但逐篇追查後只有 **4/44 是「訊號真的指向該篇的招式」**，其餘是「稿好但訊號打在別處」——真實訊號精準度約 9%，並未優於 v1。**已決定（2026-07-26）**：24 條建議全數入指南——C 級 2 條就地改既有規則（吐槽 OS 密度分級、台語配額分級）、白話解釋判準改為「台灣觀眾熟不熟」、台灣文化嫁接升級為取材優先序、SB 原話劇場加台灣語氣詞；其餘新招寫入 `common/10` 新增 **14e 節**（素材三讀／政治外交／災難人道／開場結構／標題用字五組）；**AICG 資訊圖卡規格書另立第 15 節**（含無臉插畫倫理硬規則），並修正 B1/B2「不要學」清單第 2 項為「不可混排但可產出」。8 條「太個案」**暫存 B5 報告 §6-2，待使用者日後決定**。
- [x] **B 步第四輪（2026-07-25 完成，Fable，異常側寫篩選精讀）**——全量掃描 1375 篇未讀稿的風格異常訊號 → 前 40 名精讀 → [`style-corpus/analysis_B4_fable.md`](style-corpus/analysis_B4_fable.md)。真陽性 28/40（嚴格 20/40），12 條全新招式（N1–N12）＋8 項既有招補證。**重大發現：第五種語體「影音變色強調體」**（社群短影音改寫規格，僅 2 標本待補齊）。篩選器評價：值得再用但需修訂（ellipsis 最佳訊號；redup/onoma 要加專名白名單）。語料衛生：**已制度化（2026-07-25）**——`tag_unread.py` 產生 `quality` 標記（ok 1632／stub 63／practice 3／ai_assisted 3），抽樣鐵則改為「`unread` 且 `quality=ok`」（可用池 1473 篇），`screen_for_B4.py` 已內建過濾。**已決定（2026-07-25）**：10 條建議項全數入指南（`common/10` 新增 14d 節＋語體表加第五語體）；影音體全量掃描與篩選器修訂再跑**暫不做**（篩選器修訂建議保留在 B4 報告第四節，日後要再挖時可用）；5 條存檔項不再挑選、維持存檔。
- [x] **B 步第三輪（2026-07-25 完成，Fable，獵稀有招式）**——60 篇未讀池（50 一般＋2 超長＋8 短稿）→ [`style-corpus/analysis_B3_fable.md`](style-corpus/analysis_B3_fable.md)。16 組主招＋5 題材特化小招：全篇延伸隱喻（「別人賞煙火」本尊出土，可完整模板化）、場景接力雙關、超長稿「主題辭典＋群像輪唱」（與 B2 長專題是兩種動物）、短稿壓縮公式（金句從逐字稿挖不自己造）等。附兩級增補建議（10 條值得入指南／8 條太個案僅存檔）。⚠️ 抽樣警訊：2026 語料混入 AI 輔助產出稿（0622-2 留有產稿模板痕跡），萃取人味時應降權（已記入 style-corpus README 與指南維護註記）。**已決定（2026-07-25）**：10 條建議項全數入指南；8 條個案由使用者親選 6 條「其實是常用招」一併升級（諧音替字、辣個男人、迷因指標、判斷前置稿頭、文學典故盲盒、拆字頓挫排版），反向成全反諷留提醒、自鑄詞維持存檔——全部寫入 `common/10` 第 14 節「稀有招式庫」。
- [x] **B 步第二輪（2026-07-24 完成，Fable）**——再讀 50 篇（40 一般＋10 長篇專題，排除第一輪）→ [`style-corpus/analysis_B2_fable.md`](style-corpus/analysis_B2_fable.md)。B1 九組結論 7 成立 2 修正（比喻開關判準改為「有無死亡悲劇」而非題材類別；「不用疑問句」限縮於稿頭層級）。新發現：設問引擎（正文每篇 1–3 個自問自答）、長篇專題六條模板（問題清單骨架/四層推軌/專家SB三連發等）、雙關隱藏簽名（諧音改字+物件雙關回扣收尾）。新增 2 點規則張力待裁定（專家 SB 連發 vs SOT 模板、問號不可被 14 字斷行切開）。
- [x] **B 步：抽樣質性分析（2026-07-24 完成，由 Fable 模型執行）**——[`style-corpus/sample_for_B.py`](style-corpus/sample_for_B.py) 抽 50 篇（`sample_B/` 5 批）→ Fable 產出 [`style-corpus/analysis_B_fable.md`](style-corpus/analysis_B_fable.md)。三大核心發現：①情緒外包紀律（情緒由 SB 承載，OS 只做動作白描＋一句 15–25 字人性註腳）；②比喻有題材開關（戲謔比喻只開在武器/體育/政治攻防稿，人道災難稿比喻歸零）；③收尾即簽名（末句固定為不含新事實的 12–30 字判斷/展望句）。含 8 層面模式+原句證據+可操作指引、「不要學的東西」、「與現行規則的張力」（4 點，待使用者裁定）、一頁式生成檢核表。
- [x] **產出（2026-07-24 完成）**：[`common/10-寫稿風格指南.md`](common/10-寫稿風格指南.md)——合成 A+B1+B2 為 13 節可操作指南（語體判定/稿頭四型/敘事節奏/比喻死亡開關/情緒外包/簽名層額度/標題/收尾/長篇專題/內幕改寫/用字/檢核表/待裁定張力）。已接進寫稿流程：`cnn/01` 與 `common/06` 的「開始前必讀」各加「寫稿階段另讀 10」指引，定位為風格參照層、格式規則優先。
- [x] **6 點規則張力已裁定（2026-07-24，逐點問答）**：①先成句後切行（14字上限不變，只改產生方式）②BAR雙軌並行（長版18–22不進完成文稿本體，驗證器只驗壓縮版）③鉤子優先明文化（A型可到180字接受WARN，絕不砍鉤子）④驚嘆號留指南軟約束（不進驗證器）⑤長專題另立模板（明確下令才套用，豁免SB連發與120秒；common/06 新增專節）⑥設問句指南層斷行保護（≤14字整句一行，不動驗證器）。全部落實於 `common/10` 第13節裁定表＋`cnn/01`＋`common/06`。
- [x] **語料個資／憑證清除（2026-07-25 完成，commit `ffda02f`）**——實際規模比 B2 回報嚴重：不只教授分機，而是**歐新社（EPA）與 TVBS 數位帳號的帳密明文**被當頁尾樣板貼進 401 篇稿件。已用 [`style-corpus/redact_secrets.py`](style-corpus/redact_secrets.py) 兩階段清除（①錨定「標籤＋值」行 ②嵌在 markdown 連結內的帳號信箱／裸帳號 token），範圍涵蓋 `raw/` 401 檔、`cleaned/` 401 檔、`sample_B`～`B4` 14 檔、`draft_corpus.jsonl` 336 筆，共 819 檔，值改為 `[已移除]`、標籤保留可稽核。11 項單元測試確認新聞內文提到「帳號／密碼」者零誤刪，公開網址與外部信箱保留。機密字串清單移至 gitignored 的 `style-corpus/.secret-tokens.txt`，腳本本身不含真實憑證。
- [ ] **實戰驗證迴圈（進行中）**：首篇實驗已完成（2026-07-24）——`魚群暴斃1600` 依風格指南改寫另存 `完成文稿-風格改寫.txt`＋`網路標.txt`（BAR長版，裁定②雙軌）於雲端 `CTV自動寫稿測試\魚群暴斃1600\`，驗證 `--mode ctv` 全過；套用手法：先成句後切行、設問×2、對照句簽名、SB前後橋、比喻半開、21字判斷句收尾、鉤子優先稿頭。**待使用者改稿回饋**→走 auto-script-learning 校稿機制比對，才算完成第一輪驗證。原完成文稿未動（P-010）。
- [ ] `Note`（CG 文字稿）欄位目前只原樣保留在 `draft_corpus.jsonl` 裡，尚未使用；未來若要做 CG 文字風格研究可另外處理。

以上不影響現有字數/格式規則（`P-001`~`P-021`），純粹是額外的風格參照層，見 [`style-corpus/README.md`](style-corpus/README.md)。**使用者明確要求：等他說「開始」再動手，現在只記錄不執行。**

## `.git` 肥大清理（2026-07-24 規劃，暫緩執行）

> 💡 **2026-07-25 註**：上方資安項的「改寫歷史移除舊憑證」與本節都需要 `git filter-repo` 改寫全部歷史。真的要動手時**兩件事一起做**（同一次 filter-repo 同時 `--invert-paths` 剔除肥大路徑、並用 `--replace-text` 抹掉憑證字串），只承受一次 hash 變更與 force-push 的代價。

- [ ] 用 `git filter-repo` 從全部歷史（所有分支含 `agent/rules-v2-sandbox`）剔除三個已棄用的舊路徑：`.tmp-sot/`（141MB whisper 模型 `ggml-base.en.bin`）、`.tmp-yt-dlp/`（83MB `ffmpeg-win-x86_64-v7.1.exe` 及整包 vendored yt-dlp）、`downloads/`（幾支測試下載的 mp4）。三者皆已被 `.gitignore` 排除、現在的 tracked 樹裡完全不存在，純粹是歷史包袱。
- [ ] 指令：`git filter-repo --path .tmp-sot/ --path .tmp-yt-dlp/ --path downloads/ --invert-paths --force`（`git-filter-repo` 已裝好，2026-07-24 用 `pip install git-filter-repo`）。
- [ ] 執行前已做好完整 mirror 備份：`E:\GitHub\TVBS-AIHunter-backup-20260724.git`（198MB，含所有 refs，改壞可從這裡復原）。
- [ ] **會改寫全部 commit hash**，之後必須 force-push 才能同步回 GitHub；GitHub 上已合併完的舊分支（`agent/add-bite-and-batch-rules` 等）建議一併刪除，否則會繼續留著舊肥大 blob 抵銷清理效果。
- [ ] force-push 後，Mac 那台 clone 的 `git pull --ff-only` 自動化 hook 會失效，需要在 Mac 上重新 clone 或手動 reset 到新歷史。
- [ ] 使用者 2026-07-24 裁決：先不動手，列入 TODO 待之後再決定執行時機。

## AP／RT／CNN 網站定時盯盤試跑（2026-07-28 提出）→ **已升格為 S2，最優先**

> 🔴 **2026-07-30 使用者定調**：這就是 [`common/12-子系統地圖.md`](common/12-子系統地圖.md) 的 **S2**，且是**打通整條主線的阻塞點**——Dashboard 沒建之前，S2 定時掃帶產出的純文字清單（「晚班交接」格式）要**暫代資料庫當 S3 庫存層**，AI 再從庫存整理出 S4 rundown 稿單、分派給記者，後面編號→下載→寫稿才跑得動。
> 連帶：**S2 的輸出格式＝晚班交接格式**。**2026-07-30 已處理**：三層骨架（大分類→中主題→小分題，素材行掛小分題底下）定案、中主題確認為**要實際寫出來的一行**（範例裡缺的是人工省略瑕疵）、README 的「兩層」與「①②③結構完全不同」錯誤描述已訂正、分隔符確認為 `======{名稱}======`。**仍未定**：中主題的分類方式（暫定 AI 判斷，見下節）、彙整時歸位的實作。


使用者問：如果要 AI 在一段時間內（例如 4 小時）持續監控外電網站，有新素材進來就摘要彙整進素材清單，token 會不會炸？初步討論結論（原本只針對 Reuters Connect，2026-07-28 追問後確認 **AP／CNN 一併適用，且實測上反而更穩定**）：

- **會比一次性查詢貴，但做法對的話可以壓低**。最貴的是「截圖盯畫面」（螢幕截圖本身很吃 token）與「每次都展開全部素材寫完整摘要」；改成**純文字比對**（`get_page_text` 抓清單、跟上次記錄的素材編號集合做差集，只有真的出現新素材才展開該則詳情摘要）可以省下大部分空轉輪詢的成本。
- 建議輪詢間隔拉長到 **20–30 分鐘**一次而非分鐘級盯盤——三邊更新頻率都不到分秒必爭，即時性犧牲不大，換到的 token 節省很明顯。⚠️ **2026-07-30 訂正：已被更省的設計取代**——不是固定分鐘級輪詢，改成 [`common/13`](common/13-S2-定時掃帶.md) 定案的 **5 個檢查點**（16:00／18:00／20:00／22:00／23:00）＋批次分立、增量整併，估算成本比密集輪詢再省約 3 成。
- 機制上可以用 `ScheduleWakeup`（動態排程喚醒）搭配一份「已見過的素材編號清單」存檔比對，而不是掛著單一長對話一直輪詢。
- **三邊實測穩定度不同**（2026-07-28 當次操作觀察）：AP 搜尋結果頁與 CNN Newsource 首頁「New Results」清單都是乾淨純文字、`get_page_text` 一次就穩定拿到；**RT（Reuters Connect）的 SPA 常卡在內容沒更新、抓到舊快取**，好幾次得靠截圖救回來，token成本會比另外兩個高、可靠度也較低。CNN 若要用分類篩選（如 Russia-Ukraine）而非首頁清單，勾選互動偶爾要重試。

- [ ] **待處理**：實際抓一輪跑跑看（AP／RT／CNN 都測），量出「純文字輪詢空轉一次」與「展開一則新素材寫摘要」的實際 token 花費，再決定要不要真的排 4 小時的三邊盯盤。~~或要不要把這個機制寫進 `common/11` 的「輸入模式」變成第四種模式~~ ⚠️ **2026-07-30 訂正：不掛成 `common/11` 的第四種輸入模式**——A／B／C 的共同點是**使用者主動提供**（給清單或給主題），而定時監控產出的是 **AI 自己生成的整理清單**（庫存）。定時監控是獨立子系統 **S2**（規格見 [`common/13`](common/13-S2-定時掃帶.md)），產出 **S3 庫存**。（`common/11` 也不是 S4 決策層，而是 **S4c 補充素材搜集**；真正的 S4 尚未建構——見下方「S4 rundown 稿單」一節。`common/11` 已把「先查既有庫存」訂為最優先取材路徑。）RT 的可靠度問題（SPA 卡快取）需一併評估是否要繞過或接受較高成本。**進行中，邊做實驗邊建構流程，還沒定案。三邊（RT/AP/CNN）都已各測過一輪最新N則摘要，見下方三輪紀錄；下一步是實際排一段時間的盯盤、量真實輪詢token成本，再決定要不要寫進 `common/11` 正式流程。**

> 🔍 **第一輪實測（2026-07-28，只測 RT）**：`https://www.reutersconnect.com/all?media-types=vid`＋Newest First，測「摘要最新 7 則」。
> - **清單頁 `get_page_text` 只抓到單一卡片，抓不到整份清單**——跟上方「RT 常卡舊快取」是同一族群的毛病，清單只能靠**截圖讀**（本輪用了 3 張截圖：初次進頁 1 張＋往下捲 1 張＋回頂再進第一則前 1 張，共看到約 9 筆項目才湊到 7 筆候選）。
> - **每則詳情頁改用右上角「‹ ›」項目導覽**（不用每次「Back to results」回清單重點一次），順序跟清單一致，換頁後 `screenshot`＋`get_page_text` 各一次就能一次拿到完整 STORY／shotlist／Restrictions／Details，不用捲動。單則成本＝**1 截圖＋1 次頁面文字擷取**，7 則共 7 組。
> - **並非每則「最新」都已有完整文稿**：這輪最新 7 則裡有 2 則（`EUROPE-WEATHER/FRANCE-FIRECLOUD`、`ASIA-WEATHER/INDIA-UPDATE`）標示「SHOTLIST ONLY / TEMPLATE ONLY, COMPLETE SCRIPT TO FOLLOW」，稿子晚點才會補上——**盯盤機制要處理「先收到 shotlist、稿子隨後才到」這種延遲到稿的情況**，不能假設清單上出現就代表可以馬上摘要，可能需要記錄「已見過但文稿未到」的狀態，之後補跑一次確認是否補齊。
> - 尚未測 AP／CNN 這一輪；也還沒實際排過 4 小時盯盤，仍待後續實驗才能給出「真的要不要排」的建議。

> 🔍 **第二輪實測（2026-07-28，測 AP）**：`https://newsroom.ap.org/home`（Latest／Video），測「摘要最新 5 則」。
> - **清單頁本身可以正常截圖讀取**（不用像 RT 一路靠截圖逐段拼），且**這輪抓到的 5 則全部已有完整 STORYLINE**——跟 RT 那輪 2/7 卡在「稿未到」不同，AP 的「Newsroom Ready」項目看起來是稿齊了才上架，不會像 RT 先放 shotlist 佔位。
> - **清單是真即時更新，會被插隊**：抓到第 4 則中途，清單頂端被新進一則高球稿擠進來，原本 5 則全部往下推一位——盯盤邏輯不能只看「頂端是不是變了」，要用素材 ID／編號集合做差集，不能假設新項目只會「加在最上面而已」。
> - 單則成本流程＝點卡片標題開 modal（**不能只點一次就假設成功，偶爾點到舊 modal 沒真的換頁，需截圖確認標題對不對**）→ 點「Open in a new tab」開真詳情頁 → 該分頁 `get_page_text` 一次拿全 Shotlist／STORYLINE／Metadata／Restrictions／「More like this」相關清單。彈窗 **Close 有時點一次沒關乾淨**（點了又立刻重開同一則），需視情況多點一次確認。
> - 5 則裡有 **2 則是 SNTV（非 AP 原生）供稿**（`Provided by: SNTV`，頁尾註明「This content is not produced or verified by the Associated Press」），其中 1 則還是舊畫面剪輯的「FILE」重發稿——跟既有規則的「非通訊社正式供稿要標註」一致，AP 站內清單並不會特別把這類內容跟原生內容分開排列，得逐則看 Provided by 欄位才能分辨。
> - ✅ **規則已改（2026-07-31）**：**SNTV 可收**，備註必標 `(SNTV)`；不要整批排除。見 `common/13`／`common/05`／`ap/01`。
> - ⚠️ **2026-07-31 晚再修**：SNTV **必為體育且編號 `AP5…`（非 `AP4`）**。0731 曾誤把 `AP4675927`（ASSOCIATED PRESS／中東）標 SNTV——根因是列表扁平文字把**下一則** `5466549` 的 `SNTV` 徽章黏在 `4675927` 後面；結構化 parse 當時其實是 `sntv:false`，寫庫存時未守 ID 規則。規則與 0731 交接已改。
> - 尚未測 CNN Newsource 這一輪；AP／RT 兩輪都還沒實際排過 4 小時盯盤。

> 🔍 **第三輪實測（2026-07-28，測 CNN Newsource）**：`https://newsource.ns.cnn.com/`（Latest／All Stories），測「摘要最新 5 則」。
> - 清單頁本身可正常截圖讀取，跟 AP 一樣即時更新（過程中 `Video` 總數從 1121 跳到 1123）。左側 **「Has Script」篩選勾選框點了沒反應**，清單沒被過濾，改用**逐則辨認清單列尾的圖示**：純 🔍（放大鏡）＝ Script 欄位還是「Information to follow」佔位符；**≡🔍（多一條列表線）＝ Script 欄位已有內容**——這個圖示差異比篩選器好用，一眼就能篩掉還沒到稿的項目（例如當次最新的漁夫獲救、地震地圖、Fauci聽證都還是佔位符）。
> - **⚠️ 關鍵發現：「有 Script 圖示」≠「有記者旁白正文」**。這輪 5 則裡有 4 則（3 則龍捲風空拍＋NYSE開盤鐘聲）點開後 Script 欄位只有 `--SUPERS--`＋`--VIDEO SHOWS--` 的極簡標記（等同 RT/AP 的裸 shotlist），只有 1 則（BuzzFeed裁員）有完整 `--LEAD IN--`＋`--VO SCRIPT--` 敘事正文。要用「文稿完整度」篩選時，CNN 這邊得再多判斷一層：**有沒有 `--LEAD IN--`／`--VO SCRIPT--` 標記，而不是只看有沒有 Script 欄位**。
> - 單則成本流程＝點清單列尾的 ≡🔍 圖示直接開 **inline Preview modal**（不像 AP 要多開一個分頁），modal 內容用**截圖**讀（沒試過對 modal 直接 `get_page_text`，效果未知，下次可以測）；modal 關閉後畫面偶爾跳回頂端輪播區塊，需重新往下捲找回原位置。

> ⚠️ **三輪操作過程中遇到的小狀況（不算卡死，換個方式都排除了，記錄給下次省摸索）**：
> - **RT**：清單頁 `get_page_text` 只抓到單一卡片，抓不到整份清單，只能靠截圖讀。
> - **AP**：①空白關鍵字查詢網址（`search?query=&mediaType=video`）會回空白頁，得改走 `/home` 首頁「Latest」分頁；②操作途中畫面自己跳轉到 `/live`（Live Feeds）分頁一次，原因不明，點回左側「Latest」才拉回正確清單；③彈窗 Close 偶爾點一次沒關乾淨，下一次點擊會重新開回同一則舊 modal，需視情況多點一次。
> - **CNN**：「Has Script」篩選勾選框點擊無效，改用逐則辨認列尾圖示排除未到稿項目；modal 關閉後畫面偶爾跳回頂端輪播，需重新捲動定位。
> - **共同點**：AP／CNN 清單都是即時更新（會被新進項目插隊，AP 那次全部往下推一位），盯盤邏輯不能假設「新項目只會加在最上面」，要用素材編號集合做差集判斷。

> 📄 **格式參考案例（2026-07-28 使用者補充，共 3 份，已問答一輪釐清）**：使用者提供三份自己既有的格式範例，存於 [`common/wire-rundown-cases/`](../common/wire-rundown-cases/)（完整差異說明與問答記錄見該資料夾 `README.md`，不在此重複）。重點結論：①這其實是「晚班交接稿單」（14:00–23:00 整段收，下班時 23:00 一次彙整），不是逐步即時產出；②格式**沒有稿頭**（三份範例都沒有），是**三層**骨架——「大分類（跨主題，例如伊朗/烏俄/財經，會隨時事機動增減）→**中主題**→小分題」，素材行掛在小分題底下（⚠️ **2026-07-30 訂正**：原寫「大分類→小分題**兩層**」漏掉中主題，同一錯誤另有兩份副本已一併修正，見 `common/11` 與 `wire-rundown-cases/README.md`；另確認**中主題是要實際寫出來的一行**，範例裡缺的是人工省略瑕疵）；③CNN 連線／記者分析等重量級內容會整段附精確 6 碼 TC 展開，但**不掐 BITE**——這訂正了先前誤判的「6 碼側錄素材適用 BITE 標記」，正確答案是外電摘要階段不掐 BITE，掐 BITE 是下游另一流程的事；④另外還有一種完全不同的「單一大新聞掃帶詳單」格式（範例③，熊本地震），依來源分類（NHK/APRT/CNN）、帶授權下架時限與製作進度標記，用途是持續追蹤素材有沒有被用進完成稿，跟晚班交接稿單是兩回事，不要混為一談。**「定時監控模式」若要正式定案，仍有 README 列出的新問題待跟使用者對齊，不要自行套用。**

## RT 掃帶成本偏高的根因（2026-07-30 釐清，待評估是否值得繞過）

**根因**：RT 清單頁是 SPA，`get_page_text` 只抓得到單一卡片、抓不到整份清單，唯一替代方案是截圖——而截圖本身就比純文字擷取貴很多。加上 RT 的 SPA 會**卡舊快取**（畫面沒真的更新、抓到上一次殘影），常需要多截一張確認才拿到當下真實內容。已量進 `common/13` 的成本模型：AP／CNN 每輪清單掃描各 2,000，**RT 是 4,500（3 張截圖）**，單這項就貴約 2.25 倍。另外 RT 常出現 `SHOTLIST ONLY / TEMPLATE ONLY` 佔位稿，同一則要被重複查看的機率也比 AP／CNN 高。

- [ ] **待評估**：目前決策 1 是「RT 允許單獨失敗、不拖垮另外兩邊」，等於接受這個成本差異。實測後看要不要進一步嘗試繞過（例如換別的擷取方式、或針對 RT 拉長檢查間隔跟另外兩邊脫鉤），而不是照單全收

## ~~V2 掃帶變慢的根因：s2_state.py 缺 batch 模式~~ ✅ 已上線（2026-08-02 熱修）

**症狀**：使用者回報改用 V2 後每輪掃帶明顯變慢。

**根因**：V2 省的是 token，代價是**工具呼叫次數暴增**——而拖慢速度的是呼叫次數（每次都是一個完整模型往返，數秒），不是 token 量。

`s2_state.py` 的 `add`／`update-entry`／`set-category` **全部一則一次呼叫**，而且 `raw_entry` 只能從 `--entry-file` 讀、**不支援行內字串**（`scripts/s2_state.py` 的 `read_entry()`）。所以每收一則素材要**兩次呼叫**：先 Write 暫存檔、再 Bash 跑 `add`。

以一輪 25 則估算：

| | V1 | V2 |
|---|---|---|
| 批次擷取 | 讀完列表，1 次 Write 整份 JSON | resume 1＋diff 1＋每則 2 次＝**52 次** |
| 整併 | 1 次 Write txt | to-compile 1＋set-category 25＋Write txt 1＋mark-compiled 1＝**28 次** |
| 品質掃 | LLM 逐行（慢但零呼叫） | check 1＋stats 1＝2 次 |
| **合計** | **約 10 次** | **約 80 次** |

**次因（今天才出現）**：4c YouTube 流程。0802 狀態檔有 22 則 YT，每則至少 `yt-dlp` 抽字幕＋`--print` metadata 兩次呼叫，沒字幕還要下載＋`video_analyze`，光這條多出 50–65 次呼叫且帶網路等待。

**第三（不建議動）**：RT 每則強制 `scroll` 比 V1 多一次呼叫——但那是為了修「換頁後讀到上一則舊快照」的無聲錯誤，成本該付。

- [ ] **建議改法：只改腳本、規則文字幾乎不動**
  1. `s2_state.py` 加 `add-batch --file entries.json`，一次吃一整批（每筆含 `id`／`source`／`checkpoint`／`status`／`entry`），**`entry` 直接放 JSON 字串**，免去每則一個暫存檔。一輪 52 次 → **2–3 次**。
  2. `set-category` 加批次形式（`--file cats.json` 或 `--pairs "RT2333=社會/休達移民,RT2360=國際/野火"`），整併 25 次 → 1 次。
  3. `add`／`update-entry` 保留 `--entry` 行內參數當單筆快速路徑（短內容不必落暫存檔），`--entry-file` 續留給長內容。
  4. 規則面只需在 `13b` §2 的指令清單補上批次用法，執行流程（收集 ID → diff → 只對新的開詳情 → 寫入）完全不變。
  5. 改完要在 `13b`「未定／實測後要回填」補一欄**時間**——現行那份差異總表只估 token、沒估呼叫次數與耗時，這正是這次沒被預見的原因。

> 🟢 **2026-08-02 晚進度**：上列 1–3 的熱修提案已由使用者核准（含 4 點補充：13b 文件必須同步更新、`--pairs` 分隔符優先認分號防中主題含逗號、batch 檔用 `utf-8-sig` 讀、跳過回報要含原因）。已在 scratchpad 副本實作完成並用 0802 真實狀態檔通過 10 項迴歸測試，**等 23:00 定版整併完成後才 commit**（避免改到工作 agent 正在呼叫的腳本）。

> 📋 本節與下方「AP 掃帶：先 diff 後整批開分頁」兩案的完整計劃書（量化目標／驗收條件／時序依賴／待裁定決策點）見 [`common/plans/2026-08-02-S2提速計劃.md`](common/plans/2026-08-02-S2提速計劃.md)。

## ~~V2 整併慢的下一刀：晚班交接 txt 改腳本渲染~~ ✅ 已上線（WP1，2026-08-03）

`s2_render.py` 從狀態檔全量渲染 txt，agent 輸出趨近 0。原本的兩個前提已用不同方式解掉：**人工編輯反向同步**——使用者確認 txt 全由 AI 編寫、人工不改，diff3／snapshot 直接廢除；**側錄區塊**——側錄入庫成 `SIDE_CNN`／`SIDE_NHK` items，render 按 source 分流排版。另加了原案沒有的三道防呆：覆蓋前 sha 比對防手改被無聲洗掉、render 後印對帳三行、`.prev.txt` 輕量備份。完整規格見 [`common/plans/2026-08-02-S2提速計劃.md`](common/plans/2026-08-02-S2提速計劃.md)，執行細節見 `common/13b` §2／§3。

## 晚班交接 txt 能否進化成 HTML 版（2026-08-04 使用者提出，待評估）

WP1 已經把 txt 變成**從狀態檔全量渲染的單向投影**（狀態檔＝唯一真相源，見上節與 [[project_s2_wp1_render_pipeline]]）——既然渲染路徑已經打通，`s2_render.py` 理論上可以多輸出一份 HTML，不必再手刻樣板。

- [ ] **先想清楚 HTML 版要解決什麼問題，不要為了炫而做**：純文字 txt 現在的痛點是什麼（例如：大分類/中主題/小分題三層縮排不好一眼分辨？△▲●◆ 時段標記純文字不夠醒目？🔴 重大提醒不夠突出？長度太長難捲動找東西？）——沒有具體痛點就先不排入。
- [ ] **釐清消費端是誰**：晚班／早班編輯是直接開 txt 看，還是會複製貼到別的系統（Notion／CMS／記者群組）？HTML 若貼不進下游流程，等於白做——這點會決定要做「純瀏覽用網頁」還是「保留可複製純文字，只是加樣式」。
- [ ] **技術路徑**：`s2_render.py` 現有的分類／標記／側錄／YouTube 各段渲染邏輯已經是結構化的（讀狀態檔 items 逐則組字串），改成輸出 HTML（例如用標記符號對應顏色／icon、大分類做可摺疊區塊）是加一個新的 render target，不用動狀態檔或整併邏輯本身；風險是**維護兩份輸出格式**，`s2_validate.py` 的品質掃/檔頭統計目前只認 txt 格式，HTML 版要嘛額外驗證、要嘛以 txt 為準另外轉換。
- [ ] **決定後再動手**：先出一個最小可行版本（例如只加時段標記顏色＋大分類摺疊）給使用者看，不要一次做齊所有想像中的功能。

## AP 掃帶：先 diff 後整批開分頁（WP2）（2026-08-02 立案，**多數已上線但非優先，2026-08-03 使用者裁示不處理**）

⚠️ **§1a API 直查上線後，這個問題在主路徑已經不存在**——AP 現在清單與稿全文都走 API，完全不開任何詳情頁分頁。下面的 §1b 清單直開流程已上線，但**只在 API 失敗、退回 §1b 時才用得上**（退路層）。`分波上限` 那條 bullet 保留但降為低優先，不主動排入。

**構想**（使用者 2026-08-02 提出）：批次擷取時不要「開一則→讀→回目錄頁→再開下一則」，改成**目錄頁先 diff 出全部新素材 → 一次把新素材的詳情頁全開成分頁 → 一頁一頁收錄**。消掉的正是 AP 已知三個坑的發生場景（modal Close 關不乾淨、點到舊 modal、清單即時插隊推位）——全都出在「回目錄頁重新定位」這個動作上。

- [x] **驗證加強版可行性（2026-08-02 深夜實測，✅ 成立，比預期更好）**：
  - 卡片沒有 `<a href>`（純 JS 點擊），但**每張卡的縮圖網址就帶素材 GUID**（`mapi.associatedpress.com/v2/items/{32碼hex}...`），一次 JS 查詢可撈整份清單的 GUID＋標題對照表。
  - 詳情頁網址格式 `https://newsroom.ap.org/detail/{slug}/{GUID}/video`，**slug 隨便填（實測用 `x`）、query 參數全部可省**，直開就是正確頁面。
  - 已驗 2 則（休達移民＋SNTV 拉力賽）：h1 標題與清單一致、STORYLINE/SHOTLIST 齊全、SNTV 標記正確。**modal 全程不需要開**。
  - ⚠️ 直開後是 SPA 載入，**要等約 3 秒再讀**，太快讀會拿到空頁（第 2 則首讀 h1=null，等 3 秒後正常）。讀取前照舊核對頁面 GUID／標題與預期一致。
- ~~不行的話退回基本版~~（不需要了，加強版成立）
- [ ] **分波上限**：一次開 5–8 個分頁為一波，讀完關掉再開下一波，避免記憶體／站方 session 問題。
- [x] **13b 已上守門版（2026-08-02 深夜，AP＋RT 一起）**：§1b 清單直開流程——清單一次 JS 撈（AP 從縮圖網址挖 GUID；**RT 卡片本身就有 href**，連 Edit No／版次／RAW·SCRIPT／稿到片未到狀態都在清單層可判）→ diff → 直開分頁（每波 5–8 頁、等 3 秒、核對編號）→ batch.json 一次 `add-batch`。守門條款：撈 0 筆／頁面編號不符／連續 2 頁失敗 → 該站當輪退回 §5 舊流程。RT Load More 實測＝**換頁替換不是累加**（先撈完才准按、按完聯集去重、只認真實點擊）。00:00 實驗輪當金絲雀，CNN 未驗證照舊流程（inline modal 同構優化可日後順帶看）。

## ~~NS 掃帶卡點：API 破口~~ ✅ 全解並上線（2026-08-03）

`POST /api/v3/stories` 一次回傳編號／形式／時長／摘要／稿件全文／embargo，一輪 NS 從 40+ 次呼叫壓到 2 次。規則已寫進 `common/13b` §1a-3（含 auto mode 安全審查通過的寫法、抽取白名單、footageType 排除純音軌與初稿佔位公告）。配方原文見 [`common/investigation-logs/2026-08-03-NS掃帶卡點報告-回覆.txt`](common/investigation-logs/2026-08-03-NS掃帶卡點報告-回覆.txt)。⚠️ 僅 Playwright 瀏覽器可用，claude-in-chrome 走不通。

## ~~NS 掃帶：卡登入頁~~ ✅ 根因查明並訂出保活機制（2026-08-04 實測定案）

- [x] **✅ 根因確認：NS 沒有任何長效認證 cookie，登入態 100% 只存在 1 小時 JWT 裡**。三站對照——**AP** `session_user`（httpOnly，7 天）、**RT** `mexlogin`（23 小時）＋`rcp-sid`；**NS 一個認證 cookie 都沒有**（該網域下全是 `_ga`／`_cb`／`_chartbeat2`／`SigniantAppInstalled` 這類 analytics 與廣告）。所以 **NS 天生無法像 AP／RT 一樣自動登入**，這是站方設計不是設定問題。
- [x] **✅ 機制確認：sliding session（滑動時效）**。`exp - iat` 恰為 3600 秒；**token 未過期**時每次載入頁面就拿舊 token 換新的、時效重算 1 小時；**一旦過期**沒有任何後備憑證，直接掉登入頁。**用可還原的破壞性測試驗證**（備份 session→刪除→reload→觀察→還原）：刪掉後 reload 被踢回 `/`、出現密碼欄位、`has_session: false`，**確認無法自動恢復**；還原後 `isAuthenticated: true`、API 實測正常，使用者無須重登。
  - ⚠️ **這個坑會誤導診斷**：若剛好在 1 小時內測試，會看到「重開瀏覽器也能自動登入」的假象——0804 第一輪測試就被誤導、得出錯誤結論，是使用者質疑「如果一小時內都沒登入呢」才抓出來。**要驗證必須模擬 token 不存在的狀態，不是只重開瀏覽器。**
- [x] **✅ 解法已訂案並寫進規則：58 分鐘保活（使用者 2026-08-04 訂案）**。掃帶 agent 在兩輪之間等待期，距上次接觸 NS 接近 58 分鐘就開一次 NS 頁面續期（1–2 次呼叫，不查清單不打 API）。⛔ **必須由掃帶 agent 自己做，不可另開獨立 agent／排程**——Playwright 只有一個 persistent profile，另開行程會跟掃帶互鎖（0803 空窗八小時的坑）。已寫進 `13b` §1a-3（機制＋證據＋保活做法）、`13b` §5 防卡設計第 2 條（執行時機）、`common/13` 排程節（為什麼需要）。
- [ ] **待觀察**：保活機制實際跑幾天後，確認 58 分鐘這個間隔夠不夠保險（會不會有 token 提早失效的情況），以及無人值守時段（`▲`）真的遇到過期時，「記進 needs-review 跳過 NS」這個處置會不會造成素材缺口太大。

## ~~工作 agent 回報兩個腳本／規則 bug~~ ✅ 兩項都已修（2026-08-04 深夜）

工作 agent 實際跑掃帶時回報，當下先記錄不打斷素材處理，現已補修。

- [x] **① `s2_validate.py` 已支援 `--file`**：`check`／`stats` 的 `path` 改成可選位置參數，新增 `--file` 旗標，兩種給法都收（同時給以 `--file` 優先），都不給會印清楚的錯誤訊息而不是 argparse 原生的 `unrecognized arguments`。已測位置參數／`--file`／都不給三種情境。
- [x] **② AP `id` 已補 `AP` 前綴**：`13b` §1a-3 白名單抽取程式碼（`slimList`／`slimDetail`）的 `id` 欄位從 `s.editorialid`（裸數字）改成 `'AP' + s.editorialid`，並在文件裡加警語提醒「照抄過舊版程式碼的地方都要回頭補前綴」。
  - [ ] **仍待查（不影響修法本身）**：有沒有實害——既有素材代碼裡有沒有出現重複收錄的痕跡，用來判斷這個 bug 過去幾天實際造成了多少無謂的重複判斷或庫存污染。

## 「無BITE」誤判：成因不明，需多方測試（2026-08-04 使用者回報，待診斷）

**症狀**：晚班交接裡很多素材標「無BITE」，但使用者懷疑其實**有 BITE**。**目前有兩個未排除的假說，需要分開驗證，不要只認一個就下手改**：

- [ ] **假說 A：pending→has_script 轉正時沒補回 BITE**。素材首次入庫時稿件還沒到（只有 shotlist／佔位稿），當下判「無BITE」是對的；但下一輪 pending 清查稿真的到了、轉成 `has_script` 時，BITE 沒被補回去，`raw_entry` 停留在最初版本。`common/13`「稿未到／pending 清查」A 節第 3 步其實已經明講轉正要重新判斷 BITE——**規則寫對了，問題可能出在執行面沒確實做**（agent 只確認「稿到了沒」，沒有真的重新逐句找引言）。
- [ ] **假說 B：瘦身腳本在入庫當下就把 BITE 砍掉了**（2026-08-04 使用者新提出）。`13b` §1a-0-1 的抽取白名單理論上「不准砍：…SOUNDBITE 段與講者」，但**規則寫了不代表程式真的沒砍**——三站（尤其 AP `script.nitf`／RT `story`）的 HTML 去 tag、欄位截取邏輯如果哪個環節誤判、正則沒吃到 SOUNDBITE 標記，就會在**第一次 `has_script` 入庫時**就已經是「無BITE」，跟 pending 轉正完全無關，連 pending 都沒經過。
- [ ] **驗證方式要能分辨兩個假說**：抽幾則標「無BITE」的素材：
  1. 查 `first_seen_checkpoint`／`script_status` 有沒有經過 `pending`——**沒經過 pending、一入庫就是 `has_script` 卻無BITE** → 指向假說 B（瘦身腳本問題），跟 pending 清查無關。
  2. **有經過 pending→has_script** → 兩個假說都可能，要再往下查：比對「瘦身後存進 `raw_entry` 的稿件全文」跟「原始 API 回應（未瘦身）」，看 SOUNDBITE 段落在瘦身前有沒有出現、瘦身後有沒有消失——**若瘦身前有、瘦身後沒有 → 假說 B**；**若瘦身前後都有、只是沒被寫進三段式 → 假說 A**。
- [ ] 兩個假說可能同時成立（不是互斥），蒐證時每一則都要各自判定，不要抓到一個案例就假設全部同一個成因。
- [ ] **可能的補強方向（蒐證後再定，不要先動）**：假說 A 成立就加重 pending 清查步驟 3 的指令語氣，或強制回報「本則轉正後 BITE：有／無」；假說 B 成立就要回頭查瘦身腳本（`13b` §1a-0-1）的 SOUNDBITE 擷取正則，可能需要针對 AP／RT 各抓一個「瘦身後遺漏 BITE」的真實樣本，比照當初「欄位名不能猜」的教訓，逐欄位 dump 出問題所在，不要憑印象改正則。

## 🔴 重大提醒漏標：診斷完成，**先觀察幾天再決定要不要加機制**（2026-08-04）

**使用者 2026-08-04 裁示：先別動，再觀察幾天**——0804 只跑了四輪、樣本太少，先看今晚剩下幾輪與隔天是不是真的持續漏標，再決定。

### 已確認：機制沒壞，是判斷步驟被跳過

| 日期 | 素材 | 正文 🔴 | 比例 | 說明 |
|---|---|---|---|---|
| 0731 | 179 | 0 | 0% | 「正文也要標 🔴」規則 **0803 才訂**，之前不存在，掛零不是退步 |
| 0801 | 127 | 0 | 0% | 同上 |
| 0802 | 302 | 2 | 0.7% | |
| **0803** | 435 | **25** | **5.7%** | 13 輪有 **10 輪**至少標 1 則 |
| **0804** | 138 | **1** | **0.7%** | **只有第 1 輪標，後 3 輪掛零** |

**排除的可能**：`set-alert` 正常、render 正常輸出檔頭、`check` 單向比對正常且 0 命中、第一輪標的 `RT2612` 全程都在。**不是 🟤 已播造成的合理省略**——漏標的那幾則都沒有 🟤。

**0804 後三輪至少 6 則明顯符合判準卻沒標**：`RT3092`（休達邊境死亡升至 **75 人**，且是當天最大的跑動中故事）、`AP4676537`（俄無人機襲黑海度假地**死亡增至 7 人**，有現場封鎖與醫院畫面）、`RT3085`（加薩同一家族 **112 人**集體安葬）、`AP4676516`（烏克蘭蘇梅**兩名女童與老太太身亡**）、`RT3020`（斯里蘭卡洪水 **5 死**）、`RT3109`（印尼渡輪火警 **5 死**）。

**最可能的成因是認知負荷**：0804 一天內新增六件每輪必做的事（`scratch-dir`／`window_start`／`src_text`／`sb_count`／`footage_type`／🟤），而「評估重大」在 `13b` §2 只是整併流程長句裡的一個子句。**軟性步驟夾在長串裡最容易被跳過，而且漏了完全沒有訊號**——`check` 只驗「檔頭有、正文沒有」，不管「整份一則都沒標」。

### 設計約束：半夜無人值守，不能靠「回報給人看」

⚠️ 「每輪強制表態一行」這種做法**對半夜無效**（`▲` 時段沒人讀回報，也不能 `AskUserQuestion`）。機制必須是**機器在對的時機主動提示、agent 自己就能處理完**。

- [ ] **候選方案 A（觀察後若確認要做，優先此案）**：render 後自動列出重大候選——機械掃 `fields.summary` 的傷亡數字與災難關鍵字，印「本輪 N 則重大候選未標 🔴」，位置比照現有的主題重複偵測。**不擋、不自動標**，規則要求逐則處置（標 🔴 或在回報寫明為何不標）。成本近乎零（純本地正則，`fields` 已結構化）。
- [ ] **候選方案 B（不建議單獨用）**：超門檻自動標 🔴。風險：正文 🔴 **標了永久保留**、誤標難撤；且傷亡數字不等於重大（資料畫面、歷史回顧也有數字，如 `HIST-02TU` 貝魯特大爆炸回顧）。
- [ ] **觀察重點**：①今晚 23:00 之後幾輪與 0805 是否仍掛零 ②若某輪突然標了好幾則，看是不是「早班回頭補」而非當輪判斷 ③與 [[正文 🔴 永久累積會不會太吵]] 那條合看——一邊嫌太多、一邊漏標，可能是同一個判準不清楚的問題。

## ~~正文 🔴 永久累積會不會太吵~~ ✅ 回查後推翻：0803 密度合理、品質良好（2026-08-04）

> ⚠️ **這條原本的疑慮已被實證推翻，保留記錄以免有人照舊版結論去砍紅標——砍掉的會是對的東西。**

**原本的疑慮（我提出，方向錯了）**：0803 檔頭剩 3 行、正文卻累積 25 個 🔴（362 則裡），擔心早班翻開滿江紅、標記失去篩選價值。

**回查後推翻（使用者 2026-08-04 指出「我反而覺得標得不錯，都有標真的重點」，逐則核對後成立）**：

- **25 則紅標其實只有 15 個事件**，不是 25 個獨立的重要性判斷。集中在兩件大事：**華州斯波坎野火 6 則**（700 棟燒毀、6.5 萬撤離）、**川普對伊「最後機會」／荷莫茲 4 則**；熊本地震與匈牙利多瑙河各 2 則；其餘 11 件各 1 則。
- **21 小時的國際新聞週期裡 15 件重大事件，密度完全合理。**
- 照 `13` 四條判準逐則核，**22/25 站得住**（傷亡規模、第一手畫面、國際級突發，理由明確）。較弱的三則：`AP4676479`（聯合國秘書長候選人聚烏拉圭，程序性新聞）、`AP4676476`（休達移民尋親，人情追蹤非突發首出）、`PO-110MO`（皮羅進白宮西翼，②的邊緣）——但規則明寫「**拿不準就標，漏標的代價遠大於多標**」，這三則標了不算違規。
- ❌ **我原本的算法把「同一事件的多角度素材」當成多個獨立判斷**，這是錯的框架。

**真正剩下的、可討論的設計點（低優先，不急）**：

- [ ] **同一事件的每則素材是否都該帶 🔴**？這是取捨不是缺陷——找素材時全部標出來反而好找，但確實會拉高視覺密度。要動之前先確認早班編輯的實際使用感受。
- [ ] ⚠️ **野火那 6 則分散在三個中主題**（`美國/美國野火`、`天氣/華州野火`、`美國/華盛頓州野火`）——**這是分類不一致，不是紅標的問題**，併入「全盤檢討分類問題」處理。
- **與 [[🔴 重大提醒漏標]] 那條合看**：0803 密度合理、0804 卻掛零，所以真正的問題是**漏標**不是太多。兩條原本看似矛盾，實則同一個判準的兩端。

## 三層分類（大／中／小）精準度仍不足（2026-08-03 使用者回報，待診斷）

大分類歸位通則已訂（見下一節，`db6a7c0`），但實際跑下來**大／中／小三層的分類仍不夠精準**——使用者 0803 晚班觀察到的。

- [ ] **先蒐證再訂規則**：從 0803（或近幾天）已定版的晚班交接，挑出實際歸錯／歸得不好的案例，分清楚是**哪一層**出問題：
  - **大分類**：通則已訂（越前面優先），還錯就是通則沒涵蓋的組合，或 agent 沒照做。
  - **中主題**：目前只有「用 `【】` 標記」的格式規定，**沒有任何粒度規則**——多細算一個中主題？同事件不同面向要不要拆？這很可能是主要問題來源。
  - **小分題**：`13` 有「通常 4 個全形字元、要夠粗、相似素材共用同一小分題、不要一則一個」的規定，但**沒有判斷「哪些算相似」的操作型判準**。
- [ ] 蒐證後才決定要補什麼規則——不要在沒有實例的情況下先寫一套規則，容易寫出用不上的東西。

## 🔴 瀏覽器安全分類器攔截 RT 導覽：原因不明，其他 model 沒遇到（2026-08-05 立案，觀察中）

**症狀**：0805 工作 agent 回報第一次開 Reuters Connect 時**被瀏覽器安全分類器攔截**，需要使用者重新授權才能繼續掃 RT。使用者指出**其他 model 沒有遇到這個問題**。

### 已排除的可能（0805 我用同一套 MCP 實測）

| 假設 | 實測結果 |
|---|---|
| 網域被擋 | ❌ `browser_navigate` 開 `reutersconnect.com` 完全沒被擋 |
| `browser_evaluate` 這個動作被擋 | ❌ 前後跑了六次 JS（含 `fetch` 打 `/api/item`）都沒被擋 |
| RT 站方反爬蟲 | ❌ 全程 HTTP 200，登入態正常 |
| 內容觸發（戰爭／傷亡素材） | ❌ 抓了 36 則含加薩集體葬禮、基輔遇襲等，沒有任何攔截 |

**唯一遇到的阻礙是 `Browser is already in use`（profile 被殘留 Chrome 行程鎖住），那是技術性錯誤、不是授權問題**——清掉殘留行程後一切正常。

### 待辦

- [ ] **下次再發生時，請工作 agent 貼出攔截的原始訊息**。目前只有「被安全分類器擋住」這個轉述，不知道是哪一層擋的（工具層？權限層？模型自身拒絕？），沒有原文無法定位。⚠️ 這三種的處置完全不同，不要憑猜測改設定。
- [ ] **釐清是否與 model 有關**：使用者指出其他 model 沒遇到。若確認是特定 model 的行為差異，處置方向是換 model 或調整該 model 的提示，而不是改 repo 規則。
- [ ] **與 [[RT 漏收 36 則]] 分開看**：0805 那次大漏收的根因已查明並修掉（清單虛擬捲動只看到首屏 10 則＋`/api/item` 回應格式從 transit 改純 JSON 導致抽取全空，見 `8091a65`），**跟安全分類器是兩件事**。不要把漏收都歸因到攔截。

## 🔴 全盤檢討分類問題（2026-08-04 使用者立案，**最優先，先於任何分類相關的自動化**）

**使用者定調：「我發現我們國際組原有的規則其實非常沒有邏輯。」** 現行「大分類清單越前面＝優先層級越高」這條通則（2026-08-03 訂、`db6a7c0`）**與實務做法系統性衝突**，不是個案微調，要整套重新檢討。**在檢討完成之前，不要做任何分類的機械檢查或自動修正**——照現行規則寫出來的檢查會把正確的歸位判成錯的（見下方實測）。

### 使用者已給出的方向（2026-08-04）

- **`財經（股市）`／`娛樂`／`體育` 是「題材格」，優先級高於地緣格**——理由：**這些主題非常明確，要獨立放**，不能照「越前面越優先」硬套。所以「美股」歸**財經**是對的，不是錯；美國體育歸**體育**、好萊塢歸**娛樂**同理。
- **例外**：財經若涉及**川普政策**（例如關稅），應歸 **`關稅`** 或 **`美國`**，不留在財經。
- ⚠️ 這與現行 `common/13`「大分類歸位通則」的表格**直接矛盾**——該表寫「美股 → 美國（美國在財經之前）」，依使用者澄清這條是**錯的**，要改。

### 蒐證：0804 實測資料（機械檢查可行性調查的副產品）

用 0803 定版 435 則實測三種比對範圍後**逐則人工判讀**，結果是：

| 比對範圍 | 命中 | 判讀 |
|---|---|---|
| 素材行全文 | 33 則 | 大量誤報（DAX 稿引述川普、瑞士賽事有中國選手） |
| 備註＋摘要 | 28 則 | 體育／娛樂／話題整片命中 |
| 只比第一括號備註 | 6 則 | **僅 1 則是真錯**（`RT2813` 伊朗朝聖掛在中東，依主體歸屬制應歸美伊）；**其餘 5 則美股掛財經，經使用者澄清是正確歸位** |

**這份資料的價值不在找到錯誤，而在證明規則本身有洞**——依現行通則字面執行會產生的「錯誤」，實際上多數是正確做法：

- **體育 5 則**（美國女足、波切蒂諾續約 ×3、MLB 薪資帽）：字面規則該歸「美國」，實務歸體育 ✅
- **娛樂 2 則**（好萊塢票房、美國歌手）：同一形狀 ✅
- **財經 5 則**（美股／NYSE）：字面規則該歸「美國」，實務歸財經 ✅
- **話題 4 則**（狗衝浪、雙髻鯊）：通則表自己寫「軟性趣味 → 話題」，**與優先序自我矛盾**

### ⭐ 先分析人工歷史交接，用實證找規律（2026-08-05 使用者提議，**優先於自行訂規則**）

**使用者提議**：「我自己想一定也會有缺漏，如果已經有現成的資料可以分析是不是比較快？」——**同意，而且應該排在自行訂規則之前**。歷史交接是**人工**做的，裡面就是實際的編輯慣例，等於現成的標準答案；我們現在卡住的問題它都能回答，而且不會像憑空想那樣互相矛盾（0805 就發生過我先誤判題材格、使用者才糾正）。

**資料在哪**：`G:\我的雲端硬碟\Claude共用\自動掃帶系統\晚班交接歷史範例\`，**23 份**（0501–0620，跨兩個月）。另有 `Archive/` 底下 10 份 AI 產出的可當對照組。

| 目前卡住的問題 | 歷史檔能回答的 |
|---|---|
| 中主題該多細 | 一格通常幾個中主題、每個幾則——**量得出來** |
| 命名該用什麼軸 | 人工實際寫的是事件名、議題名還是地名 |
| 題材格 vs 地緣格 | 美股放財經還是美國、美國體育放哪、颱風傷亡放天氣還是社會 |
| 同一事件多角度 | 收在一個中主題還是拆開 |

- [ ] ⚠️ **前置：前 10 份是 `.gdoc`，本機讀不到內容**（只是 Drive 捷徑檔，正文在雲端）。要嘛請使用者轉成 `.txt`，要嘛用 Drive API 讀。**後 13 份是 `.rtf`，本機可讀**（需剝格式碼），可以先跑這批。
- [ ] ⚠️ **先看格式有沒有分期**：跨兩個月，慣例可能變過。分期統計再合併，**不要把不同時期混在一起算平均**——那會得出一個誰都沒用過的「平均慣例」。（同 `歷史稿單語料分析` 那條的教訓。）
- [ ] **分析方式（省 token，不把 23 份全文讀進對話）**：跑腳本只輸出聚合結果——①各份的大分類清單與順序（驗證樣板是否穩定）②每格中主題數量分佈、每個中主題的則數分佈（**量粒度**）③中主題名稱全表（人工掃一遍就看得出命名軸）④交叉表：特定主題歷史上都放哪一格（如「美股」）。成本比讀全文低兩個數量級。
- [ ] **誠實的但書**：歷史檔是人工做的，人工可以憑經驗當場判斷，我們需要的是**可寫成規則的部分**。分析目標是找出**穩定重複**的模式，不是完整複製人工的每個決定。
- 📌 **與本節其他項的關係**：這條的產出應該**先於**「重新定義大分類層級模型」與「中主題命名規則」——有實證再訂，不要倒過來。

### 🔴 中主題重複／散落：排序規則治標不治本（2026-08-05 實證）

**已做的（治標，有效但不夠）**：`s2_render.order_topics()` 讓字面相近的中主題自動靠攏（共同連續子字串 ≥2 字），0805 起生效。實測**確實有效**——密西根兩個、野火兩個、槍案兩個整理前就已相鄰。

**但擋不住語意重複**，0805 實例（77 個中主題，人工合併後剩 55）：

| 重複組 | 共同字 | 排序有沒有靠攏 |
|---|---|---|
| 【基輔空襲】11 則 vs【俄襲烏克蘭】3 則（同一晚攻擊） | 0 | ❌ |
| 【台海】【台海軍演】【台灣】（同一場漢光演習） | 台海系列有、【台灣】沒有 | ⚠️ 半 |
| 【歐洲刑案】vs【希臘】（同一起雅典棄屍案） | 0 | ❌ |
| 【動物保育】vs【英國】（都是切斯特動物園） | 0 | ❌ |
| 【中國科技】vs【科技】vs【中國太空】 | 部分 | ⚠️ 半 |

**根因（0805 判定）**：**agent 每輪獨立命名，看不到既有的中主題清單**。20:00 那輪替基輔攻擊開【俄襲烏克蘭】時，並不知道 16:30 輪已經有【基輔空襲】。**這是「看不到」不是「不想用」。**

- [ ] ⏳ **D 案（每輪重新檢視分類）：使用者 2026-08-05 提出，裁示「明天新制度上線後再觀察要不要推」**

  **構想**：每輪掃完後請工作 agent 重新檢視一次分類、修正偏差，再 render。

  **可行性：有實證**。0805 我人工做過兩次（77→55、92→62），內容零增減、品質掃 0 命中。而且**每輪做比整晚做完再做好**——每輪只處理新增的 20–30 則，偏差不會累積到 92 個那種規模。

  ⚠️ **要加一條硬限制才能上：只併不改名**
  > 新素材可以**併進既有的中主題**，但**既有中主題的名字一旦寫下就不准動**（使用者明講要改才例外）。

  理由：**中主題名稱在輪次之間變來變去，比重複更糟**——編輯 20:00 看到【加薩】、22:00 變【加薩局勢】、再一輪又變回去，會對不上自己記得的東西。重複只是佔空間，改名會讓人找不到。這條也讓半夜無人值守時的最壞情況變成「該併沒併」（多一個中主題），而不是「亂改一通」。

  ⚠️ **順序要在 `set-category` 之前，不是之後**：
  ```
  掃完 → add-batch → list-topics 看既有清單 → 新素材優先併進既有中主題
                   → 只有真的是新事件才開新名字 → set-category → render
  ```
  「一次就分對」而不是「先分錯再修」——後者會產生無謂的改名。

  📌 **為什麼先觀察而不是直接做**：`list-topics`（事前預防）今天才上線、agent 還沒用過。**如果事前看清單就足夠，這層就是多餘的**。0805 已有教訓——排序規則、`list-topics`、命名判準一次疊三層，出問題時分不清哪層有效。**一次加一層才量得出真實效果。**

  - [ ] **觀察指標**：0806 同量級素材，中主題數能否自然壓在 40 以內（0805 是 194 則／62 個，人工整理後）。壓得住就不做 D 案；壓不住再加。

- [ ] **A 案（治本，優先）：`s2_state.py` 加 `list-topics` 指令**——印出目前各大分類底下的中主題與則數，規則要求**開新中主題前先跑一次**。成本極低（讀狀態檔、零瀏覽器呼叫），直接消滅「看不到」這個成因。
- [ ] **B 案（輔助）：`s2_topic_dedupe.py` 擴到比中主題**——目前只比小分題。render 後自動提示「同格內名稱互相包含」與「跨格同名」。純字面比對、不涉語意，是我 0804 已經寫過的偵測邏輯，搬過來即可。⚠️ 但同樣抓不到零共同字的語意重複，只能當第二道網。
- [ ] **C 案（規則層，要跟本節其他項一起想）**：訂中主題的**粒度**與**命名軸**——0805 社會格出現【印尼】【澳洲】【德國】【希臘】【捷克】這種純國名，那是地點不是主題，也是重複的溫床。
- ⚠️ **不要再往排序演算法加東西**：字面相似度已經做到能做的極限，再調門檻只會製造誤湊（`美股` vs `亞股`、`政治` vs `政壇` 都只差 1 字）。剩下的是語意問題，得靠上游「看得到既有清單」解決。
- 📌 **0805 已人工整理過一次**（77→55 個中主題，內容零增減），可當合併後的樣貌參考。

### 待辦

- [ ] **重新定義大分類的層級模型**：現行的單一線性優先序（15 格排排站、越前面越優先）顯然描述不了實務。至少要區分**題材格**（財經／娛樂／體育／科技，主題明確、獨立收）與**地緣格**（大陸／美伊／中東／烏俄／美國），並訂出兩者相遇時誰優先、例外條件是什麼。
- [ ] **把使用者已給的三條澄清寫進 `common/13`**（題材格優先、財經涉川普政策的例外、美股歸財經），並**移除／改寫**現行通則表裡與之矛盾的列（「美股 → 美國」那列）。
- [ ] **釐清 `話題` 的定位**：它同時是「最後收容格」與「軟性趣味專格」，這兩個身分會打架（美國的軟性趣味該進美國還是話題？依使用者邏輯應該是話題，因為題材明確）。
- [ ] **釐清 `社會`／`天氣` 是不是也算題材格**：現行表寫「天氣災害有傷亡 → 社會」，但若題材格優先，颱風傷亡是否該留天氣？0803 實例 `HE-66MO`／`RT2968`（美國環孢子蟲疫情）目前放社會。
- [ ] 檢討完成後才回頭決定 **`s2_validate.py` 要不要做機械檢查**（原第 321 行待辦，已併入本節）。技術上可行且成本低（只掃第一括號備註即可、不需要維護會長大的關鍵字表），**但規則沒理順之前做了只會製造假警報**。

## ~~每天掃帶排程時間應固定下來~~ ✅ 固定排程已定案（2026-08-04）

**最終定案**（使用者 2026-08-04 給出完整時刻表，原則性、當天可隨時特例調整）：

| 檢查點 | 標記 | 班別 |
|---|---|---|
| 16:00／18:00／20:00／22:00／23:00 | `△` | 晚班（23:00 下班前最後一輪） |
| 01:00／04:30 | `▲` | 無人值守 |
| 07:00／08:00 | `●` | 晨班 |
| 10:00／12:00／13:00 | `◆` | 早班 |

**過程中經過兩次推翻，最後定案時邊界其實回到最原始版本（23:00／07:00），只是現在有明確固定時刻表＋多了 `◆`**：
- [x] 2026-08-03 深夜第一次訂正：`04:40→05:00` 誤標 `●`，改成「標記看實際開掃時間」——這條原則本身維持有效。
- [x] 2026-08-03 晚一度把邊界改成 `22:00／05:00`——**已被這次定案推翻**，使用者確認 22:00／23:00 兩輪仍算晚班有人值班。
- [x] `mark_for()`（`s2_render.py`）、`MARKS`（`s2_validate.py`）、`common/13`／`13b`／`14-S2b` 全部同步改回 `23:00／07:00` 邊界，並補上固定排程表；12 組時間點單元測試全過，對今天實際 307 則資料 regression 測試通過。
- [ ] **仍待確認**：01:00 到 04:30 中間空約 3.5 小時、13:00 到隔天 16:00 中間空約 3 小時，這兩段沒有排程是否合理。
- [ ] **仍待決定**：要不要真正自動化排程觸發（`ScheduleWakeup`／Windows 工作排程器），目前仍是人工／agent 手動照表操課，不是系統定時觸發。

## CNN/NHK 側錄整併功能要修（2026-08-03 深夜使用者提出，待設計）

**症狀**：使用者若一次餵超多行、超多 TC 段的側錄逐字稿，會讓整份 `{MMDD}晚班交接.txt` 被拉得非常長——現行 [`14-S2b-側錄轉譯摘要.md`](common/14-S2b-側錄轉譯摘要.md) 規則是**逐字全收、不設字數上限、不壓縮**（2026-08-02 訂案，`例外 1／2`），這條本身就是**覆蓋掉更早一版「摘要」的舊定案**才訂出來的（理由：下游記者寫稿／掐 BITE 需要精確原話，二次摘要會磨掉可用原話）——現在使用者要的「整併功能，減少 TC、減少行數」等於要再修一次同一個張力點，**這是第三輪拉鋸，需要跟使用者把「要保留什麼、犧牲什麼」問清楚再動手，不要直接翻案**。

- [ ] **先釐清「整併」的定義，這決定要不要違反現行逐字規則**：
  - **(a) 純結構性合併**（不動文字內容）——例如把同一講者連續的多個小段 TC 合併成一個較大區塊、減少 TC 標記的**數量**，但**逐字內容完全不刪減**。這不違反「逐字」規則，只是換一種分段方式。
  - **(b) 真正的內容壓縮／摘要**——砍字數、只留精華，這會**推翻**2026-08-02 訂的「維持逐字」規則，等於走回原本被覆蓋掉的舊定案，需要重新評估「記者寫稿需要精確原話」這個理由是否還成立、或者可以摘要＋保留關鍵句原話並存。
- [ ] **蒐證**：抓一份實際超長的側錄案例（行數／TC 段數／字數），量化「多長算太長」，不要憑印象定規則。
- [ ] 決定後同步修 [`common/14-S2b-側錄轉譯摘要.md`](common/14-S2b-側錄轉譯摘要.md) 的「兩行式」寫法規則與 `s2_state.py add-side`／`s2_render.py` 的側錄渲染邏輯。
- [x] **✅ 附帶 bug 已修（2026-08-03 深夜）**：6 碼格式的 TC 範圍（如 `211313-211522`）被 `normalize_side()` 處理時，「取起點丟訖點」失效——`s2_validate.py` 的 `_TC` 正則原本只有**冒號範圍**（`16:53:50-16:56:38`）才支援 `-訖點` 分支，6 碼範圍只吃得到起點，`-211522` 被當成沒吃到的殘餘文字，誤判成一行獨立內容，跑出 `△ CNN 211313` 後面多一行 `--211522` 的殘留。已補上 6 碼範圍分支，兩種格式測過都正常收斂成單一起點。**這只解決「多吃一行殘留」的格式 bug，跟上面「整併太長」是兩件事，不衝突**。

## ~~S2 每日暫存檔應集中放進獨立日期資料夾~~ ✅ 已定案並上線（2026-08-03 深夜）

**現況（定案前）**：`自動掃帶系統/` 資料夾裡每一輪掃帶都會留下暫存檔，一晚下來散一整層，跟正式庫存檔混在一起，愈積愈亂；且已有自發性但不一致的雛型——`20260803/` 子資料夾只收了 22:30 之後的暫存檔，更早幾輪的還留在外層。

**已定案（規則見 `common/13`「每日暫存檔歸檔」節）**：
- [x] **範圍**：只收暫存檔（`_ap_*`／`_rt_*`／`_mk_*`／`{MMDD}-batch-*.json` 等）。**正式庫存檔（`{MMDD}-s2-state.json`、`{MMDD}晚班交接.txt`）維持留在 `自動掃帶系統/` 這層，不搬**——現行腳本 `DEFAULT_FILE` 與文件路徑都假設在這層，範圍先縮小、風險較低。
- [x] **命名規則**：統一用 8 碼 `{YYYYMMDD}/` 資料夾（例：`20260803/`），汰除 `{MMDD}-` 前綴散在外層的舊做法；日期用**晚班起始日**，不是實際掃帶當下的日曆日（跟「檔名不換日」同一邏輯）。
- [x] **今晚已整理**：把散落外層的舊暫存檔（`0803-batch-*.json`／`_ap_*`／`_rt_*`／`_ns_*`／`_mk_*` 等）搬進 `20260803/`，與原本已在裡面的那批合併。過程中一度誤把正式狀態檔也搬進去（另一個並行 session 動的，見下方「多 session 併發」提醒），已驗證內容完整後搬回父層。
- [x] 規則寫進 `common/13`「輸出位置與檔名」節。
- [x] **✅ 強制執行機制已寫（2026-08-04）**：新增 `s2_state.py scratch-dir --mmdd {MMDD}` 指令，印出並自動建立當晚資料夾，不用 agent 自己記得規則。寫進 `13b`「防卡設計」第 1 條，緊接在 `resume` 之後、每輪開工必跑。已測：建立、驗證存在、重跑冪等（已存在不報錯）。

⚠️ **多 session 併發提醒（2026-08-03 深夜實錯）**：這輪整理過程中發現有另一個 session／流程正在同時對這個資料夾與這個 repo 做事（`s2_validate.py` 被外部改過、正式狀態檔一度被搬進日期子資料夾）。**目前沒有互斥機制**，多個 agent／session 同時寫同一份狀態檔或同一個 Drive 資料夾有覆蓋風險，這次算運氣好（內容經比對後沒遺失），不代表每次都安全。要不要設計簡單的鎖檔或協調機制，列入待評估。

## ~~NS 初稿不收錄~~ ✅ 已解並上線（2026-08-03 晚實測，判準是機械欄位）

**結論：不用猜關鍵字，`footageType` 直接判得出來**，而且順帶確認了純音軌那條也是同一個欄位。已寫進 `common/13`「NS 兩類素材不採納」與 `common/13b` §1a-3。

| 排除對象 | 機械判準 | 實例 |
|---|---|---|
| 純音軌 | `footageType === "AUDIO TRACK"` | `PY-04MO`／`PY-02SU`（今天手動刪掉的那則就是） |
| 初稿佔位公告 | `footageType === "GRAPHIC"` 且 script 含 `THIS IS NOT THE FINAL SCRIPT` | `PRE-400aET` |

**初稿那則的真面目**：它不是「早期版的新聞帶」，而是站方發的**佔位公告**——`PRELIM SCRIPT & EMBARGO INFO FOR 400amET PKG`，內文明講「THIS IS NOT THE FINAL SCRIPT」並點名最終版在 `PY-03MO`、4:00aET 會出。所以規則除了排除它，還要**把它點名的最終版 ID 撈進回報**（那則才是要收的）。

**實測排除的誤判線索**（避免日後有人重走）：
- `hideScript: true` **與初稿無關**——那幾則稿件一樣完整含 `--LEAD IN--`／`--VO SCRIPT--`，只是站方顯示設定。
- API 回應**沒有任何** `status`／`version`／`draft` 類欄位（100 則樣本＋`api/refdata` 都掃過）。
- 關鍵字掃 script 會**大量假陽性**：`early`（"still in the early stages"）、`UPDATED`（標題 `(UPDATED) DANGEROUS HEAT DOME…` 是正常素材的更新版，**要收不要排除**）。

- [x] **~~待決：AP `++PRELIMINARY SCRIPT++` 要不要排除~~ ✅ 已訂案並上線（2026-08-04 使用者裁示：不排除，標 `pending` 下一輪補）**。AP 這種是**真素材**（`AP4676355` 伊朗外交部簡報，有畫面有 SOUNDBITE），只是稿為初稿版、正式稿之後會出——與 NS 的 `footageType==="GRAPHIC"` 佔位公告（本身不是新聞帶、整則排除）性質完全不同，**不要混為一談**。處置：素材照收、`script_status=pending`、備註加 `(初稿)`、內容照初稿正常寫三段式（有 BITE 就寫，`sb_count`／`has_sot` 兜底照常適用），下一輪 pending 清查補正式稿後轉 `has_script`。AP 沒有機械欄位可判，抽取白名單新增 `prelim` 旗標掃 `script` 字樣，正則**錨定開頭**（`^\s*\+\+`）避免內文提到就誤判（5 項正反例測過）。規則寫進 `common/13`「有稿判準」節與 `13b` §1a-3 AP 白名單。

## ~~大分類歸位規則~~ ✅ 已訂案並上線（2026-08-03，commit `db6a7c0`）

**症狀（0803 實錯兩例）**：中國經濟相關被分到「財經」而非更前面的「大陸」；美國政治被分到「政治」而非更前面的「美國」。

**使用者已定調的通則（待寫進規則）**：**大分類清單越前面＝優先層級越高。一個中主題同時滿足兩個大分類時，一律放最前面那一個。**

**現行順序**（權威來源：`G:\我的雲端硬碟\Claude共用\自動掃帶系統\晚班交接大分類樣版.txt`，共 16 格）：
`(時效性特殊)` → 大陸 → 關稅 → 美伊 → 中東 → 烏俄 → 美國 → 政治 → 財經 → 社會 → 天氣 → 體育 → 科技 → 娛樂 → 話題

- [x] **通則已寫進 `common/13`**「大分類歸位通則」節，含兩個實錯反例。
- [x] **重疊風險已正面表列**（六組配對表寫進 13）。地緣三格採**主體歸屬制**：美伊＝所有涉伊朗事務（含以伊衝突）／中東＝除伊朗外中東／烏俄＝烏俄及他國涉烏俄。⚠️ 天氣 vs 社會標為最易直覺出錯（社會在前）。原始清查如下：
  - **大陸 vs 財經／科技／政治／社會**（中國經濟、中國科技、中共人事、中國社會案件）→ 依通則全歸「大陸」
  - **美國 vs 政治／財經／社會／天氣**（美國政治最常誤判；美股、美國槍案、美國野火）→ 依通則全歸「美國」
  - **關稅 vs 財經／美國／大陸**（關稅本身就是經貿議題，且多半是美中）→ 關稅在前，優先「關稅」
  - **美伊／中東／烏俄 三者互相**（以伊衝突算美伊還中東？加薩算中東；烏俄專屬）→ 需明確界線定義
  - **烏俄 vs 政治**（歐美對烏俄的外交政治動作）
  - **天氣 vs 社會**（災害傷亡：颱風、野火、洪水）
  - **話題 vs 娛樂／社會**（軟性趣味題材的收容邊界）
- [x] **機動格已定案**：由**使用者在當天晚班交接開啟時決定**放什麼（如 0802 的「熊本地震」），agent 不自行判斷、不自行命名。
- [ ] 順帶確認：`s2_validate.py` 能否機械檢查歸位（例如「大陸」以外的分類出現中國地名關鍵字就提示），或這只能靠 LLM 判斷。

## `s2_state.py` 待修小問題（2026-08-03 立案）

- [x] **~~`needs-review add` 會建出空殼素材、灌水 pending~~ ✅ 兩半都已修完（前半 2026-08-03 `ebc324c`，後半 2026-08-04）**。原症狀：`cmd_needs_review` 對不存在的 id 用 `setdefault` 建 `script_status: "pending"`、`raw_entry` 空白的項目，每記一筆備註 pending 就虛增 1——0803 早上 60 則 pending 裡有 **7 則是這種空殼**（`NS r8 pending 群`／`PENDING清查-r10未完成`／`SCRIPT-BUG-ENCODING`／三則 `RT-*-空白` 等）。
  - **前半（灌水）已修**：備註殼改建 `script_status="note"`。三處都認得：`changed_since_render()` 不算它待整併、`cmd_resume` 的 pending 計數排除它、`s2_render.py group_items()` 跳過它不出 txt。
  - **後半（只進不出）0804 補齊**：原本只有 `add`／`list`，**沒有任何結案方式**，標記一旦記下就永久留在 `resume` 的「待人工」清單，久了真正該注意的新項目會被淹沒。新增 **`needs-review done --ids`**：備註殼→整筆刪掉（本來就不是素材）；真素材→只脫掉 `needs_review` 旗標、素材與內文原封不動。批次採**全有全無**（含不存在 id 或本來就沒標記者，整批不處理並報錯），避免清一半。
  - **測試**：新增 [`scripts/test_s2_needs_review.py`](scripts/test_s2_needs_review.py)，14 項全過（涵蓋兩半症狀、真素材保留、四種防呆、全有全無）。另用 0803 真實狀態檔（435 則）重跑 render，**產出與現行定版逐字元一致**，確認無下游影響。
  - **既有那幾則不必清**：0803 狀態檔已完全乾淨（435 則全 `has_script`、零 pending、零空殼）；那 9 筆殼留在已封存的 `Archive/0802-s2-state.json`，早已是 `note`、不影響任何計數，且內容是有價值的查核記錄（RT profile 鎖死的三筆訂正、兩筆腳本 bug 回報）。**已確認那兩筆 bug 都修掉了**——🔴 誤判已改成只取行首代碼（同 `ebc324c`）、`entry-file` 讀取本來就明確指定 `utf-8-sig`。刪掉只會失去記錄、換不到任何好處，故保留。
- [x] **~~to-compile／resume 字串比較漏抓待整併~~**（2026-08-03 已修，commit `8f850c4`）：checkpoint 標籤字串比較 `"r10" < "r8"`，第 10 輪後的更新對上第 1–9 輪 compiled 被判成沒變動而靜默漏掉（實錯 50 則沒進 txt）。已改用腳本自寫的微秒時間戳比較。**待查**：0802 那晚是否也跑到兩位數輪次、有沒有更早的漏網素材。

## 三站省 token 持續優化研究（2026-08-03 立案，方向盤）

使用者定調：AP／RT／NS 三站都還有「更省 token 的處理方式」與「更多步驟改腳本替代」的空間，當持續研究方向，不是一次性任務。優先順序原則：**API ＞ DOM 直撈 ＞ 開頁面讀**；每站逐步往上游搬。

- [x] **✅ 三站 API 全破（2026-08-03 02:45 端到端實測通過）**，完整配方見 [`common/investigation-logs/2026-08-03-三站API破解總表.txt`](common/investigation-logs/2026-08-03-三站API破解總表.txt)：
  - **NS**：清單 API 直接含稿全文（見上節，最省）。
  - **RT**：清單 `POST /api/search-api`（同源 cookie 重放 OK，transit 格式、cursorMark 翻頁、fragment 300 字＋early-access 訊號）；單則 `GET /api/item/{guid}` 全文 8KB 重放 OK。建議組合：清單照 §1b DOM 直撈＋詳情一次 `Promise.all` 打 API ×N——免開分頁免等 8-9 秒。
  - **AP**：清單 `POST api.newsroom.ap.org/v1/nrsearch/search/topic`（cookie 重放 OK，`editorialid`＝AP 編號）；詳情 `POST /v1/nrsearch/search/item/details`（STORYLINE/SHOTLIST/Restrictions 全文 27KB）。ItemIds 逗號批次不支援，用 Promise.all。⚠️ 清單重放排序偏 relevance 待驗證，保險做法照抄頁面實際 request body。
  - 每輪呼叫數估算：10 則新素材，舊流程 AP/RT 各 20-30 次、NS 40+ 次 → **API 流程每站 2-3 次**。
- [x] **待驗五項已全部驗完（2026-08-03 09:40，結果回填 Drive 總表「四之二」節）**：①AP 照抄頁面 body 重放**順序與 DOM 完全一致** ②⚠️**AP `PageNumber` 不可靠**（Page 2 回 100 筆、與 Page 1 零重疊）→ 固定 `PageNumber=1` 加大 `PageSize`（16/50/100/200 實測皆精準）③AP TopicId 跨 session 穩定（兩次都是 `116e9ab7…`）④RT hash 跨 session 穩定（兩次都是 `klwn20`，但像 build hash、改版會變→規則寫「照抄當下請求」）⑤RT transit 抽取定案：**清單走 DOM 直撈**（regex 解清單會因 `^N` 參照只抓到第一則）、**詳情走 `/api/item` regex**（單則回應每欄只出現一次、必為字面值），Edit No 可由 guid `newsml_RW{4碼}` 機械推出互相校驗；⚠️ `editnumber` 在 item 回應抓不穩，一律以清單／guid 為準。
- [x] **✅ 已上線（2026-08-03，commit `1d897b9`）**：13b 新增 **§1a 三站 API 直查**為掃帶首選路徑，含守門與退路階梯（§1a → §1b DOM 直開 → §5 UI 舊做法，同站失敗 2 次即退）。下載段另已於 `reuters/05`（commit `eb84294`）改走 API。
- [x] **✅ 已寫進 13b §1a-0（2026-08-03，commit `1d897b9`）：每輪開工必做**（檢查殘留／收工必關／`navigate` 失敗或清單 0 筆時第一個懷疑 profile 被鎖而非帳號失效）。原始案例如下：**Playwright profile 佔用防呆**——**已造成實害**：0803 RT 連續三輪（04:40／06:40／08:40）回報「全站 0 items」，實測是**帳號正常、profile 被殘留 Chrome 鎖住**（01:28 的殘留鎖掉前兩輪，NS agent 08:22 開的 Chrome 鎖掉第三輪），RT 素材因此空窗 01:00–09:00。同一天 NS agent 也被 01:28 那個殘留擋過。根因：Playwright 只有**一個** persistent profile，多 agent 並行時互相鎖死，且 agent 收工不關瀏覽器。
  - 規則要寫：①**開工前檢查殘留**（`Get-CimInstance Win32_Process` 篩 `playwright-mcp-profile`，看 `MainWindowTitle`／CPU／存活時間判斷閒置，閒置就 `Stop-Process -Force`）②**收工必關瀏覽器**③**navigate 失敗或清單回 0 筆時，先懷疑 profile 鎖，不要直接判定「站方無素材」或「帳號失效」**（這正是 0803 誤判的形狀）。
  - [ ] 另評估治本解：多 agent 並行時改用 `--isolated`，或每個 agent 分配獨立 profile 目錄——但要先確認**登入狀態是否跟著 profile 走**（isolated 會不會每次都要重登，那就得不償失）。
- [x] **✅ RT 即時快訊（右上角鈴鐺）也能抓（2026-08-03 09:50 實測）**——**這是全新料源，不是省 token，是多一條資訊**。`GET .../ws-active/messages/user?_type=breaking-news,…` 一次回約 40 則、涵蓋 24 小時，每則含快訊全文＋精確時間戳＋已讀狀態＋來源文章 ID。⚠️ 不能直接 fetch（跨網域擋，實測掛住 120 秒），要**讓頁面自己發、再讀網路紀錄**（navigate → network_requests → network_request 取 body，共 3 次呼叫）。詳見 [`common/investigation-logs/2026-08-03-RT即時快訊API.txt`](common/investigation-logs/2026-08-03-RT即時快訊API.txt)。
  - **價值實例**：昨晚列 🔴 重大的希臘直升機相撞，**21:32 快訊就發了、23:51 更新「兩死」**，影片素材更晚才進庫——快訊可在影片到之前就示警，也補得到官方傷亡數字。
  - [ ] **待裁定用途**：①當 🔴 重大提醒行的候選來源（Reuters 編輯台自己認定值得推播，比 agent 自判可靠）②pending 素材的數字補充（需訂規則：來源要不要標註、能不能寫進素材行）③每輪開工抓一次（成本僅 3 次呼叫）。⚠️ **24 小時就消失，不抓就沒了**，要留存得每輪存檔。
  - [ ] 未探索：socket.io 長連監聽（免輪詢）、calendar-event／account-alert 兩型內容、分頁與時間區間參數。
- [ ] **AP 也有快訊區（2026-08-03 使用者指出，待探索）**：AP Newsroom **首頁右上角有一塊快訊區域**，昨天就是在那裡看到快訊的。比照 RT 的做法探索——先看網路請求有沒有對應端點（`api.newsroom.ap.org` 底下已知有 `nrsubscription/alerts/nr_breaking_news`，很可能就是它），確認欄位、保留時限、能否重放。若可行，與 RT 快訊合併成同一條「突發示警」料源。
- [ ] **通則**：每次優化上線都要在 13b「未定回填」記一筆呼叫次數／耗時基準，跟 00:00 實驗輪（7 則 52 次 12m18s）比對，量化每一刀的實際收益。
- [ ] **清單能否三站都直接用 API 拿，不必再退回 DOM 直撈**（2026-08-03 使用者立案）：目前現況——**NS** 清單即文稿，全走 API，已是最佳狀態；**AP** 清單 API 已可用（照抄頁面 body 重放順序與 DOM 一致）；**RT** 清單 API 技術上存在（`POST /api/search-api`），但回應是 **transit 壓縮格式**（欄位名只出現一次、後續用 `^N` 參照），現行規則因此放棄 API 清單、改走 §1b DOM 直撈（見上方「RT transit 抽取定案」）。**待研究**：這個 transit 格式能不能用**正確的 transit 解碼**（不是 regex）穩定解開？若可行，RT 清單也能併入純 API 流程，省掉 §1b 那層。
- [ ] **API 回應文字量仍偏大，能否先讓腳本瘦身、AI 只看瘦身後結果**（2026-08-03 使用者立案，同日**離線初步研究已完成，方向確認可行，待實測量化**）：三站回應格式固定（AP 詳情 27KB、RT 全文 8KB、NS NDJSON），代表**抽取所需欄位是機械可判斷的**，不必讓 AI 自己讀整包 JSON 邊解析邊摘要。可比照 WP1 render 的思路——腳本先做結構化抽取（拿掉 HTML 標記、無關 metadata，只留 title／description／script 全文／時長／編號等），AI 只處理「摘要與判斷」那一層。
  - ✅ **初步研究結論（2026-08-03，未開瀏覽器、用手上現成資料判斷）**：**方向可行，且解法已現成**——今天修 NS auto mode 卡點時用的寫法（`page.evaluate()` 內「讀→抽取→只回傳白名單欄位」）本來就是這條要的東西，不必另外發明，直接把同一套 pattern 套到 AP／RT。
    - **證據一（舊樣本，證實雜訊確實存在）**：0731 DOM 擷取樣本 `Archive/_s2_raw_0731/rt_walk_1925.json`（21 則，59KB）拆開看，`script` 欄位佔整份 JSON 79.9%，但欄位內文本身混了跟新聞無關的下載價位表（`HD 60fps (MP4) - Included`／`Download Audio (WAV split) - 3pts`）、UI 選單文字（`Video Transcript / Hide / Show Scene List`）、機器學習免責聲明，每則幾乎逐字重複、機械可辨識可切除。⚠️ 這是**舊版 DOM 整段擷取**的樣本，新版 API 是結構化 JSON，這塊雜訊很可能已經因為改走 API 而消失，**不能直接套用到新版**，只能證明「這類供稿網站原始內容習慣混雜訊」的前提成立。
    - **證據二（現行規則文件本身的欄位表，不需要實測就能確定的浪費）**：`13b` §1a-3「2) AP」清單回應欄位表列了 `renditions`（媒體檔案各畫質／格式變體陣列，這類供稿 API 的典型肥大來源），規則沒寫「這欄位要丟掉」，代表**目前很可能原樣跟著清單一起被讀進 AI context**，而它對「收不收、怎麼摘要」完全沒用——這條不需要開瀏覽器驗證，看欄位表就確定。
  - ✅✅ **實測完成（2026-08-03 18:3x，Playwright 實打 AP／RT API，數字如下）**：**確認可行，省幅遠大於預期**。
    - **AP 清單**（`search/topic`，10 則）：**102,751 → 1,689 字元，省 98.4%**。最大宗雜訊 `renditions`（單則就 6,193 字元＝媒體檔案各畫質變體），10 則約 62KB 全是廢的。白名單只留 `editorialid`／`itemid`／`title`／`dateline`／`friendlykey` 就足以做 diff 與判斷。
    - **AP 詳情**（`item/details`，單則）：**24,237 → 5,051 字元，省 79.2%**（**已保留 `script` 稿件全文 4,528 字元**）。純雜訊：`filings` 8,719（產品發布清單，一長串 product id）＋`renditions` 6,193 ＝ **61.5%**。
    - **RT 詳情**（`/api/item/{guid}`）：實測兩則 5,952／15,423 字元。雜訊是**整套授權計價規則**（`transactional-rules`／`rendition-rules`／`points`／`price-per-point`／`currency`／`ayce`／`rule-type-id`／`overview-rule`）＋**各種檔案 URL**（`image-url`／`screener-url`／`xml-url`／`video-shotlist-url`／`audio-file`／`rendition`／`filename`），佔絕大部分。
    - **NS** 已經有白名單抽取（今天修 auto mode 時寫的），**這站不用再動**。
  - ⚠️ **實測中發現的三個坑（寫規則時必須避開）**：
    1. **欄位名不能猜**：AP 詳情的稿件全文在 **`script.nitf`**（HTML 字串），不是 `storyline`／`shotlist`——我第一次照文件描述的名稱抓，結果抓到空字串、還誤以為省了 98.9%。**每個欄位都要先實際 dump 確認名稱與型別**。
    2. **欄位型別也不能猜**：RT 的 `duration` 是**字串** `"00:03:59"` 不是數字，用 `(\d+)` 抓不到。
    3. ⚠️ **RT 清單再次實證不能用 regex 解**：limit=10 的回應 57,787 字元，regex 只抓得到 **2 個 guid**（transit `^N` 參照所致）——與既有規則「清單走 DOM 直撈」的結論一致，**不要因為看到「省 99%」的假數字就以為清單也能 regex 瘦身**。
  - **AP `script.nitf` 是 HTML**（`<p>SHOTLIST:</p><p>SOUNDBITE...`），去掉 tag 還能再省一小截，但**內文一個字都不能動**（同側錄逐字規則）。
  - [x] **✅ 已上線並驗證完（2026-08-03，commit `c6d60e1` 起共 4 個 commit）**：白名單寫進 `13b` §1a-3「0-1) 抽取白名單」節，AP/RT 一輪實測總省 91.1%、內容零遺失；上線後再加碼 8 輪驗證（HTML 實體解碼補完、交叉核對、全欄位殘留掃描、冪等性測試）全過。三個易猜錯欄位（AP 稿全文在 `script.nitf` 非 `storyline`；無 `duration`，改用 `shots[0].end` 換算；`caption`／`script` 皆為物件）已記錄在案。

## S4 rundown 稿單（2026-07-30 立案，**未建、無規則文件**）

從 S3 庫存依議題整理出「今天開哪幾則新聞、每則配哪些素材」，再接 S4b 分派給記者。**這一格原本被誤填成 `common/11`**，但 `common/11` 其實是 **S4c 補充素材搜集**（人工發動、寫稿期間缺素材才啟動），兩者只是共用同一種稿單輸出格式，觸發者與目的完全不同（2026-07-30 使用者訂正）。

- [ ] 設計 S4：輸入是 S3 庫存，輸出是可開稿的 rundown。要決定選題判準（怎麼從一整天的庫存挑出「今天要做的幾則」）、一則新聞怎麼把多個素材綁在一起、輸出格式是否沿用稿單格式（稿頭＋代碼＋摘要）
- [ ] 與 S4b 分派的介面：rundown 上要不要直接帶「指派給誰」欄位
- [ ] 順序上 S4 依賴 S2／S3 先跑起來，目前排在 S2 之後

## 素材行寫法：尚未討論的三類（2026-07-30 立案）

素材依 [`common/13`](common/13-S2-定時掃帶.md)「素材分成四大種類」分四類。第 1 類側錄已定案，第 2 類的 AP／RT／CNN Newsource 討論中，**以下三項使用者明確表示先擱置**：

- [ ] **第 2 類的 `ENEX` 與 `ABC`**：先不管。註記——[`common/05`](common/05-material-numbering.md) 目前寫著這兩種「只做編號，不要自行猜測去哪個網站下載，下載階段要停下來問使用者」，代表連下載流程都還沒有，不只是稿單寫法未定
- [ ] **第 3 類 YouTube 頻道**（韓聯社／CNA／CNN 官方頻道等）：素材行怎麼寫未定。註記——這類是**剪輯過的新聞短片、不是可下載母帶＋Shotlist 的正式供稿**，且韓聯社頻道**僅限南北韓新聞、禁用其國際編譯新聞**（既有規則，見 [`common/11`](common/11-wire-rundown-organizer.md) 模式 C）
- [ ] **第 4 類其他網址素材**（烏俄等戰事的社群／OSINT，如 X @NOELreports、@wartranslated、UNITED24media、@OSINTWarfare、@CENTCOM）：素材行怎麼寫未定。註記——既有規則要求這類**必須在備註標明來源性質與查證提醒**，且**務必點進原始貼文核對發布日期**（曾誤把 2024 年舊素材當最新）。可找名單見 `common/11` 模式 C，DVIDS 已明訂算正式素材、不需加社群備註

## S2 庫存格式：時長標記 `▎MM:SS`（2026-07-31 **已定案**）

**定案**（使用者 2026-07-31）：素材行在 `BITE：`／`無BITE。` **之後**加 **`▎MM:SS`**；**缺則省略**；**不溯及既往**（不回填舊庫存）。權威寫法見 [`common/13`](common/13-S2-定時掃帶.md)。

- [x] **是否新增**：要——納入晚班交接／`raw_entry` 固定格式（有時長時）
- [x] **寫在哪**：整行**最後** `…▎BITE：…▎MM:SS` 或 `…▎無BITE。▎MM:SS`（不進備註、不夾在中間）
- [x] **資料從哪來**：列表／詳情 Duration 皆可；看不到就省略，不為填時長去下載量片
- [x] **與 `SOT` 關係**：PKG／DONUT >1 分仍標備註 `SOT`；`MM:SS` **不**取代 `SOT`
- [x] 已改 [`common/13`](common/13-S2-定時掃帶.md) 樣板與範例；**不**回溯洗舊則

## S2：最終整併時清查「稿未到／pending」（2026-07-31 **已定案**）

**定案**（使用者 2026-07-31）：維持決策 4「先進庫＋稿到覆寫」；**但書**——**最終整併**晚班交接（23:00 定版或明確下令收班整併）時，**順便清查**所有 `script_status=pending` 與 txt 內仍含「稿未到」的項目，逐則回站確認是否稿到並補齊。中繼整併不強制全量清。權威見 [`common/13`](common/13-S2-定時掃帶.md)。

- [x] 決策 4 補上後補節奏與最終清查但書
- [x] 整併職責表＋獨立小節步驟（列 pending → 回站 → 覆寫／保留 → 回報）
- [x] 明訂中繼不強制、不掃已 has_script、不回溯舊日檔

## S2：定版／收班整併必做品質掃（2026-07-31 晚 **已定案**）

**定案**（使用者 2026-07-31 晚）：最終整併除 pending 清查外，還要掃 **格式異常／明顯可疑／缺漏**；**有命中才回外電網站補**，不做全量無差別回站。中繼不強制。權威見 [`common/13`](common/13-S2-定時掃帶.md)「最終整併：稿未到清查＋品質掃」B 節。

- [x] 決策 4／整併職責表寫入品質掃
- [x] 獨立小節拆 A pending + B 品質掃（表格：異常／缺漏／可疑／非缺漏）
- [x] 明訂「先掃可疑再回站」、回報格式、不做全量 has_script 重掃

## S2：RT WRAP＝整理包（2026-07-31 晚 **已定案**）

**定案**：Reuters **WRAP**＝整理包；**備註第一括號**標 `整理包`，摘要不要 `WRAP：` 前綴。權威見 [`common/13`](common/13-S2-定時掃帶.md) 備註詞彙。

- [x] `common/13` 備註規則＋正反例
- [x] 0731 `RT2295` 庫存列同步改標

## 歷史稿單語料分析（2026-07-30 使用者提議，待提供檔案）

素材行的 `(備註)` 詞彙目前只從三份範例歸納到 11 個（空拍圖／氣象圖／棚內解說／記者STAND／有google街景前後對比／共4張照片／有多張圖／白天／標來源／180天下架／炸瞬 無聲）。**正面表列一定會漏**；2026-07-31 另已定 **`資料畫面`**（取代 FILE／檔案）、**`SOT`**（Newsource PKG/DONUT>1分）、備註**禁重複 BITE**——見 [`common/13`](common/13-S2-定時掃帶.md)。使用者提議改用歷史稿單反推其餘詞彙。

- [ ] **取得歷史稿單**。⚠️ 這台 Mac 的 Google Drive 桌面程式沒開、掛載是 7/17 舊快取，讀不到雲端；要嘛把 Drive 開起來讓腳本直接掃資料夾，要嘛檔案放本機路徑
- [ ] **分析方式（省 token）**：不把稿單全文讀進對話，改跑腳本只輸出聚合結果——備註詞彙頻率表、依四面向（畫面種類／品質評價／使用限制／聲音）分群、位置統計（接代碼後 vs 接最後 vs 掛小分題）、長度分佈。三份範例已用此法處理，數百份成本相當
- [ ] 若稿單跨越較長時間，**先看格式有無演變**（早期與近期慣例可能不同），分期統計再合併，不要混在一起算
- [ ] 產出後回頭補進 [`common/13`](common/13-S2-定時掃帶.md)「素材行的寫法」，並訂出「非常簡短」的實際字數上限

## 中主題怎麼分（2026-07-30 立案，暫定由 AI 判斷）

三層骨架的中間層。**中主題是要實際寫出來的一行**（使用者 2026-07-30 訂正；範例檔裡有些地方看不到，那是人工省略的瑕疵，不是規範）。

- [ ] **分類方式未定案，暫定由 AI 依素材內容判斷**。分軸不固定——國別（西班牙山火／法國山火）、面向（戰事本身／普欽表態／澤倫斯基外交行程）、階段都可能，不套固定字庫。實用一段時間後再看要不要收斂成準則
- [x] **標記方式已定（2026-07-30）**：中主題寫成 **`【名稱】`**（全形方括號），小分題維持裸行。原本兩層都是無符號單行、機器分不出來；加標記後三層各有依據（`======大分類======` ／ `【中主題】` ／ 裸行小分題），且不影響閱讀。三層標記與完整範例寫在 [`common/13`](common/13-S2-定時掃帶.md)「庫存檔格式」，`common/11`、`common/14`、`wire-rundown-cases/README.md` 均已指向該節
- [ ] 中主題的**顆粒度**也沒定：一個大分類底下通常幾個中主題？範例① 天氣段看起來是「西班牙山火／法國山火／成因」這種等級

## S2b 側錄轉譯摘要（2026-07-30 立案；**現行版 A 已建並實測，長期版 B 仍未做，非優先不處理**）

`wire-rundown-cases/` 範例①② 的庫存清單裡，除了通訊社素材，還有大量**側錄逐字**內容（`SB 151328`＋`CNN記者 Eleni Giokos`＋整段 6 碼 TC 逐字，例如伊朗「談判有望」、烏俄「川普態度」）。這些 6 碼是側錄檔時間戳，資料在「掃帶歐印萬」資料夾，**CNN Newsource 網站上沒有——S2 輪詢永遠抓不到**。

使用者 2026-07-30 定調：這是**獨立子系統 S2b**，先前討論過做法但未落地，未來要併進來源庫，跟 S2 一起餵同一份 S3 庫存。

- [x] **✅ 現行版 A 已建並實測（2026-08-02～08-03）**，見 [`common/14-S2b-側錄轉譯摘要.md`](common/14-S2b-側錄轉譯摘要.md)：使用者篩選內容 → `s2_state.py add-side`（`--normalize`／`--source`／`--homes`）入狀態檔 → `s2_render.py` 按 source 分流渲染。維持**逐字**（不是摘要，07-30 這條已被覆蓋）；0802 實測 68 段側錄往返 byte 級一致。
- [ ] **長期方案未設計**：自動監看資料夾出現新側錄檔 → 自動轉逐字 → 自動入庫。觸發方式、轉譯工具、全轉 vs 只轉重量級段落都還沒定
  - [ ] **排程機制待評估（2026-07-31 提出）**：目前用 Claude Code 內建 `CronCreate` 做每 15 分鐘監控「歐印萬掃帶」資料夾（TC中文大段翻譯），但這排程是 **session-only**——只存在單一 CLI session 記憶體裡，不寫入磁碟，session 關閉/重啟就消失，且最長 7 天自動過期。若要「長期方案」真正跨重啟穩定運作，這個機制撐不住。**候選替代方案**：Windows 工作排程器（Task Scheduler）呼叫 `claude` CLI，可跨機器重啟存活。使用者 2026-07-31 裁示先不做，列入 TODO 待之後評估。
- [ ] **格式刻意偏離範例檔，需在實用中驗證**：範例① 有 105 行是裸 6 碼、只有 1 行 `SB 151328`，範例③ 靠檔頭 `\=NHK=` 標來源；改成「來源＋6 碼」是因為 ENEX 素材代碼也是 6 碼數字會撞號，且 CNN／NHK 混排時光看 6 碼分不出是哪台母帶。若實際使用覺得囉嗦再調
- [ ] **摘要密度與切段方式待驗**：摘要壓多短（範例約 2–6 句）、一段連線切成幾個 TC（範例是講者換話題就換一個 6 碼），都要確認逐字稿本身有沒有現成 TC 可用
- [ ] 現有的側錄規則只有**下游消費端**：[`common/07`](common/07-bite-assistant.md) 方式 2（6 碼側錄掐 BITE）、[`common/02`](common/02-tc-offset-filename.md)（TC offset 換算）、[`common/01`](common/01-shared-folders.md)（掃帶歐印萬資料夾）。上游的「轉譯進庫存」這段完全沒有規則文件，要新寫
- [x] **與 S2 的介面已定（2026-07-30）**：素材識別用「來源＋6 碼」（`CNN 151328`／`NHK 222009`），與通訊社代碼不撞號，兩者混排在同一份庫存 txt；庫存檔位置與檔名由 [`common/13`](common/13-S2-定時掃帶.md) 統一定義，S2b 不另開檔

## 目錄結構重構（2026-07-30 提出，待處理）

[`common/12-子系統地圖.md`](common/12-子系統地圖.md) 已把現有 26 份規則歸成 9 個子系統（S1–S9）＋共用地基＋參考知識，但**只加地圖、沒搬檔案**——目錄仍是按來源切的 `reuters/`／`ap/`／`cnn/`，跟子系統邊界垂直交叉。

- [ ] **決定要不要真的按子系統重構目錄**（例如 `subsystems/S1-素材發現/`）。搬檔的代價：全部交叉連結要重寫、`Claude-memory` 裡多筆路徑引用會失效需同步修、`_archive/` 的對應關係要重新標。好處是找檔案不用先知道它屬於哪個來源。
- [ ] 重構前先確認 S1–S9 的切法與命名定案（目前為 2026-07-30 暫定）。
- [ ] 若重構，`README.md` 目錄與 `AGENTS.md` 入口要一併改寫，`更新 AI HUNTER` 那節列的必讀路徑（`common/`／`reuters/`／`cnn/`）也要更新。

> Aigent-Fable 目前**只當 roadmap 藍圖參考**，不動、不同步。兩邊分塊的對照與已知落差登記在子系統地圖末節。

## 已完成

- [x] **DVIDS 也要抓文稿**（2026-07-24 完成）：已在 [`reuters/05-batch-download.md`](reuters/05-batch-download.md) 補 DVIDS 來源分派與處理細節（影片本體 `yt-dlp`＋詳情頁 `get_page_text` 抓文稿、欄位清單、B-Roll 拍攝日非當日的提醒），並在 [`common/05-material-numbering.md`](common/05-material-numbering.md) 補 DVIDS URL 的編號歸屬。
