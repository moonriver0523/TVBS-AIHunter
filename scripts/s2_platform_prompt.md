你要去**交換平台**（ENEX Members Website／ABC NewsOne）掃一輪，把素材整理成 TVBS 晚班交接的素材行。

**規則全部在這裡，先讀完再動手，不要憑記憶：**

```
E:\GitHub\TVBS-AIHunter\common\18-交換平台素材整併.md
```

那份會再指向 `common/plans/` 底下對應的 API 規格書（端點、查詢、欄位白名單），**一併讀完再動手**。

⚠️ **本檔不重複規則內容**——規則天天在改，`18` 才是最新的。本檔與它衝突時以 `18` 為準。

站別：{SITE}
時間窗：{WINDOW}
