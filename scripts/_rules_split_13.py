# -*- coding: utf-8 -*-
"""13 切成**三部**（v2）。相對 v1 的兩個修正：

1. **「已定案的六項決策」留在必讀區**——v1 把它移進 rationale，但實際複核發現
   決策 4／5／6 帶操作性語句（稿未到處理、庫存維護、大分類歸屬）。
   使用者要求「核心機制不得有刪減遺漏」，一律從嚴：不確定就留在必讀。
2. **改切三部**，讓每一份都有充裕餘裕（估計載入量 < 25,000），
   不再出現「兩部各自貼著 30,000 上限」的情形。

進 rationale 的只剩三節，逐節人工複核過、確認是純背景：
  - Token 成本（2026-07-30 估算）
  - 待補（七項有五項自己劃掉寫「已解」）
  - 與範例①② 的差異（明寫「只登記，不自行調和」）

⛔ 內容逐字保留，不改寫任何一條規則；切完會做逐字節守恆證明。
"""
import os

SRC = r"E:\GitHub\TVBS-AIHunter\common\_rules_backup_20260824\13-S2-定時掃帶.md"
OUT = r"E:\GitHub\TVBS-AIHunter\common"
STAMP = "2026-08-24"

# (起, 迄, 去哪)；1-indexed 含頭含尾，必須完整覆蓋且不重疊
PLAN = [
    (1, 8, "P1"),        # 原檔頭（會被新檔頭取代，內容併入 rationale 存查）
    (9, 206, "P1"),      # 六項決策／批次分立／隔夜續掃／時段標記／狀態 JSON
    (207, 228, "R"),     # Token 成本（純估算）
    (229, 345, "P1"),    # 輸出位置／_待整併/／HTML 檢視版／暫存檔歸檔
    (346, 792, "P2"),    # 檔頭／輸出格式／庫存檔格式／素材行寫法
    (793, 926, "P3"),    # 大分類清單／套樣板／list-topics／中主題排序／歸位通則
    (927, 936, "R"),     # 與範例①②的差異（只登記）
    (937, 949, "R"),     # 待補（多已劃掉）
    (950, 1053, "P3"),   # 各站已知狀況／時區／RT 連讀
]

HEADS = {
"P1": ("""# S2 定時掃帶（通訊社網站輪詢）— 第一部：決策、流程與輸出

> 🔴 **2026-08-24 分成三部（R15）**：原檔 1052 行／53,676 字元，而 Read 工具結果
> 約 30,000 字元就**靜默截斷**——實測 0820／0821／0823 每一輪都只載入約一半，
> 大分類清單、素材行寫法、時區換算、RT 連讀鐵律**從沒進過 context**。
> **內容逐字保留，未改寫任何一條規則**（切檔附逐字節守恆證明）。
>
> | 部 | 檔 | 內容 |
> |---|---|---|
> | 一 | **本檔** | 已定案六項決策／批次分立與整併／隔夜續掃／時段標記／狀態 JSON／輸出位置／`_待整併/`／HTML 檢視版／暫存檔歸檔 |
> | 二 | [`13e-S2-素材行與分類規則.md`](13e-S2-素材行與分類規則.md) | 晚班交接檔頭／重大提醒／已播標記／**庫存檔格式**／**素材行寫法（含摘要 150 字硬上限）** |
> | 三 | [`13f-S2-大分類與各站規則.md`](13f-S2-大分類與各站規則.md) | **大分類清單**／**大分類歸位通則**／`list-topics`／各站已知狀況／**時區換算**／**RT `‹ ›` 連讀鐵律** |
>
> ⛔ **三部都是必讀，只讀一部不算讀完規則。**
> （[`13-rationale.md`](13-rationale.md)＝早期估算與已解待辦，**不必每輪讀**。）
""", "13"),

"P2": ("""# S2 定時掃帶 — 第二部：檔頭、庫存檔格式與素材行寫法

> 🔴 **2026-08-24 由 [`13`](13-S2-定時掃帶.md) 切出（R15）**，內容逐字保留、未改寫。
> **本檔與第一部 [`13`](13-S2-定時掃帶.md)、第三部
> [`13f`](13f-S2-大分類與各站規則.md) 同為必讀。**
>
> 本檔涵蓋：晚班交接檔頭（含 🔴 重大提醒行、🟡 次級重大、🟤 已播標記）、
> 已定案的輸出格式、**庫存檔格式（三層標記）**、
> **素材行的寫法（含摘要 150 字硬上限、`▎` 分隔、`BITE：` 必寫講者、行尾時長）**。
""", "13e"),

"P3": ("""# S2 定時掃帶 — 第三部：大分類、各站狀況與時區

> 🔴 **2026-08-24 由 [`13`](13-S2-定時掃帶.md) 切出（R15）**，內容逐字保留、未改寫。
> **本檔與第一部 [`13`](13-S2-定時掃帶.md)、第二部
> [`13e`](13e-S2-素材行與分類規則.md) 同為必讀。**
>
> 本檔涵蓋：**大分類清單（權威樣板）**、新開／整併必須整份套樣板、
> **開新中主題前必跑 `list-topics`**、中主題排列順序、**大分類歸位通則**、
> 各站已知狀況（含 **NS 沒稿也要收**、per-source 數量回報）、
> **各站時間／時區（CNN 換算口訣）**、**RT 詳情頁 `‹ ›` 連讀鐵律**。
""", "13f"),

"R": ("""# S2 定時掃帶 — 早期估算與已解待辦

> 📌 **2026-08-24 由 [`13`](13-S2-定時掃帶.md) 切出（R15）**。
> 這裡**只放三節純背景**，逐節人工複核確認不含任何現行操作規則，
> 因此**不列入掃帶必讀清單**：
>
> - **Token 成本**：2026-07-30 的事前估算（非實測，實測見 `_token_metrics.jsonl`）
> - **待補**：七項中五項自己劃掉寫「已解」，其餘是待辦不是規則
> - **與範例①② 的差異**：原文明寫「**只登記，不自行調和**」
>
> ⛔ **凡有一絲操作性的內容一律不放這裡**——「已定案的六項決策」原本要移進來，
> 複核發現決策 4／5／6 帶操作語句（稿未到處理／庫存維護／大分類歸屬），
> 已退回第一部必讀區。內容逐字保留、未刪改。
""", "13-rationale"),
}

FILES = {"P1": "13-S2-定時掃帶.md", "P2": "13e-S2-素材行與分類規則.md",
         "P3": "13f-S2-大分類與各站規則.md", "R": "13-rationale.md"}


def canary(tag):
    return (f"\n<!-- RULES-EOF {tag} {STAMP} — 讀到這一行才算讀完本檔；"
            f"沒讀到＝被截斷，必須用 offset 補讀。 -->\n")


def main():
    lines = open(SRC, encoding="utf-8").read().split("\n")
    n = len(lines)
    cov = []
    for a, b, _ in PLAN:
        cov.extend(range(a, b + 1))
    assert sorted(cov) == list(range(1, n + 1)), f"PLAN 未完整覆蓋 1..{n}"

    seg = {"P1": [], "P2": [], "P3": [], "R": []}
    for a, b, where in PLAN:
        seg[where].extend(lines[a - 1:b])

    # 原檔前 8 行是舊檔頭，用新檔頭取代；原文附進 rationale 末尾存查（不刪任何字）
    old_head = seg["P1"][:8]
    seg["P1"] = seg["P1"][8:]
    seg["R"] = seg["R"] + ["", "---", "",
                           "## 附：分檔前的原始檔頭（2026-08-24 存查，逐字保留）", "",
                           "```"] + old_head + ["```"]

    for key, fname in FILES.items():
        head, tag = HEADS[key]
        # ⛔ 不可 strip 尾端換行：段落邊界的空行也是原檔內容，strip 掉就不是逐字保留。
        #    用 BODY 標記把「新檔頭」與「原文正文」明確隔開，還原時才能逐字節取回。
        text = (head + "\n---\n<!--BODY-->\n" + "\n".join(seg[key])
                + "\n<!--ENDBODY-->\n\n" + canary(tag))
        open(os.path.join(OUT, fname), "w", encoding="utf-8").write(text)
        nl = text.count("\n") + 1
        est = len(text) + nl * 5
        flag = "✅" if est < 26000 else ("⚠️" if est < 30000 else "❌ 會截斷")
        print(f"{flag} {fname:<34}{len(text):>7} 字元 {nl:>4} 行  預估載入 {est:>7}")


if __name__ == "__main__":
    main()
