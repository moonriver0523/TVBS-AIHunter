# 13c3　S2 定時掃帶 V8（執行版）— 下：收工與防卡

> **V8 五層合一（2026-09-07）**：來源＝13c2（本檔原身，§3/§3b/§3a/§4/§4c/§4b/§5/§5c/§5a）＋13d §10/§11（hook 相關防護，因 `13c2` 字元預算改放本檔，與 §5 防卡設計同主題相鄰）＋13f 尾「收工清單總表」（原放 13c2 §6，因預算改放 13f 尾，V8 再搬回本檔）。單層無疊合。
> 13f 該處已改一行指路，不再重抄全表。
> 同為必讀：`13c`（上）、`13c1`（ENEX）、`13c1b`（ABC）、`13c2`（狀態檔與指令）。

---
<!--BODY-->
## 3. 品質掃／統計／渲染：`s2_validate.py`＋`s2_render.py`

```
python scripts/s2_validate.py check "G:\...\0802晚班交接.txt"    # 格式異常掃描（行號＋原因）
python scripts/s2_render.py --file "G:\...\{MMDD}-s2-state.json" --out "G:\...\{MMDD}晚班交接.txt"
python scripts/s2_render.py --file "…" --base-date 0802          # 不給 --out ＝ 印預覽
```

- `check` 涵蓋 V1 格式異常表全部可 regex 項目；**LLM 只處理命中清單**，不整份逐行讀。
- render 產出順序：檔頭（🔴 取自 `alerts`）→ 16 格大分類（空格保留）→ `【中主題】` → 小分題（裸行＋`+` 分隔）→ 素材行。走 render 不必再跑 `stats`（檔頭由 render 自生）。
- 時段標記由 render 依 `first_seen_checkpoint` 自動補：23:00 前＝`△`、23:00–07:00＝`▲`、07:00–09:00＝`◇`、09:00–14:00＝`◆`。補掃輪判不準就 `set-mark` 寫死。
- `raw_entry` 零加工輸出；側錄照 `14-S2b` 兩行式原樣帶出。
- YouTube 兩行式的網址行照樣帶時段標記（render 已保證標記後有半形空格）；⚠️ 小分題不要塞進 `raw_entry` 第一行——那是 `category.小分題` 的位子，`raw_entry` 只放「網址行＋備註行」。
- 寫檔 tmp+rename 原子；覆蓋前另存 `.prev.txt`（只留最近一版，救 render 壞檔用）。
- ⛔ **手改偵測**：現行 txt sha 對不上 `last_render_sha` → 拒絕覆蓋 exit 3。看到先把 txt 上的內容寫回狀態檔，確認可丟才 `--force`。
- 📊 **每輪必看對帳三行**：「素材 N 則（±X）／側錄 M 段（±X）／YouTube K 支（±X)」＋點名「現行 txt 有、本次 render 沒有」的代碼——**出現點名就是漏了 `add-side`／`update-entry`**。回報要貼這三行。
- render 完自動跑 `check` 與 `s2_topic_dedupe`（`--no-check`／`--no-topic-check` 可略）。
- 重大提醒行用 `set-alert` 存狀態檔（≤3 則），判準與撤除見 `13`。
- 腳本連續失敗 2 次 → 記「未執行（腳本錯誤）」，⛔ 禁止 agent 手掃替代。

## 3b. 中／小分題檢視：`s2_topic_review.py`（**render 前必做，每輪**）

```
python scripts/s2_topic_review.py --file "<狀態檔>"
```

**看過整張表**（約 60 行），同義／同事件的合併掉再 render（`set-category --pairs`）。
⚠️ **不要只看它列的候選**——機械只認字面：港譯「美斯」對台譯「梅西」零共同字永遠挑不出來，只有你看得出來；候選也會誤報（「足球 vs 美式足球」），自己判斷。實例形態：【高爾夫】＋【高球】；【泰國校園槍擊】＋【校園槍案】；足球下「悼念老梅西／梅西家族／梅西父喪／美斯家事」四個同一件事。
⛔ 只印不改；本支只碰中／小分題同義合併，不碰大分類歸屬。

## 3a. 主題重複偵測：`s2_topic_dedupe.py`（render 自動叫）

- 【今天內部】同一小分題名稱出現在不同大分類 → 命中（只比大分類；中主題名稱允許每天微調，比了誤報）。
- 【跨天】昨天的小分題今天全搬到不重疊的大分類 → 命中（只回看前一天一份，不多天累積）。
- 只提示不寫 state，人工 `set-category` 確認後改。
- ⛔ 不要加回「內文 bigram 模糊比對」——實測假警報蓋過訊號。

## 4. SNTV 列表級收錄（機械白名單）

體育畫面必然是比賽或訪問，是「跳過詳情不漏判斷資訊」的少數類別；白名單**只給機械可判的 `AP5`**，其餘一律照常開詳情。

- 僅限代碼 `AP5` 開頭：以列表可見資訊（標題＋時長＋Source）寫簡版三段式，不開詳情。備註標 `(SNTV)`。
- 🎯 **結尾標記兩層判準**（`role` 含 SOT 是必要條件不是充分條件，不可直接標 `(BITE)`）：

  | 列表級看到的 | 怎麼標 | 開詳情？ |
  |---|---|---|
  | `cap` 裡有引號引言（`"…"`） | 標 `(BITE)`，引言**照抄** `cap` 原文 | ❌ |
  | `role` 含 SOT 但 `cap` 沒引言 | **開一次詳情**取 `sb_count`：>0 標 `(BITE)`、=0 標 `無BITE。` | ✅ 只有這格 |
  | `role` 是 `VO` 等無聲形式 | 標 `無BITE。` | ❌ |

  ⛔ **禁止「寫不出引言原文就寫講者＋內容概述」**——那是假 BITE 產生器（概述是從標題猜的）。寧可標無BITE 讓編輯自己看帶。開詳情那格記得帶 `sb_count`（§2 批次規則），否則稽核③變盲區。
- ⚠️ **`畫面：` 寫依標題可推的實際畫面**（`▎畫面：比賽精華、遠射進球與重播。`），⛔ 不可寫操作註記（`(列表級,未開詳情)` 之類，`s2_validate` 會抓）。推不出就只寫最低限度一句。
- 使用者點名要開稿的 SNTV，回站補完整三段式。
- ⚠️ SNTV 同主題重複只收一則（判準與例外見 `13`「SNTV 例外」）；列表判讀階段就跳過，不進 `batch.json`。

## 4c. YouTube 網址素材（人工投餵）

**觸發**：使用者貼 YouTube 網址要求加入晚班交接。S2 不主動掃 YouTube。

**兩行式**（與通訊社單行三段式不同）：

```
{YouTube 網址}
({來源} {形式} {MM:SS}) {摘要}
```

- 第一行＝網址原樣（不縮短、不加括號），就是代碼。
- 第二行括號＝`({來源} {形式} {MM:SS})` 固定順序；來源用中文慣稱；時長寫括號內。
- 摘要約 200 字，直接寫內容。❌ 不寫 `▎畫面：`／`▎BITE：`／`無BITE。`；❌ 不加 `(BITE)`；❌ 不寫佔位字樣（`（200字摘要）`／`（AI摘要）` 等）。

**形式三選一（必填）**：

| 形式 | 判準 | 給編輯的意義 |
|---|---|---|
| **記者連線** | 記者出鏡對鏡頭報導，主體是記者講話 | 可整段當連線帶播 |
| **SOT** | 有可直接使用的成音（受訪／官員談話或配好旁白的包裝） | 有料可掐 |
| **主播BS** | 只有畫面、無可用成音 | 只能當背板／背景畫面 |

判斷順序：先看有無可用成音→記者出鏡＝連線；受訪／旁白＝SOT；沒有＝主播BS。**拿不準標 `主播BS`＋`needs-review add`**（高估成音會害編輯排帶落空）。

**判讀流程（字幕優先省 token）**：
1. `yt-dlp --write-auto-sub --write-sub --sub-lang "ko,en,zh-TW,zh,ja" --skip-download` 先抽字幕，有就直接寫。
2. `yt-dlp --print "%(title)s|%(duration_string)s|%(uploader)s" --skip-download` 拿 metadata（時長由此取，不下載）。
3. 無字幕才 `yt-dlp -f "best[height<=480]"` 下載跑 `video_analyze`（frame_resolution 256）。
4. 形式判不準只抽數張低解析影格確認，不跑全片。

**狀態檔**：`--source YT`；`--id` 正規化成 `YT:{video id}`（原始網址寫法多變，用它當 id 會重複收錄）；檔頭統計歸「其他」。

## 4b. 整併時的分類修正（歸錯位要歸位）

「沿用既有 `category`」是省 token 預設不是鐵律，與「同組必須相鄰」衝突時**歸位優先**。三個修正時機：
1. `raw_entry` 被覆寫時（pending→has_script）順便重檢分類——稿到後全貌常與初判不同。
2. 同事件素材已在其他大分類 → 併過去同組相鄰，不要兩邊各留一半。
3. 23:00 最終整併順掃「同事件跨大分類分裂」。

機械判準：slug 前綴相同、備註含相同專名（地名＋事件詞）＝同事件。拿不準→維持沿用＋`needs-review add`，不亂搬。

🔴 **整併 `_待整併/` 的批次時，T／C 跟 `set-category` 同一批下完**（2026-08-25 補）。
韓聯社／CNA／YouTube（[`17`](17-網址素材整併.md)）的交件檔**檔尾附了一行
`id=T/C;…`**，直接貼進 `set-tc --pairs` 即可，不必自己重判。
⚠️ ENEX／ABC（[`18`](18-交換平台素材整併.md)）與側錄（[`14`](14-S2b-側錄轉譯摘要.md)）
不走這條——那兩條是各自的 agent 直接寫進狀態檔，你不用管。
⚠️ 交件檔沒附那一行時才自己判，名單見 [`13f`](13f-S2-大分類與各站規則.md)。
⛔ **不要留到 render 之後**——那樣網頁與 txt 會停在補標前的覆蓋率（T10）。

## 5. 防卡設計（低階 agent 必讀）

> 路徑順序：§1a（首選）→ §1b（第一層退路）→ 本節 UI 舊做法（最後退路）。8／8b 兩條只在 §1b 也失敗時用；其餘條目不分路徑一律適用。

1. **每輪開工第一步 `resume`**，緊接著 `scratch-dir --mmdd {晚班起始日MMDD}`——本輪所有中間檔（`_ap_*`／`_rt_*`／batch 等）寫進它印出的路徑，不要寫在 `自動掃帶系統/` 這層。日期用晚班起始日。
2. ⚠️ **`checkpoint` 一律 `{MMDD}-{HHMM}` 格式**：時段標記靠 checkpoint 裡的 4 位數字群判斷，只寫 `01:30` 會**無聲**退回 `△`（0806 實錯 85 則）。補掃後綴可加（`0806-0430-RT補漏`），日期不能省。
3. 🎫 NS 保活已由獨立排程負責（見 §1a-3）；收工前照 `s2_scan_prompt.md` 第 7 步最後摸一次 NS。
4. ⛔ **不准 `taskkill`／`Stop-Process` 殺 Playwright 或 node**（0806 實錯：agent 自救把整個 browser MCP server 殺掉，之後每輪都跑不動；排程情境沒人能重連）。profile 被佔 → `needs-review` 記下、跳過該站、往下跑。同理不准刪 profile 的 `Singleton*` 鎖檔。真正的互斥在呼叫端 `s2_scan.ps1`。
5. **RT 卡住跳站**：連續 2 次 Next 沒反應 → `needs-review add` 記 Edit No、跳去掃下一站。不死磕。
6. **半夜禁問**：無人值守遇到需確認的事 → `needs-review add`＋寫進交接檔備註，**繼續往下跑**。⛔ 不得 `AskUserQuestion` 等回應、不得停住。NS 登入過期是典型場景：記錄、跳過 NS。
7. **fallback 全部單向**：只往下走，不回頭重試上一步。
8. ⚠️ **RT UI 連掃＝單分頁 Next 鏈＋換頁後必 `scroll` 再讀**：`get_page_text` 讀的是擷取快照，SPA 換頁不刷新——換頁後立刻讀會**無聲拿到上一則內容**；純 wait／連讀兩次都無效，只有 `screenshot` 或 `scroll` 能強制刷新。每則四步：①從大列表頂端點最新那則進詳情②按 Next③**scroll 3–5 格**④讀取並**核對文中 Edit No 與 URL 一致**才寫入。不符→再 scroll 重讀；仍不符→照第 5 條跳站。
8b. ⚠️ **Next 灰掉＝進入路徑不對**：必須從 `all?media-types=vid` 大列表、頁面在最頂、點最新那則進去，‹ › 才綁得住。灰掉→回大列表重進；仍失效→逐張點卡進詳情（每則同樣 scroll→讀→核對）；窗內素材已捲出首屏且 Next 仍失效→記中斷點跳站。
9. **AP `/home` 偶發跳轉**（`/live`／`/home/foryou` 等）：每次讀列表前先確認 URL 是 `/home`，不是就重點側欄 Latest，最多重試 2 次，仍失敗跳站記錄。
9b. ⚠️ **NS 連線逾時（非 401）比照 AP `/home`（`13c2:387`）**：navigate 逾時最多重試 2 次，仍失敗即判定全站連線異常，直接記入 `needs-review` 跳站，禁止無限重試（0903-0100 連試 4 次逾時浪費時間與 token）。（2026-09-07 自 prompt 搬入，T13）
10. 🔴 **這一輪由你自己從頭做到尾，不准轉包**：⛔ 不准用 Agent／Task 工具把掃帶工作委派給子代理（`subagent_type` 開 agent 去「執行本輪掃帶」那種）。**你就是這輪的工作 agent**，掃描、分類、稽核、render 全部自己做完再收工。
    - **理由（2026-08-11 實錯，整輪被腰斬）**：1800 那輪的 agent 把整輪工作丟給背景子代理，自己沒事做就提早交卷；外層行程收工時子代理才剛掃完 NS＋AP，**背景任務只有 600 秒緩衝，時間到就被強制砍掉**——RT 一則沒收、稽核與 render 全沒跑到。
    - ⚠️ **這種死法完全無聲**：離開碼是 0、沒有任何錯誤訊息，log 看起來像正常結束。當輪還白燒了約 $2.9，而且補掃時清單得整個重撈一次。
    - 📌 **背景跑一支 `bash` 指令沒問題**（`find`／`ls` 這種 `local_bash` 是常態），禁止的是**把整輪的實質工作交給另一個 agent**。
    （2026-09-07 自 prompt 搬入，T13）
11. 🔴 **讀完規則要驗證有沒有被截斷**（2026-08-24 新增，R15）：Read 工具的結果約 30,000 字元就會靜默截斷——沒有錯誤訊息、沒有提示，就是安靜地少一半。
    **① 開工第一件事，先跑這一支（唯讀、不到一秒）：**
    ```
    python scripts/s2_rules_check.py
    ```
    它會印出**八份**必讀檔的尺寸、有沒有超出安全預算、以及每份的 `RULES-EOF` 代號。**它報 ❌ 就不要開始掃帶**，直接回報使用者。
    **② 每讀完一份，確認你看到了它最後一行的 `RULES-EOF` 標記。**沒看到＝那份被截斷了，立刻用 `Read` 的 `offset` 從斷點往下補讀，直到看到標記為止。
    **③ 開工回報要附一行**，列出實際讀到的代號：
    ```
    規則載入：13 ✅／13e ✅／13f ✅／13c ✅／13c1 ✅／13c1b ✅／13c2 ✅／13c3 ✅
    ```
    **少任何一個就不要開始掃帶**，先補讀完。
    （2026-09-11 訂正：例句原本仍列 V8 前的 `13d`／`13g`／`13h`——那三份 2026-09-07 已退役刪檔、`13c1`／`13c1b` 反而沒列，照抄就會漏讀兩份；已直接改成現行八個代號）
    （2026-09-07 自 prompt 搬入，T13）
12. ⚠️ **開工先看：上一輪有沒有整站失守**：`resume` 之後**看一下 `needs-review`**。若出現「某站 N 則不是站方無素材，是進不去」這類記錄，代表**那段窗根本沒被掃過**——本輪要**先把那段窗補掃回來**，再處理本輪的新素材。
    ⚠️ **不要假設上一輪有掃到東西。**0807 22:00 輪三站全部進不去、收錄 0 則，但狀態檔與交接 txt 照樣產出、離開碼也是 0——**空輪看起來跟成功的一輪一模一樣**。
    ⚠️ 窗拖越久，素材越可能已經捲出站方清單範圍。撈不回來的就記 `needs-review` 寫明，不要裝作沒事。
    （2026-09-07 自 prompt 搬入，T13）

## 5c. 排程啟動器：`scripts/s2_scan.ps1`

排程不走 `CronCreate`，由 Windows 工作排程器叫：

```
pwsh -NoProfile -File "E:\GitHub\TVBS-AIHunter\scripts\s2_scan.ps1" -Model opus
```

| # | 做什麼 | 為什麼 |
|---|---|---|
| ① | 互斥鎖（OS 層獨佔檔案握把） | 程序當機握把由 OS 回收，不留死鎖 |
| ② | 撞鎖就跳過不排隊（記 `_跳過紀錄.txt`，離開碼 0） | 漏的事後補掃，比兩輪打架好 |
| ③ | checkpoint 由它算（`{MMDD}-{HHMM}`） | prompt 寫死的值幾小時就過期 |

- prompt 範本 `scripts/s2_scan_prompt.md`（`{CHECKPOINT}` 代換）——範本只放本輪參數，⚠️ 不複製規則內容（會過期並與本檔衝突）。
- 跑完會驗 `{MMDD}-s2-state.json` 與 txt 在不在——**離開碼 0 不等於掃帶做完了**。
- 排程設定完先 `-DryRun` 跑一次。⛔ 這支腳本自己也不殺任何進程。

## 5a. 每天第一輪（建檔輪，17:00）的額外動作

> 🔴 **歸檔與建檔已由腳本做掉**：`s2_scan.ps1` 的 `New-ShiftState` 在 `{MMDD}-1700` 開頭的輪次
> ①建好 `{MMDD}-s2-state.json`（`window_start` 直接寫好＝當天 13:00）②把上一班的檔（含 `.html`）
> 全部搬進 `Archive/{YYYYMMDD}/`。冪等，重跑不會重建也不重複歸檔。
> `default_file()` 遇到「已過 17:00 卻沒有今天的檔」會直接報錯，不再靜靜用昨天那份。
>
> **你要做的只剩**：開工確認 `{MMDD}-s2-state.json` 是今天日期、且 `items` 是空的。
> 不是就**停下來回報**，⛔ 不要自己建、不要退回用昨天那份。

其餘只在建檔輪做一次的事：

| # | 動作 | 說明 |
|---|---|---|
| 1 | **確認自己真的是建檔輪** | ⛔ 「共 0 則」不是證明，它同時是最常見的失敗形狀（`resume` 沒帶對 `--file` 就會 0 則）。**兩個旁證缺一不可**：①現在是 16:00 那一輪嗎（建檔輪只有 16:00，其餘 11 輪一律續掃）②資料夾裡還有沒有 `{MMDD}-s2-state.json`——有就是它還在服役（晚班交接檔**跨夜**，`checkpoint` 是今天日期就不准歸檔）。0809 實錯：還在用的 0808 檔被歸檔、資料斷成兩份 |
| 2 | `resume` | `--file` 一定明寫（自動挑最新是防呆，不是授權不寫） |
| 3 | `scratch-dir --mmdd {MMDD}` | 建當晚暫存資料夾 |
| 4 | `window_start` | 腳本已寫好＝**前一天 13:00**（排程設計上的交界點）。⚠️ 不要照「前一檔實際跑到哪」算——漏跑時會把失守窗算進涵蓋範圍；失守窗用 `needs-review` 記，不是靠 window_start 表達 |
| 5 | **機動格（第一格大分類）** | ⛔ 使用者沒指定就**空著**，不自行開、不沿用昨天 |
| 6 | pending 清查 | 跳過（新檔沒有 pending） |
| 7 | 跨日主題檢查 | 不用手動——render 完 `s2_topic_dedupe` 自己讀前一天的檔 |
| 8 | `list-topics --yesterday` | （P1b-2，2026-09-07）建檔輪第一站 `add-batch` 前跑一次：列前一天 ≥3 則的中主題＋charter，**同事件沿用昨天的名字**，不要另起爐灶（0805 實錯：同一事件兩輪兩個名） |

## 10. 工具層防護：臨時 `python -c` 檢查 json 會被 hook 直接攔下（2026-08-18 起）

launcher 掛了 PreToolUse hook（`scripts/s2_bash_guard.py`，MASTER D9）：
**Bash／PowerShell 指令裡出現 `python`/`python3`/`py` 的 `-c` 內聯程式、
且碰到 `.json` 檔或 `-s2-state.json` 狀態檔時，呼叫會被拒絕**。

- 被攔到不是錯誤，是指路——**deny 訊息裡就有可直接貼的替代指令**
  （`s2_batch_prep.py inspect/search/compare/dedup-check/snapshot` 或
  `s2_state.py show/…`），照著換工具重下即可，**不要**改寫成其他變體
  （heredoc、先寫 .py 檔再跑、PowerShell `ConvertFrom-Json`）去繞——那還是 §4 違規，
  遙測看得到。
- 純計算（`python -c "print(...)"` 不碰檔）、`.txt`、呼叫 repo 標準腳本都不受影響。
- 字數檢查（摘要 150 字上限）用 `inspect --lengths`，不必也不准自己寫 python 數。

## 11. hook 補洞：Write 落 `.py`／跑存檔腳本也擋，並收編跨站時間軸對帳（2026-08-30）

**背景**：§10 的 hook 只擋 `python -c` 內聯程式，MASTER A9③ 早就記過繞道
變體——`Write` 先落一支 `.py` 檔（hook 完全看不到 Write），再用
`python 檔名.py` 執行（不是 `-c`，正則對不上）。0830-0730 輪實錯：自寫
`_mk_audit_snapshots.py`／`_mk_ns_snapshot.py` 兩支腳本，把 AP／RT／NS
三站清單的上站時間換算成台北當地時間，好判斷跟前一輪有沒有銜接落差——
NS 站因此多花數分鐘、`inspect` 被叫到 34 次（當天最高）。

- ⛔ **`Write` 工具寫一支不在 `scripts/` 目錄下的 `.py`** → deny。
- ⛔ **Bash／PowerShell 執行一支不在 `scripts/` 目錄下的 `.py`**（跑存檔腳本，
  不是 `-c`）→ deny，堵住「Write 先落檔→再跑」這條完整路徑。
- 判斷邏輯只認路徑裡有沒有 `scripts` 這一段，不維護檔名白名單——加新工具
  不用跟著改 hook。

**這次不是「先擋再說」**：查過 0730 那兩支腳本的實際內容，換算邏輯是真正
的機械工作（NS `created`／AP `ts` 是 UTC 要 +8h，RT `at` 已經是台北當地
時間、13c2 §1b 原文寫「判窗內外用」不可再轉）——**這是真缺口，不是「工具
在但沒被想起來」**，所以先把它收進 `s2_batch_prep.py` 再上擋，不是空手
攔死一條沒有替代路徑的合法需求：

```
python E:/GitHub/TVBS-AIHunter/scripts/s2_batch_prep.py timeline <raw檔> --site ns|ap|rt [--out <檔>]
    # 印「id｜MM/DD/YYYY HH:MM（台北當地時間）」，依時間排序、缺時間的印警告不吞
    # NS／AP／RT 三站皆自動 UTC+8（RT 的 at 欄位也是 UTC，見下方「RT 時區訂正（B1）」，
    # 舊版曾誤判 RT 免轉，已修正）
```

跨站對帳／查某輪窗內外有沒有漏收，一律用這個，不要再自己算時區。

**2026-08-31 補查（原文有誤，已訂正——見下方「二次訂正」）**：§11 上線後使用者
要求「再確認有沒有漏掉工具缺口」，抽查 0808–0830 部分 Write 落 `.py` 的實例
對照內容，查到兩個一樣機械、一樣沒有現成工具的缺口（其餘抽查到的實例是編輯
內容本身——`_fix_ns_bite.py` 這類手寫 entry／BITE 文字——或單輪一次性資料
修正，本來就該逐輪自己判斷，不算工具缺口）：
⛔ **原文宣稱「22 次全部撈出來」是假的**——當時只抽查了一部分，`D:\Downloads\S2掃帶log\`
實際有 195 份 log，獨立 review 用 grep 重掃後估計約 96 支不同的一次性 `.py`、
約 108 個實例、散落在 34 份 log 裡，差了約一個數量級。詳見下方「二次訂正」。

- **raw 正文回填 batch 的 `src_text`**：`_merge_src_text.py`／`_merge_src_ap.py`／
  `_merge_src_rt.py`／`_fill_ap_srctext.py`／`_rt_add_srctext.py`／`_fix_src_0730.py`
  在 0818／0820／0825／0828／0829 至少六次重複同一件事——raw／detail 檔有正文、
  batch.json 裡對應的 `src_text` 還空著，逐 id 對應貼過去。已收進：

  ```
  python E:/GitHub/TVBS-AIHunter/scripts/s2_batch_prep.py fill-src-text \
      --batch <batch.json> --raw <raw/detail檔> --site ns|ap|rt
      # 只填 src_text 為空的那幾則，不覆寫已經有內容的（避免蓋掉手動修過的原文）
      # --fields 可覆寫取正文的欄位順序，預設 NS=desc/script、AP=head/script、RT=head/story
  ```

- **合併多份 json 陣列檔（分頁清單、多來源分批 batch）**：`2026-08-31` 補查
  的第三個工具——不是原本這波查出來的，是**手動補跑 0831-0114 那輪時被 hook
  攔下**（想用臨時 `python -c` 把 `ap_batch_0114.json`／`ap_sntv_batch_0114.json`
  兩份 AP 分批檔合併，被 deny 沒有替代路徑）才回頭全庫重掃，結果找到
  **23 個命中、約 9-10 個不同日期**（0813／0814／0820／0821／0825／0827／
  0829／0831）——比 `fill-src-text` 出現得還頻繁，形狀卻更單純（純合併陣列，
  頂多再去重，沒有 `timeline` 那種要猜時區的語意風險）。已收進：

  ```
  python E:/GitHub/TVBS-AIHunter/scripts/s2_batch_prep.py concat <檔1> <檔2> [<檔3> ...] [--site ns|ap|rt] [--out <檔>]
      # 給 --site 才去重（保留第一次出現的 id，其餘丟棄並警告）；不給就純合併全部保留
      # 例：分頁清單合併用 --site 去重；三站 batch 合併（0813-1800 那種）不給 --site
  ```

- **三站 batch.json 的 `category` 收成 `set-category --pairs` 字串**：
  `run_setcat_1000.py`（0828-1000，原文誤寫成「0827/28-1000」）示範了這個形狀——
  agent 習慣先把分類標在 batch.json 各則的 `category` 欄位草稿裡，三站掃完後
  再收成一條 `--pairs` 字串送出。⚠️ **獨立 review 覆核：這個形狀目前只找到這一次
  真實案例**（`_tmp_ns_cat.py` 0811-0430 那次是不同形狀，手寫 `{id: (大,中,小)}`
  字面量直接餵 `set-category`，沒有讀 batch.json），證據強度比 `timeline`／
  `fill-src-text`（各 6+ 次）弱很多，比較接近「單一實例但形狀合理，先備著」而
  不是「已驗證的高頻缺口」。工具本身邏輯查過沒問題（見下方 review 結論），
  先留著，之後有沒有再出現同一形狀再觀察。已收進：

  ```
  python E:/GitHub/TVBS-AIHunter/scripts/s2_batch_prep.py collate-category <batch1.json> [<batch2.json> ...]
      # 只組字串印出來，不呼叫 set-category——送不送、送之前要不要再看一眼，仍是你的判斷
      # 缺 category（或缺大分類／中主題）的則不會收進來，印警告列出
  ```

⚠️ **原本判斷「NS HTML 清理是閱讀輔助、沒有進 src_text」是錯的，已被獨立
review 用原始碼證據推翻**：原文只抽到四次（0821／0824／0828／0829），且說
「看起來是閱讀輔助」；獨立 review 重新在抓出的腳本原始碼裡逐支查了一遍，
命中 **9 支**（`_strip_ns`／`_clean_ns`×3／`_clean_ns_src`／`_ns_striptext`／
`build_ns_batch_0730`／`_build_ns_batch`×2），其中 **5 支的輸出確實寫進了
`src_text`**（例：`_build_ns_batch...` 裡 `src_text = strip_html(rec.get('script',''))`
後直接 `"src_text": src_text` 落進 batch）。也就是說：
①「不是要進 src_text」這個判斷本身不成立，清乾淨版本**歷史上已經進了生產
`src_text`**；②`13c2` §5「站方原文帶什麼標記就該存什麼」這條慣例**在現有 state
資料裡早就不一致**（部分則被剝過標記、部分沒有），不是一個尚未發生、可以
擱置的假設性問題，而是已經存在的資料不一致；③新 hook 上線後這個 6–9 次量級
的重複需求會被硬擋，而 `SCRIPT_HINT` 目前**沒有指向任何替代路徑**——下一輪
NS 站真的需要清 HTML 時很可能卡在 deny 迴圈裡出不去。
**這件事需要使用者裁決**（要不要把 `src_text` 的定義從「原始標記」改成
「清乾淨後的文字」，或是另外開一個不影響 `src_text_of` 的獨立閱讀輔助工具），
在裁決之前本檔不擅自選邊；deny 訊息也暫時不指路到不存在的工具。

**另查到但尚未處理的可能缺口（needs-review，非本輪範圍）**：獨立 review 估計
還有約 57 個實例／27 份 log 是「載入 raw → 建 id 索引 → 組 `{id, source,
checkpoint, status, entry, src_text, …}` → 落 batch.json」這個外殼樣板，理論上
`cmd_build`（`s2_batch_prep.py build`）已經做這件事，但這批腳本仍手寫了一份——
是 `build` 沒被想起來，還是 `build` 的欄位形狀（`entries.json` 分離檔）跟 agent
實際習慣（entries 字面量直接寫在同一支腳本裡）對不上，兩種可能性沒有進一步查證
就不確定該不該再做第四個工具（RT 時區那次教訓：形狀猜錯比沒有工具更危險）。
下次有餘裕時再深入查證，不在這次倉促補上。

**RT 時區訂正（B1，最重要的一條）**：原本以為 RT 的 `at` 欄位已經是台北當地
時間、`timeline` 對它免轉換——這是**錯的**，獨立 review 用 `20260830/_rt_list_1600.json`
（落地台北 16:08，40 筆 `at` 最大值卻是 `08/30/2026 07:57`，比擷取時間早 8 小時
11 分）對照同輪 AP `ts` 換算 UTC+8 後（16:27）跟擷取時間吻合，交叉驗證出 RT 的
`at` 其實也是 UTC，需要跟 NS／AP 同樣 +8h。已修正 `TIMELINE_SPEC`，原本會讓
`timeline` 對 RT 站每次都算錯 8 小時、而且不報錯不警告，比沒有工具更危險。

**hook 生產環境漏擋根因找到並已修（2026-08-31c）**：0831 五輪體檢查到 0430 那輪
14 次 `python -c` 全數沒被 hook 攔下（0730/1000 也各有部分漏擋），使用者另開
session「萬用」深入調查，查到真因**不是** MASTER A11 記過的「Claude Code 平台層
flakiness」——是本 repo `s2_bash_guard.py` 自己的 bug：`main()` 原本用
`json.load(sys.stdin)`，這條路徑吃**行程 locale 編碼**（本機 Windows 預設
cp950）解 stdin，但 Claude Code 送進來的 payload 是 UTF-8。中文字的 UTF-8
位元組在 cp950 下是雙位元組序列，`\`(0x5C) 剛好落在 cp950 合法尾位元組區間，
於是**指令裡的路徑只要「中文字後面緊接反斜線」**（`G:\我的雲端硬碟\…` 這種
Windows 慣用寫法）JSON 的 `\\` 就會被吃掉一半、解析失敗，被 `except Exception:
return 0` 的 fail-open 靜默吞掉、整條指令直接放行；路徑寫正斜線
（`G:/我的雲端硬碟/…` 或 `/g/我的雲端硬碟/…`）則正常 deny。0831 五輪 22 筆
`python -c` 逐筆對照路徑寫法與實際攔截結果，100% 吻合此規律。

**已修**：`json.load(sys.stdin)` → 自行讀 `sys.stdin.buffer.read()` 再固定用
UTF-8 解碼，不再讓 locale 決定；並新增 `scripts/_guard_calls.log`（每次呼叫留
一行 DENY／ALLOW／ERROR），讓「hook 沒觸發」與「觸發了但放行」以後不再無法
區分。沒有動任何 regex／判定邏輯。41/41 單元測試 PASS，24 筆真實歷史 payload
重放驗證修正前後（commit `e1f639c`）。**MASTER A.md 的 A11 結論已追加 A11-訂正
一列推翻**（只加不改，原敘事保留）。A11-1000 記過的另一個獨立根因
（`--setting-sources project` 沒載到 hook 導致 scratch cwd 下 hook 沒註冊）
不受本次推翻影響，兩者可能疊加發生，遇到漏擋仍要兩邊都排查。

⚠️ 待辦、未動工：repo 內其他走 `json.load(sys.stdin)` 的 hook／腳本要掃一遍
同一顆地雷（本機 cwd 本身就是 `G:\我的雲端硬碟\…`）。

## 收工清單總表（跨檔案索引，2026-09-07 自 13f 尾遷入 `13c3`，V8）

> 本表**只指路，不重抄內文**——各步細節看「規則出處」欄對應的檔案／節次。
> 原規劃放 `13c2` §6，2026-09-07 自 `13f` 尾遷入本檔（V8 五層合一）。

| 步 | 動作 | 規則出處 |
|---|---|---|
| 0 | `s2_apply_reclass.py --dry-run` → 套用；`s2_mark_ingested.py --apply` | `13`「入庫後不必手動清」／`13c2` §2a-2 |
| 1 | 三站清單各存一份快照到 scratch dir | `13c2` §2a-2 |
| 2 | `s2_audit.py --mmdd … --rt-list --ap-list --ns-list`；ENEX／ABC 走 `s2_platform_reconcile.py` | `13c2` §2a-2／`13c1` V5-5／`13c1b` V7-5 |
| 3 | 稽核嚴重級處理或 `needs-review add` | `13c2` §2a-2 |
| 4 | 逐則判重大：`set-alert`（前三）／`patch-entry --alert`；涉臺至少 🟡 | `13e` 重大提醒行 |
| 4.5／4.6 | ENEX／ABC platform 三件套（`--in-round`）＋ `set-tc` | `13c1` V5-3／`13c1b` V7-3 |
| 5 | `s2_topic_review.py --compact` 看整張表、`set-category --pairs` 合併 | `13c2` §3b |
| 5.5 | ⚠️ **執行順序鐵律：各站素材入庫完畢 → `set-top checkpoint {CHECKPOINT}` → `set-tc` → `render`**（開局絕對不要下 `set-top`；理由：`set-tc` 配額桶按頂層 checkpoint 分桶，開局就下若中途斷線會使整段窗永久漏收） | 2026-09-03 ddc71b9（**取代**本檔上方「兩個時序陷阱」②的 8/25 舊規則，2026-09-07 R23 裁定） |
| 6 | `s2_render.py`，看收工閘門喊什麼 | `13c2` §3 |
| 7 | 關瀏覽器前摸 NS landing ＋ ABC cmspage（硬性） | `13c1b` V7-5 |
<!--ENDBODY-->

<!-- RULES-EOF 13c3 2026-09-07 — V8 五層合一，讀到這一行才算讀完本檔；沒讀到＝被截斷，必須用 offset 補讀。 -->
