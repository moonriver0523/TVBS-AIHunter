# S2 定時掃帶 V4（疊在 V3 上的增量）——**現行版本**

> **狀態：2026-08-13 生效**（疊在 [`13c`](13c-S2-定時掃帶-v3省token.md) 上）。
> **2026-08-14 增補**：§7 訂正（AP Page2 不可信）＋新增 §8／§9（0814-1200 體檢）。
> **2026-08-18 增補**：§4 工具升級（AP `_source` 看穿／`--lengths`）＋新增 §10
> （D9 hook：臨時 `python -c` 檢查 json 會被工具層攔下並指路）。
> **2026-08-30 增補**：新增 §11（D9 hook 補洞——連 Write 落 `.py` 檔、`python 檔名.py`
> 執行存檔腳本都攔；新增 `s2_batch_prep.py timeline` 收編跨站時間軸對帳）。
> **2026-08-31 補查**：§11 追加兩個同批查出的工具缺口——`fill-src-text`（raw
> 正文回填 batch 的 src_text）、`collate-category`（batch.json 的 category 收成
> `set-category --pairs` 字串）；NS HTML 清理需求記錄但故意不收編（見 §11 尾段）。
> **2026-08-31b 獨立 review 訂正**：上面這波補查本身有多處查證不足——
> 「22 次全部撈出」是假的（只抽查一部分）、`run_setcat` 的 0827 是編的、
> NS HTML 清理其實出現 9 次且有 5 次已經寫進生產 `src_text`（不是「閱讀
> 輔助」，是已發生的資料不一致，需要使用者裁決）。**更嚴重的是 `timeline`
> 對 RT 站的時區判斷是錯的**（誤判 `at` 免轉，實際也是 UTC，導致每次算
> 錯 8 小時且不報警告）——已用真實落檔交叉比對修正，`TIMELINE_SPEC`／
> 對應測試都已更新。hook 的 `_is_repo_script()` 也補了兩輪：先前只認路徑
> 有沒有 `scripts` 這一段，任何叫 `scripts` 的資料夾都能繞過（含 repo 裡
> 確實存在的 `scripts/_tmp/`），現在改成錨定在 repo 根目錄或 `s2_`／
> `test_s2_` 檔名慣例；另外補上串接指令（`&&`）與 heredoc 兩種原本沒擋到
> 的繞道路徑。
> 排程 agent 讀的規則堆疊＝`13` ＋ **`13e`** ＋ **`13f`** ＋ `13c` ＋ **`13c2`** ＋ `13d`（本檔）。
> ⚠️ **2026-08-24 訂正（R15）**：
> ①Read 結果約 30,000 字元會靜默截斷，`13`（53,676 字元）與 `13c`（42,998 字元）
> 實測每輪都只載入約一半。已切成 `13`＋[`13e`](13e-S2-素材行與分類規則.md)＋[`13f`](13f-S2-大分類與各站規則.md)
> 與 `13c`＋[`13c2`](13c2-S2-定時掃帶-v3省token-下.md)，**逐字保留、未改寫**，六份全部必讀。
> ②⛔ **`13b` 不在堆疊裡**——`s2_scan_prompt.md` 早就明令不要讀它（已改為歷史檔案），此處原文有誤。
> ③本檔引用的 `13c §N` 節號**未變動**，其中 §1b 之後的節已搬到 `13c2`，節號照舊可對照。
> 本檔**只記這波的變更**，不重抄 13c 全文；13c 未提到的內容照舊沿用。
> **Rollback**：把 `scripts/s2_scan_prompt.md` 規則清單第 3 項拿掉即可回退到純 13c；
> 本檔原檔不動、永久保留。

## 1. 中間暫存檔一律直接寫 scratch-dir 絕對路徑，禁止先落 repo 根目錄再搬

**⛔ 這是硬規則，不是建議**：batch/list/detail 這類本輪中間檔（`batch.json`／清單快照／
卸載檔整理結果等），**開檔當下就寫到 `s2_state.py scratch-dir --mmdd {晚班起始日}`
印出的資料夾裡**，不准先落在 repo 根目錄再搬過去。

- **2026-08-13 D7 起，你的工作目錄一開場就是本班 scratch 夾**（launcher 設好才叫你）——
  相對路徑落檔（`ns_list.json` 這種寫法）天然就落在對的地方，這是最省事的合規方式。
  呼叫 repo 腳本一律用絕對路徑或先 `cd "E:/GitHub/TVBS-AIHunter"`（你本來就這麼做）。
- 需要確認路徑時再跑：
  ```
  python scripts/s2_state.py scratch-dir --mmdd {晚班起始日}
  ```
  本輪剩下的暫存檔都寫進這個資料夾，路徑記住就不用每次重跑。
- ⛔ **以下三種變體都算違規**（0812-2200／0813-0100／0813-1000 三輪各撞一次實錯，
  每輪多花約 3 分鐘在「發現寫錯地方→搬檔→確認 git status」上）：
  1. `Read`＋`Write` 先讀到 repo 根目錄再另存到 scratch dir。
  2. PowerShell 先 `New-Item`/`Set-Content` 到 repo 根再 `Move-Item`。
  3. `python -c` 先寫檔到當前工作目錄（通常是 repo 根）再 `mv`／`Move-Item` 搬移。
- 這條跟 13c §0-2 的「卸載檔算本輪中間檔，事後搬進 scratch-dir」不衝突——本條是把
  「事後搬」提早成「一開始就別落錯地方」，兩條同時成立：能一開始就寫對路徑的，不要
  再繞去 repo 根目錄；卸載檔那種由工具層自動決定落點的，才照 13c §0-2 事後搬。

## 2. `set-category` 改「一站一批」，取代舊「一輪一次」

13c §2 原規則（2026-08-12 立規）要求 `set-category`／`update-entry`／`add-batch` 三個
寫入指令**一輪各只叫一次**。實測發現「一輪一次」在長輪次（含建檔輪、補掃窗較長的輪次）
會把分類累積到收工前才送出，中途沒有查核點，一旦某站分錯要等到很後面才發現。

**新規則：`set-category` 改成「一站一批」**——NS／AP／RT **各站掃完各下一次**
`set-category --pairs`，不必等三站全掃完才一次送出。

- `update-entry`／`add-batch` 仍照 13c §2 原規則，**一輪一次**（不受本條影響）。
- **收尾允許 ≤1 次補刀**：三站都跑完、`topic_review` 看過整張表、發現同義／同事件
  需要合併分類之後，**最多再補一次** `set-category --pairs` 做收斂調整。
  超過這一次補刀就是踩線——真的還有錯，累積到下一輪或用 `needs-review add` 記錄，
  不准為了「這則也要改」再叫第三次。
- 順序仍是**先累積草稿、看過沒問題才送出**（13c §2 ①b 稽核前置那條不變）：
  一站的分類清單在心裡／草稿湊齊，確認沒問題才下 `set-category`；不是「掃到一則
  就下一次指令」。

## 3. 分類工具新用法：`--compact` 為每輪預設用法

`list-topics`／`topic_review` 新增 `--compact` 旗標，**每輪預設用這個**（省掉開新
中主題前、render 前各要讀一次全表時的 head/tail 雙讀）：

```
python scripts/s2_state.py list-topics --compact
python scripts/s2_topic_review.py --file "<狀態檔>" --compact
```

- 開新中主題前（13c §2 的 `list-topics` 那步）、render 前檢視中／小分題
  （13c §2a-2、`s2_scan_prompt.md` 步驟 5 的 `s2_topic_review.py`）**都改用
  `--compact` 版本**，不必再另外讀一次未壓縮輸出。
- 需要看小分題明細時，`list-topics` 仍可加 `--subs`（`--compact --subs` 可並用）。

另外新增 `show --uncat`，用來查「有 id 但分類欄位還沒填」的項目：

```
python scripts/s2_state.py show --uncat
```

批次 `add-batch` 後想確認有沒有漏 `set-category` 的則，用這個查，比逐則 `get` 快。

## 4. 檢查 scratch raw 一律用 `s2_batch_prep.py` 的 inspect/unwrap 子指令

⛔ **不要每輪即興檢查暫存 raw 檔**——0813 三輪各用了不同的臨時工具（`Read` 整檔
讀進 context、PowerShell 切片、`python -c` 寫一次性腳本），三種變體都算違規，
理由跟 13c §2「不准自己寫 python 讀狀態檔」同一套（呼叫次數與臨時腳本自傷風險）。

**改用 `s2_batch_prep.py` 的兩個子指令**：

```
python scripts/s2_batch_prep.py inspect <raw檔路徑> [--ids A,B] [--fields f1,f2] [--limit N] [--site ns|ap|rt] [--lengths]
    # 摘要模式印筆數＋id＋標題行；讀取時自動卸殼，不整包讀進 context
    # --lengths 只印各欄位字數不印內容（檢查摘要 150 字上限就用這個，不要自己寫 python 數）
    # 🔴 --ids 一次帶多則。全文總長吃得下 28,000 字元就整批印全文，塞不下才退預覽並
    #    附分批指令。查 1 則跟查 20 則**成本一樣**（每次呼叫都重付整個 context），
    #    逐則翻是純浪費（T9：0825-0100 有 31 次一次只看一則，約 $2.5）
python scripts/s2_batch_prep.py unwrap  <raw檔路徑> [--out 檔]
    # 卸殼成裸陣列另存 _unwrapped.json；無殼會明講「原樣複製」不報錯
python scripts/s2_batch_prep.py search  <raw檔路徑> --contains 關鍵字 [--field script|story|head|all]
    # 列出命中項 id＋命中欄位＋前後片段
    # ⚠️ --contains 搜的是「欄位的內容」，不是欄位名稱
python scripts/s2_batch_prep.py snapshot <raw檔路徑> --site rt --checkpoint 0817-1600
    # 壓成純文字稽核快照（預設落 _audit_{site}_{HHMM}.txt，同目錄）
python scripts/s2_batch_prep.py compare --raw <raw檔> --batch <batch檔> [--site rt]
    # 只印差異：raw 有 batch 沒有／batch 有 raw 沒有／batch 缺欄位／batch 內重複 id
python scripts/s2_batch_prep.py dedup-check <raw檔> --ids A,B[,C] [--site ns] [--fields script,desc]
    # 兩則以上逐字比對，判斷是不是同一則的不同版本
```

- 🔴 **要確認「站方到底有沒有給稿」，用 `inspect --lengths`，不要用 `search`**
  （2026-08-18 A13 實錯）：`search --contains script` 是拿 **script 這五個字母去搜
  欄位的「內容」**，不是問「有沒有一個叫 script 的欄位」——正常的稿子內文裡本來就
  不會出現 "script" 這個字，所以它回 0 命中是**正確行為**，不代表沒有稿。
  查欄位在不在、有多長，一律 `inspect --lengths`（不指定 `--fields` 會把該筆
  所有文字欄位連同 AP 的 `script.nitf`／`caption.nitf` 一起列出來）。
  - ⛔ **「工具說沒有」不等於「站方沒給」，退 §1b 之前先換一支工具確認**。
    0818-2200 就是這樣賠掉的：AP API 其實成功、15/15 筆都有完整 SHOTLIST／
    SOUNDBITE，但當時的 `--lengths` 有 bug 沒列出 `script`（已修）、`search`
    又被誤用，兩個訊號都像「沒有稿」，於是整站退 §1b **逐則開了 14 個詳情頁**，
    12 則燒掉 89 次呼叫／10.5 分（前一輪同站 14 則只要 31 次／5.7 分）。
  - `--lengths` 現在會分清楚 **`<無此欄位>`**（真的沒有）與
    **`<欄位存在但非文字>`**（欄位在、只是不是文字，例如 `shots` 是陣列）——
    看到後者不要當成站方沒給。

- **`compare` 在 `add-batch` 之前跑**最划算：它一次查完「有沒有漏收」「有沒有
  來源不明的 id」「有沒有整批漏帶 `src_text`」（13d §5 那個踩過兩次、合計 145 則
  的坑）。乾淨就只印一行，不會佔 context。
- **判斷「這兩則是不是重複」一律用 `dedup-check`，不要自己寫 python 算 `a==b`。**
  它把四種情況分開講：逐字相同／只差空白／**只差 HTML 標記**（NS 的 script 夾
  `<p>`、`<b>`，同一則的不同剪輯版常常只差在這）／真的不同（並指出剝掉標記後
  第幾字起不同）。0817-2200 那輪 NS 去重就是缺這個工具，花了 5 次臨時 `python -c`。
  ⚠️ 它**只回答機械問題**——要不要當成重複、留哪一則，是編輯判斷，工具不做也不猜。
  id 打錯或找不到會直接失敗中止，不會默默只比得出來的那幾則。
- **`snapshot` 不覆寫舊檔**——快照的價值就是「當時長什麼樣」，覆寫等於靜默銷毀
  稽核依據。要另存就給不同的 `--out`。
- 兩個都吃 `--site`：**RT 的 id 欄位是 `code` 不是 `id`**，不給 `--site` 會退回
  通用猜測。
- **AP 清單檔（ES 形狀）自動看穿 `_source`**（2026-08-18 起）：`inspect`／`search`／
  `snapshot`／`dedup-check` 的 id 一律解成 `AP<editorialid>`（跟 state 對得起來的那個），
  `--fields headline` 這類欄位也直接取得到——不必再自己剝 `_source`。

- 用途是**檢查**（結構、筆數、有沒有拆對層），不是 13c §1a 開頭撤回的那套強制
  `raw.json → dump → entries.json → build` 三步流程——那條「規定要用整套流程」的
  規則仍然撤回，`browser_evaluate` 抽完直接累積 `batch.json` 仍是現行主路徑。
  `inspect`／`unwrap` 只在**需要檢查某份 raw 檔**時才叫，不是每輪必經步驟。
- 兩個子指令由另一支 agent 同步實作中；若指令還不存在或報 `unrecognized arguments`，
  照 13c §2「腳本連續失敗 2 次」退化：記進回報，當輪改最小必要的一次性讀取繼續，
  不卡住，收工後在回報裡註明工具還沒到位。

## 5. batch 每筆**必帶 `src_text`**（站方原文）——NS 站送出前必查，不准事後回補

`add-batch` 的每一筆都要帶 `src_text`（抽取白名單瘦身後的站方原文，13b §543）——
它是事後離線查證的**唯一依據**，漏帶就得重開瀏覽器。已知整批漏帶四次，**全部發生
在 NS 站**：0812-2200（65 則）、0813-1200（80 則）、0818-1000（39 則）、
0821-0730（35 則），AP／RT 至今未曾中招。

- 🔴 **2026-08-21 訂正（MASTER R9 第四次復發後立規）**：原本「`add-batch` 印警告、
  看到當輪回補」這套**事後補救機制驗證過擋不住**——四次都是警告照印、agent 照樣
  先送出再回頭補一輪 `_fix.json`＋`update-entry`，每次多燒 4–5 分鐘。警告的時機
  在送出**之後**，來不及。
- ⛔ **NS 站 `add-batch` 之前，必跑一次**：
  ```
  python scripts/s2_batch_prep.py compare --raw <NS原始清單/raw檔> --batch ns_batch_{HHMM}.json --site ns --require src_text
  ```
  印「無差異」才准 `add-batch`；印「batch 缺欄位」就地把 `ns_batch_{HHMM}.json`
  補齊 `src_text` 再送出，**不准先送出再回補**。
- 萬一還是漏網送出了（`add-batch` 照樣會印 ⚠️ 列出漏帶的 id 當最後一道防線）：
  ```
  python scripts/s2_state.py update-entry --batch <檔>
  ```
  批次檔每筆只要 `{"id": "...", "src_text": "..."}` 就是純回補原文，不動稿子本體；
  單筆也可用 `update-entry --id X --src-text-file <檔>`。
- AP／RT 目前沒中招，暫不強制跑 `compare --require src_text`，但順手查一次不吃虧。
- 🔴 **2026-08-21 補充（MASTER A18）**：AP 站另有一組不同機制的復發——清單未套白名單瘦身
  （整包 ES 回應落檔，850~930KB／50 則）＋詳情擷取解包失敗（`APundefined`／欄位全空），
  0820-2200／0821-0430／0821-1200 三次真實復發。**Playwright 查證確認站方 API 完全穩定**
  （官方前端自己的 request/response 每次都同一種 ES 整包格式），根因不在站方而是**沒有
  現成範本可照抄**，agent 每輪臨場手寫解析邏輯品質不穩。修法不比照 R9 走「送出前硬性
  關卡」（A18 不是「漏帶欄位」而是「解析邏輯本身寫壞」，關卡擋不住寫壞的邏輯本身），
  改在 `13c` §1a 補上 fetch＋解包＋瘦身一次做完的完整可照抄範本（清單＋詳情各一支），
  從源頭消除「臨場手寫」的變因。落檔後仍建議順手檢查 `ap_list_{HHMM}.json` 大小
  （正常 20~30KB／50 則，>100KB 即代表沒套到範本）。

## 6. `browser_evaluate` 大回應落檔連結＝資料遺失，不是「檔案存哪了」

**⛔ 這是硬規則，不是建議**：`browser_evaluate` 回傳太大時，Playwright MCP 不會把
資料寫進任何實體檔案，而是回一個 `[Evaluation result](./檔名.json)` 這種**看起來
像檔案連結的字串**。**那個檔案不存在**——連 MCP 自己的 output-dir
（`D:\Downloads\PlaywrightMCP`）都沒有落地。

- **2026-08-13 2200 輪 NS 站實錯**：一次抓 60 則清單，回應太大，MCP 回了
  `[Evaluation result](./ns_probe_2200.json)`。agent 把它當成「檔案真的存在、只是
  不知道路徑」，花約 9 分鐘全機找檔（含 `find /` 全磁碟掃描、120 秒 timeout，加上
  多次 60 秒 `find`），一無所獲。**同一輪又踩第二次**（`ns_full_2200.json`），
  再損失約 4 分鐘。最後靠「分段回傳」才解掉——證明資料從頭到尾就沒寫到磁碟，
  不是找錯地方。
- ⛔ **看到 `[Evaluation result](./檔名)` 這種連結，一律視同資料遺失，禁止去找
  那個檔**。它不在工作目錄、不在 `D:\Downloads\PlaywrightMCP`、也不在任何地方——
  找了只是白燒時間。
- **正確動作（2026-08-14 訂正，蓋過「分段貼回對話」）**：
  1. 清單先小欄位、不含 `script`（13c §1a-3 已寫）。
  2. 只要收的那批全文，**一次** `browser_evaluate`，並設 `filename` 為 scratch-dir
     **絕對路徑**（例如 `{scratch}/ns_full_{HHMM}.json`）——結果落磁碟，不要
     把稿全文倒進對話。
  3. 若仍出現 `[Evaluation result](./檔)`：視同遺失，**立刻重跑上一步**（縮小
     `size`／只帶 NEW id），**禁止**去找那個假檔（0814-1200 又找了一次）。
  4. ⛔ **禁止把 script 全文分多次 evaluate 貼回 context**（0814-1200 NS 連打 5 次
     詳情，再花約 7 分鐘在對話裡組 49 則中文稿——分類階段 10 分就是這樣肥的）。
     真的必須切段：每段用 `filename` 落到 scratch，下一段不要再帶前段全文；
     組 `batch.json` 讀磁碟檔，不要靠對話裡殘留的陣列。
- ⛔ **任何情況下禁止用 `find /` 做全磁碟搜尋**（單次就燒 2 分鐘 timeout，而且找的
  東西根本不存在）。要找本輪暫存檔，只准看兩個地方：**目前工作目錄**（＝本班
  scratch 夾）與 `D:\Downloads\PlaywrightMCP`；兩處都沒有就是不存在，回頭用
  `filename` 重抓，不要再往下找第三個地方。

## 7. AP 清單改打完整 topic 頁，PageNumber 正常翻頁（0820 訂正 MASTER R6／推翻本節舊版）

**2026-08-20 重大訂正，本條整段推翻 2026-08-14 版「AP 對帳只信 Page1 的 16 則」。**

舊判斷（0803 調查筆記、0814-0100 的 117 則、0814-1200 的 116 則）測的是
`newsroom.ap.org/home` 首頁的 Latest 小工具，**不是真正的完整清單頁**——這個
小工具本身清單範圍/排序邏輯跟完整清單不同，過去看到的「Page2 回 100+、時間序
亂、與第 1 頁幾乎不重疊」很可能是測錯對象，不是站方真的有分頁 bug。

真正的完整清單在 **`newsroom.ap.org/topic?id=116e9ab7aa044476925398d731289267
&mediaType=video&navsource=latest&parentlnk=false`**（AP 官方「All Latest」topic
頁）。0820 實測：照抄該頁面實際發出的完整 request body（`PageSize:50`，見 13c
§2 新版範本）連續打 `PageNumber 1→2→3`，`TotalRows` 穩定約 108260、逐頁銜接
自然無跳號、無跳回舊 archive 垃圾；過去被判定漏抓的 `AP4679409` 在新 recipe 下
正確落在 Page2、位置完全吻合（MASTER R6/E1 0820 訂正詳情）。

- **掃描可信清單＝`browser_navigate` 開 topic 頁（不是 `/home`）整輪只做一次，
  清單查詢用 13c §2 的 0820 新版範本（`PageSize:50`）**。
- 🔴 **2026-08-20 當天再驗，發現 `PageNumber` 翻頁不是無條件可信，訂正為「Page1
  可信、Page2+ 只能當補充來源」（推翻本節上一版「已恢復可用」的樂觀結論）**：
  實測不論 Page1 要求 `PageSize` 是 16／50／100，**`PageNumber:2` 一律固定回
  100 則**、而且對齊的是伺服器內部一個固定 offset（不是「緊接 Page1 繼續」），
  導致 Page1／Page2 之間視 `PageSize` 不同，會出現**兩種相反的風險**：
  - `PageSize` 用 16 或 50（比 100 小）：Page1 尾端與 Page2 頭端之間量到
    20~31 分鐘的時間落差、**中間可能有素材完全沒被任何一頁涵蓋到**（空窗）。
  - `PageSize` 用 100：Page1／Page2 有 50 則重疊，**同一則會被算兩次**，
    需要额外去重。
  - 加上清單本身是即時新增的 live feed，兩次分開呼叫之間又會插入新素材，
    offset 對齊只會更不穩，不是靠固定某個 `PageSize` 值就能根治。
- ✅ **正確用法**：**Page1（`PageSize:50`）當唯一可信、無空窗風險的主要來源**，
  照舊全部收錄。**Page2+ 只能當「候選補充池」**：拿到後先跟 Page1 已收的
  `editorialid` 去重，剩下的候選則**不能直接當『本輪確定漏抓』寫入狀態檔**，
  要嘛靠 13c 的 term／§1b DOM 針對該 ID 再單獨查一次核實時間窗與內容，要嘛先
  用 `needs-review add` 標記待人工複核，不要假設 Page2 給的東西一定對、也不要
  假設 Page2 沒給到的東西就代表不存在（可能只是卡在那個尷尬的空窗裡）。
- **對帳快照**：Page1 那 50 則照舊直接寫入。Page2 衍生的候選需標明「Page2
  補充、未逐一核實」，不能跟 Page1 的則混在一起算同等可信度。

## 8. 開工只精讀 13c＋本檔；NS body 用固定配方

**0814-1200 實錯**：開工讀了標示「規格草案」的 V1 `13.md` 全文、`13c` 讀了兩次，
NS 又用自拼 `{from,size,sort:[{createdDate}]}` 打出 400，再去翻
`investigation-logs/` 三次才撈到配方——NS 段白燒約 6–8 分。

- ⛔ **開工精讀只准 `13c`＋本檔。** `13` 只在 13c 明講「內容格式沿用 13」、而你
  **真的要查那一節**（素材行／分類／時區）時才翻**對應節**，禁止整本從頭讀。
  本條蓋過 `s2_scan_prompt.md`「先讀 13 全文」那句。
- ⛔ **禁止翻 `investigation-logs/` 找 NS／AP body**——配方以 13c §1a＋下面這段為準。
- **NS `POST /api/v3/stories` 照抄即用**（token 全程留在 `page.evaluate` 內，
  13c §1a-3 安全形態不變；此處只補 13c 範本裡空白的 `body`）：

  ```js
  () => {
    const tok = JSON.parse(localStorage.getItem('newsourceSession')).token;
    const body = {
      bundlesOnly: false, date: new Date().toISOString(),
      facets: {contentType:[], videoTypes:[], categories:[], digitalCategories:[], videoFormats:[]},
      filters: {contentType:{}, videoTypes:{}, categories:{}, digitalCategories:{}, videoFormats:{}},
      from: 0, language: ['en','es'], scriptOnly: false, size: 30,
      sort: {order: 'ascending', type: 'relevancyDate'}, term: null
    };
    return fetch('https://newsource-content-api-530.ns.cnn.com/api/v3/stories', {
      method: 'POST',
      headers: {'Content-Type':'application/json', authorization:'Bearer '+tok},
      body: JSON.stringify(body)
    }).then(r => r.text()).then(t => {
      const d = JSON.parse(t.split('\n').find(l => l.includes('"stories"')));
      return (d.stories.content || []).map(it => ({
        id: it.alternateIds && it.alternateIds.bitcentralId,
        ft: it.footageType, dur_ms: it.duration, created: it.createdDate,
        desc: (it.description || '').slice(0, 200),
        has_script: !!(it.content && it.content.bitcentral && it.content.bitcentral.script)
        // 清單這輪不要帶 script；全文另一次、用 filename 落到 scratch（§6）
      }));
    });
  }
  ```

  - `size` 可改 30／60，**不要**改 `facets`／`filters`／`sort`／`language` 的形狀
    （自拼 `sort:[{createdDate}]` 就是 400）。
  - 先這包小欄位 → `diff` → 只對 NEW id 再打一次帶 `script` 的查詢，`filename`
    寫進 scratch（§6）。

## 9. 素材行不要加「摘要：」、不要自創分隔

`raw_entry` 格式（`s2_parse.py`）：

```
{時段標記} {代碼} (備註) ▎摘要正文 ▎畫面：… ▎BITE：…|無BITE。 ▎MM:SS
```

- ⛔ **第一個 `▎` 後面直接寫摘要正文**，禁止加 `摘要：`／`摘要:` 標籤
  （0814-1200 約 31 則 NS 這樣寫，`fields` 空、`parse_ok=false`——入庫不擋，
  但結構化欄位整批沒了，下一輪若要修又是一輪 token）。
- `畫面：`／`BITE：`／`無BITE。`／時長**各佔一個 `▎` 段**，不要把「無BITE」黏在
  畫面段尾。
- 時段標記只用 `△▲■◆`（09:00–14:00 用 `◆` 沒錯），不要自創其他符號。

## 10. 工具層防護：臨時 `python -c` 檢查 json 會被 hook 直接攔下（2026-08-18 起）

launcher 掛了 PreToolUse hook（`scripts/s2_bash_guard.py`，MASTER D9）：
**Bash／PowerShell 指令裡出現 `python`/`python3`/`py` 的 `-c` 內聯程式、
且碰到 `.json` 檔或 `-s2-state.json` 狀態檔時，呼叫會被拒絕**。

- 被攔到不是錯誤，是指路——**deny 訊息裡就有可直接貼的替代指令**
  （`s2_batch_prep.py inspect/search/compare/dedup-check/snapshot` 或
  `s2_state.py show/…`），照著換工具重下即可，**不要**改寫成其他變體
  （heredoc、先寫 .py 檔再跑、PowerShell `ConvertFrom-Json`）去繞——那還是 §4 違規，
  遙測看得到。
- 純計算（`python -c "print(...)"` 不碰檔）、`.txt`、呼叫 repo 標準腳本都不受影響。
- 字數檢查（摘要 150 字上限）用 `inspect --lengths`，不必也不准自己寫 python 數。

## 11. hook 補洞：Write 落 `.py`／跑存檔腳本也擋，並收編跨站時間軸對帳（2026-08-30）

**背景**：§10 的 hook 只擋 `python -c` 內聯程式，MASTER A9③ 早就記過繞道
變體——`Write` 先落一支 `.py` 檔（hook 完全看不到 Write），再用
`python 檔名.py` 執行（不是 `-c`，正則對不上）。0830-0730 輪實錯：自寫
`_mk_audit_snapshots.py`／`_mk_ns_snapshot.py` 兩支腳本，把 AP／RT／NS
三站清單的上站時間換算成台北當地時間，好判斷跟前一輪有沒有銜接落差——
NS 站因此多花數分鐘、`inspect` 被叫到 34 次（當天最高）。

- ⛔ **`Write` 工具寫一支不在 `scripts/` 目錄下的 `.py`** → deny。
- ⛔ **Bash／PowerShell 執行一支不在 `scripts/` 目錄下的 `.py`**（跑存檔腳本，
  不是 `-c`）→ deny，堵住「Write 先落檔→再跑」這條完整路徑。
- 判斷邏輯只認路徑裡有沒有 `scripts` 這一段，不維護檔名白名單——加新工具
  不用跟著改 hook。

**這次不是「先擋再說」**：查過 0730 那兩支腳本的實際內容，換算邏輯是真正
的機械工作（NS `created`／AP `ts` 是 UTC 要 +8h，RT `at` 已經是台北當地
時間、13c2 §1b 原文寫「判窗內外用」不可再轉）——**這是真缺口，不是「工具
在但沒被想起來」**，所以先把它收進 `s2_batch_prep.py` 再上擋，不是空手
攔死一條沒有替代路徑的合法需求：

```
python E:/GitHub/TVBS-AIHunter/scripts/s2_batch_prep.py timeline <raw檔> --site ns|ap|rt [--out <檔>]
    # 印「id｜MM/DD/YYYY HH:MM（台北當地時間）」，依時間排序、缺時間的印警告不吞
    # NS／AP 自動 UTC+8；RT 的 at 欄位免轉（已經是當地時間）
```

跨站對帳／查某輪窗內外有沒有漏收，一律用這個，不要再自己算時區。

**2026-08-31 補查（原文有誤，已訂正——見下方「二次訂正」）**：§11 上線後使用者
要求「再確認有沒有漏掉工具缺口」，抽查 0808–0830 部分 Write 落 `.py` 的實例
對照內容，查到兩個一樣機械、一樣沒有現成工具的缺口（其餘抽查到的實例是編輯
內容本身——`_fix_ns_bite.py` 這類手寫 entry／BITE 文字——或單輪一次性資料
修正，本來就該逐輪自己判斷，不算工具缺口）：
⛔ **原文宣稱「22 次全部撈出來」是假的**——當時只抽查了一部分，`D:\Downloads\S2掃帶log\`
實際有 195 份 log，獨立 review 用 grep 重掃後估計約 96 支不同的一次性 `.py`、
約 108 個實例、散落在 34 份 log 裡，差了約一個數量級。詳見下方「二次訂正」。

- **raw 正文回填 batch 的 `src_text`**：`_merge_src_text.py`／`_merge_src_ap.py`／
  `_merge_src_rt.py`／`_fill_ap_srctext.py`／`_rt_add_srctext.py`／`_fix_src_0730.py`
  在 0818／0820／0825／0828／0829 至少六次重複同一件事——raw／detail 檔有正文、
  batch.json 裡對應的 `src_text` 還空著，逐 id 對應貼過去。已收進：

  ```
  python E:/GitHub/TVBS-AIHunter/scripts/s2_batch_prep.py fill-src-text \
      --batch <batch.json> --raw <raw/detail檔> --site ns|ap|rt
      # 只填 src_text 為空的那幾則，不覆寫已經有內容的（避免蓋掉手動修過的原文）
      # --fields 可覆寫取正文的欄位順序，預設 NS=desc/script、AP=head/script、RT=head/story
  ```

- **三站 batch.json 的 `category` 收成 `set-category --pairs` 字串**：
  `run_setcat_1000.py`（0828-1000，原文誤寫成「0827/28-1000」）示範了這個形狀——
  agent 習慣先把分類標在 batch.json 各則的 `category` 欄位草稿裡，三站掃完後
  再收成一條 `--pairs` 字串送出。⚠️ **獨立 review 覆核：這個形狀目前只找到這一次
  真實案例**（`_tmp_ns_cat.py` 0811-0430 那次是不同形狀，手寫 `{id: (大,中,小)}`
  字面量直接餵 `set-category`，沒有讀 batch.json），證據強度比 `timeline`／
  `fill-src-text`（各 6+ 次）弱很多，比較接近「單一實例但形狀合理，先備著」而
  不是「已驗證的高頻缺口」。工具本身邏輯查過沒問題（見下方 review 結論），
  先留著，之後有沒有再出現同一形狀再觀察。已收進：

  ```
  python E:/GitHub/TVBS-AIHunter/scripts/s2_batch_prep.py collate-category <batch1.json> [<batch2.json> ...]
      # 只組字串印出來，不呼叫 set-category——送不送、送之前要不要再看一眼，仍是你的判斷
      # 缺 category（或缺大分類／中主題）的則不會收進來，印警告列出
  ```

⚠️ **原本判斷「NS HTML 清理是閱讀輔助、沒有進 src_text」是錯的，已被獨立
review 用原始碼證據推翻**：原文只抽到四次（0821／0824／0828／0829），且說
「看起來是閱讀輔助」；獨立 review 重新在抓出的腳本原始碼裡逐支查了一遍，
命中 **9 支**（`_strip_ns`／`_clean_ns`×3／`_clean_ns_src`／`_ns_striptext`／
`build_ns_batch_0730`／`_build_ns_batch`×2），其中 **5 支的輸出確實寫進了
`src_text`**（例：`_build_ns_batch...` 裡 `src_text = strip_html(rec.get('script',''))`
後直接 `"src_text": src_text` 落進 batch）。也就是說：
①「不是要進 src_text」這個判斷本身不成立，清乾淨版本**歷史上已經進了生產
`src_text`**；②13d §5「站方原文帶什麼標記就該存什麼」這條慣例**在現有 state
資料裡早就不一致**（部分則被剝過標記、部分沒有），不是一個尚未發生、可以
擱置的假設性問題，而是已經存在的資料不一致；③新 hook 上線後這個 6–9 次量級
的重複需求會被硬擋，而 `SCRIPT_HINT` 目前**沒有指向任何替代路徑**——下一輪
NS 站真的需要清 HTML 時很可能卡在 deny 迴圈裡出不去。
**這件事需要使用者裁決**（要不要把 `src_text` 的定義從「原始標記」改成
「清乾淨後的文字」，或是另外開一個不影響 `src_text_of` 的獨立閱讀輔助工具），
在裁決之前本檔不擅自選邊；deny 訊息也暫時不指路到不存在的工具。

**另查到但尚未處理的可能缺口（needs-review，非本輪範圍）**：獨立 review 估計
還有約 57 個實例／27 份 log 是「載入 raw → 建 id 索引 → 組 `{id, source,
checkpoint, status, entry, src_text, …}` → 落 batch.json」這個外殼樣板，理論上
`cmd_build`（`s2_batch_prep.py build`）已經做這件事，但這批腳本仍手寫了一份——
是 `build` 沒被想起來，還是 `build` 的欄位形狀（`entries.json` 分離檔）跟 agent
實際習慣（entries 字面量直接寫在同一支腳本裡）對不上，兩種可能性沒有進一步查證
就不確定該不該再做第四個工具（RT 時區那次教訓：形狀猜錯比沒有工具更危險）。
下次有餘裕時再深入查證，不在這次倉促補上。

**RT 時區訂正（B1，最重要的一條）**：原本以為 RT 的 `at` 欄位已經是台北當地
時間、`timeline` 對它免轉換——這是**錯的**，獨立 review 用 `20260830/_rt_list_1600.json`
（落地台北 16:08，40 筆 `at` 最大值卻是 `08/30/2026 07:57`，比擷取時間早 8 小時
11 分）對照同輪 AP `ts` 換算 UTC+8 後（16:27）跟擷取時間吻合，交叉驗證出 RT 的
`at` 其實也是 UTC，需要跟 NS／AP 同樣 +8h。已修正 `TIMELINE_SPEC`，原本會讓
`timeline` 對 RT 站每次都算錯 8 小時、而且不報錯不警告，比沒有工具更危險。

<!-- RULES-EOF 13d 2026-08-31b — 讀到這一行才算讀完本檔；沒讀到＝被截斷，必須用 offset 補讀。 -->
