# S2 MASTER 追蹤清單（唯一列管帳本）

- **建立**：2026-08-12（接手 agent，彙整交接文件＋複核報告＋省Token計畫＋全流程分析）
- **定位**：S2 所有改進項目的**唯一狀態帳本**，不同 agent 之間以本表交接。
  規格與理由不重抄——看「來源」欄指向的文件；本表只管**狀態、優先序、驗收與依賴**。
- **衝突時**：使用者裁決 > 本表狀態 > 來源文件。

## 使用規則（每個 agent 都要遵守）

1. **開工先讀本表**，認領項目把狀態改 🔶 並寫日期；收工更新狀態＋一行進度（含 commit hash）。
2. **不刪列、不重用 ID**。完成的移到最下方「已完成歸檔」；新項目往後編號。
3. 進度紀錄**只加不改**——寫錯就再加一行更正，保留判斷軌跡（0812 教訓：先自證清白、
   沒查完就叫「完整」都是因為軌跡被美化）。
4. 動手前檢查「依賴/前置」欄；裁決類（D-）**沒有使用者明示不得自行動工**。
5. 本表隨 main 走，改完就 commit＋push（TODO 類文件不留在功能分支）。

**狀態代號**：⬜待做｜🔶進行中｜⏸等證據｜🗳待使用者裁決｜✅完成（移歸檔）｜❌不做

**來源文件**：
- 交接 = `G:\我的雲端硬碟\Claude共用\20260812-S2-交接文件.txt`
- 複核 = `G:\我的雲端硬碟\Claude共用\20260812-S2-1600輪異常分析-複核意見與改善計畫.md`
- 省T = `common/plans/2026-08-02-S2省Token優化計畫-修訂版.md`
- 全流 = `common/plans/2026-08-12-S2全流程與腳本自動化分析.md`
- 診斷 = `G:\我的雲端硬碟\Claude共用\自動掃帶系統\0812-2000掃帶診斷.txt`
- 落差報告 = `common/plans/2026-08-18-ENEX-ABC與三站排程掃帶技術落差報告.md`（ENEX/ABC vs 三站自動化落差，A9 唯一來源）
- 分類規劃 = `common/plans/2026-08-18-S2分類系統化規劃.md`（大／中／小分類系統化＝TODO「全盤檢討分類問題」結案路徑，A10 唯一來源）

---

## E — 等證據（0812-2200 輪與後續輪次）

| ID | 項目 | 來源 | 狀態 | 要看什麼／驗收 |
|----|------|------|------|----------------|
| E1 | RT Load More 迴圈判定：頁面行為改變 vs 策略漂移 | 交接§6-1 | ⏸半結 | **0812-2200 實測：迴圈消失**（捲動探測 145→11、RT 階段 7.2分/28次，回到正常水位）。傾向策略漂移或短暫頁面狀態，非持續性頁面改版；⚠️ 與 effort medium 同輪上線，歸因不純。再觀察 0030 輪無迴圈即結案；R1 是否仍要把配方寫死當預防，交使用者裁決 → **0813-0100 續驗：RT 僅 13 次/4.7分、scroll 關鍵字 13 次（2000 輪=145），連兩輪無迴圈，✅結案＝策略漂移偶發**。R1 預防性寫死留使用者裁決 |
| E2 | effort=medium 實效驗證 | 093c713 | ✅ | **0812-2200(medium) vs 0811-2200(high) 同時段對照：111次/27.0分/29.4M/106.5k vs 117次/29.5分/29.5M/106.5k（收 65 vs 70 則）——各口徑幾乎相同**。與 TODO「量不出差別」結論一致。數據已備，交 D1 裁決 |
| E3 | 鎖檔收工自動清理實戰驗證 | c9614ac | ✅ | 2200 輪 DONE 後鎖檔已不存在，收工推播同輪首次實戰成功（curl 版），移歸檔 |
| E4 | ①b 稽核前置驗收：update-entry 次數 7→1~3 | TODO | 🔶 | 2200 輪 update-entry=**4**（目標 1~3，接近未達）；set-category=8（①目標 1，未達，65 則大輪情有可原）。再看一兩輪趨勢；`s2_state:?`=6 待 A1 認齊後才能斷言。**0813-0100：update-entry=5（仍超標）、set-category=5（收 71 則，較 2200 的 8 略改善）**，①/①b 規則實質未被遵守，繼續觀察。0100 分類健檢發現規則與「邊掃邊分類」流程打架，驗收口徑擬改（見 T7）。**0813-0430/0730 續觀：set-category=3（收158則，佳）/6；update-entry=8/3。0730 收尾又出現 5 次 show 試探＋needs-review 3 次＋ls _待整併 2 次的固定尾巴（T5/T6 同款）** |

## R — 修復（品質與可靠性）

| ID | 項目 | 來源 | 優先 | 狀態 | 說明／驗收 |
|----|------|------|-----:|------|------------|
| R1 | RT Load More 迴圈修法：0811「一次抓完」evaluate 配方寫死進 13c §1b＋⛔禁迴圈 | 交接§6-1 | 1 | ⏸等E1 | 每輪省 ~38 次呼叫/6 分。⚠️ 禁令下不准附範本（交接§11-3 教訓） |
| R2 | RT 對帳只用 Edit No 判歷史已收，跨日撞號→靜默漏收（P0-1） | 複核 | 2 | ✅281d380（0813-1600 首次真實命中 RT4607） | 唯一真資料風險（RT3609 實例）。改 Edit No＋日期複合判斷。0812 未踩中但每天在骰 |
| R3 | audit finding fingerprint 去重／紅燈冪等（P0-4） | 複核＋交接§6-4 | 3 | ✅281d380 | PO-11~14WE 已登記 needs-review 仍每輪紅燈重查，1600 輪燒 7.7分/51次。修好順帶消掉目前唯一殘留紅燈 |
| R4 | truncate() 加標記後總長 4014 > 4000（P0-2） | 複核 | 4 | ✅281d380 | 17:05 只改了標記字元，長度溢出沒修→已修：標記算進 limit |
| R5 | audit △ 判準尊重合法 set-mark override（P0-3） | 複核 | 5 | ✅2026-08-13 | 17:05 只修了假警報那一半。修法：`mark` 欄位存在＝set-mark 留痕（s2_state 唯一寫入點已核），有 override 者不再報「判日失敗」；副本合成雙情境測試 PASS（無 override 照抓、有 override 不誤報） |
| R6 | AP 對帳清單抓到 page 2（≥32 則） | 接手查證 | 6 | ✅2026-08-13（13d §7）；**2026-08-14 訂正** | 現況 list=16 恰為 page 1 容量，被擠出 page 1 的素材對帳網接不到（AP5467302 實例）。掃描本身有翻頁（三輪 log 均有 PageNumber=2），弱的是對帳快照。修法走規則層：13d §7 原要求對帳快照 page1+page2 合併（≥32 則，當窗不足 17 則才允許單頁）。屬規則文字，待下輪實戰驗收快照筆數。**0814-1200 體檢訂正：PageNumber=2 回 100+（與 0100 的 117 同形），合併 page2 會把 116 寫進對帳分母。13d §7 已改為「只信 Page1＝16；禁止打 Page2；被擠出的用 term／DOM 定向補，不准 116 假綠燈」**。**0814-0100 實戰：agent 改走 API 一次撈 117 則（超額達標），但落地格式變 tab＋ISO UTC，audit parser 只認 `\|` 分隔→整行當 code、時間空白→117 則全進「窗外」假綠燈（已收 0 竟無紅燈）。main 當場修 parse_list_file（tab 分隔／total 表頭跳過／ISO UTC→本地時區），重跑：AP 已收 58＋前幾天 33＋**窗內未收 23**——全窗對帳首戰就抓到被 page1 快照時代遮住的累積漏收。**後續（0814 上午）：23 之中 1 則是稽核不看日期的誤報（已修 73e172e，窗判斷帶日期）；真漏收 22 則，使用者裁定回補 9 則硬新聞（含哥倫比亞強震搜救）、13 則運動/軟性放掉。sonnet 子代理已回補完成並經 main 獨立驗收：9 則全入庫帶 src_text、分類歸位（哥倫比亞強震那則進機動格）、render 444 則（+9）品質掃 0 命中。⚠️ 流程教訓：main 把「待 0430 回補」寫 MASTER 但排程 agent 不讀 MASTER→訊息斷鏈兩輪，跨輪交辦要走狀態檔（needs-review/alert），不是帳本** |
| R8 | audit 其餘輸出補完：各清單 shown/total/more（P0-5）＋ --json-report（P0-6) | 複核 | 8 | ✅P0-5（2026-08-13） | §4 已做一處（384d9de）。P0-5 已補齊：more_note() helper 套到 Ⓐ/②/③/④/⑥/⑦ 全部截斷點（生產稽核實測「還有 14 則未列出」8+14=22 對得上）。--json-report（P0-6）仍 ⬜ 留案不動 |
| R9 | update-entry 同步 src_text／footage_type（P1） | 複核 | 9 | ✅6ecfc17；**0818-1000 第三次真實復發** | src_text 已補（--src-text/--src-text-file/批次每筆可帶；{id,src_text} 純回補不動稿）；footage_type 原本就會傳入 apply_update。**0818-1000（使用者要求查浪費，main 查證）**：NS 整批 39 則再度整批漏帶 src_text（`add-batch` 印警告，確認防呆機制正常運作），跟 0812-2200／65 則、0813-1200／80 則同一種失誤第三次出現；AP／RT 兩站這輪正常，僅 NS 中招。防呆只做到「警告＋可回補」，沒有做到「組 batch 當下就擋下漏欄位」，所以每次復發都要付一次現場回補的時間成本——這輪回補（Write ns_fix_1000.json 重寫 27 則措辭＋Write ns_srctext_1000.json 補齊全 39 則 src_text＋兩次 update-entry）從 10:08:37 燒到 10:13:53，約 5.3 分鐘。⚠️ 續觀：若之後幾輪還在復發，代表「事後警告」不夠，該考慮把 src_text 缺欄位做成組 batch 前的硬性檢查（例如 `s2_batch_prep.py` 的 build/compare 流程擋下不完整的 batch，不是等 add-batch 才報）|
| R10 | BITE 三態 CONFIRMED／NO_BITE／REVIEW_REQUIRED（P1） | 複核 | 10 | ⬜ | |
| R12 | truncate 3000 字截斷 SOUNDBITE 段→BITE 無法驗證 | RT9878 實例 | 4.5 | ✅281d380 | RT9878 sb_count 機械數到 9，但 truncate 只取前 3000 字未含逐字引言，agent 只能標無BITE 待人工。修法與 R4 同區：truncate 應保證 SOUNDBITE/SUPERS 段落優先保留，不是傻取前 N 字 |
| R14 | browser_evaluate 大回應落檔＝資料遺失：MCP 回 `[Evaluation result](./檔)` 連結但檔案從未寫出，agent 全機找檔 | 0813-2200健檢 | 2.5 | ✅2026-08-13（13d §6）；**0814-1000 首戰驗收過**：agent 遇到同款連結自述「per V4 §6 資料沒落地」直接分段重抓，前天 13 分鐘的坑這次 ~20 秒繞過 | 0813-2200 NS 站兩踩（ns_probe/ns_full_2200.json）：一次抓 60 則回應過大→MCP 聲稱落檔→實際連自家 output-dir（D:\Downloads\PlaywrightMCP）都沒有→agent 燒約 9＋4 分鐘搜檔（含 `find /` 全磁碟 120s timeout）。該輪 NS 15.8分/46次（正常約3分），整輪 33.7 分。agent 最後自己用分段回傳解掉＝正解。修法：13d 新章節硬規則（見連結視同資料遺失、禁找檔、禁 `find /`、立即分段重抓） |
| R13 | batch 檔寫錯位置→雙重搬運（浪費 ~3 分/輪） | 2200健檢 | 6.5 | ✅281d380 | 2200 輪把 rt/ap batch 先寫 repo 根目錄，再 Read 回來重 Write 到 scratch 目錄（卡點 91s+102s 就在這）。repo 根目錄已累積 9 個各輪殘留 json（0700/0100/1500/2000…）。修法：規則明示 batch 一律直接寫 `scratch-dir` 路徑＋清一次現存殘留。**0813-0100 再犯：ns/ap/rt_new_0813.json 又先落 repo 根（該輪三個 >60s 停頓 345s/136s/148s 全在這組檔的 Read 之後），事後有自清但雙重搬運照舊——已連兩輪，建議優先度上調**。1000 輪第三種變體：ns/ap/rt_list_1000.json 先落 repo 根、就地 python -c 檢查、再 `mv` 進 scratch（比 Read+Write 便宜但同病）。**0813-1200（13d §1 硬規則生效首輪）仍再犯**（detail 檔先落 repo 根再 mv）——規則文字管不住，剩下的硬解是 launcher 把工作目錄設成 scratch-dir（候補案，需裁決，見 D7） |

## T — 省 Token（既有計畫未完成項）

| ID | 項目 | 來源 | 優先 | 狀態 | 說明／前置 |
|----|------|------|-----:|------|------------|
| T1 | Task 2 最小啟動設定（A0→A3 逐旗標實驗） | 省T | 1 | 🔶2026-08-18 測試全過＋已熱修上線，待正式輪驗收 | 預估 ~4.7M/16%，最大單筆在 `--setting-sources`。⚠️ 唯一有實質風險的一刀：先確認 auth 不來自 user scope；`--tools` 先不砍 Write。骨架 `test_s2_launcher.ps1` 從未執行過。前置：④已修（DryRun 不再污染紀錄）。**2026-08-18 使用者指示：先做測試版本，測試 OK 才可熱修**。⚠️ 與 D9 交會點：A3（`--setting-sources project`）是否會排除 `--settings` 掛的 guard hook，文件推不出結論，A3 測試組要親驗 deny 仍生效。**2026-08-18 執行紀錄**：骨架先補三洞才實跑——①claude cwd 改 `_task2_test_out\`（transcript 落專屬目錄，量測不搶互動 session 的檔）②量測改 `--transcript-dir`＋`--dry-run`（TASK2-* 不寫進生產 `_token_metrics.jsonl`）③四組都掛 D9 guard settings（對齊現行生產）＋verify prompt 加 hook 探測步驟。**A0→A3 四組實跑全過（sonnet／各 4 次呼叫／離開碼 0）**：prefix 62,015→51,869（+strict-mcp-config）→47,311（+disable-slash-commands）→43,538（+setting-sources project），合計 **−30%**；cache_read/輪 245k→171k。四組皆 browser MCP 可用、OAuth 正常、**hook 探測=有擋（A3 沒吃掉 --settings 的 hook，D9 交會點排除）**。已熱修 `s2_scan.ps1`：三旗標預設開，回退開關 `-NoMinBoot`（獨立於 -NoToolBan/-NoBashGuard，單變因可歸因），遙測 flags 加記 NoMinBoot，DryRun 驗證參數組對。`--tools` 白名單照但書未動。⚠️ 下一正式輪同時帶 D9＋T1 兩變因上線，歸因靠 A1 的 `launcher_flags` 指紋。**0818-0430/0730 續驗（使用者要求體檢）**：離開碼皆 0、收 102／57 則、`python -c:json` 仍是 0／0（`s2_batch_prep.py` 78／26 次），D9/T1 持續穩定；耗時 38.2／23.9 分主因同 0100——量大（0430 是本輪 checkpoint 史上最大 102 則）＋工具執行本身極快（RT inspect 逐筆迴圈每次 <2 秒），時間集中在模型思考組稿。「分類」桶偏高（15.0／9.4 分）查證是遙測誤記（見 A1 子項新發現），非真異常。**0818-1000（使用者要求查浪費／熱修有沒有反而變慢，main 查證）**：29.5 分／54 則，離開碼 0；**熱修沒有讓輪次變慢**——cache_read/call 260k，跟同日其他三輪（0100 243k、0430 291k、0730 232k）同一水準，hook 這輪只攔截 1 次（負擔可忽略），D9/T1/分類桶修法皆非變慢原因。**確實抓到兩筆真浪費，跟熱修無關**：①`inspect --ids` 明明支援逗號分隔一次查多筆，10:03–10:04 卻連續 19 次單筆呼叫，多燒約 300 萬 cache_read tokens（見 A2 子項新發現）；②NS 整批 39 則第三度漏帶 src_text，燒約 5.3 分鐘現場回補（見 R9 新發現）。AP／RT 這輪正常，只有 NS 這兩處出包。**0818-1200（使用者要求體檢）**：27.6 分／54 則，離開碼 0，cache_read/call 239k（同日水準內），三站 add-batch 都沒漏 src_text、沒有單筆 inspect 迴圈——效能面乾淨，熱修沒有問題。**但查證意外抓到 A11：這輪有 2 次 `python -c` 直讀狀態檔完全沒被 D9 hook 攔下**（同天其他輪同類嘗試都攔到），詳見 A11，比效能問題優先度更高，待使用者裁決。**0818-0100 首個正式輪（使用者問「異常久」，main 查證）**：32.1 分／197 次／離開碼 0／**收 90 則＝0100 輪史上最大量**（歷史 0100 輪：71 則 22.8 分、82 則 23.8 分、30 則 18.8 分），每則 0.36 分 vs 歷史 0.29–0.32 同量級——**長是因為量，不是新變因出事**。偏高的兩段：分類 8.4 分（平常 2–3 分，90 則＋隔夜主題樹最肥時段）、NS 10.0 分/67 次；>60s 停頓 4 段共 ~6.6 分，其中 3 段是收尾寫大檔前的長生成、1 段是 `s2_state show` 單次跑了 118 秒（1.15MB 狀態檔在 G: 雲端硬碟上，I/O 慢，非迴圈）。T1 效果待多輪對照（cacheC 376k vs 前三輪 335–355k，此輪對話量大難單輪歸因） |
| T2 | A4 實驗：排除 Agent／TaskCreate／TaskUpdate 工具 | 全流§4.4 | 2 | ✅281d380 | **0813-1200 首戰：Task 類工具歸零**（前一輪 0430 還有 12 次），硬排除生效 | 1600/1800 輪 Task 類 26/17 次呼叫零產出。併入 T1 流程但**單獨一輪測**，一次一個變因。**0813-0430 復發：TaskCreate 4＋TaskUpdate 8＝12 次純開銷（2200/0100 兩輪原本歸零）——行為靠 agent 自律會漂移，硬排除的必要性再添一證** |
| T3 | Task 3 清理 prompt 衝突 | 省T | 3 | ✅2026-08-13 | 三項查核完：①window_start 改為「launcher 寫死＝本班建檔日當天 13:00，agent 只確認不改寫」（⚠️子代理初版誤寫「前一天 13:00」，main 對 s2_scan.ps1:182 複核後更正——`mmdd`＝本班日期）；②「其餘 11 輪」→「8 輪」（9 輪制同步）；③taskkill 鐵律確認仍在未動。順手：步驟 5 topic_review 補 `--compact`（13d §3 連動）。衍生新案見 T8 |
| T8 | 13/13c 過期描述清理＋prompt「第一輪」整節重寫 | T3查核 | 7 | ⬜ | T3 查出但超範圍未動：①13/13c 內文多處仍寫 23:00 為晚班最後一輪（現行 2200 收尾）；②prompt「如果這是當天第一輪（狀態檔還不存在）」整節框架與 13c §5a「腳本已建檔、agent 只確認」現況矛盾（本次只修了節內 window_start 段）。低風險純文字，待批 |
| T4 | Task 4 規則分片（八片＋manifest） | 省T | 延後 | ⬜ | ~16% 但中風險 3–5 天，單獨排期。完整性對照測試與 A/B replay 比 state mutation 兩條不准省 |
| T5 | topic_review 誤報時直接印出 id | 0100分類健檢 | 4 | ✅281d380 | 0100 輪 topic_review 報「1 則(無中主題)」但沒印 id，agent 燒 6 次呼叫/40s 翻 state 檔追兇，最後查無收場（needs-review 已留痕）。一行工具修改可消滅整段白追查。**先多收幾輪資料再動工（使用者 0813 裁定）**。**1000 輪健檢破案：鬼＝AP4678031（2200 輪入庫、category 空 dict），空分類項用 `show --cat \"?\"` 查不到→懸置 4 輪；已於 0813 人工補分類（哥倫比亞強震/佩雷拉生還者救援）＋重 render。修 T5 時順帶讓 show 支援查空分類** |
| T6 | list-topics／topic_review 長輸出精簡模式 | 0100分類健檢 | 5 | ✅281d380 | 兩工具輸出過長，agent 拆 head/tail 各讀兩次（0100 輪合計 ~88s、4 次呼叫）；主題樹隨當日累積成長，晚輪更肥（2200 輪 list-topics 後接 270s 長思考）。加 --compact 或保證單次可讀。**先多收幾輪資料再動工**。0730 續證：list-topics --sub 後接 244s 長思考（隔夜主題樹 300+ 則最肥時段）、topic_review 又拆 head/tail 兩讀 |
| T7 | 「set-category 一輪一次」規則改「一站一批」 | 0100分類健檢 | 6 | ✅281d380（13d §2） | 實際流程是邊掃邊分類（NS/AP/RT 各一批＋零星補刀），規則與流程天生打架，E4 每輪都「未達標」是量錯了尺。改規則同時更新 E4 驗收口徑（目標：一站一批＋補刀 ≤1）。**先多收幾輪資料再動工** |

## A — 腳本自動化（全流程分析 Phase 0–4）

| ID | 項目 | 來源 | 優先 | 狀態 | 說明／驗收 |
|----|------|------|-----:|------|------------|
| A1 | Phase 0 剩餘：metrics 細分＋報表 | 全流§4.5 | 1 | ✅2026-08-17（子項全數完成） | 驗收：報表能解釋任一輪多出的呼叫去了哪裡 → `s2_metrics_report.py --diff` 達成。子項見下 |
| A2 | Phase 1：`s2_batch_prep.py` 擴充成 raw 工具層 | 全流§4.1-4.2 | 2 | ✅2026-08-17 子項全上線；**行為遵守度仍待觀察** | 最高投報（保守省 10~22% 呼叫）。只讀或寫 scratch 不動 state；先拿 1600/1800 已保存 raw replay。驗收：tool-result 一次 unwrap、`python -c` 30/54→≤10/≤15。子項見下。**0813 三輪鐵證：同一個「檢查 scratch raw」需求，0430 用 Read×16、0730 用 PowerShell×29、1000 用 python -c×45——每輪換工具即興發揮，正是缺 `inspect` 標準工具的症狀**。inspect 上線後續觀：0813-2000 輪仍有 `python -c`×14（13d §4 未被完全遵守），1800/2200 輪已收斂——再觀察。**0817 續驗：0430/0730 兩輪 `python -c:json` 又回到 26/33 次（1000 輪只 2 次、有叫 s2_batch_prep×4）——查證內容確認是「檢查 raw json 印筆數/欄位/比對」，屬 13d §4 應用 inspect/search 的場景，判定真違規非誤判。根因查到：13c §1a「`s2_batch_prep.py` 保留為選用工具」沒點名是哪個子指令，agent 讀到容易連 inspect/unwrap 一起跳過。已在 13c §1a 補澄清「選用只涵蓋 dump/build，inspect/unwrap/search 一律必用」（commit `b9e0fb6`）。**0817-1200 首輪驗證：`python -c:json` 0 次（前一輪 1000 是 2 次）、`s2_batch_prep.py` 叫了 7 次，14.2分/29則/99req，離開碼 0、無異常——澄清後最乾淨的一輪，但只有一輪樣本，繼續觀察後續輪次是否維持**。**0817-1600 續驗：`python -c:json` 0 次、`s2_batch_prep.py` 4 次，維持乾淨。🔴 但 0817-1800 復發且更嚴重：`python -c:json` 36 次、`s2_batch_prep.py` 0 次（148 次呼叫／35.6M cache 讀／26.4 分，收 73 則）**——13c §1a 的文字澄清**沒有真正解決問題**，只是讓中間兩輪剛好照做。四輪樣本 26／33／0／0／36（0430/0730/1000+1200/1600/1800）看得出是**行為漂移**，跟 T2 的 Task 工具復發同一種形狀，而 T2 最後的解是**在工具層硬排除**而非靠 prompt 自律。下一步建議：比照 T2，別再補規則文字（見 D9）。**0817-2000 逐筆查證（D9 的實證基礎）**：`python -c` 共 14 次，拆開來是三類——①**8 次違規**：拿 ad-hoc python 檢查 `ap_list_2000.json`／`rt_details_2000.json`／`batch_*.json` 的筆數、型別、JSON 合不合法，全部是 13d §4 該用 `inspect`／新增的 `compare` 的場景；②**3 次違規**：直接 `json.load` 生產狀態檔讀 `alerts`／欄位，13c §2 明文禁止；③**3 次不算違規但是工具缺口**：數中文摘要字數（150 字上限）——目前沒有任何工具做這件事，agent 只能自己寫。⚠️ 查證陷阱：agent 寫的是 **`python3 -c`** 不是 `python -c`，用後者 grep 會漏掉 11/14 筆（0817 體檢時實際踩到，分桶器本身兩種都認、數字正確） **0817-2200 續驗：`python -c:json` 回到 23 次**（`s2_batch_prep.py` 7 次、`python -c:read-state`／`length-check` 都降到 0）。逐筆看 23 筆**全部落在第①類「檢查 raw json」**——印 ns/ap/rt 的 list/full/details 筆數、欄位名、id 比對、兩則 script 逐字比對。其中第 23 筆最刺眼：agent 自己寫 python 產 `_audit_ap_2200.json`，做的正是當天剛上線的 `snapshot` 子指令的事，代表**新工具存在但沒被讀到／沒被選用**。0817 全日序列 26／33／2／0／0／36／8／23（0430→2200），確認是行為漂移而非規則不清，D9 的必要性再加一筆。 |
| A3 | Phase 2：state 查詢擴充＋輪次閘門 | 全流§4.3/4.6 | 3 | ⬜ | 唯讀優先、不做第二套 parser、初版只報告不阻斷。驗收：postflight 對 0811-1800/2200 類異常能命中。子項見下 |
| A4 | Phase 4a：`s2_schedule_check.py` 排程一致性唯讀比對 | 全流§4.8 | 4 | ✅2026-08-17 | XML 與 watchdog $Slots/model/TestMode 多真相源，曾險發生取消輪被看門狗補跑。只報不改。**基準改為「工作排程器裡的實際定義」**（repo 的 `S2掃帶.xml` 只是匯出備份，不是真相源——首跑就抓到它停在 0807 的 12 輪／opus／TestMode 版本，已同步刷新）。🔴＝看門狗 `$Slots`／代打 model／TestMode 與實際排程不符；🟡＝備份 XML 過期；ℹ️＝看門狗排程停用或旗標檔不在（代打與 A5 都不會動作）。離開碼 0／1／2，**2＝讀不到，不是一致**。11 項測試全過（`scripts/test_s2_schedule_check.py`，六項負向全都驗「解析不出來要明確失敗、不可回空清單」）|
| A5 | Phase 4b：watchdog「START 無 DONE」中途死亡偵測 | 全流§4.7 | 5 | ✅2026-08-17（只推播不代打）**待實戰驗收** | 第一版只推播不代打（自動代打見 D5）。不可只看 0-byte log、鎖被占時絕不重跑。**真實樣本＋1：0814-1000 輪收工前被 API Connection lost 打死（terminal_reason=api_error）——render 已完成但 checkpoint 未推進、DONE 推播未發，無任何機制發現，靠使用者「這輪異常久」人工起疑才查到；main 手修 checkpoint=0814-1000。D5 樣本數 2/3**。**2026-08-17 上線**（`s2_watchdog.ps1` A5 區塊）：四條件全中才推播——①`_輪次紀錄.txt` 有 START 無 DONE／CRASH（比**時間先後**不是比有無，同 checkpoint 重跑後死亡才抓得到）②距 START >75 分（歷史最久 0811-0430＝56.7 分）③無 s2_scan.ps1 行程 ④`_token_metrics.jsonl` 無該輪（量測寫在 DONE 之後，有量測＝外殼跑到最後、只是 DONE 沒落地）。⛔ **判活不准開鎖檔**：s2_scan 用 FileShare::None 獨佔，探測會讓正要啟動的一輪 SKIP＝為偵測異常而製造異常，改看行程命令列。冪等靠 `_看門狗紀錄.txt` 的「中途死亡警報 [cp]」標記。**判活收緊（同日修）**：原本只要有任何處理程序的命令列「提到」`s2_scan.ps1` 就算在跑，實測被自己的互動 shell 誤觸而讓警報整個靜音；改成三訊號任一成立才算活著——①鎖檔存在**且 LastWriteTime 比嫌疑輪的 START 新**②該 checkpoint 的 `掃帶log-*.txt` 10 分鐘內還在長③有 `-File …s2_scan.ps1` 形式的行程（排除帶 `-Command` 的，排程與代打都用 -File）。🔴 ①的時間比對不可省：`s2_scan.ps1` 在 finally 刪鎖檔，**被硬砍就不會走到 finally**（排程 `ExecutionTimeLimit=PT1H`，0811-2200 的 `0x8007042B` 正是逾時終止），死掉那輪會留下自己的鎖檔，「鎖檔在＝還活著」等於讓 A5 在最該作用的情境下失效。另：候選改為回傳全部並先濾掉已警報過的，否則同日死兩輪時舊的會被已警報的新輪永久擋住。22 項 replay 測試全過（`scripts/test_s2_watchdog_deadcheck.ps1`，含 0811-2200／0811-1800／0814-1000 三個真實樣本的正反驗證；真實路徑測試把子行程 USERPROFILE 指到空家目錄，Send-Ntfy 找不到 topic 檔即靜默回傳，因此不會真的推播）。⚠️ **A5 跟代打共用同一個旗標檔**——看門狗關閉時中途死亡也沒人守 |
| A11 | 🔴 D9 guard hook 生產環境**確認會漏放**——不是每次都攔得住 | 0818-1200 體檢（使用者要求，main 查證） | — | ⬜待查／需裁決 | **0818-1200 輪 2 次 `python -c` 直讀 `0817-s2-state.json`（讀 checkpoint/window_start 欄位、讀 alerts 欄位）都沒被攔下、直接執行成功**——同一天 0100 輪 2 次、1000 輪 1 次同類嘗試皆正確攔下（訊息、指令特徵完全一致），對照鮮明，不是規則寫錯：離線重放這兩則真實指令進 `guard.decide()`、以及模擬生產環境的 stdin 管線（Windows 預設 cp950，非 UTF-8）都**正確判定 deny**，regex／JSON 協定／編碼三個懷疑點全部排除，問題不在 `s2_bash_guard.py` 本身的邏輯。兩次執行延遲都只有 0.5～0.6 秒（正常執行速度），無法從 transcript 內證據進一步定位到底是「hook 沒被叫到」還是「hook 叫到了但平台層某個機制蓋掉了 deny」——這超出本 repo 可觀測範圍，屬 Claude Code 平台層行為。**後果評估**：這兩次都是唯讀（`json.load`+`print`），沒有寫入，本輪沒有造成資料損毀；但代表 **D9 目前是「多數時候擋得住的嚇阻」，不是「保證擋住的硬閘門」**——13d §2／§4 的規則文字仍是唯一每次都在的防線，不能因為上了 hook 就假設規則文字可以放鬆或砍掉。⚠️ **裁決需要**：①是否要在 MASTER／13d 明講「hook 是輔助，非唯一防線」；②要不要加低成本自我體檢（量測器記錄「本輪 `python -c` 嘗試次數 vs 實際被攔次數」，長期觀察漏放率，不要只在使用者要求體檢時才發現）；③是否值得花時間往平台層追根因，或先接受「非 100% 但比沒有好」現況繼續用。使用者未裁決前不擅自關閉或大改 D9 |
| A12 | 臨時腳本繞道分桶診斷（0818-1600 一輪 9 支 .py／19 次執行）＋改進提案 | 0818-1600 體檢（使用者要求「研究能否改進」，main 查證） | — | 🔶1️⃣patch-entry 已上線待實戰驗收／2️⃣3️⃣4️⃣🗳待裁決 | 0818-1600 輪在 scratch 目錄留下 9 支自寫 .py（`_strip_ns` / `_build_batch_ns|ap|rt`（ap 跑兩次）/ `_fix_ns_bite` / `_fix_ap` / `_fix_combined2` / `_fix_markers` / `_build_reconcile_lists`），比 0818-0100 的 2 支多。⚠️ **逐支讀完＋實測後，「工具缺口」這個單一根因不成立，要拆成四桶，處方完全不同**——不可照 0100 那句「補子指令再談擋」一路推到底。**桶① 加標記卻重打整條 entry＝唯一的純損失，且每輪必發（最高優先）**：`_fix_ns_bite`／`_fix_ap`／`_fix_markers`／`_fix_combined2` 四支的**全部內容**就是把已寫好的 entry 一字不差重打一遍，只為了插入 `🔴`／`🟡` 或補第二括號 `(BITE)`。根因是 `s2_state.py update-entry` **只有全文替換、沒有部分修補**。而 🔴/🟡 是 `s2_scan_prompt.md:113–122` 的**每輪必做步驟**（明文「拿不準就標 🟡、沒有名額限制」），`(BITE)` 更是從 `▎BITE：` **100% 機械可導**、`s2_validate.py:350` 早就會偵測（「有 ▎BITE： 但缺 (BITE) 第二括號」），但 13c §497 明訂品質掃**只警告不修**，於是「重打全文」被制度化成官方修復路徑。鐵證：`WE-021MO` 同一條 entry 在 `_build_batch_ns`／`_fix_ns_bite`／`_fix_combined2` 被輸出**三遍**。量級 ~12KB 中文重打／輪（≈4–5k output token，且 output 是最貴的一種，另計長生成的 wall-clock）。**桶② `build` 早就存在、而且這輪真的能直接用——0812「撤回強制」的前提已經過期**：13c §62 於 0812 把 `dump`／`build` 降為選用，理由是「raw 不是 build 吃得下的格式，轉檔本身就要寫 python」（實測 24.0M→56.9M）。但 **0818 的抽取白名單已經收斂到 `SITE_SPEC` 的 schema**——`ns_full_1600.json`(id/ft/dur_ms/desc/script)／`ap_full_1600.json`(id/head/cap/script/role/sb_count/has_sot/prelim)／`rt_full_1600.json`(code/head/story/sb_count) 欄位全對得上。**main 已用該輪三份真實 raw ＋ 從 batch 反推的 entries.json 實跑 `build`：三站 exit=0、零轉檔、則數一致（NS 17／RT 22）**，即 13c 那句「只在 raw 本來就已經是它吃得下的格式時才划算」的但書**這輪成立**，agent 卻仍手寫。⚠️ 但**先別急著把 build 改回強制**，實跑同時抓到三個 delta，不補的話導引會空轉（＝D9 步驟②「inspect 看不穿 `_source` 就被彈回」同一個教訓）：**(a) 🔴 靜默判錯**——RT 的 `early` 欄位已不在抽取白名單裡，`status_of` 會把該 pending 的判成 `has_script`（本輪 RT6164／RT6193／RT6194 三則，agent 手寫版是對的），**「工具靜默判錯」比「不用工具」更危險，這條要先修**（`{"entry":…,"status":"pending"}` 物件覆寫雖是現成出路，但那要 agent 記得，不是防呆）；(b) `src_text` 格式差異——build 加 `HEAD:`／`DESC:` 前綴且**不剝 HTML**，agent 的 `_strip_ns.py` 剝得更乾淨（`s2_batch_prep.py:709` 的 `strip_markup` 現成可重用，別寫第二套）；(c) build **不吐 cat-pairs**，agent 得另外自己產 `ns/ap/rt_cat_pairs.txt` 餵 `set-category`。**桶③ 對帳清單＝只有半個缺口**：`snapshot` 實跑只吐 `id \t 標題`、**沒有時間欄**，做不了對帳（已驗）；但 `s2_audit.py parse_list_file` **本來就吃 JSON**——**RT 的 `rt_list_1600.json`（`code`＋`at`）可直接餵、那支腳本對 RT 根本不必要**，只有 NS（鍵是 `created` 不是 `at`/`time`）與 AP（`Items`→`_source`→`firstcreated` 兩層殼）認不出來。修法極小：`parse_list_file` 補認 `created`／`firstcreated` ＋ 沿用既有 unwrap 卸 AP 殼，之後三站零腳本。**桶④ `_strip_ns.py`** 併入桶②(b)。**建議順序（照風險由低到高，一次一項）**：1️⃣ 桶①的 `patch-entry`／機械標記修補（殺掉每輪必發的最大損失；⚠️ 重用 `s2_validate.py` 既有括號／段落解析，**不做第二套 parser**（不變式清單）；⚠️「`(BITE)` 自動導出」vs「明令 patch 才動」是**裁決題**——寫入時靜默自動改會牴觸 13c §497「只警告不修」的既有原則）2️⃣ 桶②的三個 delta（(a) 最優先，它是正確性不是省 token）3️⃣ 桶③ 的 `parse_list_file` 兩行 4️⃣ **最後**才談把 D9 hook 擴到「執行 scratch 新落地的 .py」——維持 0818-0100 自己的結論「補對應子指令再談擋」，且 A11 已證 hook 會漏放（是嚇阻不是閘門），⛔ 不可把 hook 寫成解方主體。⚠️ 1️⃣2️⃣3️⃣ 都會改變 agent 行為／規則文字，屬 D- 類，**使用者未明示不得動工**；本列只交付研究結論。**2026-08-18 使用者裁決：只做 1️⃣（patch-entry），2️⃣3️⃣4️⃣ 不動、維持待裁決**。**1️⃣ 已完成上線**：`s2_state.py` 新增 `patch-entry --ids A,B,C [--alert red|yellow|none] [--bite]`（`patch_marks()`／`bite_tag_patchable()`／`insert_bite_tag()`／`cmd_patch_entry()`）。設計要點：①**不做第二套 parser**——標記位置、互斥、素材行判準全部走 `s2_validate` 既有的 `strip_mark`／`RED_RE`／`SUBALERT_RE`／`AIRED_RE`／`HILITE_RE`／`LINE_RE`／`SIDE_RE`（不變式第 2 條）；②**改完走 `apply_update()`** 而不是直接寫 `raw_entry`，讓 `entry_updated`／`sp.derive`／`bite_doubt`／`needs_review` 結案這串記帳邏輯只有一份；③🔴/🟡 互斥、換標記自動剝舊插新且**保住 🟤／🔖**；④冪等（已是該標記就回「未變更」，不寫檔）；⑤不存在的 id **先全擋不改一半**（同 `set-aired`）；⑥`--bite` **只補 `s2_validate` 已經在報的那一種**（有 `▎BITE：` 段但缺 `(BITE)`），已有／寫著無BITE／沒有 BITE 段／沒有備註括號可接一律拒絕不動——⛔ 刻意**不**做成寫入時自動補，13c §497 訂的是「只警告不修」，靜默改稿會牴觸它（要不要改自動導出仍是裁決題，未做）。**驗證**：新增 `scripts/test_s2_patch_entry.py` 共 **28 項全過**（標記插入／切換／撤除／冪等／保住🟤🔖／剝乾淨後 LINE_RE 仍認得／`--bite` 四種拒絕情境／側錄與非素材行守門／**0818-1600 那四支臨時腳本的真實 replay**）；⚠️ 首版有一項測試字串寫錯（「沒有 ▎BITE： 段」那條誤含「無BITE」→ 走到上一個分支，看起來通過其實沒測到），已修並在測試檔留註解。**E2E 對 0818 真實狀態檔複本實跑**（生產檔未動）：①RT6172 `--alert none`→`--alert red` 往返後 `raw_entry` **與原檔一字不差**，58 則裡只有它變、且只動 `entry_updated`／`entry_updated_ts` 兩個記帳欄位；②把真實資料裡 28 則的 `(BITE)` 拿掉模擬「剛寫完還沒補標記」，一次 `--bite` **28 則全部一字不差還原**——這正是 `_fix_ns_bite.py`＋`_fix_ap.py` 重打 18 條 entry 在做的事。**文件同步**：`s2_scan_prompt.md` 步驟 4（每輪必做的 🔴🟡）改指 `patch-entry` 並明令⛔不准重打／不准自寫臨時 .py；`13c §2a` 口令對照表三列改用 `patch-entry`（含新增「撤掉紅圈／橘圈」）；`13c §497` 品質掃那條補上 `--bite` 的用法與「刻意不自動補」的理由。⚠️ **尚未經正式輪實戰驗收**，下一輪起看 `_fix_*.py` 這類臨時腳本會不會消失。📌 **順帶發現（未修，待裁決）**：`test_s2_subalert.py:58` 是**過期測試**——它期望檔頭圖例寫「🟡=重大未進檔頭」，但現行 `header_from_lines` 產出的是「🟡=次重大」，純措辭漂移不是產品 bug，但它讓整套測試長期紅一項、會蓋掉真的迴歸訊號；另 `test_s2_render.py` 需要三個 CLI 參數才能跑（是 diff 工具不是自走測試），裸跑必失敗，不算失敗項。**🔴 上線當天 hotfix（同日，複核時抓到）：`patch-entry` 會靜默清掉 `needs_review`**——`apply_update` 的收尾是「重算 `bite_doubt`，算不出來就 `pop('needs_review')`」，那條的前提是呼叫端**帶著新的 sb_count 進來**（`update-entry` 補完整稿＝真的有新資訊才有資格結案）；`patch-entry` 只動標記、手上沒有 sb_count 也沒有 footage_type，重算出的 doubt 幾乎必為 None，於是**光標一個 🟡 就把留痕清掉**。**真實資料實測確認**：RT6164（稿未到）`needs-review add` 後跑一次 `--alert yellow`，留痕就消失。⚠️ 這踩的正好是 **R6 的教訓「跨輪交辦要走狀態檔的 needs-review，不是帳本」**——留痕被無聲吃掉等於那條路也斷了。修法：`cmd_patch_entry` 在 `apply_update` 前後把 `needs_review` 原樣放回（本來沒有就維持沒有），本指令對它**一律中性**；補 2 項測試。⚠️ **為什麼首輪 E2E 沒抓到**：兩個 E2E 都**只比對 `raw_entry`**，其他欄位的差異根本不會浮出來——已改成**全欄位比對**重跑，現在差異只剩 `entry_updated_ts`（記帳）＋刻意塞的 `needs_review`。**同批另修兩處**：①`bite_tag_patchable` 的判準原本掃整行、`insert_bite_tag` 卻只切第一個 `▎` 之前的 head，括號裡夾了 `▎` 的畸形行會放行卻 `rfind(')')` 回 −1 → 產出開頭多空格的壞行，而 `check_entry()` 對 LINE_RE 不 match 的行**回空清單＝壞掉還不報**、整行從品質掃與檔頭統計靜默消失（本 repo 記過三次的同一種坑）；已改成兩邊看同一段字＋補測試。②`s2_scan_prompt.md` 補「**排程輪只加不撤**」——既有不變式是「🔴/🟡 標過永久保留」，以前撤標記要重打全文＝有摩擦擋著，現在一個指令就撤，不寫明的話排程 agent 會自行「順手」撤掉；撤除與 🔴→🟡 降級一律只有使用者口令才做（比照 `set-aired` 的「人工判斷，agent 不自行標」）。**✅ 0818-1800 首輪實戰驗收 PASS（使用者要求體檢，main 查證）**：23.1 分／收 40 則／離開碼 0／品質掃 **0 命中**／三站 `src_text` **零缺漏**／對帳 **窗內未收 0**。①**臨時腳本歸零**——1600 輪 9 支自寫 `.py`，1800 輪 **0 支**（transcript 91 次 Bash 逐條掃過，`python -c` 只試 1 次、被 D9 hook 攔下並指路，A11 未復發）。②**`patch-entry` 真的被用了、而且是設計中的用法**：兩次呼叫 `--ids AP4679087,AP4679085,RT6134 --alert red` 與 `--ids RT6233,RT6194,RT6211,RT6199,RT6105,AP4679081,RT1216,RT6209 --alert yellow`，**11 則標記 2 次呼叫做完**，換成舊路徑就是 11 條完整 entry 重打（1600 輪的 `_fix_markers.py` 是 6,315 bytes 換 9 個 emoji）。③連帶效應：`Bash（其他）` 28→9、`Write` 14→4、`Edit` 7→1、`update-entry` 6→1（僅剩的那 1 次是 3 則 RT pending 補完整稿＋src_text，**是 update-entry 的正當用途，不是浪費**），`s2_batch_prep.py` 17→54（改用標準工具）。整輪 cache_read **35.8M→28.0M**、每呼叫 245k→**226k（今日最低）**。⚠️ **驗收的限制，別過度宣稱**：(a) 這 11 則**都沒有 `needs_review`**，所以 needs_review 那個 hotfix **這輪沒被實際觸發**——它目前只有單元測試背書，尚未經生產驗證；(b) 1600→1800 之間同時改了 `13c` 與 `prompt` 兩份規則，遙測報表自己也標了「數字變化不一定是腳本的功勞」，單輪不宜歸因到底幾成是 patch-entry；(c) `--bite` 這輪**零次使用**（三站 batch 組稿當下就把 `(BITE)` 寫對了＝更好的結果，但等於這條路徑仍未實戰驗過）。📌 **同輪新發現（A2 的新變體，非舊 bug 復發）**：`inspect` **沒有 offset／範圍分頁**——只有 `--limit N`（前 N 筆）與 `--index i`（單筆）。RT 站要逐則讀 `story` 全文時，`--limit` 會把看過的重印一次，於是 agent 只能用 `--index` 單步走：**10:15:03–10:19:19 連續 16 次單筆 `inspect`**（中間還為此查了一次 `--help`），約 4.3 分鐘、多燒約 2.7M cache_read（12 次多餘呼叫 × 226k）。這跟 0818-1000 那次「`--ids` 支援逗號卻連打 19 次」**根因不同**：那次是沒想到用逗號（行為問題），這次是**工具真的沒有分頁能力**（能力缺口）。修法建議：`inspect` 加 `--offset`（配 `--limit` 成範圍）或 `--range a-b`，屬 A12 桶②的同一批工具 delta，未動工 |

| A13 | 🔴 `inspect --lengths` **靜默漏掉巢狀欄位**→agent 誤判「AP API 缺 script」→整站退回逐頁開詳情 | 0818-2200 體檢（使用者要求，main 查證） | — | ✅2026-08-18（`3e18165`）**待實戰驗收** | **0818-2200 那輪 AP 只收 12 則卻燒掉 89 次呼叫／10.5 分**（對照 0818-1800：14 則、31 次、5.7 分——**更少的素材、將近三倍的呼叫**）。逐段還原：①14:11:16 agent 用官方 API 一次撈 15 則詳情，**成功**，落檔 `ap_details_2200.json`（1.26 MB，⚠️ 不是 R14／檔案確實寫出來了）；②14:11:28 跑 `s2_batch_prep.py inspect … --site ap --lengths` 看欄位——**輸出裡完全沒有 `script`／`caption`**；③14:11:37 跑 `search --contains script --field all` → 「共 15 筆，命中 0 處」；④兩個標準工具都說沒有，agent 於 14:12:23 判定「AP 官方 API 詳情端點欄位缺 script/caption（連兩則皆然），依規則退到 §1b 直開頁面」——**這是照 13c §1a「失敗兩次退 §1b」走的，agent 沒有違規**；⑤於是逐則 navigate 14 個 `detail/x/<hash>/video` 詳情頁，正是 13c §1a 明訂要避免的「完全不開詳情頁」的反面。**但資料一直都在**：main 離線驗證該檔 **15/15 筆的 `_source.script.nitf` 都有完整 SHOTLIST／SOUNDBITE**，等於整段 fallback 全是白做的。**根因是工具說謊，不是站方改版**：`cmd_inspect` 的 `--lengths` 分支只挑 `isinstance(v, str)` 的欄位列出（`s2_batch_prep.py:458`），而 AP 的 `script`／`caption` 是 `{'words': N, 'nitf': '<p>…</p>'}` **dict**，於是被靜默略過；明確指名 `--fields script,caption` 更糟——回的是 **`<無此欄位或非文字>`**，一個存在且有內容的欄位被工具斬釘截鐵說成不存在。⚠️ **這是既有 bug 的第二層**：D9 步驟②的前置已經修過「inspect 看不穿 AP ES `_source`」，但只修了第一層巢狀；同一支腳本的 `cmd_search`（`:513-514`）與 `_title_of_any`（`:411-412`）**都已經會處理 `{k}.nitf`**，唯獨 `--lengths` 沒跟上，三條路徑不一致。**修法**：`--lengths` 比照 `cmd_search` 把 `{k}.nitf` 攤平後再量長度（⛔ 不要寫第二套攤平邏輯，抽共用 helper），並讓「欄位存在但非字串」與「真的沒有這個欄位」講不同的話——現在兩者共用同一句 `<無此欄位或非文字>`，正是誤導的來源。📌 **另一半是 agent 誤用、非工具 bug**：`search --contains script` 是拿 **script 這個字串去搜「內容」**，不是查「有沒有叫 script 的欄位」，回 0 命中完全正確（main 實測 `--contains SOUNDBITE --field all` 就正常命中 `[script.nitf]`）。所以修完工具還要在 13d §4 補一句：`--contains` 搜的是值不是欄位名，要確認欄位存不存在請用 `inspect --fields`。****✅ 已修（`3e18165`，使用者指示專心修本輪 bug）**：查證時另抓到**第三處**同病——`cmd_search` 的**指名欄位**路徑（`--field script`）也只認 `isinstance(str)`，在 AP 檔上同樣永遠 0 命中，等於**同一份檔案換個參數會得到相反的答案**（`--field all` 命中、`--field script` 0 命中）。修法：抽 `_nitf_text()`／`flatten_text_fields()`／`text_field()` 三支共用 helper，`--lengths` 與 search 指名欄位路徑都改走它，⛔ 不再讓任何一條路徑自寫一份；並把 `<無此欄位>`（真的沒有）與 `<欄位存在但非文字>`（欄位在、只是不是文字，如 `shots` 是陣列）拆成兩句話——原本共用同一句正是誤導的另一半。**驗證**：`test_s2_batch_prep.py` 47→**54 項全過**（新增 7 項，含 NS／RT 純字串形狀無回歸）；⚠️ **並用 stash 反驗新測試不是空過**——撤掉修正後新測試 4 項轉紅、還原後全綠。用 0818-2200 那份真實 `ap_details_2200.json` 實跑：三條路徑現在都看得到 `script.nitf=len:3586`／`caption.nitf`。**文件**：13d §4 補兩條——①查欄位在不在用 `inspect --lengths`，⛔ 不要用 `search --contains`（它搜的是**內容**不是欄位名，正常稿子內文本來就不會出現 "script" 這個字，回 0 命中是正確行為）；②**「工具說沒有」不等於「站方沒給」，退 §1b 之前先換一支工具確認**。**其餘同輪次數據**：33.3 分／56 則（全晚最多）／離開碼 0／品質掃 **0 命中**；每則 0.59 分 vs 1800 的 0.58＝**逐則效率沒退步，長是長在 AP 這段**；`/home` 在 14:08:27–14:11:04 連續 navigate **11 次**（已知的 AP `/home` 偶跳 `/live`，另計約 2.5 分）；>60s 停頓 5 段共 10.3 分（多為長生成）；RT 呼叫數與 1800 相同（36 次）但耗時 7.4→12.6 分，收 24 則為三站最多，屬量的差異未見異常 |

| A14 | 0818-2000「本輪 0 則——多半是三站都進不去」是**誤報**（實收 45 則） | 0818-2200 體檢連帶查出 | — | 🔶另一 session 已修但**未 commit＋弄壞測試** | `_輪次紀錄.txt` 的 `0818-2000 DONE … 本輪新增=0 則 ⚠️ 本輪 0 則——多半是三站都進不去`，但狀態檔實際有 **45 則** `first_seen_checkpoint=0818-2000`（NS 12／AP 12／RT 21），transcript 也顯示三站 `add-batch` 分別在 20:05／20:10／20:16 正常執行、20:24:26 `set-top` 推進、20:24:29 render，DONE 在 20:25:01。⚠️ **這個警報正是用來抓 0807 那種「三站全進不去卻離開碼 0、看起來跟成功的一輪一模一樣」的無聲全失敗**，一旦會喊狼來了，真的失敗那天就會被當雜訊略過——`s2_scan.ps1:434` 自己的註解寫著「誤報比漏報更傷」。**根因（另一 session 已查出並寫進程式碼註解）**：2000 輪 agent 組 `add-batch` 來源檔時，`checkpoint` 誤填成**查詢視窗的講法**（如 `18:00`）而不是 `{MMDD}-{HHMM}`，`first_seen_checkpoint` 就以錯誤格式入庫，外層 `s2_scan.ps1` 用精確比對算「本輪新增」時全部漏算 → 報 0。⚠️ **main 本次體檢一度誤判為「G: 雲端硬碟讀到舊檔」**（因為事後重算得到 45，而當時資料早在 20:05 就落地）——那個假設是錯的，真因是格式，事後已被 `fix-first-seen` 修正過所以現在看起來正常，記在這裡免得後人重走同一條錯路。**現況風險（本次體檢發現，需處理）**：該 session 的修法**還躺在工作目錄沒 commit**——`scripts/s2_state.py` 未提交 39 行：`add-batch` 加 `CHECKPOINT_RE` 格式閘＋新增 `fix-first-seen` 子指令（函式／argparse／dispatch 都已接上、可正常呼叫）。但 **stash 對照實測：這段改動弄壞 `test_s2_ftguard.py`**（改動前 PASS、改動後 FAIL 5+ 項），因為測試 fixture 用的是 `"checkpoint": "t"` 這種樁值，被新的格式閘擋掉、素材沒入庫。**生產不受影響**（狀態檔內所有真實 checkpoint 都是合法格式，已核）。待辦：把 fixture 改成合法格式（`0818-2000` 之類）讓測試綠回來，然後 commit——⛔ **main 未擅自 commit 他人在途改動**。另注意 0818-2200 的遙測記著 `repo.dirty: True`，代表每 2 小時的排程輪正跑在這份未提交的程式碼上 |

| A6 | 建檔輪自動帶入**常駐中主題**（固定中分類） | 使用者 2026-08-17 | — | 🔶待實戰驗收 | 「烏俄底下永遠要有【俄轟烏】【烏轟俄】」原本得每天手動 `set-resident-topics`，忘了不報錯只靜靜沒有（同 0810 建檔那件事的形狀）。修法：`scripts/s2_resident_topics.json` 存每日預設，`s2_scan.ps1 New-ShiftState` 建檔時寫進 `resident_topics`；設定檔壞掉不弄死建檔但留 `NEWDAY WARN`。文件見 `13b §5a` ③。**已離線驗**（scratch 假狀態檔→render：`======烏俄======` 下出現裸的【俄轟烏】【烏轟俄】、品質掃 0 命中、JSON 往返中文與陣列無損）。⚠️ 0817-1600 建檔已過，**首次自動生效是 0818-1600**；今天要有得下 `set-resident-topics` 當日補設。排程 agent 會在 `_輪次紀錄.txt` 看到沒見過的 `NEWDAY 常駐中主題已帶入` 行，屬正常 |
| A8 | ENEX 整則排除規則新增 **`TAIWAN PLUS`**（原本只排 `TVBS`） | 使用者 2026-08-17 | — | ✅2026-08-17 | 文件 `18-交換平台素材整併.md` §5 已加第二條排除（`/\bTAIWAN\s*PLUS\b/i.test(partner)`）。0817-2215 那批已入庫的 2 則 TAIWAN PLUS 素材（ENEX927257 漢光軍演假訊息攻勢／ENEX927267 臺灣所得差距）依使用者指示一併 `remove` 撤出狀態檔＋重 render（293→291 則，品質掃 0 命中）。⚠️ 這是規則文字修改，尚未有機會被下一輪掃帶 agent 讀到／實戰驗證，agent 若跳過 TAIWAN PLUS 素材屬正常，看到照收才是漏規則 |
| A9 | **ENEX／ABC 整併入排程掃帶**（自動化補完，使三站以外的交換平台也能進固定輪） | 落差報告（2026-08-18） | — | 🔶2026-08-18（③⑥ 完成，餘待做） | 先行研究已完成，規格與查證依據一律看 `common/plans/2026-08-18-ENEX-ABC與三站排程掃帶技術落差報告.md`，本表不重抄。該報告核實：MASTER 原本**零筆** ENEX/ABC 自動化待做項（只有已完成的 A8），9 項落差全在列管之外。⚠️ 「是否真的進每 2 小時固定輪」本身是裁決題——`18-交換平台素材整併.md` §0 現行明文是「人工下令才跑，不進固定排程」，且外部 agent 禁止直寫狀態檔（0810 有 44 處差點被無聲覆蓋的實例）；提高頻率＝排程外殼／鎖／補漏三件要從零補。動工順序建議由落差 #3（候選檔→`add-batch`/`set-category` 轉換工具，本次整併已證實需求）起步，它不改變執行行為、風險最低。**🔴 2026-08-18 使用者訂：本項的工具一律當「獨立流程」做，⛔ 不併入三站掃帶輪**——`s2_scan.ps1`／13b／13c／13d 一個字都不改，工具不被固定輪呼叫。後人不要順手把 lint／merge 接進掃帶輪。**進度**：③⑥⑦⑧ 已完成（`2e69b1f`／`ae312ad`／`d77f079`）。⚠️ 已知縫：lint 對「`src_text` 混入 agent 中文說明」只擋得住「與 `raw_entry` 一字不差」與過短兩種，**RT4131 那種「原文尾巴附加一段中文說明」的形狀擋不到**——三站是用「原文全英文、出現中文即 agent 所寫」的機械判準，但 ENEX 的 CCTV 稿天然可能含中文，那條判準不適用，故刻意未做（`2e69b1f`＋跟進 commit：兩支腳本自行 `reconfigure(stdout/stderr, utf-8)`——本機 python 預設 cp950，`print('⛔')` 實測會 UnicodeEncodeError，不修等於 agent 在裸 PowerShell 一跑就炸；`--apply` 的子行程另帶 `PYTHONIOENCODING=utf-8`）（`s2_platform_merge.py`／`s2_platform_lint.py`，49 項測試 `scripts/test_s2_platform.py`，含 0817 兩份真實候選檔 replay——32 則 `raw_entry` 與實際入庫逐字相同；ENEX 有 1 筆分類不同＝當時人工複核搬過大分類，非工具錯）。用法寫進 18 檔新增的 §9。⑦ 未動規則文字，但 merge 報告尾會提示跑 `s2_topic_dedupe.py`。子項見下 |
| A10 | **S2 分類系統化**（大／中／小三層精準度＋跨輪一致性，TODO「全盤檢討分類問題」結案路徑） | 分類規劃（2026-08-18） | — | 🗳待裁決啟動 | 規格一律看分類規劃文件，本表不重抄。結構：P0 歷史交接語料分析→一次性裁決包（層級模型／社會與話題定位／中主題粒度命名／小分題機制擇一）→P1 主題登記簿 charter/aliases＋分類判例庫＋§4c 入庫前內容比對（防「同事件散落他處」，2026-08-18 使用者補案）→P2 收斂輪制度化。⚠️ 三道閘：①「檢討完成前不做分類機械檢查／自動修正」禁令仍生效，P0 沒結案只准做 P0；②P0 歷史分析啟動前要先向使用者報價確認（高 token）；③裁決包 7 題（D-a~D-g）屬 D-類，未裁決不動工。前置：歷史範例前 10 份 `.gdoc` 本機讀不到，需請使用者轉檔或走 Drive |
| A7 | reconcile `missing` 無法區分「刻意去重」與「真的漏收」 | 0817-2200 體檢 | — | ⬜待做 | 0817-2200 首次出現 AP `missing=4`（AP5467678/80/81/82）。查證結果**不是漏收**：agent 判定它們與 AP5467677／AP5467679 標題及 script 逐字相同（SNTV 同主題不同剪輯／Vertical 版），刻意只收 1 則，並在 state 建了 `script_status=note` 的存根項、把理由寫進 `needs_review`——處置本身正確且有留痕。但 reconcile 只比對「list 有、batch 沒有」，把刻意去重也算成 missing，使得 `missing≠0` 這個最重要的漏收警訊被稀釋。建議：reconcile 比對時把 state 內 `script_status=note` 的 id 視為已處置，另立 `deduped` 計數與 `missing` 分開報。⚠️ 動工前要先確認 note 存根不會被拿來掩蓋真漏收（存根必須有 `needs_review` 理由才算數）。

**A1 子項**（各自可勾）：
- [x] 站別/階段分類（`phases` 欄位，093c713）
- [x] （281d380）階段分類器修誤標：TaskCreate/TaskUpdate 的待辦文字含「set-category」等關鍵詞會被誤判成分類階段＋黏性繼承放大（0813-0430 實例：分類顯示 7.2 分，攤開 NS 寫摘要的長思考被誤記，真值約 2.5 分）。修法：Task 類工具一律不參與階段判定、直接走繼承
- [x] （2026-08-13）`s2_state:?` 子指令認齊——對照 s2_state.py subparser 抓齊 20 個（含漏網的 `add`；`update-batch` 不存在未加；`done` 是 needs-review 次動詞不另開桶），token 邊界比對防 `add` 誤配 `add-batch`。三輪 replay：?=9/4/5 → 2/0/0（殘留 2 筆經查為 --help 與 find 檔名，正確落 ?）
- [x] （2026-08-13）`python -c` 依用途分桶（read-state/json/length-check/time-convert/other）——2000 輪 14 筆全數歸桶（json:11、read-state:3），other 佔比後續觀察 <10%
- [x] （2026-08-17）每輪記版本指紋＋launcher flags：`rule_shas`（prompt/13/13b/13c/**13d**，缺檔記 null 不省略）、`repo`（head＋dirty）、`launcher_flags`（`s2_scan.ps1` 傳 `--flags model=…;effort=…;TestMode=…;NoToolBan=…`）。⚠️ 0817-2000 起才有，之前的紀錄沒有——`--diff` 會明講「無法判斷規則是否同時變動」而不是假裝沒變
- [x] （2026-08-17）`s2_metrics_report.py`：`--last` 總表／`--diff A B` 兩輪逐桶相減（主力，對應驗收「多出的呼叫去了哪裡」）／`--tool` 單桶跨輪趨勢／`--rules` 規則版本變動。驗收實測：`--diff 0817-0730 0817-1200` 無需人工提示即指出 `python -c:json 33→0`、`s2_batch_prep.py 0→7`。離開碼 2＝讀不到（不是「沒差異」）
- [x] （2026-08-17）分桶不變量 `classified_total == tool_calls` 寫進 measure() 並在對不上時警告；`test_s2_token_metrics.py` 用 12 份真實 transcript replay（4~687 次呼叫）＋子指令 token 邊界＋版本指紋，39 項全過
- [x] **（2026-08-18 0430/0730 體檢發現並修復）「分類」桶把 NS/AP/RT 組稿寫入的長思考也吞了**：0813-0430 那次修的是 Task 工具關鍵詞誤判，這次是**不同觸發源、同一種現象**——`classify_phase` 判斷順序是「先比對關鍵字，比不到就繼承前一階段」，而 `list-topics --compact`／`show --mid <中主題>`（agent 查現有分類寫法以求措辞一致，是正確習慣）本身比對到「分類」關鍵字，但**緊接著的 `Write batch_ns/ap/rt_*.json`（組稿本體）與 `add-batch` 指令內容都不含 NS/AP/RT 關鍵字**（`ns_batch`／`batch_ns` 這種檔名不含 `_ns_`），於是整段組稿的長思考被黏著繼承成「分類」。逐筆核對量化：0818-0430 報 分類=15.0 分，其中 NS 組稿前思考 425s＋AP 組稿前 94s＋RT 組稿前 213s＝732s（12.2 分）其實是三站寫稿；0818-0730 報 分類=9.4 分，NS 250s＋AP 116s＋RT 75s＝441s（7.4 分）同樣是寫稿。**修法（不動掃帶行為，只修報表）**：①`classify_phase` 新增檔名 token 判站 fallback（`_site_from_filename`，只認完整 alnum token，`ns_batch.json`→`ns`，`AP4678987` 這種 id 不會誤配，因為正則把連續英數字整包吃掉不會拆出 `ap`）；②`phase_stat` 計算加**回看重歸屬**：`t[i]→t[i+1]` 這段時間若 ≥60 秒（跟本 repo 其他停頓健檢工具的門檻一致）且 `i+1` 是**直接命中**（非繼承）且站別跟 `i` 不同，改記給 `i+1` 的站別，短 gap／`i+1` 也是繼承一律不動。**驗證（用真實 transcript 重跑，非估算）**：0818-0430 分類 15.0→2.4 分（NS 6.0→18.7、RT 8.4→8.7）；0818-0730 分類 9.4→2.3 分（NS 4.2→9.4、AP 4.5→6.5）；0818-0100 分類 8.4→5.0 分（那筆已知的 118s 單次 G: I/O 事故因為後面沒接別站直接命中，正確地沒被回看沖掉，跟先前人工診斷一致）。新增 15 項單元／合成測試（檔名判站 10 項＋回看規則 5 個情境：直接命中前置／繼承前置／短 gap 不動／下一筆非直接不動／同站別不變）＋12 份真實 transcript replay，`test_s2_token_metrics.py` 57/57 全過

- [ ] **（2026-08-18 0818-1000 體檢發現，未修）`inspect --ids` 支援逗號分隔多筆一次查，但常被一筆一筆分開呼叫**：0818-1000 輪 10:03:00–10:04:30（~90 秒）連續 19 次 `inspect --ids <單一id>`，本可一次 `--ids A,B,C,…` 查完。量化成本：這 19 次呼叫合計燒掉 output 7,922 tokens、**cache_read 累計 2,994,809 tokens**（對話越長，每次回合重讀的快取就越貴，round-trip 次數本身就是成本，不是只看單次執行時間）。同輪比較：0818-0430 的 RT 段有用 shell for-loop 把 2–3 筆包進一次 Bash 呼叫（`for i in 2 3 4; do inspect --index $i; done`），比 1000 這種完全不批次的寫法省——但兩者都沒用到 `--ids` 本身支援的逗號分隔批次功能。修法方向：13d §4 補一句明講「多筆要查優先用 `--ids A,B,C` 一次帶完，不要逐筆呼叫」，或替 `inspect` 加更醒目的用法提示（例如印出結果時順帶提示可批次）|

**A2 子項**（各自可勾；動工前先過 D2 裁決）：
- [x] （281d380）`unwrap` — 自動偵測 AP Items 殼/RT items 殼/裸陣列/NS 純文字清單，卸成裸陣列；失敗回報實際頂層格式不靜默
- [x] （281d380）`inspect --ids/--fields/--limit/--index` — 讀取時自動卸殼、摘要模式印 id＋標題行，取代「印整批→agent 再寫 python -c 篩」
- [x] （281d380）`search --contains --field script|story|head|all`
- [x] （2026-08-17）`snapshot` — 稽核快照，預設 `_audit_{site}_{HHMM}.txt`；**已存在一律不覆寫**（覆寫＝靜默銷毀稽核依據）；清單檔沒有標題欄時退而印 `at=…` 等短欄位，不留一排空白
- [x] （2026-08-17）`compare --raw --batch` — 只印 raw 有 batch 沒有／batch 有 raw 沒有／batch 內重複 id／batch 缺欄位（預設含 `src_text`，直取 13d §5 那個踩過兩次共 145 則的坑）；乾淨只印一行。順帶修掉 `_load_raw_any` 檔案打不開會噴 traceback 的洞（`unwrap`／`inspect`／`search` 原本都中招）。15 項測試全過（`scripts/test_s2_batch_prep.py`），13d §4 已同步收錄用法
- 護欄：per-site adapter（RT 用 `code` 不是 `id`）；冪等；不覆寫舊 snapshot；子步獨立 status

**A9 子項**（＝落差報告「落差總表」9 項，各自可勾；編號對應報告章節）：
- [ ] ①ENEX／ABC 登入態保活腳本（§2）——**先研究可行性**：ABC 已實測「比照 NS 載入頁面續期」走不通（無 `Set-Cookie`），ENEX 完全沒調查過
- [ ] ②ENEX／ABC 版 `s2_batch_prep.py`：把兩份可行性計畫書裡的端點／查詢／抽取白名單／逐字保真驗證封裝成程式碼（§3；現況每輪由 agent 在 `browser_evaluate` 內從零手刻）
- [x] ③（2026-08-18）`scripts/s2_platform_merge.py` 上線（§4；需求由 0817 那批 42 則整併證實，當時靠現場手寫一次性 python）。候選鍵名→`add-batch` 參數名的對照集中在這支；預設**只產檔＋印指令**，`--apply` 才動狀態檔。護欄：**每次**呼叫 `s2_state.py` 前重查鎖檔（⛔ 只看存在不 open——open 會讓正要啟動的掃帶輪 SKIP，同 A5 的坑）／輸出落候選檔旁不落 CWD（R13 形狀）／不覆寫既有輸出／缺欄位的單筆進 `dropped` 不靜默丟／分類含 `;=/` 明確失敗帶原值／整併後翻 `merged_into_handover` 並留 `merged_log`（`reviewed` 不動）／候選檔的 `needs_review`／`known_gaps`／`needs_review_note` 逐條列出提醒手動留痕。**已對複本實跑 `--apply` 端到端驗過**（入庫＋分類＋翻旗標，生產狀態檔未動）
- [ ] ④ENEX／ABC 版清單對帳：輕量健康檢查查詢比對候選 JSON 的 `counts.掃描`，取代「agent 自己宣稱」（§5；ENEX 附錄 A 已有可延伸的查詢範本）
- [ ] ⑤`s2_mark_ingested.py` 認得候選檔「排除清單」區塊，不把 `skipped` 代碼誤判成缺件（§6；本次任務實錯，得人工改名）
- [x] ⑥（2026-08-18）`scripts/s2_platform_lint.py` 上線（§7；0810 那批 38 個 HIT 中 ABC 佔 12、ENEX 佔 3，全是進了正式狀態檔才被抓到）。⛔ **不做第二套 parser**：素材行判準轉呼三站同一支 `s2_validate.check_entry()`。加做候選檔階段特有的：**行首時段標記**（`check_entry` 會先 `strip_mark` 剝掉，違規反而看不見——含 18 檔沒列到的舊符號 `●`）／`id`↔`enex.itemId`／`abc.storyNumber` 交叉比對／ABC Story Number 前 6 碼須為合法 `MMDDYY`／ENEX 禁時長 vs ABC 必帶時長／分類名不得含 `;=/`／成對 `.txt` 在不在。**分級**：§4 格式表＋結構＝❌；缺 `src_text`／`counts` 對不上＝⚠️（⑧ 未落地前一律紅燈會讓工具直接被跳過——0817 `snapshot` 的教訓），⑧ 落地後改用 `--strict-src-text` 升級 ⚠️**0818 更正**：⑧ 當天就落地了，旗標改成反向的 `--allow-missing-src-text`（預設即 ❌，舊檔才降級），`--strict-src-text` 不存在，不要照上一句去下指令
- [x] ⑦（2026-08-18）18 檔 §7 新增「整併完的收尾」節：`s2_topic_dedupe.py --file` 列為**必跑**（不是建議），命中逐條覆核、決定不動要 `needs-review add` 留痕。理由寫進文件：0817 那 3 筆該搬大分類的素材是靠使用者臨時下令「分類全部 REVIEW 一次」才抓到，那道複核不該每次都靠人想起來。merge 的報告尾同步改成指向 §7 的收尾指令（含 --file）
- [x] ⑧（2026-08-18）18 檔 §2 candidate schema 補 `src_text` 並立為**必帶**（範例＋專節：只准站方原文／⛔ 不准混入自己的判斷說明，直接引 RT4131 連四輪誤報＋0804 那批「捲出清單就永遠查不回來」的 RT4 則／AP9 則）。lint 同步把「缺 src_text」由 ⚠️ **升為 ❌**，舊檔用 `--allow-missing-src-text` 降級（merge 也轉傳）；另加兩條實測會踩的：`src_text` 與 `raw_entry` 一字不差＝拿成品充數 → ❌、長度 <50 字 → ⚠️。`fields`／`parse_ok` 仍缺，優先度低，**未動**
- [ ] ⑨ABC 時間窗前端過濾變通法固定成腳本（§10；端點日期參數只到「日」，現靠 agent 臨場想）
- 另記（§1 順帶查出、不屬本項但該有人管）：`13-S2-定時掃帶.md` 寫 12 個檢查點、`S2掃帶.xml` 快照顯示 9 個，何者生效未核實——與 A4 的「XML 只是匯出備份不是真相源」同一類問題

**A3 子項**（各自可勾）：
- [ ] `s2_state show` 補 `--source/--status/--id-prefix/--unclassified/--missing-field/--contains`
- [ ] `resume --json`（供其他工具組合，開工查詢至少取代 3 個）
- [ ] `s2_round_gate.py` preflight（state 路徑/班次日/掃描窗/pending/needs-review/scratch/待整併/既有中主題）
- [ ] `s2_round_gate.py` postflight（頂層 checkpoint/三站 reconcile/本輪 snapshot/未結案嚴重項/last render/txt+html hash）
- [ ] （後步）launcher 記 gate 結果→依結果決定通知級別；**不以 gate 阻斷正式輪**

## D — 待使用者裁決（未裁定前不得動工）

| ID | 議題 | 來源 | 狀態 | 給裁決者的脈絡 |
|----|------|------|------|----------------|
| D1 | effort 定案：high or medium | 省T§三 vs 0812 裁定 | 🗳等E2 | 三方說法：省T計畫書寫「medium 無效不要再試」；TODO 更正為「量不出差別，先做 Task 1」；使用者 0812 21時裁定改 medium（已上線 093c713）。E2 的 P2 遙測數據出來後定案，**定案後要回寫省T計畫書§三**，免得下個 agent 被「不要再試」誤導 |
| D2 | `s2_batch_prep.py` 路線：擴充成 raw 工具層（=A2）or 就地封存 | 省T② vs 全流§8 | ✅已裁決 | 使用者 0813 裁定走工具層；unwrap/inspect/search 已上線（281d380），13d §4 立為標準用法 | 強制版已回滾（25133eb），腳本現為選用。全流分析主張「已完成第一版、應擴到 raw 工具層」；1600/1800 實測 dump/build 只補到一部分 |
| D3 | audit §4「還有 N 組未列出」提示去留 | 交接§8 | 🗳 | 抓真問題（今日抓到 RT4881 錯分類）但每輪多花時間。接手 agent 建議保留 |
| D4 | 無變動快速路徑（diff=0 輪跳過語意分類與全量 topic review） | 全流§Phase5 | 🗳 | 中高風險，改變執行行為 |
| D5 | watchdog 自動代打（A5 第二階段） | 全流§4.7 | 🗳 | 累積 3 次真實「中途死亡」樣本後再議 |
| D6 | S2b C 通道排程化 | 全流§Phase5 | 🗳 | 使用者已明示先人工跑幾天，觀察期未結束不接進 22:00 |
| D7 | launcher 把 agent 工作目錄設成 scratch-dir（R13 硬解） | 1200驗收 | ✅255313d | 使用者 0813 裁定做。launcher 解析班次日→Set-Location scratch 夾；transcript 目錄跟 cwd 走，metrics 加 --transcript-dir（退回保護）。**0813-1600 實戰：暫存檔 20 個全落 scratch、repo 根零新增＝R13 根治**。⚠️ 連動踩雷：transcript 目錄淨化規則猜錯（非英數一律換 `-`，非只換 `:\`），量測器退回舊目錄把 1200 數字記成 1600；已修（3f83554）＋誤記已刪並重記真值 |
| D9 | `python -c` 硬排除（A2 行為漂移的硬解） | A2 0817 觀察 | 🔶2026-08-18 步驟②③已熱修上線，待實戰驗收 | 0817 五輪 `python -c:json` 呈 26／33／0／0／36——13c §1a 的文字澄清擋不住，形狀跟 T2「Task 工具靠自律會漂移」完全一樣，而 T2 的解是 launcher `--disallowedTools` 硬排除。可行做法：把 `Bash` 裡的 `python -c` 走**不排除但改導引**（風險低）或比照 T2 直接擋掉（風險高——`python -c` 有正當用途，例如查狀態檔以外的一次性計算，全擋可能逼出更糟的變通）。**兩條路都會改變 agent 行為，未裁決前不動工**。0817-2000 逐筆查證後，建議收斂成三段式而非一刀全擋：①**檢查 raw／batch**（8/14 筆）→ 已有 `inspect`／`compare`，可硬擋；②**讀生產狀態檔**（3/14 筆）→ 13c §2 早就明文禁止，硬擋無爭議；③**數中文字數**（3/14 筆）→ 沒有替代工具，硬擋會逼出更糟的變通，應先補一個 `s2_state.py`／`s2_batch_prep.py` 的字數檢查子指令再談。⚠️ 若要走工具層硬擋，比對字串必須同時涵蓋 `python -c` 與 **`python3 -c`** **0817-2200 補證**：新做的 `snapshot` 子指令當輪就被 agent 用 ad-hoc python 重造了一次（自寫 `_audit_ap_2200.json`）——顯示「補工具」單獨做不夠，必須配合工具層導引／硬擋，否則新工具只會被忽略。 **✅ 2026-08-17 使用者裁決：照三步走，一次一項。** ⚠️ 裁決前把 0817-2200 的 23 筆逐一對照現有工具能力，結論修正了原本的建議：1800（`s2_batch_prep`=0、`python -c`=36）是**工具完全沒被想起來**，2200（`s2_batch_prep`=7、`python -c`=23）是**用了還是繞過去**——兩種失敗模式不同。23 筆拆開是：**17 筆現有工具做得到**（印 id、數重複、欄位探索、set 差集、依 id 撈內容、產稽核檔）＝真漂移該擋；**5 筆現有工具做不到**＝NS 去重要比兩則 script 是否逐字相同，`inspect` 只能印出來、沒有「相同與否」的答案；**1 筆本來就不該擋**＝寫修正後的稿字串（產出內容不是檢查資料）。**❗️ 更正原本「比照 T2」的寫法**：T2 擋的是 `Agent`／`TaskCreate` 這種完整工具名，`--disallowedTools` 剛好適用；`python -c` 是 `Bash` 的參數內容，前綴比對擋不乾淨——0817-2200 有現成鐵證，#4／#17／#18／#19 四筆開頭是 `export PYTHONIOENCODING=utf-8` 換行才接 `python3 -c`，前綴規則會**直接漏掉 23 筆裡的 4 筆**。因此第二步要走 **PreToolUse hook 做整串 regex**，不是 `--disallowedTools`。hook 另有一個附帶好處，正好治 2200「新工具存在卻沒被選用」：**擋下來時可以回一句話指路**。 **步驟①（2026-08-17 完成）**：`s2_batch_prep.py dedup-check` 上線，34 項測試全過，已用 0817-2200 真實檔重演（NS EN-32/EN-31、MI-15/MI-14、WE-003/WE-002，AP 清單三則），結論與當輪 agent 的判斷一致。實作中查出兩件原設計沒想到的事：**(a) NS 的 script 夾 `<p>`／`<b>`**，只比空白的話「第一個差異」會指到 `<b>` 這種排版雜訊（EN-32 vs EN-31 原本指到第 39 字，剝掉標記後才看到真正差異在第 193 字），所以多加一層「只差 HTML 標記」判定；**(b) AP 清單檔是 ES 形狀**，id 在 `_source.editorialid`，不看穿這層的話 AP 去重完全比不動——而 0817-2200 的 AP 去重正是在清單檔上做的。**步驟②（hook）與③（只擋碰暫存夾／狀態檔的呼叫）待 dedup-check 實戰驗過再上。** ⚠️ **步驟②的前置條件（覆核時發現）**：hook 的拒絕訊息會把 agent 導向 `inspect`／`dedup-check`，但 `inspect` **沒有 `--site`、且在 AP 清單檔上會把 id 解成 ES 的 `_id` 雜湊**（實測印出 `dcf0346f…` 而不是 `AP5467681`）。若導向一支在「AP 去重真正發生的那份檔案」上找不到 `AP5467681` 的工具，agent 會直接彈回 `python -c`，hook 變成空轉。所以步驟② 上線前，要嘛先讓 `inspect` 也看穿 `_source`（比照 dedup-check 的 `_dedup_view`／`_dedup_id`），要嘛拒絕訊息要**按情境指名正確的子指令**。 另記：`dedup-check` 首版覆核抓到一個測試沒蓋到的破口——`same_markup` 判定沒被納入結論行的彙總，導致「只差標記」的配對在結論行印出**空白**（`MK-01 vs MK-02：` 後面什麼都沒有），正好違反本表護欄「空欄位必須明確失敗」，而且那一行是掃帶 agent 唯一會讀的一行。已修：四種判定在結論行各有措辭（相同／去空白後相同／剝標記後相同／不同／兩邊皆空），任何配對出現未判定就直接 exit 1，並補了 5 項測試（39 項全過）。 另：`test_s2_token_metrics.py` 的 PASS 數會隨當日可用 transcript 份數浮動（0817 是 12 份／35 項），但它會明印「replay：N 份 transcript」，不是靜默跳過，無違反護欄。 **步驟②③（2026-08-18 完成熱修上線）**：依**使用者 2026-08-18 明令「把 D9 剩餘部分完成、熱修上線」**執行，蓋過原「待 dedup-check 實戰驗過再上」的排程。內容：①前置先修——`inspect`／`search`／`snapshot` 全面看穿 AP ES `_source`（id 解成 `AP<editorialid>`、`--fields` 取得到欄位；`editorialid` 存在時不給 `--site` 也自動判 AP），補 `inspect --lengths` 字數子指令（0817-2000 三筆「數中文摘要字數」的工具缺口）；②`s2_bash_guard.py` PreToolUse hook 上線——整串 regex 涵蓋 `python`/`python3`/`py`＋中間旗標＋export 換行前綴（0817-2200 的 4/23 案例），matcher 同時掛 `Bash|PowerShell`（0730 輪 29 次 PowerShell 繞道也蓋到）；③範圍照裁決收窄——只擋「內聯 python ＋ 碰 `.json`／`-s2-state.json`」，狀態檔與 raw json 各給指路訊息（deny 訊息含可直接貼的替代指令＝教學機制），`.txt`／純計算／標準腳本呼叫 fail-open；hook 自身解析失敗一律放行不弄死掃帶輪，輸出 ensure_ascii 防 cp950。接線：`s2_scan.ps1` 掛 `--settings`（絕對路徑正斜線），獨立 `-NoBashGuard` 回退開關（不與 -NoToolBan 共用），遙測 `launcher_flags` 加記 NoBashGuard。驗證：`test_s2_bash_guard.py` 20/20（含負向自我 DoS 檢查：dedup-check／--checkpoint／s2_state show／純計算必放行）、`test_s2_batch_prep.py` 47/47（新增 AP ES 殼 10 項）、DryRun 參數組對、**真接線探測 PASS**（單發 claude -p＋bypassPermissions，deny 訊息確實回到 agent 手上）。文件：13d §4 更新＋新增 §10。⚠️ 待辦：與 T1 的 A3（`--setting-sources project`）交會——A3 測試組要親驗 hook 仍生效（見 T1 列）。**同日補**：①RAW_HINT 加「要產出／寫入 json 改用 Write 工具」出路（0817-2200 那 1 筆「寫稿字串不該擋」的迴圈封掉，防 heredoc／先落 .py 繞道）；②RAW_HINT 分支也過真接線探測（單發 haiku probe，檔名代入、訊息完整回到 agent 手上；先前兩次 probe 都走 STATE_HINT 分支）。**⚠️ 驗收口徑（下個 agent 必讀，防 E4 式量錯尺）**：被 hook deny 的呼叫**照樣被遙測計數**（A0 測試實證：probe 被擋、`python -c:read-state` 仍記 1），所以 D9 上線後 `python -c:json` 桶**不會歸零**——「歸零」不是驗收標準，拿它判定會誤診 hook 失效。正確口徑：每筆嘗試後緊跟工具切換（`s2_batch_prep.py` 呼叫數上升）、且無同款指令連續重打。**0818-0100 首輪實戰驗收 PASS**：`python -c` 全輪只試 2 次（一次直讀狀態檔、一次檢查 ap_list raw）→ 兩次都被 hook 攔下 → agent 立即改用標準工具**無重試迴圈**；`s2_batch_prep.py` 共 56 次（inspect 50／search 6／dedup-check 4／compare 3），對照 0817-1800 的 `python -c:json`=36 次＋batch_prep=0，行為完全翻轉——正是 hook 指路要的效果。⚠️ 續觀項：inspect 50 次略密（90 則大輪情有可原），看後續正常量輪次會不會收斂。**0818-0100 深查補記（使用者追問分類/NS 兩段）**：全輪執行僅 4.5 分、思考 27.2 分＝思考主導。①分類 8.4 分裡有 **118 秒是單支 `s2_state show` 的執行時間**（同輪其他 52 次 s2_state 都 ≤1 秒，判定 G: 雲端硬碟單次 I/O 塞住，非迴圈非程式問題，再發生才立案）；其餘是 90 則的正常一站一批分類。②NS 10 分最大兩筆是 inspect 完**想 122 秒→Write batch_ns**（把 NS 大宗寫成素材行的純生成）＋數段 30–40 秒挑則思考；inspect 逐批用法=單次便宜筆數多，屬新工具正常形狀。③**發現 hook 預期中的繞道變體**：agent 自寫 3 支臨時 .py 落地執行（`_merge_src_ap/rt.py` 合併 src_text 進 batch、`_build_audit_lists.py` 轉 audit 清單格式）——但這三件事**目前沒有標準工具**，屬工具缺口非惡意繞（有自清 rm），需求形狀同 A9③ 轉換工具；若要收編，補 `s2_batch_prep.py` 對應子指令再談擋。④一筆 Write 到打錯字的幻覺路徑 `G:\我的雲端裸端硬碟_placeholder.txt`，ENOENT 失敗無殘留，一次性瑕疵不立案 |
| D8 | 2200 的 65 則＋1200 的 80 則 src_text 人工回補 | R11 | ❌裁定不補 | 使用者 0813 裁定不補（高 token、素材老化）。防呆已上線（6ecfc17）之後不會再發生；這 145 則缺原文為已接受的既成事實，後人不要再立案 |

## ❌ 明文不做（防止後人重提）

| 項目 | 出處與理由 |
|------|-----------|
| Task 6 三站固定 collector | 省T§六：API 漂移會從「agent 當場繞過」變「無聲失敗」，投報率九項最差 |
| Task 7 LLM 只讀 normalized delta | 依賴 Task 6，一併延後 |
| Task 9 Message Batches | 降 API 計價不降 token，本流程走 subscription CLI 不適用 |
| Haiku 模型分流 | 前綴 64.7k 未砍前，雙模型雙冷啟動不可能划算 |
| Prompt caching 專項 | 降為觀察項，Task 1 已順手記 cache 欄位 |
| ~~R7~~ src_text 混入中文說明 9 筆清洗（RT1395/3501/3613/3657/4161/4631/4641/4820＋截斷標記） | **使用者 2026-08-18 裁定直接刪除**，原列已從 R 表移除。理由：要回站方重抓原文成本高、素材已老化，只影響事後離線查證依據。防呆已上線（6ecfc17），不會再新增同類。後人不要再立案 |
| ~~R11~~ 0812-2200 的 65 則＋0813-1200 的 80 則缺 src_text | **使用者 2026-08-18 裁定直接刪除**，原列已從 R 表移除。根因早已破案（agent 組批漏欄位，非工具 bug）、防呆已上線（6ecfc17），回補部分先前已由 D8 裁定不補；本列已無殘留待辦。後人不要再立案 |

## ⛔ 不可 merge 的殘留分支（2026-08-18 全分支盤點，使用者要求）

**內容都已經用別的路徑進了 main，分支本身是殘骸；merge 回去不是補齊而是倒退。**
後人看到「有分支沒 merge」不要好心合併，先讀這張表。

| 分支 | 為什麼不可 merge |
|---|---|
| `agent/nhk-side-recording-6digit-tc`（2026-08-02） | P-045 早已在 main，**且已被 P-047 修正／部分作廢**（`00-寫稿通則.md:105,107`、`06-auto-script-sot.md:95` 都明寫「修正 P-045」）。merge 會把作廢的舊規則帶回來 |
| `agent/verified-update-without-footage`（2026-08-02） | P-044 的規則文字早已在 main（`06-auto-script-sot.md:44`）。但這支還帶著 **262 行版的 `scripts/s2_state.py`，main 現在是 1680 行**——merge＝砍掉 1400 行、把狀態檔工具打回 8/02。⚠️ 另有 1 個**從未推上遠端的本機 commit**（`51c8782`），只存在當時那台機器 |
| `agent/rules-v2-sandbox`（2026-07-21，7 commits） | 廢棄的規則去重實驗。main **沒有** `sandbox/`，merge 會憑空加回 20 個檔的舊沙盒（內容是 7/21 版規則，早被推翻） |

📌 同次盤點的其餘結論：**main 本身乾淨**（與 origin/main 零落差；追蹤內容只有 4 個根檔＋目錄，repo 根那堆 `川想擴戰2200 #NN`／`_ns_list_*.txt`／`NE-022SA *` 散落檔**全被 gitignore 擋住**，沒有一個進版控）。`docs/tai-character-convention` 是唯一真的有東西沒進 main 的——臺／台完整規範（對照表＋專有名詞例外＋不溯及既往）當時只活在該分支，main 上僅 `17-網址素材整併.md` 一行摘要，而 17 是講網址素材整併的檔、agent 讀 `00-寫稿通則.md` 根本看不到，**規則與實作脫節了 9 天**；已於本次 cherry-pick 進 main（`fcae283`）並把 17 那行改為指回 00＝唯一來源。另 `docs/learning-0731-俄炸朝彈2200`／`docs/x-material-detection-fix` 遠端已刪、內容全數已合併（0 個未合併 commit），本次一併刪除本機空殼。

⚠️ **流程風險（同日觀察，寫給所有 agent）**：2026-08-18 有多個 session 同時在這個 repo 工作並**反覆切換 HEAD 分支**，一天內至少被切走 3 次。已造成一次實害——main 的 S2 改進 commit 被誤推到 `feat/human-dub-digest` 上（`git push origin main` 推的是沒動過的本機 main＝空推，看起來成功其實什麼都沒推），事後才靠 `git status --short --branch` 發現並 cherry-pick 回 main。**鐵律：每次要寫 repo 前先 `git status --short --branch` 確認 HEAD，不要相信上一次的分支狀態**（全域 CLAUDE.md 早有這條，該次就是沒照做）。

## 實作共同護欄（摘自全流§九，全項目適用）

一次只上一項；歷史 replay→shadow→TestMode→1 正式輪→3 連續正式輪；比 normalized
state mutation 不比文字回覆；不覆寫舊 snapshot；錯誤帶原始訊息不回單一 boolean；
欄位全空必須明確失敗；新路徑跑完一個完整班次＋人工確認前，不刪現行規則與腳本。

---

## 附錄：來源文件 → MASTER 對照（防漏收查核表）

| 全流程分析 | MASTER | | 省T計畫 | MASTER |
|---|---|---|---|---|
| §4.1 unwrap | A2 | | Task 2 A0-A3 | T1 |
| §4.2 inspect/search/snapshot/compare | A2 | | Task 3 prompt 衝突 | T3 |
| §4.3 show 篩選＋resume --json | A3 | | Task 4 規則分片 | T4 |
| §4.4 排除 Task 工具（A4 實驗） | T2 | | ② batch_prep 路線 | D2→A2 |
| §4.5 遙測細分＋版本 hash | A1 | | Task 5 | 併A2 |
| §4.6 round gate | A3 | | Task 6/7/9、Haiku、caching | ❌不做 |
| §4.7 watchdog 中途死亡 | A5＋D5 | | Task 8 effort | D1 |
| §4.8 schedule check | A4 | | 複核 P0-1~P0-6/P1 | R2~R5/R8~R10 |
| Phase 5 快速路徑/S2b/collector | D4/D6/❌ | | 交接§6-1 RT/§6-2 推播/§6-3 漏收 | R1/歸檔/歸檔 |
| §九 護欄 | 護欄段 | | 交接§6-4 稽核/§6-5 P0 | R3/R2~R10 |

## ✅ 已完成歸檔

### 2026-08-13（V4 批修日：13d 生效，七個 sonnet 子代理執行、main 逐項複驗）
| 項目 | commit / 證據 |
|------|---------------|
| 13d V4 增量規則文件（scratch-dir 硬規則/一站一批/--compact 預設/inspect 標準工具），prompt 加讀 V4（附回退法） | 281d380 |
| T5：topic_review 異常逐條印 id＋show --uncat（AP4678031 隱形四輪的直接解） | 281d380；生產檔 --uncat=0 則、備註殼正確排除 |
| T6：list-topics/topic_review --compact（單次讀完不再 head/tail；預設輸出逐位元組不變） | 281d380；145/61 行實測 |
| T7：set-category 一站一批＋≤1 補刀（13d §2），E4 驗收口徑連動 | 281d380 |
| T2：launcher --disallowedTools 硬排除 Task 類/Agent（-NoToolBan 回退），DryRun 驗證、BOM 完好 | 281d380；16:00 輪實戰驗收 |
| R2：RT 跨日撞號偵測（audit reconcile 清單日期 vs first_seen，僅 RT，帶⚠️標記；合成副本測試 3 情境 PASS） | 281d380 |
| R3：audit 紅燈冪等（needs_review 留痕→黃燈一行、done 後復紅；副本實測不是無腦壓燈） | 281d380；順帶照出 JL-143WE/NE-028WE 真待處理 |
| R4＋R12：truncate 含標記 ≤ 上限＋SOUNDBITE 段保留（標記純 ASCII 避 strip_agent_note 誤殺；5 情境單元測試 PASS） | 281d380 |
| R13：13d §1 硬規則＋repo 根 8 個殘留 json 搬 _repo根清理20260813/（NE-022SA 不明舊檔未動） | 281d380 |
| A1 子項：遙測 Task 工具不參與階段判定（0430 分類 7.2→2.4 分，1000 輪無回歸） | 281d380 |
| D2 裁決落地：unwrap/inspect/search 三子指令（三站真實檔實測，自動卸殼含 NS 純文字型） | 281d380 |
| 順手修：AP4678031 空分類（懸置四輪的「無中主題」之鬼）補分類＋重 render | state 紀錄（commit f1cb0fe 記載） |

### 2026-08-12（接手 agent 清理日）
| 項目 | commit / 證據 |
|------|---------------|
| ntfy 推播修復（curl.exe 取代 .NET，根因：本機 .NET 走主機名稱連 ntfy.sh 必逾時） | 313d930；實測手機收到，13 項測試全過 |
| 殘留鎖誤判源修除（收工 Dispose 後 best-effort 刪鎖檔；互斥本靠獨佔握把從未失效） | c9614ac；DryRun 兩情境驗證 |
| P2 分段耗時遙測（phases 欄位：稽核/render/分類/NS/AP/RT 每輪自動記 calls/minutes） | 093c713；0812-2000 對照手工拆帳同量級 |
| effort high→medium（使用者裁定，待 E2 驗證、D1 定案） | 093c713 |
| RT4881 北京昌平淹路 天氣→大陸＋needs-review 留痕＋txt 重渲染 | state＋render 紀錄 |
| §7 查證：NS 2000 輪低量=真清閒（對帳 40/38 僅漏已留痕的 PY-04WE）；AP 三輪 13 則=巧合（三輪均有翻頁），漏收皆已留痕個案 | 接手進度檔 |
| 0812-2000 診斷覆核：耗時正常區間、AP Failed to fetch 為一次性偶發、「殘留鎖」為誤判 | 本表 c9614ac 項 |

### 2026-08-12（前任 agent，摘自交接文件）
| 項目 | commit |
|------|--------|
| ②強制流程＋cp950 樣板回滾（自改自修，2000 輪驗證有效） | 25133eb |
| ①強制批次寫入（一半有效：set-category 9→4）＋①b 稽核前置 | f71c623／c3d188e |
| ④DryRun 污染修復、遙測雲端化、watchdog BOM、Task1 量測器、⑤AP PageSize=16 | 2cc03dc／852a743／a74cfc7／1b9d986 |
| 烏俄中主題固定＋機動格（使用者要求，不要動）、audit §2 誤報修正、§4 more 提示 | 163320d／08d64a7／384d9de |
| new_item() 存 src_text/footage_type（1600 輪掃帶 agent 自修） | dd0e95f |
| 排程 12→9 輪（實省 ~15%） | 2026-08-12 01:39 |
