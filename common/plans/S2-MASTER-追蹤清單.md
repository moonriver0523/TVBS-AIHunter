# S2 MASTER 追蹤清單（唯一列管帳本）

- **建立**：2026-08-12（接手 agent，彙整交接文件＋複核報告＋省Token計畫＋全流程分析）
- **定位**：S2 所有改進項目的**唯一狀態帳本**，不同 agent 之間以本表交接。
  規格與理由不重抄——看「來源」欄指向的文件；本表只管**狀態、優先序、驗收與依賴**。
- **衝突時**：使用者裁決 > 本表狀態 > 來源文件。
- **2026-08-19 結構重構**：本表原本是單一長表（233行/89KB），每個項目的完整查證敘事、
  逐輪數據、根因分析全部寫在同一列，導致「開工先讀本表」的成本隨項目數單調遞增。
  現已拆成「本檔＝精簡索引」＋「`master-detail/` 底下按分類（E/R/T/A/D）分檔的完整詳情」。
  **這不是內容被刪減或精簡**——所有原始文字逐字搬進對應詳情檔，只是平常開工不必整包讀入。
  詳見下方「使用規則」。

## 使用規則（每個 agent 都要遵守）

1. **開工先讀本表（索引）**，用「一句話現況」判斷要不要認領這項、要不要展開詳情。
   **真的要動工某一項，才去讀它的詳情檔**（連結見索引表最後一欄）——不必因為要動 A11
   就把整個 `master-detail/A.md` 從頭讀到尾之外的其他分類也讀進來。
2. 認領項目：本表狀態改 🔶 並寫日期；**若有實質進度細節，同步寫進對應詳情檔**（只加不改，
   規則見下）。
3. **不刪列、不重用 ID**。完成的移到最下方「已完成歸檔」（現為 `master-detail/archive.md`）；
   新項目往後編號。
4. 進度紀錄**只加不改**——寫錯就再加一行更正，保留判斷軌跡（0812 教訓：先自證清白、
   沒查完就叫「完整」都是因為軌跡被美化）。**索引表的「一句話現況」與詳情檔的完整記錄
   各自只加不改**：索引表現況摘要過期就直接改寫成最新一句話（它是摘要不是軌跡本身）；
   詳情檔的敘事仍然是舊行不動、新增一行。
5. 動手前檢查「依賴/前置」欄；裁決類（D-）**沒有使用者明示不得自行動工**——這條鐵律不因
   為敘事搬到 `master-detail/D.md` 而放鬆，D 類項目未裁決前一律不得動工，不論你是先讀了
   索引表還是詳情檔才發現它是 D-。
6. 本表與 `master-detail/` 底下所有檔案隨 main 走，改完就 commit＋push（TODO 類文件不留
   在功能分支）。

**狀態代號**：⬜待做｜🔶進行中｜⏸等證據｜🗳待使用者裁決｜✅完成（移歸檔）｜❌不做

**來源文件**：
- 交接 = `G:\我的雲端硬碟\Claude共用\20260812-S2-交接文件.txt`
- 複核 = `G:\我的雲端硬碟\Claude共用\20260812-S2-1600輪異常分析-複核意見與改善計畫.md`
- 省T = `common/plans/2026-08-02-S2省Token優化計畫-修訂版.md`
- 全流 = `common/plans/2026-08-12-S2全流程與腳本自動化分析.md`
- 診斷 = `G:\我的雲端硬碟\Claude共用\自動掃帶系統\0812-2000掃帶診斷.txt`
- 落差報告 = `common/plans/2026-08-18-ENEX-ABC與三站排程掃帶技術落差報告.md`（ENEX/ABC vs 三站自動化落差，A9 唯一來源）
- 分類規劃 = `common/plans/2026-08-18-S2分類系統化規劃.md`（大／中／小分類系統化＝TODO「全盤檢討分類問題」結案路徑，A10 唯一來源）
- 三子代理評估 = `common/plans/2026-08-21-S2三站子代理並行掃帶可行性評估.md`（排程三站改派三子代理並行，D10 唯一來源；指引報告書，非授權實作）
- 0821-1000三刀 = `common/plans/2026-08-21-0821-1000體檢-三刀建議.md`（0821-1000 浪費案例＋刀1 set-category 硬上限／刀2 hook 沒載入／刀3 from-raw；A11-1000／A23／A24 唯一來源）
- 全檢 = `common/plans/2026-09-07-S2全面檢視與改進計畫.md`（2026-09-06 使用者 11 問的整體檢視；T12／T13／A31～A35／R21／D14～D17 唯一來源，並擴充 A26 刀4、掛接 T8／T9／A10／A16／A24）
- 跨模型試跑 = `common/plans/2026-09-08-跨模型(Gemini-Codex)試跑紀錄.md`（claudeg/claudex 代理接 S2 定時掃帶的首次實測；E8 唯一來源）

---

## 索引表（各分類詳情見 `master-detail/`）

### E — 等證據 → 詳情：[master-detail/E.md](master-detail/E.md)

| ID | 項目（精簡） | 來源 | 狀態 | 一句話現況 |
|----|------|------|------|------------|
| E1 | RT Load More 迴圈判定：頁面行為改變 vs 策略漂移；0820新增AP站同族清單漏抓樣本 | 交接§6-1；0820使用者體檢+Playwright查證 | ✅結案 | 連兩輪無迴圈，判定為策略漂移偶發，結案；R1 是否仍要預防性寫死交使用者裁決。**0820新增**：AP4679409(中非礦坑倒塌UGC)在0430輪清單漏抓，經API查證非來源排除、資料本身合格，判斷為live-feed分頁窗口漏抓，與RT迴圈同屬「分頁窗口漏抓」家族但成因不同；已研擬低成本差異掃描(gap-check)防堵方案待裁決是否啟動，見detail檔 **2026-09-07 使用者裁決**：RT 迴圈與 AP gap-check **兩項皆結案不做** |
| E2 | effort=medium 實效驗證 | 093c713 | ✅ | medium vs high 同時段對照數據幾乎相同，已交 D1 裁決 |
| E3 | 鎖檔收工自動清理實戰驗證 | c9614ac | ✅ | 已移歸檔，收工推播首次實戰成功 |
| E4 | ①b 稽核前置驗收：update-entry 次數 7→1~3 | TODO | 🔶 | 規則與「邊掃邊分類」流程打架，驗收口徑已由 T7 調整，持續觀察 |
| E5 | AP detail 擷取偶發 selector 失效，靠 agent 事後自癒重跑 | 0819-2000 體檢 | ⬜列入觀察 | 首次樣本：16筆抓成全空/APundefined，agent自行重跑修正，最終資料乾淨但多花約2萬token、4分鐘；累積更多樣本再議是否加自動重試檢查 |
| E6 | 輪次局部效率下降非則數所致，疑似偶發性行為/效率漂移 | 0819-2200 體檢＋使用者覆核；第2/3樣本0819-0430、0820-0730 | ⬜列入觀察 | 首次樣本：0819-2200耗時41.5分為當日最長，但三站則數其實沒增加(RT反而最少)；真正異常是RT呼叫數/單次分類呼叫耗時翻倍，具體表現為RT/NS對同一則反覆小幅裁切字數(重複做而非資料量大)；與D9/E5同屬偶發行為漂移家族，累積樣本再議。第2/3樣本改表現在NS站單次呼叫耗時異常(29.0秒、26.1秒/次，基準5.5~14.9秒)，scratch中間檔已被清理查不到裁切證據，僅token數字佐證；累積3樣本後0430/夜間時段略高但尚不足判定，維持觀察 |
| E7 | AP 站 0830-1600 登入態掉線事故，事後即時重抓補齊（2026-08-30 22:5x 記錄） | 使用者下令補救；本次 agent 執行紀錄 | ✅ | **事故**：0830-1600 該輪 RT／NS 正常完成，AP 站因 Playwright `.playwright-s2-profile-v4` 登入態掉線，僅留 `ap_check.md`（未登入首頁快照），無 `ap_list/ap_detail/ap_batch/_audit_ap` 四份快照，狀態檔僅留 `AP-LOGIN-0830` 待人工殼。**補救**：使用者手動重新登入 v4 profile 後，確認 `mcp__browser__*` 已連上已登入 session；依 13c§2／13d§7（R6 0820 訂正）recipe 開 `topic?id=116e9ab7aa044476925398d731289267` 頁、`PageSize:50` 打 Page1（唯一可信來源，未動用 Page2+），50 則落檔 `ap_list_1600.json`（25KB，落在自檢區間）；用 `window_start`(13:00)–`checkpoint`(16:00) 台北時間換算 UTC 05:00–08:00 篩出窗內 20 則，其中 2 則依 13e SNTV 重複/直式規則排除（`AP5468647` 直式版、`AP5468645` 與 `AP5468644` 內容完全重複的橫式重出），實收 18 則；`item/details` API 補全文，自檢無 `APAP` 雙重前綴／無空 script。18 則寫 `ap_batch_1600.json`＋`add-batch`＋`set-category`＋`set-tc`（含機動 T「尼泊爾洪災」加掛）入庫，`AP4681246`／`AP4681224`（熊貓圓圓22歲慶生）依「涉臺一律至少🟡」規則標記，並把中主題由「台灣熊貓慶生」改名「圓圓22歲慶生」避開 `misplaced_topics()` 地緣詞誤判。落 `_audit_ap_1600.txt`（code\|ISO時間，比照 `_audit_ns_1600.txt` 格式）後跑 `s2_audit.py --mmdd 0830 --rt-list --ap-list --ns-list` 三站齊對：**RT／NS／AP 窗內皆零漏收**；剩 2 個 🔴 為已知非問題——AP 那 2 則正是上述刻意排除的 SNTV 重複/直式（13e 明文「純粹略過，不是待人工」，未寫 needs-review），RT 3 則跨日撞號（`RT7607/RT7609/RT7651`）屬 RT 站既有資料、與本次 AP 事故無關，留給例行輪次處理，未動 RT/NS 資料。`AP-LOGIN-0830` 待人工殼已 `needs-review done` 結案（純殼、無實質內容，done 後整筆移除）。渲染後 T/C 覆蓋率 100%、品質掃 0 命中。|
| E8 | S2 改接 Gemini(claudeg)／Codex(claudex) 代理首次實測 | 跨模型試跑；使用者 2026-09-08 下令 | ⬜列入觀察 | **claudex（Codex `gpt-5.6-sol[1m]`）跑通**：完整一輪 V8 全站流程（NS/AP/RT/ENEX/ABC），checkpoint `0908-1200`，離開碼=0、耗時27.3分、新增39則、txt=True、無異常標記，`0907晚班交接.txt`已重渲染；僅證明「流程通」，**尚未與 sonnet 同時段品質比對**。**claudeg（Gemini）未跑通**：`gemini-pro` 遇 429 配額冷卻；使用者指定改用 `gemini-3.8-flash-high[1m]`（已加入 `cliproxyapi-gemini\config.yaml` alias＋`claudeg.ps1` 預設值，皆有 `.bak-*` 備份）後仍持續 429 quota，使用者確認訂閱配額本身是滿的，判斷是該模型 ID 在 Antigravity 端本身異常/限流，待核對正確 ID。**中途教訓**：試跑用的 checkpoint 若帶文字後綴（如「-claudeg試跑」）會被 `s2_state.py` 的 `CHECKPOINT_RE`（嚴格 `^\d{4}-\d{4}$`）擋下、寫入 0 則，須用純數字 `MMDD-HHMM`；順手查證 `Get-ScheduledTask` 得知目前 11:00–17:00 為排程空窗，試跑未影響任何正式輪次。**要看什麼**：累積更多跨模型樣本／等 Gemini 配額或模型 ID 問題解決／完成 claudex 39 則的人工品質比對，才能判斷是否進一步評估換線可行性，目前不立案動工 |

### R — 修復（品質與可靠性） → 詳情：[master-detail/R.md](master-detail/R.md)

| ID | 項目（精簡） | 來源 | 優先 | 狀態 | 一句話現況 |
|----|------|------|-----:|------|------------|
| R1 | RT Load More 迴圈修法：evaluate 配方寫死 | 交接§6-1 | 1 | ❌不做 | E1 已結案，本項是否仍要做交裁決 **2026-09-07 使用者裁決**：**結案不做** |
| R2 | RT 對帳只用 Edit No 判歷史已收，跨日撞號漏收 | 複核 | 2 | ✅281d380 | 0813-1600 已首次真實命中案例 |
| R3 | audit finding fingerprint 去重／紅燈冪等 | 複核＋交接§6-4 | 3 | ✅281d380 | 已修，順帶照出真待處理項 |
| R4 | truncate() 加標記後總長溢出 | 複核 | 4 | ✅281d380 | 已修：標記算進 limit |
| R5 | audit △ 判準尊重合法 set-mark override | 複核 | 5 | ✅2026-08-13 | 雙情境合成測試 PASS |
| R6 | AP 對帳清單抓到 page 2（≥32 則）；0820改用完整topic頁recipe，Page2翻頁降級為補充來源 | 接手查證；0820使用者發現掃錯網址＋要求模擬複驗 | 6 | ✅2026-08-13；2026-08-14 訂正；✅0820改recipe並二次訂正(commit待補) | 全窗對帳抓到累積漏收 22 則，9 則已回補驗收，教訓：跨輪交辦要走 needs-review 不是帳本。**0820**：改用`/topic?id=116e9...`完整清單頁解決Page1只收16-17則的根因(已生效)；但使用者要求模擬複驗後發現Page2+翻頁不論PageSize都固定回100則、對齊固定offset，會有空窗(PageSize小)或重疊(PageSize=100)風險，非簡單「站方bug有無」二分。訂正為：Page1(PageSize:50)為唯一可信來源，Page2+降級為候選補充池(需去重+核實，不可直接當確定則)，已更新13c§2/13d§7。**0820再測**：使用者提議滾動到底載完再抓API(仿RT)，實測排除——此頁非無限捲動頁(15次程式化捲動無新API觸發)，改點真正「下一頁」按鈕抓到AP官方request為Page1=50/Page2=100，用此官方原版組合重測仍10分鐘空窗零重疊，證實空窗是AP官網分頁機制本身的結構性缺口，非我們抓取方式問題，滾動法假設已排除 |
| R8 | audit 其餘輸出補完：shown/total/more＋--json-report | 複核 | 8 | ✅P0-5（2026-08-13） | P0-5 已補齊；P0-6 --json-report 仍 ⬜ 留案 |
| R9 | update-entry 同步 src_text／footage_type | 複核 | 9 | ✅6ecfc17；0818-1000 第三次、0821-0730 第四次真實復發 | 防呆只做到事後警告，未做組 batch 前硬性檢查，持續觀察是否再復發 |
| R10 | BITE 三態 CONFIRMED／NO_BITE／REVIEW_REQUIRED | 複核 | 10 | ⬜ | 未動工。**2026-08-22新增**：0430體檢實測NS站12則正是靠人工中文備註模擬「待確認」狀態、事後整則重寫才補完，佐證本項代價 |
| R12 | truncate 3000 字截斷 SOUNDBITE 段 | RT9878 實例 | 4.5 | ✅281d380 | 與 R4 同區已修 |
| R13 | batch 檔寫錯位置→雙重搬運 | 2200健檢 | 6.5 | ✅281d380 | 13d §1 硬規則生效前多次復發，最終靠 D7 launcher 硬解根治 |
| R14 | browser_evaluate 大回應落檔＝資料遺失 | 0813-2200健檢 | 2.5 | ✅2026-08-13（13d §6）；0814-1000 首戰驗收過 | agent 已能照規則自行繞過 |
| R15 | `common/13`／`13c` 每輪只載入約一半（Read 約 30,000 字元上限） | 0823 TC矩陣審查子代理發現＋主線獨立複驗 | 1 | ✅2026-08-24 上線，0430／0730 兩輪已驗收 | 已切成 13＋13e＋13f 與 13c＋13c2（逐字保留，SHA-256 守恆證明）；六份加 RULES-EOF 護欄線、`s2_rules_check.py` 硬檢查、prompt 要求開工先跑。**驗收（transcript 逐一配對 tool_use_id，非靠 rule_shas）**：0430／0730 兩輪六份**全部一次讀到最後一行、canary 全中、零補讀**，agent 也照格式回報。~~⚠️ 但「載入」不等於「遵守」：`s2_topic_review.py`／`s2_audit.py` 仍是 0/2 輪~~ ← **2026-08-24 撤回，量測錯誤**：重新統計近 16 輪，兩支都是 16/16 輪有跑（見 R17 撤案）。成本：每則 $0.168→$0.195（+16%，完整載入規則的代價；表面上兩輪總成本降 32% 是則數少 40% 的假象，已排除）
| R16 | 🟠 `.s2-scan.lock` 永久殘留：保活腳本共用同一把鎖但 `finally` 不刪檔 | 0823 TC矩陣審查子代理發現＋主線實測複驗 | 3 | ✅2026-08-24 已修（`s2_keepalive.ps1:228` finally 補 `Remove-Item`，比照 `s2_scan.ps1:599`；parse 檢查過、BOM 保留。待 :50 那輪保活跑完確認鎖檔自動消失） | `s2_keepalive.ps1:22` 與 `s2_scan.ps1:86` 共用 `%USERPROFILE%\.s2-scan.lock`；`s2_scan.ps1:578` 的 finally 有 `Remove-Item`，`s2_keepalive.ps1:229` **只有 `Dispose()`**。保活每 30 分鐘（:20／:50）跑一次 → 鎖檔永遠躺著。實測 2026-08-23 22:51：鎖檔存在（22:50 保活寫的），而 0823-2200 輪 22:21 就 DONE。**「檔案存在」與「有沒有輪次在跑」零相關**，任何以「鎖檔不存在」當閘門的程序都是永久誤報。真正的互斥是 `s2_scan.ps1:302` 的 `FileShare::None` 獨佔握把（撞到即 SKIP、無退避）。修法：保活 finally 補 `Remove-Item`；要判「有沒有在跑」改用 `s2_watchdog.ps1:220` 現成的 `Test-ScanRunning` |
| R17 | ~~規則已完整載入，`s2_topic_review.py`／`s2_audit.py` 仍 0 輪執行~~ | R15 驗收（0824-0430／0730） | — | ❌2026-08-24 撤案（前提為量測錯誤） | **立案前提是錯的**：以 `tool_use.input.command` 重新統計近 16 輪（0821-1000～0824-1000），`s2_topic_review` **16/16 輪有跑**、`s2_audit` **16/16 輪有跑**（每輪 1～7 次），R15 前後皆然。原偵測器有缺陷。連帶作廢「載入≠遵守」這次的實證支持、以及 TC 矩陣評估 §14.2 建立其上的三點修訂（該文件已加更正框）。T7／A23 是獨立觀察、不受影響。詳見 [R.md](master-detail/R.md)。⚠️ 教訓：宣告「某工具從沒被呼叫」前先用第二種方式交叉驗證 ／ 以下為原始（已作廢）記載：R15 修完後兩輪 transcript 證實 `13c2` 已完整進 context，但 §3b「`s2_topic_review.py` **render 前必做，每輪**」與 §2a-2「`s2_audit.py` **交班前必跑**」**仍是 0/2 輪**（改動前 0/25 輪）。兩支都**沒有任何自動呼叫者**（`s2_render.py:554` 只印建議指令、不執行），全靠 agent 自律。對照組 `list-topics` 同樣寫在規則裡卻有 2/2 輪執行——差別在它有明確觸發時機（開新中主題前），而這兩支是「每輪／交班前」這種沒有具體觸發點的常規動作。**屬 T7／A23 家族（規則已改、行為沒跟上）**。候選修法：①比照 `s2_topic_dedupe` 由 `s2_render.py` 自動 import 呼叫（最省事、與現行成功案例一致）②在收工檢查加硬性閘門 ③維持自律但加遙測記錄未執行。⚠️ 這條直接影響 A10 v2——v2 假設「判準寫進規則→agent 照做」，本項是該假設的反例，見 TC 矩陣評估 §14.2

| R18 | 烏俄 `resident_topics` 命名跟 13f 權威命名（【烏打俄】／【俄打烏】）對不上，每天多開兩個永遠空的重複中主題 | 0824-1600 使用者體檢發現 | 3 | ✅2026-08-24 已修 | `scripts/s2_resident_topics.json` 自 2026-08-17（`eca8caf`）就寫成「俄轟烏／烏轟俄」，跟 13f（2026-08-12 `163320d` 訂案）「烏打俄／俄打烏」對不上，導致每天 render 出 4~5 個 烏俄 中主題（含 2 個永遠 0 則的空殼）。已改回 13f 命名（JSON 不含外層『【】』，render 會自動加）；順帶修正 0824 當天一則方向誤判（RT7336：烏克蘭無人機攻擊俄羅斯物流設施，原本標成「俄打烏」方向錯＋漏方括號，已更正為「烏打俄」）；13b §（2026-08-17 加）常駐中主題段落已更新註記；`0824-s2-state.json` 用 `set-resident-topics`／`set-category` 覆寫並重新 render 驗證（品質掃／主題重複偵測皆 0 命中）。**待辦**：明天 16:00 建檔輪確認 JSON 生效、不再冒出空殼格 |
| R19 | 🔖「畫面好」誤標：NS 站純受訪片段被標成畫面好 | 使用者 2026-08-30 發現（0828 晚班交接 `IN-36FR`／`IN-27FR`） | 4 | ✅2026-08-30 已修 | 查 `G:\我的雲端硬碟\Claude共用\自動掃帶系統\Archive\20260828\0828晚班交接.txt`：NS `IN-36FR`／`IN-27FR`（尼泊爾洪災美籍生還者專訪）畫面欄僅「受訪畫面、記者連線」，卻標了 `🔖 畫面好`；同場洪災另 6 則（`IN-08FR`／`IN-03FR`／`RT8511`／`IN-15FR`／`RT8503`／`IN-39FR`）畫面欄確實有隧道救援／泥濘救難等實拍內容才合格標記，判定不是單一 typo，而是判準文字只給「空拍／救援作業／大範圍毀損」等正面例子，沒有明確排除「證詞震撼但畫面只是棚內受訪」，導致掃帶時把 BITE 內容的戲劇性誤讀成畫面本身吸睛。**已修** `common/13e-S2-素材行與分類規則.md`「只標真正突出的」條目：把「受訪畫面／記者連線（含生還者專訪、目擊者證詞）」補進負面排除清單，並新增一條「證詞震撼 ≠ 畫面震撼」判準，附本次事故作為反例、同場合格案例作為對照。純文字規則修訂，無腳本改動、無需跑測試。 **牙齒見 A31**（2026-09-07 `s2_pretag.py`）。 |
| R20 | `s2_audit.py` 清單對帳 RT 站前綴不一致，誤報大量假漏收 | 使用者 2026-09-02 17:2x 質問「RT漏掉27」發現 | 3 | ✅2026-09-07 `672147c`（只修不回溯） | `scripts/s2_audit.py` 的 `reconcile()`：`cur = set(st["items"])`（`s2_state.py:148` 載入時 key 是帶前綴的完整 id，如 `RT9614`），但 RT 清單快照（`rt_list_*.json`）的 code 是站方原始 Edit No、**不帶 `RT` 前綴**（如 `9614`）；`c in cur` 逐一比對時型態永遠對不上，導致 RT 站每一輪只要清單裡有代碼，幾乎全部被誤判「未收」。0902-1700 這輪實測：`s2_audit.py` 報「RT 窗內漏收 27 則」，逐一核對狀態檔後 33 則其實早就已入庫（key 帶 `RT` 前綴能找到），**真正漏收只有 1 則（RT9574，美網Day3夜場精華，15:27，已於本次補收）**。AP／NS 的清單快照 code 本身就帶站別前綴（`AP4681xxx`／`MI-12WE` 等），與狀態檔 key 一致，不受影響——**bug 只影響 RT 站**。修法方向：`reconcile()` 內 `label=="RT"` 時比對前先把 `rows` 的 code 補上 `"RT"` 前綴（或反向：把 `cur` 的 key 去前綴），需注意 `cross_day`/`_first_seen_index` 等同函式內其他地方是否也依賴同一組未加前綴的 code 假設。**風險**：此 bug 至少從 R2（2026-08-13，RT 對帳邏輯首次寫入）就可能存在，過去所有「RT 窗內漏收 N 則」的歷史記錄都可能是同一假陽性放大——若屬實，過去靠這個數字驗收的輪次都要重新檢視是否真的漏收過。**未動工**：修改前需使用者裁決是否連同回溯過去記錄一起處理，且要覆核修好後是否會影響 R2 跨日撞號判斷（`cross_day_seen.add(code)` 同樣用未加前綴的 `code` 對 `cur`）。 **2026-09-07 使用者裁決**：**只修不回溯**——reconcile() 補 RT 前綴、覆核 R2 跨日撞號；歷史「RT 漏收 N」記錄只加註記不重算 **2026-09-07 已修**：`reconcile()` 加 `key_of()` 對 RT 站補前綴，所有 `in cur` 與 `fs_idx` 查詢統一走它；`test_s2_audit_rt_prefix.py` 4/4。發現近期 RT 清單快照已帶 `RT` 前綴，修正屬防呆；歷史「RT 漏收 N」數字**未重算**（依裁決）。 0907-0430 首輪：RT 窗內漏收 4 則（合理量級，非假漏收）。 **實作計畫**：[全檢實作/](2026-09-07-全檢實作/README.md) 07 |
| R21 | `scripts/s2_reclass_prompt.md`（收斂 agent）仍叫人讀已退役的 `13b` | 全檢 §3 | 5 | ✅2026-09-07 `f286fa5` | `13b` 含大量被推翻規則，掃帶 prompt 已明文禁讀；收斂 prompt 改指 `13c2`＋`13e`＋`13f`（非原擬 `13`，因「分類收斂」規則落在 `13c2` §2a），並明文禁讀 `13b`；比對後 `13c2` 已涵蓋計畫 Expected 六項、判定等價，未搬動規則內容；`s2_topic_review.py` 順手加 `--compact`。順帶是 A10 P2 §5a 排程化的前置 **實作計畫**：[全檢實作/](2026-09-07-全檢實作/README.md) 03 |
| R22 | `s2_batch_prep.py` `SITE_SPEC['rt']['id_of']` 只讀 `code`，RT **detail** 檔編號在 `edit` 鍵 → id 全空、`dedup_by_id` 把整批塌成 1 則 | 09-A24 真實快照試跑 2026-09-07 | 2 | ✅2026-09-07 `65e84c5` | 新增 `_rt_code_or_edit()`：`code` 優先、無則取含數字的 `edit`（裸 `RT`／無數字視同沒有），list 檔行為不變；9 案測試。殘留：0906 RT detail 8 則中 2 則 `edit` 本身是裸 `RT`（上游退化值），仍撞空 id 去重掉 1 則——要救需再加 `#index`／head 當去重鍵的 fallback，**未做**，待裁決是否值得。`inspect` 的 `_id_of_any` 早有同一坑的註解，兩處未對齊是根因。 |
| R23 | `13f` 時序陷阱②（2026-08-25）「`set-top checkpoint` 開工就下」與 prompt（2026-09-03 `ddc71b9`）「開局絕不 set-top、掃完所有站再下」互相矛盾，兩份必讀檔各說各話 | 04-T13 搬家時發現 2026-09-07 | 2 | ✅2026-09-07 `5c61019` | **裁決（監督者）**：留 9/3 版——它是實測漏收 bug（NS 斷線時開局 set-top 讓整段窗永久漏收）的修正、時間較新，且新順序 set-top 仍在 set-tc 之前，8/25 規則要防的配額分桶錯記在新順序下不會發生。`13f` 該段加訂正註記＋舊句刪除線、原文存查；`13f` 尾收工清單總表第 5.5 列採 9/3 本文並註「取代 13f 8/25 規則」。**教訓**：prompt 改規則卻沒回頭改規則檔，正是 T13／A26「規則只在規則檔、prompt 只指路」要治的病。 **0907-1700 驗**：set-top 在五站 add-batch／set-tc 之後、render 之前（tool_use #154→#155），✅。 |
| R24 | `s2_scan.ps1 -DryRun -Checkpoint MMDD-1700` 會真的呼叫 `New-ShiftState` 建當天狀態檔（L333 在 DryRun 出口 L413 之前），留下 0 則的殼檔 → 17:00 正式輪 `NEWDAY SKIP 已存在`、agent 也依「狀態檔不存在才是建檔輪」跳過建檔輪七項 | 2026-09-07 T13／A26 DryRun 驗證後發現 12:10 出現 `0907-s2-state.json` | 2 | ✅2026-09-07 23:10 `5b4476a` | 0812 只把 `_輪次紀錄` 的 START 移到出口後，沒把 New-ShiftState 一起移。修法：DryRun 出口提前到 New-ShiftState 之前，或 DryRun 時 New-ShiftState 只印不寫。2026-09-07 當日殼檔由使用者手動刪（Claude 刪檔被權限擋）。**不在空窗外改 launcher**，排明天空窗。 **修法**：`if ($DryRun) { 略過 New-ShiftState } else { New-ShiftState }`；DryRun `-Checkpoint 0908-0100` 實跑不建檔、不寫輪次紀錄；1700 分支只做程式碼審閱（對正式資料夾跑 DryRun-1700 會歸檔今天的檔，不可試）。同 commit：launcher 每輪 `$env:S2_CHECKPOINT = $Checkpoint`（R25 桶鍵）。  **0908-0100 首驗 ✅**：非 1700 輪沒誤建 `0908-s2-state.json`、沒誤歸檔，暫存續用 `20260907/`。 |
| R25 | `add-batch` 的 `category` 只吃字串「大/中/小」，agent 照狀態檔 schema 給物件 `{大分類,中主題,小分題}` 就 `str(dict)` 無 `/` → 五站 141 則全部「分類 0／退回」，T12 一次入庫＋分類＋T/C 失效，P1b 閘門也被繞過 | 0907-1700 首輪 log（監督者） | 2 | ✅2026-09-07 23:05 `f74ec76`（使用者裁決當晚上線，01:00 輪首驗） | **證據**：`tc_rejected["0907-1700"]` 141 條「cat 需為「大分類/中主題[/小分題]」」；agent 補跑 `set-category` ×7＋`set-tc` ×5（T12 想省的呼叫全數回來，與 T12 前持平、無退化）；`s2_state.py` L647 `str(e["category"])`。**修法**：`cmd_add_batch` 物件→`"/".join` 再進 `_set_one_category`；`13c2` §2 L62 與 L68 要寫清楚 batch 的 `category` 兩種形狀都收；`build --skeleton` 產物用字串（P1b-2）。⚠️ 修好後閘門會真的擋新題，13c2／13f 的 `new_topics` 規則句（P1b-2）必須同批上，否則 20:00 後 agent 會卡。 **0907-2000 觀察**：agent 改成 `add-batch` 不帶 `category`、之後另下 `set-category`（2 站 3 次）——沒再被退回（`tc_rejected` 無 0907-2000），但 P1b 閘門一樣沒觸發、登記簿零 diff。維持明日空窗與 P1b-2 同批修。 **0907-2200 再現**：三站 78 則（NS 40／AP 15／RT 23）add-batch 帶物件 category 全部退回，記在 `tc_rejected["0907-2000"]`（set-top 在收工才下，退回記到前一桶——桶鍵取自 checkpoint 而非本輪，與 R23 同根）；agent 補 `set-category` ×7 修回、結果 100% 有分類。另一面：ENEX／ABC 走 `s2_platform_merge --auto-register` 自動登記 24 題（`auto:true`、charter 40 字截斷），已 commit「登記簿：0907-2200 自動登記」——P1b 在平台線已生效、三站主線仍被繞過。 **修法**：`_cat_as_str()`／`_tc_as_str()`——batch 的 `category` 收字串或狀態檔同形物件、`tc` 收字串或 `{T,C}` 物件；`_bucket_key()`＝`S2_CHECKPOINT` 環境變數→batch 自帶 checkpoint→頂層 checkpoint，`tc_rejected`／`tc_calls`／set-tc 上限三處共用（桶鍵問題一併解）；P1b-2 救援路徑：已在庫且無分類、重送帶分類 → 只補分類（仍過閘）。`test_s2_add_batch_shapes.py` 11 案＋既有 13 支回歸全綠。  **0908-0100 首驗（桶鍵 ✅／物件形未驗）**：`tc_calls` 記在 `0908-0100`（3 次）、`tc_rejected` 本輪 0 筆、81 則分類與 T／C 100% 覆蓋（前兩輪 140／78 則被退回）。但三站 batch 這輪 `category`／`tc` **全是字串形**（26／25／14 則皆 str），`_cat_as_str()` 物件分支這輪沒被走到，只有單元測試涵蓋——物件形要等 agent 下次照 schema 寫才算實地驗過。 |
| R26 | 議題 T 照抄大分類：尼泊爾洪災 5 則（AP×3／RT×2）T 掛 `社會,尼泊爾洪災`，13f 議題表明訂「天災天氣收洪旱」「社會不收天災」且機動 T 要加掛固定 T（`颱風,天災天氣` 例），應為 `尼泊爾洪災,天災天氣` | 使用者 2026-09-07 18:30 看矩陣發現 | 3 | ✅2026-09-07 23:20 `46e68fb`（規則句上線；資料 18:40 已修） | **根因**：大分類歸位通則（13f L140）寫「洪水造成傷亡→社會」，T 軸判準在同檔下方另立（洪旱→天災天氣），但沒有一句明講「T 不照抄大分類」；17:00 輪又因 R25 被迫事後補跑 `set-tc`，趕工直接沿用大分類。**修法**：13f 議題 T 表下方加一句「⛔ T 不照抄大分類：大分類因傷亡歸社會的天災，T 仍掛天災天氣（並加掛機動 T）」，與 R25／P1b-2 同批上；`set-tc` 可加機械提示——T 含「社會」且中主題命中天災關鍵字（洪／颱／震／火災除外）就印 ⚠️，不擋。**資料修正**：狀態檔備份在 scratchpad `0907-s2-state.before-r26.json`，checkpoint 0907-1700 `set-tc` 用到 6/8。 **0907-2000 觀察**：本輪無新進尼泊爾素材；18:40 修的 5 則 T 未被覆蓋（agent 的 set-tc 沒碰它們）。 **0907-2200 觀察**：新進 AP4682951（加德滿都悼念守夜）T 掛 `尼泊爾洪災,天災天氣,社會`——天災天氣有了但社會沒拿掉（固定 T 2 個），仍是照抄大分類的殘影；規則句明日空窗上。18:40 修的 5 則未被覆蓋。 13f 議題 T 表加 ⑥「⛔ T 不照抄大分類」。`set-tc` 機械提示未做（可選）。 |
| R27 | RT 清單首抓必失敗：`13c2` §1b 第 3 條片段的上站時間 regex 用 `^` 錨定，卡片 innerText 現在以「EARLY ACCESS SCRIPT…」「04:50 RAW…」開頭，`at` 整批空字串；且片段沒等頁面套完 `media-types=vid` 篩選就撈，撈到的是另一組素材。0907-1700（13 則全空）與 0907-2000（40 則全空、窗內 9 則不在裡面）兩輪都靠 `s2_audit` Ⓐ 對帳才發現，各多花一次重抓（20:00 輪 RT 站 14 分鐘裡約 5 分鐘是重抓） | 0907-2000 log（監督者） | 2 | ✅2026-09-07 20:50 上線 `c4c92e1`，22:00 輪首驗過 | **證據**：`20260907/rt_list_1700.json`（13，`at` 全空）→ `rt_list_1700c.json`（51，有時間）；`rt_list_2000.json`（40，`at` 全空，無 RT7981／RT0876／RT0866／RT9778／RT1616／RT1615／RT0833／RT0859／RT0790）→ `rt_list_2000c.json`（40，有時間，9 則都在）。agent 第三次自己改成 `await 1500ms`＋不錨定 regex 才成功。**修法**：13c2 §1b 片段 ①regex 去 `^`；②開頭 `await 1500ms`；③撈完守門：`at` 全空或 <3 則→等 3 秒重撈一次，仍空就 `needs-review add`，⛔ 不准拿沒有 `at` 的清單做窗內判斷。改完跑 `s2_rules_check.py`（13c2 餘裕小，必要時搬 13c3）。 **0907-2200 首驗 ✅**：`rt_list_2200.json` 走新片段，回傳 `{noAt:0, items:40}`、40 則全有上站時間、單趟 5 分鐘（22:13→22:18）、零重抓；狀態改 ✅。  **0908-0100 二驗 ✅**：`_rt_list_0100.json` 單檔、`noAt: 0`、40 筆全有上站時間，零重抓。 |
| R28 | `s2_audit.py` 兩處仍指退役的 `13b`（L405「RT 見 13b §1b 第 3 條」、L544「13b §543」）；Ⓐ 對帳讀 `s2_batch_prep snapshot` 產的快照 txt 時沒跳過 `#` 檔頭 5 行，全部算進「窗外」（0907-2000 報 RT 窗外 10 則，實際 5 則；不影響漏收判斷，只是數字與清單印出 `# 來源：…`） | 0907-2000 log（監督者） | 4 | ✅2026-09-07 23:10 `b43a79e` | 修法：字串改指 `13c2` §1b／`13e`；載入清單時 `if line.startswith('#'): continue`。與 R21 同類（V8 退役 13b 後殘留指向）。 `parse_list_file` 跳過 `#` 行；兩處 13b 指向改 13c2；稽核③ 認 `{entries,…}` 新殼。 |
| R29 | `s2_audit.py` 用狀態檔頂層 `checkpoint` 當對帳桶鍵（L385 `_log_reconcile(state_path, st["_top"].get("checkpoint"), done)`），但 `set-top checkpoint` 在收工才下、audit 在它之前跑 → 0908-0100 輪的對帳結果被寫進 `reconcile_log["0907-2200"]`，**覆蓋掉 22:00 輪原本的紀錄**（ts 由 22:3x 變 2026-09-08T01:22:19）；`s2_render` 收工閘門查 `0908-0100` 找不到，誤報「三站全缺」。同一行 checkpoint 也是 audit 算窗尾用的（L256），本輪窗尾被當成 22:00 | 0908-0100 log＋狀態檔（監督者驗收） | 2 | ✅2026-09-08 02:20 `66bed14` | **與 R25 桶鍵同根**：launcher 自 `5b4476a` 起每輪 export `S2_CHECKPOINT`，`s2_state.py` 已有 `_bucket_key()`。修法：`s2_audit.py` 兩處（L385 桶鍵、L256 窗尾）改成 `os.environ.get("S2_CHECKPOINT") or st["_top"].get("checkpoint")`。**本輪未造成漏收**（RT 清單 40｜已收 40｜窗內未收 0｜窗外 0），但 22:00 輪的對帳留痕已經永久遺失，且誤報會讓這道防線被當雜訊。agent 已寫 `needs-review` 說明實情。  **0908-0430 首驗 ✅**：`reconcile_log` 桶鍵是 `0908-0430`，`0907-2000`／`0907-2200` 兩格原封不動；render 收工印「OK 清單對帳三站齊全（0908-0430）」，誤報消失。窗尾修正當場見效——AP 報窗內未收 28 則、NS 1 則（修正前這些會落進「窗外，屬下一輪」）；經 agent 覆核 AP 那 28 則全是 APefe／APsth／SNTV 這類設計性排除、NS 那 1 則是稿未到，都不是真漏收，但**這一輪才第一次看得到它們**。  **0908-0700 觀察 2/3 ✅**：桶鍵 `0908-0700`，`0908-0430` 那格原封不動，render 印「三站齊全（0908-0700）」。AP 又報窗內未收 24 則，agent 覆核為 APefe／APsth 付費 stringer 排除，非漏收。  **0908-1100 觀察 3/3 ✅，且第一次真陽性**：本輪 agent 確實沒做對帳，閘門喊出「三站全缺」——修正前這句話每輪都出現、已被當成雜訊，現在它只在真的漏做時才響。桶鍵四格無覆蓋。**R29 觀察結束。** |
| R30 | `set-category` 是 A10 P1b 新題閘門的側門：預設 `topic_mode="off"`、完全不查登記簿，所以 `add-batch` 那道閘門擋得再好，agent 事後用 `set-category` 一樣能寫進沒登記過的中主題。0908-0430（P1b-2 硬上線首輪）實證：`add-batch` 這條路 100% 乾淨（0 則被擋、33 題帶 charter 登記），但同一輪用 6 次 `set-category` 寫進 **17 個未登記中主題**，其中「暖新聞」「國際新聞整理包」「公衛醫療小品」「國家公園小品」「交通意外」正是 13f 三判準明訂禁止的收容式名稱；更糟的是有兩則被 `set-category` **從好名字改成收容式名稱**（暖新聞救援畫面→暖新聞、歐洲野火延燒→歐洲野火） | 0908-0430 log＋登記簿 diff（監督者驗收） | 2 | 待裁決 | **這不是 P1b-2 沒做好，是閘門只裝在一扇門上。** 兩個選項：①（跟 add-batch 一致）`set-category` 預設改 gate：未登記就退回、要開新名得先 `--new-topic 名:charter`；風險是 agent 拿 `set-category` 做的多半是**事後訂正**（本輪 6 次有 3 次是改自己前一版的判斷），硬擋會讓無人看的輪次卡在訂正這一步。②（低風險）先只在寫入未登記名稱時印 ⚠️＋該名稱、不擋，一輪後看 agent 會不會自己收斂。⛔ 這是 D-裁決項，等使用者定；回滾與本項無關（P1b-2 硬上線 `3f474a6` 首輪表現良好，不動）。  **0908-0700 追蹤**：`set-category` 只用 2 次（0430 是 6 次），寫的四個中主題全部已登記，本輪 0 個未登記、0 次改壞名字。累計：0430 = 6 次／17 未登記／2 則被改壞；0700 = 2 次／0／0。側門仍開著，只是這一輪沒人走——裁決前不要把「一輪乾淨」當成問題消失。  **0908-1100 追蹤**：`set-category` 2 次、0 個未登記、0 次改壞。累計 0430=6/17/2、0700=2/0/0、1100=2/0/0。 |
| R31 | 0908-1100（收工輪）agent **沒做三站清單對帳**，理由寫「時間預算不足」，改用自己的 diff 核對後 `needs-review` 留痕。`s2_render` 的閘門有正確喊出「⚠️ 這一輪（0908-1100）沒有做清單對帳（三站全缺）」——**這是 R29 修好後閘門第一次抓到真的漏做，不是誤報**。但結果是：這一班最後一輪的 89 則新收沒有任何機械驗證，`reconcile_log` 只有 0907-2000／0907-2200／0908-0430／0908-0700 四格 | 0908-1100 log＋狀態檔（監督者驗收） | 2 | 待裁決 | **留痕是規則允許的逃生門**（13c3「真的做不了就 needs-review 寫明原因」），所以 agent 沒有違規；問題是逃生門太好走。0806 的教訓正是「做了一半就忘了回頭補」，事後補到 RT 2 則、NS 19 則漏收。三個可能作法：①收工輪把對帳升為硬性（沒做就不准 render），風險是真的卡住時整輪產不出交接檔；②只在**收工輪**硬性、其餘輪次維持警告；③維持現狀，但把警告寫進交接 txt 檔頭讓早班看得到。⛔ D-裁決項。另：agent 說「時間預算不足」但本輪只花 28 分／$10.28，比 0907-1100 的 22.9 分多 5 分鐘——真正吃掉時間的是 76 筆素材行格式回頭修（見 R32）。 |
| R32 | 0908-1100 agent 首批寫入時整批漏掉兩條素材行格式規則（「有 BITE 就不能同時寫無BITE」、「(BITE) 第二括號」），事後用 `patch-entry`／`update-entry --batch` **分三輪修正 76 筆**才過關 | 0908-1100 log（監督者驗收） | 3 | 待處理 | 76 筆＝本輪 89 則的 85%，等於整批重寫一次。這不是漏看規則的偶發，是**寫入前沒有機械檢查**：`s2_validate` 有行格式 regex，但只在 render 時才跑。可能作法：`add-batch` 入庫前就跑一次行格式 lint，不合格的當場列出來（跟 R25 的 `_reject_bare_array` 同一個位置、同一種「入口擋」思路），省掉事後三輪 patch 的呼叫。與 A31 的 `lint()` 已有部分重疊，實作前先看 `s2_state.py` 裡 lint 現在掛在哪。 |
| R34 | 0909-1100 輪被 `s2_rules_check.py` 擋下沒開工——13c 字數超預算 | 0909-1100 log（監督者查證） | 1 | ✅2026-09-09 已修 | 當天稍早改 D20（01:00 收 ENEX/ABC）時在 `13c` 加了說明文字，把估計載入量從 25,544 推到 28,149、超過 28,000 硬預算，`s2_rules_check.py` 判定會被截斷、依規則不開工，11:00 輪離開碼=0 但耗時僅 0.2 分、`0909-1100` 狀態檔沒建，等於空跑一輪。已把新增文字精簡（降到 27,635，留 365 margin），重跑檢查全過。**教訓**：改 13 系列規則檔加說明時，務必改完當場跑一次 `s2_rules_check.py` 再收工，不要等下一輪自己撞——這條跟 R15 是同一個字數預算，只是這次是新增文字撐爆，不是規則檔膨脹的舊病復發。 |
| R33 | `s2_batch_prep.py` 沒有「batch json 欄位改名」子指令，一次性資料修正沒有標準工具可用 | 0909-0700 log（監督者查證） | 5 | 待做 | 0909-0700 輪 agent 想把 `ns_batch_0700.json` 裡的鍵名 `raw_entry` 整批改成 `entry`，寫 `python -c` 讀檔改字串再回寫，被 `s2_bash_guard.py` 依 13d §4 規則正確攔下（guard 本身沒問題，是攔截後無標準路可走）。現有 `collate-category`／`fill-src-text`／`concat` 都是處理特定欄位的專用子指令，沒有通用的「重命名／改值」工具，逼 agent 走 `python -c` 這條被禁的路。建議比照既有子指令模式加一個 `rename-field <batch.json> --from 舊鍵 --to 新鍵`（或更通用的 `set-field --key --value --ids`），把這類一次性欄位修正收進標準工具，不必每次繞道或卡在 guard 前找不到出路。同時段 04:30 輪的 3 次 DENY 皆是重複試同一招（`python -c`／heredoc），非工具缺口，純屬 agent 重試浪費，不需要另立項。 |
| R35 | 側錄（SIDE_CNN／SIDE_NHK）摘要句首**重複打了一個跟中主題標題同格式的『【地名／主題】』前綴**（例：`raw_entry` 第二行是「【盧比歐拉美】白宮要在西半球展影響力…」），render 出來視覺上像是「開了一個空標題、後面又接一則」，使用者兩輪都誤判成「有空的小分類」 | 0909-1700／0909-2000 log＋狀態檔（使用者發現＋監督者查證） | 3 | ✅2026-09-09 已修（兩輪存量） | **系統性、非單一 agent 誤判**：0909-1700（Sonnet）19 則中招、0909-2000（Gemini／claudeg3）另外 15 則 NHK＋4 則 CNN 又中同一個坑——兩個不同模型、兩個不同 checkpoint 都犯一樣的錯，根因大機率在側錄轉譯來源格式或 `14-S2b-側錄轉譯摘要.md`／`add-side` 沒有明講「摘要句首不可重複帶主題標籤」，不是 agent 隨機手誤。**已修**：兩輪合計 38 則（0909-1700：19／0909-2000：19，含 CNN 與 NHK 兩來源）用 `update-entry --batch` 剝掉 `raw_entry` 第二行開頭的 `【…】` 前綴，重跑 `s2_render.py` 後空標題偵測歸零。**待做**：這只是清了存量，`14-S2b` 或側錄轉譯來源沒有加規則句擋新案，明天同樣的 checkpoint 大機率再中——建議下一輪順手在 `add-side` 或 `14-S2b` 補一條「摘要句首若跟原始逐字稿一樣帶了『【…】』標籤，入庫前先剝掉」。 |
| R36 | 手動起的 peer session（如 claudeg3）代跑排程輪次時，**完全沒有掛 `s2_bash_guard.py` 的 PreToolUse guard hook**——正常排程輪次靠 `s2_scan.ps1` 的 `--settings s2_guard_settings.json` 掛上這道防線，但人工用 `SendMessage` 直接發包給一個獨立 Claude Code session 時，該 session 是照它自己原生的權限設定跑，guard 完全不會生效 | 0909-2000（claudeg3）token 體檢時查 `_guard_calls.log` 發現整段時間窗 0 筆紀錄（監督者查證） | 3 | 待裁決 | **證據**：claudeg3 執行 0909-2000 這一輪期間（20:01–20:28），`_guard_calls.log` 裡完全沒有這段時間的紀錄（正常排程輪次每輪都有幾十筆 ALLOW/DENY）；transcript 直接翻到一筆 `python -c` 讀 JSON 的呼叫（20:15:38 UTC+8），這種寫法在有 guard 的正常輪次會被攔下並導向 `s2_batch_prep.py` 標準指令，這次完全沒被攔、也沒造成實質損害，但代表**guard 這道防線在「人工代跑」這條路徑上是空的**。本輪只多這一次違規、無後續連鎖問題，尚未造成事故。⛔ D-裁決項：要不要要求代跑的 peer session 開工前先手動掛等效的 `--settings`／guard 機制？還是接受「人工代跑本來就是例外情境、由監督者事後查核取代即時攔截」？後者的風險是代跑次數變多後，guard 保護的規則（如 R33 的 python -c 禁令）會在這條路徑上形同虛設。 |
| R37 | `s2_render.py` `render_block()` 對「底下 0 則的小分題」無防呆：會印出一行孤兒小分題標題、底下什麼都沒有 | 0910 整併時 claudeg1 排查發現；使用者 2026-09-10 裁示要修 | 4 | ✅2026-09-10 已修 | **不是今天視覺誤判的成因**——0910 那次是 R35（側錄摘要句首重複帶【】前綴），狀態檔實際上沒出現 0 素材小分題；本項是同一區順手補的潛在漏洞。**修法**：`render_block()` 迴圈遇 `its` 為空即 `continue`，並把 `+` 分隔的計數從 `enumerate` 索引 `n` 改成「已印出幾個小分題」的 `shown`——不改的話第一個小分題被跳過時，第二個會冒出開頭多餘的 `+`。⛔ 同一套跳過**沒有**搬到中主題層：`resident_topics` 刻意留空字典來印出只有【中主題】標題的常駐格（`group_items` L331-339），那是規格不是瑕疵，已在測試 ④ 立回歸護欄。**驗收**：新增 `scripts/test_s2_render_empty_sub.py`（5 情境 9 檢查全 PASS，含 `+` 位置與 resident_topics 護欄）；另拿真實 `0910-s2-state.json` 前後各 render 一次，1912 行 **byte-identical**，證實對現行資料零影響。`render_block` 全 repo 只有 `s2_render.py` 自己呼叫（`s2_render_html.py`／`s2_state.py` 只在註解提及），無其他呼叫端需同步。 |

### T — 省 Token（既有計畫未完成項） → 詳情：[master-detail/T.md](master-detail/T.md)

| ID | 項目（精簡） | 來源 | 優先 | 狀態 | 一句話現況 |
|----|------|------|-----:|------|------------|
| T1 | Task 2 最小啟動設定（A0→A3 逐旗標實驗） | 省T | 1 | 🔶2026-08-18 測試全過＋已熱修上線，待正式輪驗收 | 四組實跑 −30% prefix，已熱修 s2_scan.ps1。**2026-08-21 訂正**：A3「hook探測=有擋」只在 `_task2_test_out` cwd 成立，**生產 scratch cwd 的 0821-1000 hook 沒載入**（見 A11-1000），A3 交會點假綠燈 |
| T2 | A4 實驗：排除 Agent／TaskCreate／TaskUpdate 工具 | 全流§4.4 | 2 | ✅281d380 | 硬排除生效，但曾復發過一次（行為漂移的先例，D9 借鏡） |
| T3 | Task 3 清理 prompt 衝突 | 省T | 3 | ✅2026-08-13 | 三項查核完，衍生 T8 |
| T4 | Task 4 規則分片（八片＋manifest） | 省T | 延後 | ⬜ | 中風險，單獨排期 |
| T5 | topic_review 誤報時直接印出 id | 0100分類健檢 | 4 | ✅281d380 | 已修，破案根因為 AP4678031 空分類 |
| T6 | list-topics／topic_review 長輸出精簡模式 | 0100分類健檢 | 5 | ✅281d380 | 已修 |
| T7 | 「set-category 一輪一次」規則改「一站一批」 | 0100分類健檢 | 6 | ✅281d380（13d §2）；牙齒見 A23 | 規則已改。0821-1000 仍打 8 次——執行層硬上限改走 A23，本列不再重開 |
| T8 | 13/13c 過期描述清理＋prompt「第一輪」整節重寫 | T3查核 | 7 | ✅2026-09-07 `9b76af3`→`4cf3f38` | 低風險純文字，待批 **2026-09-07 全檢 §2b③ 列出三處具體過期**：`13` 排程表（12 輪表過期三代）／prompt「rules_check 只查六份」（實已八份）／`s2_scan_prompt_v7.md` 快照與 live 分岔＋`s2_v7_switch.ps1` 橫幅仍寫 09:00 **2026-09-07 落地**：`13` 12 列排程表整段刪除線＋指路「現查七輪」、16:00→17:00 三處訂正；prompt 刪「只查六份」；`s2_scan_prompt_v7.md` 重取快照、`s2_v7_switch.ps1` 橫幅訂正；`13c`／`13g`「人工下令才跑」殘句訂正。同分支順帶落 T12／A24／A31 六處規則句（`4cf3f38`）。rules_check 0，最大檔 13c2 25,593 字元。 **實作計畫**：[全檢實作/](2026-09-07-全檢實作/README.md) 01 |
| T9 | 🟠 `s2_batch_prep.py` 呼叫次數失控：單輪最大成本項、每則呼叫數波動近 10 倍、零約束 | 0825-0100 token 體檢（使用者發現總額異常） | 2 | 🔶0903驗收：預算制守住，但分批建議未被採納＋(5) RT id 殘留，未結案 | **0825-0100 實測**：該輪 $15.79，`s2_batch_prep` 60 次 ≈ **$5.33（34%）**，是單一最大成本項；相較之下 `set-tc` 6 次僅 $0.53（3%）、13f 增肥 $0.09（0.6%），**A10 v2 全部增量只佔 4%**。跨 12 輪每則呼叫數：0.09／0.46／0.66／0.35／0.78／0.88／0.26／0.63／0.44／0.54／**0.76**，**波動近 10 倍且與則數無穩定關係**。屬 T7／A23 家族（寫入子指令呼叫次數失控），但 `set-category` 有 A23 在管、`set-tc` 有 `TC_CALLS_PER_CHECKPOINT` 在管，**`batch_prep` 什麼約束都沒有**。⚠️ 診斷前先排除兩個已知的合理漲價因子：R15 全量載入規則（每次 context 207k→236k，帳本已記每則 $0.168→$0.195）、晚班狀態檔跨夜累積（0100 輪時已 345 則／594KB，本來就是全天最貴的輪次之一）。候選修法：①比照 A23 加 per-checkpoint 硬上限 ②查清為何同樣則數呼叫數差 3 倍（是重試？分批粒度？）③遙測補記每次呼叫的 batch 大小<br>🔬 **2026-08-25 診斷（四輪 transcript 考古，上面②已查完）**。⚠️ **診斷結果修正了本列標題的假設，原文保留供追溯**：<br>**(a) 熱點只有一個子指令**——0825-0100 的 60 次裡 `inspect` **56 次**，`search`／`dedup-check`／`compare` 合計僅 4 次。四輪一致（1600：6／12、1800：51／61、2200：34／39、0100：56／60）。<br>**(b) 失控形狀＝逐則翻閱，不是批次太碎**。0825-0100 的 56 次 inspect 裡 **31 次是「一次只看一則」**（16 次 `--ids` 單值＋15 次 `--index`），而且**同 `--fields`、同一個檔連續叫**：`ap_detail_0100.json` 連續 11 次 `--fields script`、`rt_detail_0100.json` 連續 15 次 `--index N --fields story`（其中 `--index 3`、`--index 18` **各叫了兩次、完全重複**）、`ns_full_0100.json` 5 次。**這 31 次併起來就是 3 次**（每檔一次 `--ids a,b,c…`）。同輪另有 3 次已經帶了 18／21／24 個 id——**能批次的能力本來就有，只是預設行為不用它**。<br>**(c) ⛔ 硬上限（候選①）是錯的方向，不要做。** 改用「每則**總**呼叫數」正規化後：1600＝2.21／1800＝1.62／2200＝1.82／0100＝1.75——**`batch_prep` 用最少的 1600 輪，每則總呼叫數反而最高**。因為 1600 輪改用 `python -c:json` 16 次讀檔（0100 輪只用 1 次），只是換了個桶。`inspect` 是唯讀查詢、掃帶主迴圈靠它讀素材，上限會直接卡死掃帶，且比照 `set-tc` 6→8 的教訓：**拒絕合法呼叫本身就是成本乘數**。<br>**(d) 可省金額**：每次呼叫 ≈ 296k cache_read ≈ **$0.089**。0825-0100 省 28 次 ≈ **$2.5／輪**；晚班三輪（1800／2200／0100）逐則合計 60 次、可省約 51 次 ≈ **$4.5／日**。<br>**(e) 為什麼不能只靠規則文字**：R15 截斷（Read 約 30,000 字元靜默截斷）很可能**正是** agent 退回逐則的合理原因——11 則 AP `script` 一次印約 15~20k、15 則 RT `story` 會超。所以合併必須在**工具層**做成安全的（自動依 `SAFE_BUDGET` 分塊並印出下一批指令），只在規則裡寫「請批次」會重演 A10 v2「載入 ≠ 遵守」。<br>**(f) 遙測缺口（候選③的實際內容）**：`s2_token_metrics.classify_bash_tool()` 把 `s2_batch_prep` **一桶到底**，不分子指令——這就是本次得回頭爬 transcript 的原因，也表示**任何修法都無法用 `--diff` 驗收**。建議先補這一刀（`s2_batch_prep:inspect` 等），零風險且是後續驗收的基礎。<br>✅ **2026-08-25 使用者授權「兩刀都做」，已上線（熱修，0430 輪起生效）**：<br>**① `inspect` 全文改由字元預算決定**（`s2_batch_prep.py`）。根因是舊判準 `full_detail = fields is not None and len(indexed) <= 3`——**只有查 ≤3 筆才印全文**，所以「要看完整 script」在操作上就等於「一次只能查 3 筆」，逐則翻是 agent 對這條規則的**合理反應**，不是它亂來。改成 `INSPECT_TEXT_BUDGET = 28000`（沿用 R15 同一個數字）：全文總長吃得下就整批全文；塞不下退回 200 字元預覽**並明講**＋附前 N 筆的可貼分批指令；單則自己爆預算則截到預算為止（⛔ 這裡掉回 200 字元會是功能退步，舊制單則查是印全文的）。**實測**：NS 20 筆 `script` 一次拿到 20,088 字元全文零截斷（舊制要 7 次）、AP 5 筆 22,410 字元全文（舊制要 2 次且每次只有 3 筆）。<br>**② 單則查詢加批次引導**：`--fields` 且只回 1 筆、同檔還有別筆時印一行提示。比照 A10 v2 的 T/C 覆蓋率閘門——**在用到的當下給觸發點**，不是把「請批次」寫進規則檔等它自己遵守。<br>**③ 遙測分到子指令**：`s2_batch_prep:inspect`／`:build`／`:dedup-check`…，認不出的子指令回舊桶名。下一輪起 `--diff` 就能直接驗收，不必再爬 transcript。<br>**④ 順帶修好 `--index` 的根因**（既有缺陷）：RT **detail** 檔的素材編號在 `edit`，清單檔才是 `code`，`SITE_SPEC['rt']['id_of']` 在 detail 上永遠回空字串、掉到 `_id_of_any` 又撿不到，最後變 `#index`——**`--ids` 對 RT detail 形同不存在**，agent 只剩 `--index` 一則一則翻。`_id_of_any` 候選鍵補上 `edit`（排在 `id`／`code` 之後）。<br>**⑤ `13d` 補一行 `--ids` 用法**（+3 行，載入 15,393 仍遠低於 28,000）。<br>**驗收**：`test_s2_batch_prep` 54→**63 PASS**（新增 9 條含「超預算要明講不是靜默截斷」「單則爆預算不可掉回 200 字元」「已批次就不要再囉嗦」）、`test_s2_token_metrics` 55→**59 PASS**、20 支 `test_s2_*` 全綠、`s2_rules_check` EXIT=0。<br>⬜ **待驗**：0430 起看遙測的 `s2_batch_prep:inspect` 佔比與逐則次數是否下降。**預期**：晚班三輪逐則 60 次 → 個位數，約 **$4.5／日**。⚠️ RT 的 TIMELINE 長稿實測單則 30,270 字元，這種本來就該逐則查，**不會歸零也不該歸零**。<br>⚠️ **已知限制（非回歸，暫不修）**：`_plan_full_detail` 只累計 `isinstance(v, str)` 的欄位長度，但顯示端會把 dict 欄位整包 `json.dumps`。AP **清單**檔（ES 形狀）的 `script` 是 nitf dict，預算算成 0、宣告「整批全文」，實際可能衝破 Bash 的 30k 上限被靜默截斷——正是這道預算要防的事。舊版同樣沒截 dict，所以不是本次造成，且實際暴露低（要全文 `script` 的查詢都打 detail 檔、那裡是 str）。修法比照 A13：預算計算改走 `text_field()`，與另外三條查詢路徑對齊。<br>📖 **讀 0430 遙測的注意事項**：`--diff` 會看到舊桶 `s2_batch_prep.py` 掉到 ~0、新桶 `s2_batch_prep:*` 冒出來，**那是改名不是真的降**——要把舊桶＋新桶加總跟 60 比。`--tool "s2_batch_prep"` 走子字串比對，趨勢圖不受影響、仍然連續。<br>🔬 **2026-08-25 三方獨立子代理查證（使用者要求「我不放心」）。上面有數處被推翻，以下為修正後版本，原文保留供追溯**：<br>**(A) 根因查證：✅ 成立，且是實證等級不是事後合理化。** transcript 直接證據鏈：agent 先寫臨時 `python -c` → 被 D9 hook 攔下導向本工具 → **先批次查 7,7,7,7,5 筆** `--fields script,desc` → 每筆都回 200 字元 `...[略]` → 查 `inspect --help`（沒提截斷規則、也沒有解除旗標）→ 改試單筆得全文 → **agent 原話「Good — single-id queries aren't truncated. Let me pull the remaining ones ... individually.」** → 之後逐則結果直接餵 `Write` 進 batch json（不是去重、不是重試，替代解釋排除）。規則面也對得上：`13c` 明定 `src_text` 取 script 正文前 4,000 字元、SOUNDBITE／SHOTLIST 段不准砍，200 字元連 SUPERS 段都走不完。<br>**(A2) 根因要補一半：不只「門檻太低」，還有「門檻未文件化」。** `≤3` 這個數字沒寫進 `--help` 或 docstring 用例，agent 驗證到「1 筆不截斷」就停手、沒推廣成「3 筆一批」——**它其實有 3 倍空間沒用到**。本次改動兩邊都解（預算制＋docstring 寫明＋用到當下的提示行）。<br>**(B) ❌ 「以每則總呼叫數論證不該加硬上限」的論證不成立，已撤。** 那是**規模效益的假象**：每輪有固定開銷（開機／瀏覽器導覽／state 初始化／收尾約 25~35 次呼叫），則數多的輪把它攤薄；1600 那輪只有 43 則又疊加 NEWDAY 歸檔，2.21 是被灌水的。換算**成本／則**後四輪幾乎持平（$0.203／$0.181／$0.212／$0.200），看不出 batch_prep 重的輪有優勢。**結論（不加硬上限）仍成立，但唯一站得住的理由是另一條**：`inspect` 是唯讀查詢、掃帶主迴圈靠它讀素材，上限會直接卡死掃帶，比照 `set-tc` 6→8。<br>**(C) ⚠️ 成本數字修正**：$0.089／次只是 cache-read 分量，**全成本是 $0.114／次**（含 output＋cache-write），原估低 22%。且「呼叫次數 × 每次平均成本」不是可加總的成本分解，有雙向偏誤（context 單調成長→用全輪平均高估中段呼叫；但每次回傳文字進 context 後被後續所有 request 重複讀→這部分又沒算）。$5.33／34% 只能當**量級參考**。真正有意義的是邊際成本。⚠️ 另有一個未查的變數：若計費走 Sonnet 5 intro 價（2026-08-31 前），全部金額 ×⅔。<br>**(D) 🔴 對抗性程式審查抓到一個會打回熱路徑的真 bug，已修**：`edit` 上游有**退化值**——20260824 六份 `rt_detail` 有**五份**含裸前綴 `'RT'`（無編號），1800 那份 39 筆裡就有 **19 筆**（1600 那份還有 `RTRT7400` 雙前綴）。撞名的 id 比沒有 id 更糟：工具印出的分批建議會含 4 個重複的 `RT`，照建議貼回去撈到 **24 筆**（不是承諾的 9 筆）→ 再度爆預算 → 再印同一條壞建議，**不收斂**；`dedup-check --ids RT,RT7440` 則**靜默**拿第一個 `'RT'` 比（19 選 1、比錯不報錯），而改動前 `'RT'` 不是合法 id、是 fail-loud 的。**修法**：`edit` 只收含數字的值（退化值退回 `#N`）＋分批建議印出前做 **round-trip 驗證**（貼回去撈到的筆數必須相符，否則寧可不給建議）。實測已收斂：建議 9 筆 → 貼回去剛好 9 筆 → 不再爆預算。<br>**(E) 🟠 預算改量序列化後長度**：原本比 raw `len(v)` 總和，但實際印的是 `json.dumps` 後的字串。JSON escape 實測讓 NS 型內容膨脹 **4.9%**，近飽和（26.5k~28k）時足以把「宣告整批全文」的輸出推破 Bash 的 **30k 靜默截斷**線——正是這道預算要防的事。改量 `len(json.dumps(...))`＋id/tab/換行包裝，**順帶解掉上面列管的 dict（nitf 殼）算成 0 的 🟡 洞**。實測：宣告整批全文時最大 stdout 27,606 < 30,000。<br>**(F) 🟡 遙測幽靈桶已修**：`grep -n "cap" scripts/s2_batch_prep.py scripts/test_s2_batch_prep.py` 會把第二個路徑的 `scripts` 抓成子指令。改成只收 `BATCH_PREP_SUBCOMMANDS` 白名單，認不出回舊桶名。**對 0825-0100 真實 transcript replay 驗證**：`inspect 56／search 2／dedup-check 1／compare 1`＝60，與 transcript 考古數字完全一致、零幽靈桶。<br>**(G) 措辭收斂**：`--ids "#0,#1"` 原本**是可用的**（能 3 筆一批拿全文），只是沒文件化，而且 agent 沒撞過牆——它是看到 id 顯示成 `#0…#23` 就**先驗**改用 `--index`。所以正確說法是「**以 RT 編號定址**形同不存在」，不是「`--ids` 形同不存在」。另外補修一個我自己測出的回歸：補 `edit` 之後 `--ids "#0"` 會失效，已讓 `cmd_inspect` **一律受理 `#N`**（工具自己印的東西必須貼得回去）。<br>**(H) ⚠️ 省錢估算要下修**：agent 的自救能力比原估強——56 個含 inspect 的 Bash 呼叫裡其實有 **76 個 inspect 調用**（它會用 for-loop 把 22 筆塞進一次 Bash）。成本估算用的是 Bash 呼叫數（31）所以單位沒錯，但**有一部分浪費它已經自己吸收掉了**，改動的邊際效益低於原估的 $4.5／日。<br>**(I) 🟡 已知、不修**：2~3 筆總長落在 28,000~29,500 的窄窗口，舊制一次全文（Bash 塞得下）、新制先預覽再分批（1 次變 3 次）。窗口窄，且舊制在 >30k 時是**靜默**截斷，新制是誠實化的代價。<br>**(J) ⚠️ 省錢估算實測校正：$4.5／日 → 平常日 $2.5~4.5。** 三方查證的實測（唯讀跑真檔裝箱）：0825-0100 的 31 次單看呼叫（覆蓋 40 個 item）改動後 **31 → 9 次**（NS 1／AP 2／RT 6），個位數成立 ✅。但 1800／2200 是**獨立 session、不可跨輪合併**，三輪合計下限 ≈20 次、**可省 38~40 次而非 51**。金額 40 × $0.089~0.115 ＝ **$3.6~4.6／日**——原估 $4.5 的呼叫次數高估（51→40）與單價低估（-22%）恰好互抵，落在區間**上緣**。<br>⚠️ **樣本偏差**：全部 93 輪聚合後每輪成本 mean $12.31／median $12.11／range $1.87~22.44，而我抽的四輪裡 1800＝P88、0100＝P83、2200＝P78——**三輪都在最重的四分之一**；1600 反而是 P17 的輕輪（43 則＋NEWDAY 歸檔開銷，這正是它 2.21 calls/則被灌水的機制）。日總額 range $34~136。所以 **$4.5 是重量級晚班的上緣值，輕日（0822 $57、0823 $34）日省額可能砍半**，平常日實測估 **$2.5~4.5**。<br>📌 **RT 長稿的既有限制（非本次造成）**：0100 那批 RT 有 2 則（`--index 2`／`18`）單則 story 就超過 28,000 預算，**單獨叫也拿不到全文**。舊制是印全文再被 Bash 30k 攔掉（拿到 30,000），新制截在 28,000——少 2,000 字元但**有明講**。實務無影響：`src_text` 只取 script 正文前 4,000 字元。<br>**驗收（二輪）**：`test_s2_batch_prep` 63→**70 PASS**（補撞名／round-trip／`#N` 定址／escape 膨脹／dict 欄位五類，補上一輪 fixture 全是乾淨唯一 id 的假綠燈）、`test_s2_token_metrics` 59→**61 PASS**（補幽靈桶負向測試）、`s2_rules_check` EXIT=0。<br>✅ **2026-09-03 驗收（0903-0100 輪。以 `scripts/_guard_calls.log` 逐筆對帳＋對真實 json 唯讀重跑實測，非 transcript 目測）**：<br>**(1) 逐則翻閱沒有復發，預算制的「誠實化」有效。** 0825-0100 的 31 次單則（`--ids` 單值＋`--index`）→ 本輪單 id 呼叫僅 3 次、無連續單則序列、正文批次中位數 5 則。<br>**(2) `inspect` 共 29 次**（另 `dedup-check` 1 次）＝**正文 17 次**（AP 8／RT 9）＋**元資料及清單預查 12 次**（AP 6／RT 6）。⚠️ 體檢報告初版報「20～22 次」、二版報「16／13 次」**都不對**（初版把元資料混進估算；二版正文少算 1、元資料多算了一次不存在的「被 D9 擋下的 sed」——那一次其實是完整重複呼叫，見 (4)②）。**日後對帳一律以 `_guard_calls.log` 為準。**<br>🔴 **(3) 「5 則是預算上限」不成立，是本輪體檢最大的誤判——真正的根因是 agent 沒照工具給的分批建議走。** 唯讀實測：AP **10 則** `--fields script` 輸出 23,265 字元**塞得下**（9 則 19,308／8 則 18,468），**11 則才爆**；RT 5 則 `story` 僅 17,786 字元＝**63% 預算**。agent 全程用 5 則一批，**只用掉一半容量**。而且 (4)① 那次爆預算後，工具 stderr **已經印出正確建議「要全文請分批，前 10 筆可一次取」並附可貼指令**，agent 仍自行縮成 5 則。⚠️ 所以這不是「預算制沒守住」，是 **`_plan_full_detail` 的分批建議未被採納**——與 (e) 的「規則檔寫了不遵守」不同，這次引導是**在用到的當下**給的，仍然無效，下一步要查的是這件事，不是再收緊規則。<br>**(4) 可省的呼叫（29 → 約 20，≈ 9 次 × $0.114 ≈ $1.0／輪）**：①`--ids` 11 則爆預算那次——**爆的原因是一次帶 4 個欄位 `script,role,src,rights`，不是 11 則太多**（同樣 11 則只帶 `script` 也剛好爆，10 則就過），11 則全部重查；②`rt_detail_0100.json` 的 `--fields head,src,restr,slug` **叫了兩次**（一次 `--limit 32`、一次不帶預設 100；該檔正好 32 筆，輸出完全相同）；③AP 正文 8 次→最少 3 次（26 則、10 則一批）；④RT 正文 9 次→最少 5 次（長稿差異大，實測最佳裝箱：24,652／11,370＋16,156＝27,526／8,144）。<br>⚠️ **(5) RT id 退化仍有殘留**（(D)／(G) 家族）：第 27 次 `--ids RT3633` **查無此 id、回傳空**，agent 改用 `--ids "#16"` 才取到——`#N` 定址的自救路徑（(G) 補的）確實有用，但 `edit` 欄位仍撈不到某些 RT 編號，**本列未結案的殘項**。<br>🟡 **(6) 元資料分次查（次要，⛔ 未動工）**：同一份 detail 檔的元資料分 3 次查（AP：`--lengths` → `sb_count,has_sot,role` → `dur`；RT 同型）。實測 `--fields sb_count,has_sot,role,dur` 一次查得到，AP／RT 各省 1 次（`--lengths` 是獨立旗標，併進 `--fields` 會讓欄位變 `len:` 模式，仍需單獨一次）。省幅小，且比照 (e) **不要只在規則檔寫「元資料請一次查完」**；要做就做在工具層。<br>**(7) 成本校準**：體檢報告「濃縮 inspect 可現省 60% context 重複讀取」**不成立**。55 則（AP 26＋RT 29）全文本來就得進 context（約 11 萬 tokens，不可壓縮），**context 峰值 38 萬幾乎不動**。該輪 5,070 萬 cache read 的主因是「context 峰值 × 工具呼叫總數」，不是 `inspect` 批次粒度——要壓峰值得走 D13（🗳待裁決），不是再收緊本列。 **牙齒見 A24**（`from-raw` 提示表取代逐則 `inspect`，預算制照舊）。 |
| T10 | 🟠 T/C 矩陣頁常態出現「大量未分類」，要事後再請 agent 補 | 使用者觀察（2026-08-25 1200 輪後） | 2 | ✅已修待驗（0825） | **診斷結論：不是 agent 沒標，是時序。兩個獨立缺陷，都有 transcript 實證。**<br>**① 補標永遠在 render 之後，而沒有人再 render 一次。** 現行流程＝`render` → 覆蓋率閘門報「N 則還沒標」→ agent 補標 → **收工**。txt 與 HTML 是 render 當下產出的，狀態檔標好了頁面不會自己更新。**0825-1200 實證**：閘門印「T/C 覆蓋率 91%（618 則裡有 53 則還沒標）」→ agent 下 `set-tc` 53 則 → 輪次結束；已發布 HTML 內嵌 DATA 裡 `tcsrc=heur` **剛好 53 列**（C＝未分類 22 列、T＝未分類 7 列），而狀態檔是 **100%**。**每一輪都缺當輪那一批**——這正是使用者看到的東西。⚠️ 諷刺的是閘門本身就是 A10 v2 為了「載入≠遵守」加的觸發點，它成功讓 agent 去標了，卻把標的時機推到 render 之後。<br>**② `set-tc` 的呼叫上限按頂層 `checkpoint` 分桶，但 `set-top checkpoint` 是收工才下的** → 輪次中途寫的 T/C 記到**上一輪**的桶。**0825-0430 實證**：那輪的 `set-tc` 印「checkpoint **`0825-0100`** 第 7/8、第 8/8 次呼叫」，用掉前一輪最後兩格；本輪額度一格沒動就先撞牆，**131 則新增只標到 29 則**，其餘留到後面輪次才補。transcript 裡 `set-top checkpoint 0825-0430` 出現在最後一次 render 的前一行。⚠️ **撞上限是 `sys.exit(3)` 且在計數器遞增之前，被拒的呼叫在 `tc_calls` 裡不留痕跡**（0430 整個 key 不存在 → 光看狀態檔會誤判成「那輪沒下 set-tc」）。<br>✅ **修法（都放在「用到的當下」，比照 `list-topics` 的成功模式）**：`set-tc` 寫入成功且 `last_render_ts` 存在 → 印出可直接貼的 re-render 指令（⚠️ **一定帶 `--out`**：少了它 `s2_render.py:591` 的 `if not args.out: return` 只會把 txt 印到 stdout、**什麼都不寫**，照著跑會以為更新了、其實頁面沒動——那比不提示更糟）；撞上限的訊息加「checkpoint 可能還停在上一輪，先 `set-top` 再重下」把死路變可自救的路；render 覆蓋率閘門加「補標完一定要再跑一次本指令」；`13f` 加「兩個時序陷阱」節。<br>**順手修好當下的頁面**：`0824晚班交接` 重 render 一次 → `tcsrc` 618/618 全 `stored`、**未分類歸零**。<br>⬜ **待驗（1600 輪起）**：閘門報的覆蓋率 vs 頁面實際 `tcsrc=heur` 列數是否一致（理想＝0）；`tc_calls` 有沒有出現本輪 key。<br>🗳 **待裁決（D-類，⛔ 不自行動工）**：本次修的是**保險絲**不是根治。根治是 agent 在 render **之前**就跟 `set-category` 同批下 `set-tc`（`13f` 本來就這麼寫），它不做的原因是**唯一的觸發點在 render 後面的閘門**。若 1600 輪起觀察到每輪固定走「render → 補標 → 再 render」雙 render 路徑，那是每輪多一次呼叫的常態成本（≈$0.114／輪 × 9 輪 ≈ **$1／日**），屆時值得考慮把觸發點前移（例如 `set-category` 完成時就提示「同批下 set-tc」）。**先看兩輪數據再決定，不要現在就改**——多一個觸發點也可能變成雜訊。 |
| T11 | 🟡 `set-tc` 撞上限的呼叫在 `tc_calls`／`tc_rejected` 都不留痕跡 | T10 診斷副產物（2026-08-25） | 4 | ⬜列入觀察 | `cmd_set_tc` 的上限檢查 `sys.exit(3)` 在 `calls[cp] = used + 1` **之前**，`tc_rejected` 也只記名稱錯誤。後果：整輪被拒的呼叫在狀態檔零痕跡，事後只能靠 transcript 考古（T10 就是這樣才查出來的）。與 A10 v2 當初「拒絕要留得下痕跡」的設計意圖矛盾——那條只做到了名稱錯誤那一半。候選修法：撞上限時也寫一筆進 `tc_rejected[cp]`（值寫「達上限，未寫入」）。**先觀察 T10 修完後還會不會撞**，會撞再修。 |
| T12 | `add-batch` 一次帶 `category`＋`tc`，`set-category`／`set-tc` 退為事後修補 | 全檢 §5 | 1 | 🔶2026-09-07 程式已上線（`bcb231e`+`94b30fc`），規則句已落 `4cf3f38`（觀察 3 輪後 ✅） | 每輪省 3～16 次呼叫，順帶消掉 13f 兩個 T/C 時序陷阱（補標要再 render／set-top 沒先下就吃前一輪配額）；規格看全檢 **2026-09-07 程式上線**：`cmd_add_batch` 每筆 `new_item()` 後就地 `_set_one_category(quiet)`／`_set_one_tc`，退件進 `tc_rejected` 不計 `tc_calls`；`build` 透傳 `category`／`tc`；`_remind_missing_tc()` 抽共用；`test_s2_add_batch_tc.py` 13 案＋`test_s2_batch_prep.py` +4，舊 batch 相容案通過。待空窗加規則句：13c2 §2 add-batch 說明、13f「怎麼下」首選改 batch 內建、陷阱①②各加「用 batch 內建不會發生」。 **實作計畫**：[全檢實作/](2026-09-07-全檢實作/README.md) 08 **規則句已落 2026-09-07 `4cf3f38`**（13c2 §2、13f 怎麼下／陷阱①②）。 |
| T13 | 掃帶 prompt 瘦身：22.5K→≤8K 字元，只留本輪參數，規則內容一律指路不重抄 | 全檢 §2b② | 2 | ✅2026-09-07 `5c61019`+`b98d4d4` ✅三輪觀察過（0907-1700／2000／2200） | prompt 現在是「第九份規則」，與規則檔雙軌漂移；先做 T8 再做本項；A26 刀4 擴充（五層合一）與本項同一波、分開上 **2026-09-07 落地**：prompt 12,014 字元（計畫寫的 22.5K 是 bytes）→ **2,294 字元**；刪掉的 8 段全部指得到規則檔。搬入：登入態總表→`13c` §0；排程輪只加不撤→`13c2` §2a；NS 逾時／不准轉包／驗 RULES-EOF／整站失守先補掃→`13c2` §5 第 9b／10／11／12 條；收工清單總表→**`13f` 尾**（原規劃 `13c2` §6，因 13c2 估計載入 27,400／28,000 逼近上限改放，05-A26 計畫的位置假設已同步訂正）。`s2_scan_prompt_v7.md` 快照同步；rules_check 0；DryRun 0。Task 3 launcher `{SITES}` 可選項未做。順帶發現 R23。 **0907-1700 首輪（V8＋瘦身 prompt）**：end_turn、無 traceback、`total_cost_usd` **$15.41**／tool_use **161**／37 分（對照 0430 $19.8／215、0700 $10.8／137；本輪是建檔輪、五站 141 則，量最大）。 **實作計畫**：[全檢實作/](2026-09-07-全檢實作/README.md) 04（先搬規則再瘦身；0907 盤點：不准轉包／只加不撤／登入態總表／整站失守補掃／RULES-EOF 驗法五段只有 prompt 有） **0907-2000（V8 第 2 輪）**：end_turn、無 traceback、8 新碼 ✅；$12.44／155 次工具呼叫／29 分；新收 NS 8／AP 8／RT 33（24＋補漏 9）＝49 則，回補 4 則 pending；三站 Ⓐ 對帳齊全、T/C 覆蓋 100%。RT 站 14 分鐘中 5 分鐘是首抓清單失敗重工（R27）。 **0907-2200（V8 第 3/3 輪）✅ 三輪觀察結束**：end_turn、無 traceback、8 碼齊；$18.32／192 次／37.6 分；五站新收 NS 40（含 911 廿五週年資料畫面 29 則）／AP 15／RT 23／ENEX 18／ABC 9＝105 則、T/C 覆蓋 100%、對帳零漏收。三輪合計 $46.2／508 次，與 V8 前基準（0430 $19.8／215、0700 $10.8／137）同量級，瘦身 prompt 未見退化。 |

### A — 腳本自動化（全流程分析 Phase 0–4） → 詳情：[master-detail/A.md](master-detail/A.md)

| ID | 項目（精簡） | 來源 | 優先 | 狀態 | 一句話現況 |
|----|------|------|-----:|------|------------|
| A1 | Phase 0 剩餘：metrics 細分＋報表 | 全流§4.5 | 1 | ✅2026-08-17（子項全數完成） | `--diff` 已能解釋輪次呼叫差異 |
| A2 | Phase 1：`s2_batch_prep.py` 擴充成 raw 工具層 | 全流§4.1-4.2 | 2 | ✅2026-08-17 子項全上線；行為遵守度仍待觀察 | 多輪樣本顯示行為漂移（0/0/36次python -c），已導出 D9 |
| A3 | Phase 2：state 查詢擴充＋輪次閘門 | 全流§4.3/4.6 | 3 | ⬜ | 子項未動工 |
| A4 | Phase 4a：`s2_schedule_check.py` 排程一致性唯讀比對 | 全流§4.8 | 4 | ✅2026-08-17 | 11 項測試全過 |
| A5 | Phase 4b：watchdog「START 無 DONE」中途死亡偵測 | 全流§4.7 | 5 | ✅2026-08-17（只推播不代打）待實戰驗收 | 22 項 replay 測試全過，判活邏輯已收緊 |
| A6 | 建檔輪自動帶入常駐中主題 | 使用者 2026-08-17 | — | 🔶待實戰驗收 | 已離線驗，首次自動生效為 0818-1600 |
| A7 | reconcile `missing` 無法區分「刻意去重」與「真的漏收」 | 0817-2200 體檢 | — | ⬜待做 | 建議 reconcile 另立 `deduped` 計數 |
| A8 | ENEX 整則排除規則新增 `TAIWAN PLUS` | 使用者 2026-08-17 | — | ✅2026-08-17 | 已加規則並回溯撤出已入庫素材 |
| A9 | ENEX／ABC 整併入排程掃帶（自動化補完） | 落差報告（2026-08-18）；[更新規劃書（2026-08-26）](2026-08-26-ENEX-ABC技術落差更新規劃書.md)；[ABC保活0904實測](2026-09-04-ABC保活ss-tok滑動續期實測.md) | — | 🟢**2026-09-05 已上線**（ENEX 2026-08-31 V5／ABC 2026-09-05 V7 進固定輪；①保活已補做） | 🟢 已併入固定輪（2026-09-05 使用者裁示；詳情看 A.md 的 A9 回寫）。舊記錄：①ABC：`ss-tok` 可由 `/Cms/SearchMedia/{customerId}` 滑動續期（0811／0826 打錯路徑才判死）；ENEX保活仍待觀察。未授權加回 `s2_keepalive.js` SITES（09-03 才排除）。詳情檔 |
| A10 | S2 分類系統化（大／中／小三層） | 分類規劃（2026-08-18）；`a10-p0-data/TC-字典.md`；**TC矩陣評估（2026-08-23）** | — | 🔶P1a ✅（登記簿 217 題上線）／P1b 程式上線 2026-09-07（`4e0279f`+`312caf2`）／P1c–d 待 | **2026-09-07 P1a 上線**：`topic-register`／`topic-alias` 子指令、`canonical_of()` 於 `_set_one_category` 寫入前改寫 alias（印改寫說明）、`list-topics --compact` 帶 charter、`load_registry()` 缺檔時自 `s2_resident_topics.json`＋13f 生成 2 題種子；`test_s2_topic_registry.py` 5 案。**登記簿草稿未上線**：`scripts/_registry_seed_20260907_draft.json`（217 題／39 組異名合併吸收 71 別名／9 組標「?」），審閱清單 `scripts/_registry_seed_20260907_summary.txt`；**2026-09-07 12:30 使用者放行，已改名上線 `scripts/s2_topic_registry.json`（217 題，9 組「?」維持分開）；P1b 開工。** **P1b 落地**：`add-batch` 新格式 `{"entries":…,"new_topics":…}` 才閘門（未登記→category 留空＋🆕 提示；`new_topics` 有 charter 就自動 register），純陣列舊格式不閘（保護 AP/RT/CNN 主力產線與 T12 迴歸）；`set-category --auto-register`；`add-side` 預設自動登記（`auto:true`、charter 取首則摘要 40 字，`--no-auto-register` 退回）並補 alias 改寫；`s2_platform_merge.py --apply` 固定帶 `--auto-register`；`list-topics --yesterday`。修一個真實風險：直接呼叫 `cmd_add_side` 未帶 `--registry` 會寫進正式登記簿（測試曾污染 3 筆，已復原、隔離、加提示）。**P1b-2 待**：`build --skeleton` 改輸出新格式讓主力產線也過閘（規則句落地後）；規則句三處（13c2 建檔輪第 8 項、13f 開新名必附 charter、0805 三判準改定案句）等 V8 上線後加進 `13c3`／`13f`。 同日訂正 `load_registry()` 缺檔只在記憶體生成種子不落地（原會讓任何跑 `set-category` 的測試在 repo 留下檔案）。 P0 歷史分析完成。T/C 矩陣已裁決（名單只認 TC-字典.md）。**2026-08-23：矩陣網頁 v1（render 當下算 T/C、不落地、零 token）已實作並實跑 0823 驗過；使用者裁 4 項（側錄計入總則數／側欄改 T／未分類要可見／走薄殼）。v2（agent 判 T/C）已定價 ≈+2% output token，但要動 13＋14＋17＋18 四條產線交件格式。** 仍未改 13／s2_state，P1 未授權。<br>🔴 **2026-08-24 同步修訂（前一句的兩個數字都已被同一份評估推翻，保留原文供追溯）**：①**成本 ≠ +2% output token**（評估 §13.3／M3）——這條產線 74~78% 的錢在 `cache_read`，成本函數是「工具呼叫次數 × 每次約 200~250k context」≈ $0.07／次，`set-tc` 實際約 **$0.28~$0.56／輪、$2.7~$5.3／日**，原估低估 10~20 倍；且基準線已因 R15 上修（每則 $0.168→$0.195）。②**不是四條產線都要改交件格式**（評估 §12.5）——所有外部來源都先落 `_待整併/`、由掃帶輪統一入庫，**只有側錄（`common/14`）要改交件格式**，韓聯社／CNA／YT（17）與 ABC／ENEX（18）都靠整併端補 `set-tc`，交件格式與 candidate schema 全不動。③判準寫哪一份：M1 已因 R15 作廢，**改為寫進 `13f`**（餘裕 16,106 最大），⛔ 不可寫 `13e`／`13c2`，動完必跑 `s2_rules_check.py`。④§14.2「載入≠遵守」整節已隨 R17 撤案作廢，不再是 v2 的反對論據。<br>🗳 **2026-08-24 使用者裁決（藍圖 G3／G4）**：**G3＝並存**——T/C 是同一條分類軸的延伸，**大／中／小三層不動、不被取代**，[v2 上線藍圖](2026-08-24-S2-TC矩陣v2上線藍圖.md) 六階段全部建立在此前提上，成立。**G4＝v1 不算踩 2026-08-04「不做任何分類的機械檢查或自動修正」禁令**（v1 全在 render 檢視層，不碰大／中／小、不自動修正既有資料、不寫回狀態檔），評估 §10.1 原本註記的「這是判讀不是帳本白紙黑字」**至此成為白紙黑字**。<br>⬜ **仍待裁**：**G1（A10 P1 授權：改 `13f`／`s2_state` schema）**與 **G2（判準寫 `13f` 還是 `13c`——`13c:11` 訂「日後改規則一律改本檔」但 13c 自身又說分類規則沿用 V1 `13`，治理衝突）**。**G1 未解除前，藍圖階段 1／3／5 及任何 `s2_state.py` 改動一律不得動工。**<br>✅ **2026-08-24 使用者授權 G1、裁定 G2＝寫 `13f`（錨點更正：治理衝突原文在 `13c2:440/444`，而 13c2 餘裕僅 3,536 已被禁止寫入，故實質只剩 13f 一個選項）。v2 當晚上線，2026-08-25 三輪連續驗收完成。**<br>**上線內容**：`set-tc` 子命令（T/C 存 item **頂層 `tc` 鍵**，⛔ 不進 `category`——`_set_one_category` 是整包覆寫，寄生其中會被後續 set-category／apply_reclass 無聲抹掉而 render 兜底會蓋住損失）／遙測儀器（`SHOW_FIELDS`、子指令 tuple 順帶補回本來就漏的 `patch-entry`＋`fix-first-seen`、`classify_phase`）／判準寫進 13f／render join／**線上版晚班交接換成矩陣版**（兩條 render 路都接，比照 2026-08-11 事故教訓）／一鍵回滾 `scripts/tc_v2_rollback.ps1`（tag `tc-v2-rollback-base` 已推遠端）＋環境變數 `S2_HTML_LEGACY=1` 緊急退版面。<br>**三輪驗收**：0824-2000 `set-tc` **0 次**（規則完整進 context 但 agent 沒照做，以 `tool_calls_by_name` ＋ transcript 逐行掃描兩種獨立方法交叉驗證，比照 R17 教訓）→ 加 **T/C 覆蓋率閘門**（render 後報覆蓋率並印出可直接貼的指令，設計取自 `list-topics` 的「明確觸發時機」成功模式）→ 0824-2200 **2 次**、0825-0100 **6 次**，全日 345 則覆蓋率 100%、`tc_rejected` 零違規。<br>🔴 **這是「載入 ≠ 遵守」第一次拿到經得起交叉驗證的證據**（§14.2 原本講這件事，因 R17 偵測器缺陷而整節撤回）；結論修正為：**光把判準寫進規則不夠，要有觸發點**。<br>**實測成本**：v2 全部增量僅佔單輪 **4%**（`set-tc` 6 次 $0.53＋13f 增肥 $0.09，對比該輪總額 $15.79）——原評估對成本的疑慮遠高於實情。同輪次真正的成本大戶是 `s2_batch_prep`（34%），已另開 **T9** 列管。<br>~~⬜ **未動工**：側錄（`common/14`）與 ABC／ENEX（`common/18`）兩條產線的 T/C，計畫書已備妥見 [側錄與平台產線改動計畫](2026-08-24-S2-TC矩陣v2-側錄與平台產線改動計畫.md)，待點頭即可執行。~~<br>✅ **2026-08-25 使用者下令「把剩下沒有做的流程…T/C 判定也加進去」，四條產線全部收尾、已上線（熱修）**。⚠️ 先做一個 60 秒查證解掉帳本與使用者說法的矛盾：帳本記未動工的是 14＋18，使用者卻把 17（韓聯社／CNA）也列進「沒做的」。查證結果**使用者是對的**——`grep` 過 13d／13e／13f／17／18 五份，**只有 13f 有 T/C 段、而且整段只寫給三站**，17／18 一個字都沒有；藍圖階段 5-2（韓聯社／CNA／YT）雖記為「交件格式零改動」，但**規則面從來沒接上**，整併端沒有任何觸發點會叫它下 set-tc。0824 狀態檔 599 則裡零 YNA／CNA／SIDE 項目，所以三輪 100% 覆蓋率**沒有驗到這三條產線**。<br>**第 1 刀（純規則，0字碼改動）**：13f 加「各產線的 T／C 由誰下」對照表（三站／17／18 走整併端，14 走交件端）＋「來源預設 C」（🗳 使用者訂：YNA→南韓、CNA→新加坡、NHK 側錄→日本、CNN 側錄無預設；🔴 **預設是起點不是終點**，內容明顯指向他處照內容判，T 無預設，ABC／ENEX 沒給預設不得自創）；18 §7 加 set-tc 步驟；17 加 §10b（交件端不下 T/C，但**地緣例外要在回報裡點名**——只點例外、是南韓／新加坡的不用列）。13f 估計載入 15,580 < 28,000。<br>**第 2 刀（`s2_state.py`，A10 P1 範圍，挑窗口 12:20~16:00 施工）**，分兩個 commit：<br>　① `0616f16` **先修 arity 靜默丟棄**（既有缺陷，與 T/C 無關，單獨 commit 才能獨立回退）。`segs[3]` 以後原本直接丟掉、零檢查，「多一段沒人看得懂的東西」跟「格式正確」回傳碼一樣是 0。抽出 `_split_home_line()`，位置段 >3 拋 `SideHomeError` → `cmd_add_side` exit 2、**一個字都不寫進狀態檔**（比照 `s2_platform_merge.py:110-115`）。<br>　② `879edda` T/C 解析與寫入。⛔ **標籤式 `→ T:… → C:…`，不是位置式**——小分題可省略，靠 `segs[3]`／`segs[4]` 定位會把 T 讀成小分題，**靜默錯位而且錯得很像對的**；認前綴則舊格式自然落在「沒有 T/C」分支，**不需要版本旗標**。🔴 T/C 寫進區段**每一段**（`fold_side` 摺單元取第一列，只標第一段會讓「第一段沒標、後面有標」的單元整個看起來沒標）。⛔ 寫**頂層 `tc` 鍵**不塞 `category`。名稱對 TC-字典 驗名，字典外的**不寫進狀態檔**、列進回報。舊格式**永久合法**（軟收 `uncat`，⛔ 不設旗標——Fable 審查已裁定硬 ❌＋祖父旗標與軟收是兩套互斥哲學）。**已在庫但還沒標的會被補上 `tc`、`raw_entry` 一個字不動**（候選檔 append＋每輪重讀，交件端事後補 `T:`／`C:` 才有機會生效；⛔ 不可為此改走 `--overwrite`）。`fold_side(rows, stored)` 多帶 `tc_id`＝單元內第一個有 T/C 的段（⛔ 不是改 `id`，`id` 是單元身分）。`common/14` 新增 C-3b（格式／來源預設／⛔ 三件不可以做的事）。<br>**驗收**：`test_s2_side_tc.py` **29 PASS**（新檔）；21 支 `test_s2_*` 全綠（`test_s2_render`／`test_side_fluff` 是既有環境相依失敗，改動前後一樣）；`s2_rules_check` EXIT=0。⛔ 全程對**生產狀態檔的複本**離線試跑，沒碰生產檔：0823 三份真實候選檔 224 段與改動前**逐欄位 parity 相符**；改成新舊混合格式後 `add-side` 10 段全標到、`raw_entry` 未動、`tc_stats` 620 則覆蓋率 100%；**狀態檔內零則 `raw_entry` 含 T/C 字樣**（`s2_validate.py:54` 的 `LINE_RE` 不會受影響，該檔記錄踩過三次）。<br>⬜ **待驗（1600 輪起）**：真實側錄／韓聯社候選檔進來時，`add-side` 那行「T/C：新格式 N 段已標／舊格式 M 段沒標」的比例，以及覆蓋率閘門是否還會點名這三條產線。🗳 **待裁決三項照計畫書建議走**（使用者本次概括授權）：(a) 18 不做交件端前移、先跑純整併端一輪；(b) 側錄過渡期 3 天；(c) 判斷單位＝摺疊後單元。⚠️ **2.7 的外部交件者通知我沒有管道**，新格式範例在 `common/14` C-3b，要請使用者轉發。**2026-08-25 補充**：0825-0430 輪 99 則（含側錄與部分 RT/AP）因此漏標 T/C，使用者發現「未分類太多」後由 main 人工逐則判讀＋`set-tc --pairs` 分 5 批（各 20 則）補齊，checkpoint 0825-1000 用掉 5/8 次呼叫。**這是一次性人工回補，不是自動化上線**——側錄產線本身仍未動工，同類缺口會在下次側錄輪再出現，需靠此計畫書真正解掉根因。 **2026-09-07 新增 D14**：提議以 11 天 AI 產出統計（全檢 §1c／§1d）替代或補足 P0 §3a，裁決後解鎖 P1 **2026-09-07 D14 裁決「替代」→ P0 以全檢 §1c／§1d 結案、P1 解鎖**；P1 落地順序見全檢 §7（registry→charter→add-batch 硬閘→find-similar），實作計畫待第二波 **0907-1700 觀察**：P1b 閘門實際沒起作用——agent 把 `category` 寫成物件（同狀態檔 schema），`add-batch` 用 `str()` 轉後無 `/` 整批退回，改走 `set-category`（topic_mode off）→ 68 個中主題 55 個未登記、登記簿零 diff。根因見 R25；P1b-2 要一起修。 **實作計畫**：[全檢實作/](2026-09-07-全檢實作/README.md) 13 **2026-09-07 23:xx P1b-2 上線（`bf1c27a`，軟上線）**：`build --skeleton` 在 entries.json 有 `_new_topics` 鍵時出 `{entries,new_topics}` 新格式過閘；R25 修好後閘門在三站主線真的會擋（e2e：22:00 RT 批 20/23 則、15 題未登記）；規則句三處已進 13c2／13c3／13f；救援路徑（重送只補分類）已進 add-batch。P1c／P1d 未動。  **P1b-2 硬上線（2026-09-08 使用者裁決，`3f474a6`）**：軟上線把閘門掛在「agent 有寫 `_new_topics`」上，0908-0100 實測前提從不成立（三站 batch 全是 Write 手寫純陣列、`build` 一次都沒被呼叫，48 個中主題 43 個未登記）。改成 `add-batch` 對 source 含 RT／NS／AP 的純陣列 exit 2（錯誤訊息即修法：包成 `{entries,new_topics}` 重送）；`build` 兩條分支一律出殼、`concat` 保留殼；13c2 §2 兩句同步。ENEX／ABC 平台線與 `--auto-register` 不受影響。代價：04:30 起 agent 要為未登記中主題寫 charter，token 會增加，已知並接受。  **0908-0430 硬上線首輪 ✅**：退回訊息一次都沒觸發——agent 直接照 13c2 §2 新規則出新格式，三批 batch 共由 `new_topics` 登記 33 題（charter 是 agent 自己寫的，不是截斷），🆕 被擋 0 則、99 則新收 100% 有分類與 T/C。原本預估的「寫 charter 的 token 代價」有付：本輪 $22.77／230 次／41.7 分（0907-0430 同時段 $19.82／216／41.0），但收錄量 56→99，每則 $0.35→$0.23。**側門仍開著，見 R30。**  **0908-0700 觀察 2/3 ✅**：仍 0 次退回；閘門這輪**真的擋了東西**——🆕 兩批共 7 題／13 則被擋，agent 照救援路徑補 charter 重送，收工 63 則全部有分類與 T/C、0 個未登記中主題（0430 那 17 個側門名字也被補登記）。$12.05／145 次／29.1 分／63 則（0907-0700 同時段 $10.76／138／24.6／42），每則 $0.26→$0.19。⚠️ 但閘門只逼登記、不管命名：暖新聞／皇室動態／名人動態(加密貨幣)／墨西哥治安 這次是**附 charter 正式進登記簿**，比先前不登記更難清 → P1d 禁用詞優先度提高。  **0908-1100 觀察 3/3 ✅**：0 次退回、89 則新收 100% 有分類與 T/C、0 個未登記中主題。$10.28／108 次／28.0 分（0907-1100 同時段 $9.81／116／22.9／51 則），每則 $0.19→$0.12。**P1b-2 觀察結束。** 但三輪下來登記簿從 241 漲到 357 題（本班 640 則素材對 286 個中主題、**孤兒率 65%**，遠超三判準的 30% 門檻），「邁阿密貨機事故／貨機衝跑道／機場貨機意外／空難／貨機墜舉」已經是 5 個重複格共 54 則。→ **P1c（find-similar）與 P1d（禁用詞）現在是主線阻塞點**，優先於 R30。  **P1c find-similar ✅2026-09-08 12:xx `457f276`**：`s2_state.py find-similar --text` 比對登記簿 name／aliases／charter ＋今日中主題，附今日則數與命中片段；`add-batch` 登記 `new_topics` 前自動比一次，撞到印 ⚠️（**只警告不擋**，誤判率未量）。13f 開新名段補規則句。⛔ **沒照原規格掛在 `from-raw` 提示表**——那張表吃站方英文原文，中文子字串比對必然 0 命中（程式原註解已載明），真正該攔的時刻是 agent 手上已有中文名、要登記的那一刻。門檻拿 0908 真實登記簿調（名稱共同長×2＋charter×1 上限 6，門檻 8／高 12），實測邁阿密五格互相全中、「美國期中選舉造勢」不誤命中「美國緝毒行動」但正確揪出另一組期中選舉四路分裂。測試 `test_s2_find_similar.py` 21 案、44 支全綠。**剩 P1d 未做（禁用詞），下一步。** |
| A11 | 🔴 D9 guard hook 生產環境確認會漏放 | 0818-1200 體檢 | — | 🔶2026-08-21 1000 重放已定位，待修 launcher | 規格見 [0821-1000三刀建議](2026-08-21-0821-1000體檢-三刀建議.md) 刀2。regex 會攔、live 沒攔；根因是 hook 沒載入。**不准加寬 regex** |
| A11-補 | 🔴 0819-1200 體檢新樣本：漏放率比原記錄更嚴重 | 0819-1200 體檢 | — | ⬜併入 A11-1000 | 5/5 全漏放；根因已由 A11-1000 定位，本列不再單獨追平台層 |
| A11-1000 | 🔴 0821-1000 重放：hook 沒載入（D7 scratch cwd × T1 setting-sources） | 0821-1000 三刀建議 | 1 | ⬜待修 launcher | 14 次 `python -c` live deny=0、`decide()` 13/14 會 DENY。修法：開跑前把 guard settings 寫進 scratch `.claude/settings.json`。T1 A3「有擋」不適用生產 scratch cwd |
| A12 | 臨時腳本繞道分桶診斷＋改進提案 | 0818-1600 體檢 | — | 🔶1️⃣patch-entry 已上線待實戰驗收／2️⃣3️⃣4️⃣🗳待裁決 | 拆四桶處方；桶①已完成上線，0818-1800 首輪實戰 PASS |
| A13 | 🔴 `inspect --lengths` 靜默漏掉巢狀欄位→AP 誤判缺 script | 0818-2200 體檢 | — | ✅2026-08-18（`3e18165`）待實戰驗收 | 已修共用 helper，54 項測試全過 |
| A14 | 0818-2000「本輪 0 則」誤報（實收 45 則） | 0818-2200 體檢連帶查出 | — | 🔶另一 session 已修但未 commit＋弄壞測試 | ⛔ main 未擅自 commit 他人在途改動，待 fixture 修正 |
| A15 | 🔴 batch json 由 agent 經 Write 重打整包內容當 output token | 0818-1800／0819-0100 兩輪抽查 | 2 | ⬜待做／可行性改走 A24-3b | 估佔全輪 output token 20~25%。0821-1000 具體落地＝Write `_build_batch*.py` 再跑；指定修法 `from-raw` 見 A24，本列不再另開方案 **牙齒見 A24**（`build --skeleton`，agent 不再 Write 整包）。 |
| A16 | 晚班交接每則外電加可點連結（RT permalink／AP 搜尋頁） | 使用者 2026-08-19 | — | ⬜待做 | RT 用 itemid 組 `all?id={guid}&media-types=vid`，零額外呼叫、是真 permalink；AP 用 Edit No 組 `home/search?query={id}&mediaType=video`，零額外呼叫但只到搜尋結果頁（detail 頁網址含 hash，見 `ap/01-search-workflow.md:55`，無法自己組出來） |
| A17 | batch 無謂拆成多段小量 Write，多付切段開銷 | 0820-1800 體檢(使用者要求深查RT流程)；0822-0100新增AP站重複寫入樣本 | — | ⬜列入觀察 | RT/AP兩站都有樣本：RT首次樣本(35則拆6檔)；**0822-0100新增AP樣本**：`ap_batch_0100_sntv.json`(17則)經查證是`ap_batch_0100.json`(30則)的完整子集合，同樣內容被重複寫入兩次，非切段而是**重複**寫入，累積樣本再議 |
| A18 | AP清單漏套抽取白名單(原始ES回應整包落檔)＋AP詳情擷取重試 | 0821-0430/0821-1200體檢(使用者授權Playwright查根因)；main查證與熱修 | — | ✅2026-08-21(main查證+熱修，13c§1a)；0821-1600首輪實戰PASS | Playwright實測AP官方前端自己的list/detail真實request/response，確認站方API每次都回同一種ES整包格式(`Items[]._source`)，完全穩定，不是站方bug。真因是13c舊範本本身沒示範「fetch後在同一個evaluate裡瘦身/解包再回傳」，agent每輪臨場手寫解析邏輯品質不穩，才會時好時壞。修法：13c§1a補上清單＋詳情各一支fetch+解包+瘦身一次做完的完整可照抄範本(取代舊版`return await r.json()`)，並加落檔大小自我檢查(>100KB代表沒套到)；13d§5補充說明本項不比照R9走送出前硬性關卡(關卡擋不住解析邏輯本身寫壞，需從源頭給範本)。**0821-1600驗收**：`ap_list_1600.json`25.7KB/50則、正常，detail結構乾淨，範本第一輪實戰過關 |
| A19 | AP詳情擷取無謂拆成多次呼叫 | 0821-1600體檢(使用者要求查AP疑似浪費)；0822-0100復發(a/b兩次) | — | ⬜列入觀察 | 首次樣本(1600輪，a/b/c三次共28則)；**0822-0100復發**：`ap_detail_0100_a/b.json`再度拆成2次呼叫，同一種「無謂拆分」在AP擷取階段第2次出現，累積2個樣本，趨勢待觀察是否規律復發 |
| A20 | AP摘要因無計數工具被連續修剪兩次(150字上限) | 0821-1600體檢(使用者要求查AP疑似浪費) | — | ⬜列入觀察 | `ap_batch_1600.json`→`ap_fix_1600.json`(18則)→`ap_fix2_1600.json`(其中9則又修一次)，字數持續往下修(如AP4679732: 121→115→80字；AP4679694: 188→182→139字)。這是A2已記錄的已知工具缺口「150字摘要上限沒有計數工具，agent只能自己反覆猜著改」的**AP站首次具體樣本**(A2先前的樣本是RT站，見0821-0430體檢A18裡RT6853等雙重修剪紀錄)。僅1個樣本，先列入觀察，若確認規律可考慮做一支字數計數小工具堵住源頭 **牙齒見 A31**（2026-09-07 `s2_pretag.py`）。 |
| A21 | 🔴 (BITE)標籤因無自動判斷工具標錯，事後整批重寫 | 0821-2000體檢(RT，2則)；0822-0100體檢大幅升級(AP，29/30則幾乎整批)＋NS標籤位置錯；0822-1000再補一筆NS樣本(17則) | — | ⬜列入觀察，規模已升級建議提高優先度 | RT首次樣本僅2則標反；0822-0100樣本暴增(AP 29/30則＋NS標籤位置錯)；**0822-1000再增NS樣本**：`ns_fix_1000.json`17則只改`entry`文字內容(非缺欄位)，與前述同屬「entry寫一次不夠、事後整則重打」家族。三站(RT/AP/NS)累計樣本持續增加，建議列為A2缺口裡優先度較高的一項 **牙齒見 A31**（2026-09-07 `s2_pretag.py`）。 |
| A25 | AP批次標題漏帶主題名稱(僅剩「(BITE)」)，事後補回 | 0822-0730體檢(使用者要求查0430/0730/1000三輪疑似浪費) | — | ⬜列入觀察 | `ap_fix_0730.json`(11則)比對`ap_batch_0730.json`原始版本，發現標題括號內主題名稱整個漏掉(只剩「(BITE)」空括號)，事後才補回主題名稱重寫一次。與A21同屬「entry第一次沒寫完整、事後整則重打」大家族，但這次缺的是標題主題名稱而非BITE標籤本身，分開追蹤。僅1個樣本，先列入觀察 **牙齒見 A31**（2026-09-07 `s2_pretag.py`）。 |
| A26 | 必讀規則精簡（四刀）：13 狀態 JSON 欄位設計搬走／13e 庫存檔格式分層／13c2 指令表瘦身／13d 併回 | [2026-08-26 S2規則精簡評估與方案](2026-08-26-S2規則精簡評估與方案.md)（使用者問「有空間嗎」） | 6 | ✅刀4 V8 上線 2026-09-07 `980dd43`→`ffe74ee`，三輪觀察過（0907-1700／2000／2200） | **去重是死路**：六份必讀 115,688 字元，跨檔逐行重複僅 424（0.4%）。真正可動的是「該搬走而沒搬走的內容」。✅ **刀1（2026-08-26 `5c488e7`，風險無）**：「狀態 JSON 欄位設計」2,562 字元逐字搬到 `plans/S2-狀態檔欄位設計.md`——`13c2` §2 明令 agent 禁止直接開 JSON，欄位長相對執行面零用處。⚠️ **與計畫書「只留一行連結」不同**，13 留了連結＋**三條硬規則存根**（`source` 封閉字彙／`items` 不准塞待辦／`raw_entry` 與 txt 行一致）：逐條查證後只有 `first_seen_checkpoint` 的 `{MMDD}-{HHMM}` 由 `s2_state.py:518` 擋得住，`--source` 只有 `add-side` 有 `choices`、**`add-batch` 完全不驗**，搬走等於刪掉一條只靠規則文字撐住的鐵律。13 估計載入 21,374→19,586。✅ **刀2（2026-08-26 `1519967`，風險中）**：13e 庫存檔格式節照計畫書 §4a「分層而非刪除」抽出 8 段追述到 `plans/S2-庫存檔格式-沿革與理由.md`。🔴 **實際只省 525 字元（22,481→21,956），遠低於計畫書估的 4,000–5,000，計畫書該項估計錯誤、待訂正**。根因：計畫書的「敘事」判準＝「非程式碼區塊、非表格、非清單項的散文行」，但 13e 的**硬規則大量以散文 ⚠️ 段落寫成**（SNTV 同主題重複收錄判準、CCTV／CNS 收錄門檻、備註↔摘要勿重標…），被判準**誤計為敘事**。實讀該節約九成是規格，不是肥肉。📌 **這條教訓要帶到刀3／刀4**：機器量到的「敘事佔比」不能直接當可省空間，得逐段讀過才算數。⛔ **不要當省錢專案**（計畫書 §七）：成本主因是呼叫次數（0825-2200：158 次、2.29 次/則），不是規則長度；一次多餘呼叫 ≈ $0.114，省 12% 規則若換來多讀一次 `plans/` 就是負值。正當理由是降低 agent 誤讀機率。🔴 **刀4 依計畫書原樣不可行，已停工待裁決（2026-08-26 實測）**：計畫書標「風險低、省 1,500–2,500」，但 R15 預算根本裝不下。實測餘裕——`13c` 21,721 字元／估計載入 23,831／**可再塞 ≈3,799 字元**；`13c2` 22,578／24,863／**≈2,848**；兩者合計 **≈6,647**，而 `13d` 全文 **13,748 字元**，**缺口 ≈7,101**。⚠️ 就算把 `13d` 取代掉的舊文字全刪（§7 明寫推翻 13c §2 AP 舊版、§2 取代 13c2 的「一輪一次」、§8 併完即失效），也補不滿七千字的缺口。唯一裝得下的是 `13f`（餘裕 10,469／可再塞 ≈9,241），但那要把**執行版內容搬進「大分類／各站規則」檔**，違反三部＋執行版的分檔語意，且會再度撞上 `13c:11`「日後改規則一律改本檔」與 `13c2:440/444` 那組治理衝突（A10 曾裁定寫 13f，但那是因為當時沒別的選項）。🗳 **待使用者裁決**：(a) 放棄刀4，接受讀三份；(b) 允許執行版內容外移 13f；(c) 先做刀3 把 `13c2` 指令表瘦身（估省 3,000–4,000）騰出空間、再回頭評估刀4。📌 **只做局部合併沒有意義**：刀4 的價值是「少讀一份、不必自己 diff」，合一半仍是四份檔，只換來改動風險。⬜ **待做**：刀3（`13c2` 指令表留常用 8 條＋索引，風險中，估 3,000–4,000）——若採 (c) 則它是刀4 的前置。✅ **2026-08-26 順手訂正（使用者下令）**：`source` 封閉字彙從「六個」改成現行**十個**（`AP`／`RT`／`NS`／`YT`／`SIDE_CNN`／`SIDE_NHK`／`YNA`／`CNA`／`ENEX`／`ABC`，附出處對照表），並補上「來源篩選順序」漏寫的 `ENEX`／`ABC`（以 `s2_render_html.py` 的 `SRC_ORDER` 為準）。原規則停在 0802，`17`（YNA／CNA）與 `18`（ENEX／ABC）上線後沒跟上。⚠️ **順帶記一個未裁決的並存寫法**：`s2_render_html.py:140` 假設 YNA／CNA 存成 `source: YT`＋id 前綴，但生產狀態檔也有直接存 `source: YNA`／`CNA`（0825 有 32／10 則）。兩種都顯示得出來（`SRC_LABEL`／`SRC_ORDER` 都收），不是 bug，但該統一成哪一種待裁決。⛔ **執行紀律**：一次一刀、中間至少隔一輪實跑、每刀後跑 `s2_rules_check.py`（離開碼 1 就不要讓下一輪跑）、**不在輪次進行中改必讀規則檔**（熱修改檔即生效，agent 會讀到半套）。⬜ **待驗**：1800 輪起首次實跑，看有無因連結外移而多出「去讀 plans/」的呼叫。📌 **順帶回報（未動工，13c2 因 R15 餘裕僅 3,536 被禁止寫入）**：`13c2:75` 的範例寫 `--checkpoint 16:00`，正是 `add-batch` 現在會擋掉的格式，疑為 0818 checkpoint 誤填那一類錯誤的源頭。 | [A.md](master-detail/A.md) **2026-09-07 全檢擴充刀4**：`13c＋13c2＋13d＋13g＋13h` 五層合一（規格全檢 §2b①、A.md 末段） **2026-09-07 V8 落地**：`13c`＋`13c2`＋`13d`＋`13g`＋`13h` 五層 → 單層 `13c`(上 24,950)／`13c1`(ENEX 12,696)／`13c1b`(ABC 15,559)／`13c2`(下 24,382)／`13c3`(收工與防卡 16,908)＋歸檔 `13c-V8-已取代條文.md`（非必讀）；必讀 8 份合計 152,511 字元（原五層 195K）。守恆 `_rules_conserve_v8.py` 缺 0／多 41（全可溯源）；rules_check 0；DryRun 0；**回滾主路徑**：`git revert -m 1 a474987 && git push`（整個 V8 合併一次退乾淨）；離線備援 `scripts/s2_rules_rollback_v8.ps1`（還原 `common/_rules_backup_20260907/` 五檔＋rules_check／token_metrics／prompt 三處常數）。合併時補回 NS `POST /api/v3/stories` body 配方（原只在歸檔，0814 實錯坑）。取代對照表 `2026-09-07-全檢實作/05-A26-取代對照表.md` §五／§六記字元預算調整與補丁。 **0907-1700 首輪**：規則載入 8 新碼全 ✅（13d／13g／13h 只出現在規則本文的歷史註記）；NS 19／AP 31／RT 43／ENEX 33／ABC 14；建檔七項 window_start／resident_topics 齊；觀察 1/3。 **實作計畫**：[全檢實作/](2026-09-07-全檢實作/README.md) 05（4 檔 V8：13c／13c1／13c2／13c3＋已取代條文歸檔＋多重集合守恆） **0907-2000 觀察 2/3** ✅：規則載入 8 碼齊、無違規；set-top 仍在五站後 render 前。 **0907-2200 觀察 3/3 ✅**：三輪規則載入 8 碼全齊、零違規、set-top 時序對；V8 五層合一定案，`13c-V8-已取代條文.md` 與 `_rules_backup_20260907/` 保留供回溯。 |
| A22 | NS單一則被拆出主批次外個別補寫(src_text缺漏) | 0821-2000體檢(使用者要求查2000輪疑似浪費) | — | ⬜列入觀察 | `wx20fr_fix.json`：WX-20FR(長島水龍捲)未帶R9強制欄位`src_text`，事後用單獨小檔案補寫，未併入`ns_batch_2000.json`主批次一次做完。與A17(RT batch無謂拆成6段Write)同屬「該一次做完卻被拆件」浪費家族，這次樣本在NS站、單則規模。僅1個樣本，先列入觀察 |
| A23 | `set-category` 同 checkpoint 第 5 次硬拒絕（T7 牙齒） | 0821-1000 三刀建議 刀1 | 1 | ⬜待做 | T7 規則已在（3 站＋1 補刀），1000 打了 8 次。不是 D-。修 `s2_state.py set-category` |
| A24 | `inspect` 單 id 連打提示 ＋ `from-raw` 產 batch 骨架 | 0821-1000 三刀建議 刀3；連 A15／A2 | 2 | 🔶2026-09-07 程式上線（`a5578c4`+`65e84c5`），規則句已落 `4cf3f38`（觀察 3 輪後 ✅） | 1000：NS 69 次、`batch_prep` 63、Write `_build_batch*.py` 再跑。**不准砍 src_text**。3a 警告不擋；3b 磁碟產出 id/src_text/footage_type，agent 只補 entry **2026-09-07 程式上線**：`s2_batch_prep.py from-raw --site --raw --checkpoint [--state] [--out] [--page]` 產提示表（每則一行，逼近 28K 自動分頁）＋骨架 json；`build --skeleton` 讓 agent 只寫 `{id:{entry,category,tc}}`；D12 併入：`--state` 給了只排除已 `has_script`，pending／其他狀態保留並標 `prev_status`。0906 真實快照：AP 22→1 新、NS 12→5 新＋1 pending、RT 8→1 新（見 R22）。`test_s2_from_raw.py` 37 案。待空窗加句：13d §4「掃完一站先 from-raw、不要逐則 inspect」、13c2 §2「batch 由 build --skeleton 產」。 **實作計畫**：[全檢實作/](2026-09-07-全檢實作/README.md) 09 **規則句已落 2026-09-07 `4cf3f38`**（13d §4、13c2 §2）。 |
| A28 | CNN／NHK 側錄行內標記（🔴／🟡／⭐／🔖）整合 | 使用者 2026-09-02（詢問側錄為何無紅黃標／能否整合格式） | — | 🔶2026-09-02 §4a-4c 已上線（`d03cbf9`），§4d／4e 未動工 | 韓聯社(YNA)／CNA 走標準三段式格式，**不受影響、原本就支援**。**使用者裁定「首段代表整組」**。**查證結論（真實 `0902-s2-state.json` 抽測）**：items 陣列順序不保證同單元相鄰，改採「跨整份 items 依 (source,category) 分組、取 TC 最早者」判定首段（不依賴陣列位置），45 個真實分組驗證全對。**已上線**：`patch_marks()` 移除側錄排除、新增 `side_unit_head_id()`、`cmd_patch_entry` 擋非首段標記並提示正確 id（順手修復 `unchanged` 清單原本收集理由卻沒印出的既有缺口）；新測試 `test_s2_patch_entry.py` §③／§⑥ 全綠，離線對生產狀態檔複本 dry-run parity 相符，生產原檔未被寫入。**尚未動工**：4d（品質掃抓非首段誤標）、**4e（render 端顯示驗證——目前完全沒驗證過標記在真實晚班交接／矩陣版渲染後看不看得到，建議優先於 4d）**。計畫書見 [2026-09-02-S2側錄行內標記整合計畫.md](2026-09-02-S2側錄行內標記整合計畫.md)（已更新查證結論與執行狀態）。 |
| A27 | `_待整併/` 零偵測：交件檔躺著沒人整併 | 0825-2200 輪（4 份交件檔零整併）；0825 快照定調「只加 A」 | 1 | ✅2026-08-26（`7a2b8fa`）／2026-08-30 補測試與本列 | **規則字數增加 0**：`13c2` §2 指令表早就承諾 `resume` 會報待整併，`cmd_resume` 從來沒印過——補的是既有承諾，不是新規則，沒有「載入 ≠ 遵守」風險。新增 `scripts/s2_pending.py`（零副作用、不反向 import `s2_state`），掛 **`cmd_resume`**（開工，落在掃站與 `set-category` 之前，補進來的素材才趕得上同一批 `set-tc`）＋ **`s2_render.py:603`**（收工保險，agent 不保證每輪跑 `resume`）。🔴 render 那道**只警告不擋**，比照清單對帳閘門——漏整併是少收素材，產不出檔是整晚白做。判準用 **mtime 不用檔名日期**（檔名格式是外部交件端定的，跨夜時「今天」有兩個日期）。⚠️ **落點與 0825 快照的方案不同**：快照原訂「共用出口＋頂層 flag 每 checkpoint 喊一次」，實作改為 resume＋render 兩點（158 次呼叫會洗版；render 每輪必跑，保險效果相同而成本低），理由完整寫在 `s2_pending.py` docstring。**2026-08-30 補**：`scripts/test_s2_pending.py` **21 項全 PASS**（tmp dir 注入 `pend_dir`，零生產路徑），詳情見 [A.md](master-detail/A.md)。 |
| A29 | 晚班交接網頁版側欄加「只看新一輪」篩選 | 使用者 2026-09-04 | — | ✅2026-09-04（矩陣版＋legacy 版都已接，待實戰驗收） | `first_seen_checkpoint` 對上狀態檔頂層 `checkpoint` 者標 🆕；0903 檔實測 56/583 則，側欄第一段「重點篩選（本輪 09/04 11:00）」內含 🔴🟡🔖 與「🔥 只看最新」（併段＋改字＋換圖示皆使用者當場要求） |
| A30 | 晨班時段標記 `■` → `◇` | 使用者 2026-09-04 | — | ✅2026-09-04 | 照 0804 `●`→`■` 同一套：只改生成不改解析，舊檔的 `■`／`●` 全部仍認得；規則文件 13／13b／13c2／13d／13e／13g／17／18 同步 |
| A31 | pre-tagger：`build`／`from-raw` 階段機械預填 T/C 候選、`(BITE)`、涉臺🟡、150 字、🔖負面條件 lint | 全檢 §6 | 2 | 🔶2026-09-07 程式上線（`f1a8699`→`1860d48`），規則句已落 `4cf3f38`（觀察 3 輪後 ✅） | 只讀已抽好的 raw、不碰站方 API（不是 ❌ 的固定 collector）；agent 只覆寫不同意的；吸收 A20／A21／A25／R19 四個「沒牙齒」觀察項 **2026-09-07 程式上線**：新模組 `scripts/s2_pretag.py`（`suggest_tc`／`taiwan_hit`／`bite_suggest`／`lint` 四項 📏🎙🔖🌀；延後 import `s2_render_matrix` 破循環）；四出口都掛好且全 try/except 包覆、只印不存；`test_s2_pretag.py` 14 案＋`test_s2_pretag_wiring.py` 9 案。**已知限制**：`from-raw` 提示表吃站方英文原文，中文關鍵詞表在該階段近乎聾（0906 快照 26 則：T? 3.8%／C? 7.7%／🇹🇼 0%）；②③④ 出口吃 agent 中文 entry 正常。英文別名字典見 D18。`s2_validate.py:394` 既有 150 字檢查與 📏 現在會各印一次，之後可併。待空窗加句：13f「怎麼下」段、13e 涉臺段（見計畫 10 Task 4）。 **實作計畫**：[全檢實作/](2026-09-07-全檢實作/README.md) 10 **規則句已落 2026-09-07 `4cf3f38`**（13f 怎麼下、13e 涉臺段）。 |
| A32 | 中主題粒度 lint：`s2_topic_review.py` 加巨格拆分／孤兒合併／類別詞命名警告（只印不改） | 全檢 §8 | 2 | ✅2026-09-07 已合併 main（`7d48b69`+`49a42ad`），觀察 3 輪後 ✅ | 基線：每天 44–62% 中主題只有 1 則、6–23 個 ≥8 則巨格（0827–0906 外電）；只印所以不受 A10 P0 禁令影響；門檻待 D14 裁 D-d 後寫進 13f **2026-09-07**：`granularity()` lint 完成（CATEGORY_WORDS 8 詞以 0905／0906 全部 247 個中主題名驗過、無誤殺；0906 `--compact` 38 行：巨格候選 5、命名警告 3、孤兒率 60%⚠️）；HTML 折疊先落在 legacy `s2_render_html.py`，正式頁 `s2_matrix_template.html` 同步完成（`renderList()` 加 foldOn／order 副本／＋N 則鈕）；0906 頁瀏覽器實測：9 個中主題折疊、【國際外交】35 則預設露 7 摺 28、展開／收合互切正常、搜尋啟動時不折、複製取全 35 則原序；txt 對正式檔零差異。 0907-0430／0700 兩正式輪：無 traceback、📐 粒度段有印、HTML 含折疊（觀察 2/3 輪）。 **實作計畫**：[全檢實作/](2026-09-07-全檢實作/README.md) 11 **0907-2200**：三輪 `s2_topic_review` 皆有跑，22:00 輪稽核抓到「威尼斯影展跨大分類重複」＋4 則放錯格由 agent 當場修正；lint 有效，標 ✅。 |
| A33 | S4 稿單提案生成器（唯讀、獨立檔、每天一次） | 全檢 §10 | 3 | 🔶2026-09-07 評分器＋prompt 上線（`41c13b6`+`9e5dafb`），未試跑 agent／未計採用率 | 繼承 0819 議題包／0825 S 軸不變式：不寫 category、不開中主題／機動 T、不搶鎖；07:00 輪收工後跑；驗收看採用率 **2026-09-07 上線**：`scripts/s2_rundown_propose.py`（`score`／`qualifies`／`missing_of`／`group_by_canonical`／`run`，自帶更嚴格唯讀 `_load_registry`，不 import `s2_state` 以免其 module 層 `default_file()` 在當日檔未建時 SystemExit）＋`scripts/s2_rundown_prompt.md`（18 個寫入子指令全列禁用，只准 `get --id`）；0906 試跑 105 中主題→12 題，前 5：中尼邊境洪災 11 分／國際外交 10／德國地方選舉 10／荷姆茲海峽軍事對峙 10／邁阿密貨機事故 10，`ctv_ids` 正確讀到 `0906-CTV候選.json`。輸出 `_稿單提案/0906-稿單候選.json/.txt`。**2026-09-07 12:40 試跑 agent（Sonnet）**：12 題全出（`_稿單提案/0906-稿單提案.txt`，複本 `D:\Downloads`），形式 CTV 2／SOT 8／BS 2，分派 外電編譯 10／連線 1／體育 1；全程唯讀但 `get --id` 呼叫 184 次（每則一次，候選 json 沒帶摘要）。prompt 缺口 5 點：唯讀段「不足才查」與輸出段「每則都要查」矛盾；「每則一行」與 common/11「/ 相接」矛盾；單題 47 則無上限、可讀性差；角度／形式欄位置未定；分派角色無判準。登記簿漏網：德國選舉↔德國地方選舉（已加 alias）、尼泊爾邊境土石流↔中尼邊境洪災（待使用者定）。**A33 v2 落地 2026-09-07 `49f3967`**：候選 json 每則帶 `summary`／`notes`／`bite`（agent 原則不查 `get --id`，summary 空才查、上限 5）；`_select_items()` 🔴🟡🔖 全列＋其餘前 `ITEM_CAP=12`、`items_omitted`；prompt 五點全修（順序固定、每則一行、分派四判準、同 canonical 註「建議併入 #N」）。0906 重跑 12 題、省略 57 則、summary 空僅 2 則。**待**：一週記「提案 N／採用 M」；排程化要 ≥50% 採用率。 **實作計畫**：[全檢實作/](2026-09-07-全檢實作/README.md) 14 |
| A34 | CTV 候選清單：狀態檔既有欄位機械篩（NS＋has_script＋有 SOT＋時長＋無限制字）＋一行角度 | 全檢 §11 | 3 | 🔶2026-09-07 `e4a7316` 上線校準中 | 0906 實測 NS 25 則全部 has_script、14 則有 SOT（其餘條件時長／src_text／限制字未套），全條件量級待實測；併入 A33 的「建議形式」欄，另出獨立 txt 供勾選 **2026-09-07 上線**：`scripts/s2_ctv_candidates.py --file <狀態檔>`（MIN_SRC_LEN=500、60–210s、限制字正則），輸出 `_稿單提案/{MMDD}-CTV候選.txt`；0906 實測 NS 40 則→10 候選；一週校準門檻後再 ✅。同日訂正 `derive_mmdd` 改檔名 stem 優先（原 checkpoint 優先會把 01:00 後的 0906 命名成 0907、隔天被覆蓋）。 **實作計畫**：[全檢實作/](2026-09-07-全檢實作/README.md) 12 |
| A35 | `s2_token_metrics.py` `rule_shas` 補 `13g`／`13h`、去掉已退役 `13b` | 全檢 §3 | 5 | ✅2026-09-07 `1f840a3` | 純量測缺口，一行修 **2026-09-07 已修**：RULE_FILES 去 `13b`、加 `13g`／`13h`；`test_s2_token_metrics.py` 62/62。 **實作計畫**：[全檢實作/](2026-09-07-全檢實作/README.md) 02 |
| A36 | NS batch 欄位名寫錯（`raw_entry` 誤寫成該用的 `entry`）逐筆 `Edit` 改名，未用 `replace_all` 一次修完 | 0908-2200 token 體檢（ABC A17/A15 修復驗收時順便查到） | — | ⬜列入觀察 | 22:00 輪 `ns_batch_2200.json` 被 `Edit` 13 次：10 次是把同一個打錯的欄位鍵 `raw_entry` 逐則改成 `entry`（10 則各自一次 `Edit`，內容都是同一種替換），另 2 次修「政治」分類名稱錯字、1 次刪 `new_topics` 裡的重複主題。與 A15／A17「該一次做完卻被拆件」同屬一個浪費家族，但根因不同：這次不是批次寫入被拆段，是**寫入內容本身有 schema 錯字**（鍵名打錯）造成的返工，而且 10 次同款替換其實可以用 `Edit` 工具的 `replace_all:true` 一次做完，卻逐筆呼叫。僅 1 個樣本，先列入觀察，再出現同類（別站／別欄位打錯字逐筆改）再議是否要在 13c2／13d 補一句「批次內同款替換一律 `replace_all`，不逐筆改」。 |

### D — 待使用者裁決（未裁定前不得動工） → 詳情：[master-detail/D.md](master-detail/D.md)

| ID | 議題（精簡） | 來源 | 狀態 | 一句話現況 |
|----|------|------|------|------------|
| D1 | effort 定案：high or medium | 省T§三 vs 0812 裁定 | ✅已裁決 medium | E2 數據已出，待正式定案並回寫省T計畫書 **2026-09-07 使用者裁決**：**medium 定案**，launcher 預設不變；回寫省T計畫書一行即結 |
| D2 | `s2_batch_prep.py` 路線：擴充 or 封存 | 省T② vs 全流§8 | ✅已裁決 | 使用者裁定走工具層（=A2），已上線 |
| D3 | audit §4「還有 N 組未列出」提示去留 | 交接§8 | ✅已裁決（留） | 待裁決 **2026-09-07 使用者裁決**：**留** |
| D4 | 無變動快速路徑（diff=0 輪跳過分類與 review） | 全流§Phase5 | ❌不做 | 中高風險，改變執行行為，待裁決 **2026-09-07 使用者裁決**：**不做**，移「明文不做」 |
| D5 | watchdog 自動代打（A5 第二階段） | 全流§4.7 | 🗳 | 累積 3 次真實樣本後再議（目前 2/3） |
| D6 | S2b C 通道排程化 | 全流§Phase5 | 🗳 | 使用者已明示先人工跑幾天，觀察期未結束 |
| D7 | launcher 把 agent 工作目錄設成 scratch-dir（R13 硬解） | 1200驗收 | ✅255313d | 已裁定做並上線，R13 根治 |
| D8 | 2200 的 65 則＋1200 的 80 則 src_text 人工回補 | R11 | ❌裁定不補 | 高 token、素材老化，防呆已上線，後人不要再立案 |
| D9 | `python -c` 硬排除（A2 行為漂移的硬解） | A2 0817 觀察 | 🔶2026-08-18 步驟②③已熱修上線，待實戰驗收 | 三段式裁決已定案並上線；A11／A11-補顯示 hook 仍會漏放，裁決未完全結案 |
| D10 | 三站排程改三子代理並行掃帶（指引報告書） | 2026-08-21 評估 | ❌不做 | 結論不建議改、維持單代理；使用者同意評估並指示存檔列管。**未授權動工。** 後人重提前先讀來源報告 **2026-09-07 使用者裁決**：**結案不做**，移「明文不做」 |
| D11 | AP／RT Live 節目表加掛進 S2 定時掃帶 | 2026-08-23 評估 | ⏸延後 | AP 可純 API 抓（`/v1/nrlive/events`，需UTC→台北時間換算），RT 只能靠 DOM 擷取；構想：22:00/04:30/12:00 三輪加掛、不動掃帶本體、產出`Claude共用/自動掃帶系統/AP_RT_LIVE節目表.txt`＋`.prev.txt`備份。5項待決事項見 master-detail/D.md。使用者已同意開項列管，**列管≠核准實作** **2026-09-07 使用者裁決**：**延後**至第一、二波做完再議 |
| D12 | `s2_batch_prep.py` 加「跨批diff」子指令，避免重疊 ID 被重複 `inspect`／`timeline` 查詢 | 2026-09-02 複核 0902-2000 掃帶 log 後提出（獨立審查建議 1） | ✅2026-09-07 併入 A24 實作（`a5578c4`） | **2026-09-07 落地**：以 `from-raw --state` 的「已在庫標記」實現（只排除已 `has_script`，pending／prelim／early 永不排除，已寫成測試），不另開子指令。**構想**：新增子指令，輸入本輪已抓的多份清單／詳情快照，輸出「哪些 ID 上一批已經查過、可跳過」的差異清單，降低同一 ID 被重複 `inspect`／`timeline` 的呼叫次數（與 T9 已記錄的 `batch_prep` 呼叫失控屬同一家族）。**風險（未授權動工的理由）**：①現行規則要求 `pending`／`prelim`／`early` 狀態每輪都要重查——第一批常只抓到部分資訊、需後續輪次補齊，diff 若只憑「ID 是否存在」判斷會誤跳過真正需要更新的則，等於引入新的漏收管道。②ID 正規化必須等 **R20**（RT 站清單快照 code 不帶 `RT` 前綴、狀態檔 key 帶前綴，導致 `s2_audit.py` 長期誤判漏收）修好、共用同一套正規化邏輯後才能做，否則 diff 工具會複製一份同款 bug。**前提條件**：R20 定案並修復＋新 diff 邏輯需明確排除 `pending`／`prelim`／`early` 狀態的跳過判斷，兩者皆滿足才可評估動工。⛔ 本列僅列管追蹤，未授權實作 **2026-09-07 使用者裁決**：**併入 A24 from-raw 一起做**（骨架產生時標「上批已查、狀態未變」，pending/prelim 一律不跳） |
| D13 | 每站（NS／AP／RT／YNA）整併完做一次 context checkpoint（如 `/compact`），避免後段分類／稽核背負全部前段累積的 cache | 2026-09-02 複核 0902-2000 掃帶 log 後提出（獨立審查建議 3） | ❌不做 | **構想**：三站（或含 YNA）各自整併完畢後主動清理／壓縮對話 context，讓後續分類、稽核階段不必背負前站累積的完整 cache，降低長輪次的 context 膨脹成本。**風險（未授權動工的理由）**：跨站去重／整併現行流程隱含依賴 agent 記得前面站掃到的內容細節（例如同主題跨站合併判斷），checkpoint 若清空這些記憶，規則裡目前沒有明文要求「checkpoint 後跨站比對要改用 `search`／`dedup-check` 等指令回頭讀檔重查」，貿然導入容易造成整併階段誤判／漏判，且沒有像 R15／R17 那類可交叉驗證的明顯錯誤訊號能及早發現。**前提條件**：先在 13 系列規則（比照 13d／13f 的做法）補一條「checkpoint 後跨站比對一律改用 `search`／`dedup-check` 重查檔案、不得依賴殘留記憶」的明文規則，並實測 1～2 輪確認無漏判／誤判後才能上線。⛔ 本列僅列管追蹤，未授權實作 **2026-09-07 使用者裁決**：**不做**（等 T12／A31 砍呼叫後再看） |
| D14 | 用近 11 天 AI 產出統計（全檢 §1c／§1d）替代或補足 A10 P0 §3a 歷史語料分析，一次裁 D-a～D-g，解鎖 P1（登記簿／charter／find-similar／判例庫） | 全檢 §7 | ✅2026-09-07 已裁決（替代） | 本計畫最關鍵一題：散落（§7）與巨格（§8）的治本方案全在 A10 P1，被 P0 禁令鎖住而 P0 §3a 已暫停（`.gdoc` 讀不到）。裁「替代」則 A10 直接進 P1；裁「補足」則先請使用者轉檔再跑 §3a **2026-09-07 使用者裁決：①替代，直接進 P1**。D-a～D-g 擬依分類規劃 §3b「建議」欄定案（詳 D.md D14 段），個別題有異議再改 **2026-09-07 使用者裁決**：D-a～D-g 七條**全部照擬定定案**，A10 P1 正式可動工 |
| D15 | T/C 字典拆分：T 社會→治安刑案／事故急難、T 政治拆出外交、T 新增軍事國防、C 歐洲拆出英國；低量專項（北韓／泰國／新加坡）維持 | 全檢 §9 | ✅已裁決＋落地 2026-09-07 `edb4847` | 字典只有使用者能改（TC-字典.md 開頭明文）；理由是編輯用途不同、不是量大；分布數據見全檢 §1d **2026-09-07 使用者裁決**：只拆兩項——T 新增「軍事國防」、C 歐洲拆出「英國」；社會／政治不拆。落地＝改 TC-字典.md＋13f 表＋s2_state.py 名單同一次，舊資料不回改（列第二波 06 之前的小項） **實作計畫**：[全檢實作/](2026-09-07-全檢實作/README.md) 06 **2026-09-07 落地**：`TC-字典.md` T 12 類（＋軍事國防）／C 22 類（＋英國，專項獨佔、歐洲排除英國）；13f 兩表逐字同步；`build_tc_matrix_0821.py` 顯示序＋關鍵詞兜底（英國 ISO 旗碼 gb）；`_load_tc_dict()` 解析 12／22；rules_check 0。已知：`_set_one_tc` 沒做「英國＋歐洲」互斥提醒；`s2_pretag.py` 另有一套 D15 關鍵詞，未來可收斂成單一來源。下一輪驗收：`set-tc` 有下新格、`tc_rejected` 無新格名。 |
| D16 | 稿單提案（A33）的分派規則：記者名單／專長／班別／每日上限；輸出位置與節奏 | 全檢 §10d | ✅已裁決（等名單） | repo 沒有記者名單，AI 不能自創；未裁前 A33 只填角色不填人名 **2026-09-07 使用者裁決**：使用者之後提供記者名單／專長／班別；A33 等名單到位再做分派欄，其餘欄位先做 |
| D17 | 晚班交接 txt 版面調整：中主題內 🔴🟡 置頂／孤兒中主題排該格最後／巨格加則數計數 | 全檢 §4 | ✅已裁決（不動 txt） | 13e §「已定案的輸出格式不再重決」→ txt 任何改動都要裁決；HTML 折疊／排序／連結不受此限，走 A16／A32 **2026-09-07 使用者裁決**：txt 全部不動，只改 HTML（折疊／🔴🟡 排序／連結走 A16／A32） |
| D18 | T/C 核心字典要不要建英文別名層，讓 `from-raw` 提示表的 `T?/C?/🇹🇼` 對站方英文原文有效 | 10-A31 試跑 2026-09-07 | ✅2026-09-07 使用者裁「做」，落地 `a8ae7ad` | **現況**：A31 只加了 D15 兩項＋涉臺少量英文別名（Taiwan／missile／UK），0906 快照 26 則命中 T? 3.8%／C? 7.7%。**擬定**：在 `s2_pretag.py` 關鍵詞表加一層「英文別名→中文 T/C」對照（約 80–120 組，涵蓋 13f 議題 T 與地緣 C 全部名單），不動 `TC-字典.md` 與 13f 正式名單；估半天。**不做的後果**：from-raw 階段的建議欄形同虛設，A31 效益只剩 build／add-batch／lint 三出口。 **2026-09-07 落地**：`EN_T_ALIASES` 58 組／`EN_C_ALIASES` 110 組（每類 ≥4，總量 168 超出原估 80–120）；0906 快照命中率 T：AP 14→57%／RT 14→57%／NS 25→75%，C：AP 43→71%／RT 57→86%／NS 17→50%，抽 21 行零誤標。順手修 D15 `UK_C_KW` 裸字 UK 子字串誤中 UKRAINIAN（AP4682773 曾被掛英國），改詞邊界。NS 訪談類素材結構性缺訊號，留白不硬湊。 |
| D19 | 把 A24 `from-raw`／`build --skeleton` 骨架收斂機制擴展到 ABC／ENEX，讓 `s2_platform_extract.py` 那條 platform 三件套管線也能吃骨架、少走逐則 `inspect` | 0908-1700 token 體檢（使用者發現 ABC phase 70 次呼叫偏高，追查後） | 🗳 | **現況**：`s2_batch_prep.py` 的 `SITE_SPEC`（`from-raw`／`build --skeleton`／`--site` 限定）只登記 `ns/ap/rt` 三站；ABC／ENEX 走完全獨立的 `fetchXxxWindowRows() → s2_platform_extract.py {abc\|enex} → lint → merge` 管線，兩套工具沒有共用骨架收斂。0908-1700 實測 ABC 站 `s2_batch_prep:inspect`（`abc_detail`）11 次是工具內建 28,000 字元預算正常拆批，不是浪費；但沒有 A24 那種「先出提示表、agent 只判不用逐頁翻」的收斂空間。**構想**：`SITE_SPEC` 加 `abc`／`enex` 條目（`id_of`／`skip_of`／`src_text_of`／`status_of`／`extra_of`），讓 `from-raw --site abc` 能吃 platform 管線的 raw 快照，產出同款提示表＋骨架 json，agent 判完接 `s2_platform_extract.py abc --entries <骨架>` 走原本的 extract→lint→merge。**風險（未授權動工的理由）**：①`SITE_SPEC` 是 NS/AP/RT 三站共用的核心資料結構，`abc`／`enex` 的欄位形狀（`Story Number`／`script_html`／`synopsis_html` 全文、無 CSV 標準殼）與三站既有 spec 差異大，硬塞可能牽動 `dump`／`inspect`／`compare`／`snapshot` 等既有子指令對三站的既定行為；②ABC／ENEX 的 platform 三件套（extract/lint/merge）本身有自己的入口格式驗證，`from-raw` 骨架能否無縫接上 `--entries` 尚未驗證，需要先讀 `s2_platform_extract.py` 的 entries schema 逐欄比對；③`test_s2_from_raw.py`／`test_s2_batch_prep.py` 現有測試假設站別只有三種，擴充要新增對應測試組，改動面不小，屬需要專門排時間做的架構級任務，不宜掃帶輪順手改。**前提條件**：先讀 `s2_platform_extract.py` 的 `--entries`／`--raw` schema，確認 `from-raw` 骨架格式與 platform 管線輸入格式是否相容（相容則改 `SITE_SPEC` 擴充成本較低，不相容則要另開一支 `platform_from_raw` 而非硬塞進共用 `SITE_SPEC`）。⛔ 本列僅列管追蹤，未授權實作 |
| D20 | 收掉 01:00 輪的 ENEX／ABC，併回 04:30 一起掃（回到 2026-09-05 前的舊窗） | 使用者 2026-09-09 下令 | ✅2026-09-09 已裁決並落地 | **理由**：01:00＋04:30 各掃一次 ENEX／ABC 的固定開銷（`s2_platform_extract`／`lint`／`merge` 等每輪固定跑約 8-10 次呼叫、不隨則數變動）跑兩次，改回只在 04:30 掃一次可省一輪固定開銷；素材延後到 04:30 收，`add-batch` id 去重吸收重疊，不會漏收。**已知代價**：04:30 的 ENEX 窗口從 3.5 小時（01:00–04:30）回到 6.5 小時（22:00–04:30），31 天歷史基準是每輪約 19 則、**全日最重**；今天（0909）實測 04:30 輪 ENEX+ABC 段落約 11.3 分鐘，併窗後估計拉長到 18~20 分鐘、總輪次時間可能從 35.5 分推到 43~45 分。ABC 沒有可靠歷史數據（僅 0810 偏斜樣本、0831 三天人工量 18/13/5），增幅估計信心較低。**ABC session 訂正（2026-09-09 使用者指出原判斷有誤）**：原本以為獨立的 `S2-登入保活`（Windows 排程每 30 分鐘 :20/:50）能覆蓋 01:00 跳過 ABC 造成的空窗，**這是錯的**——`s2_keepalive.ps1:175-181` 寫死：拿不到 `.s2-scan.lock`（掃帶正在跑）就整支 `Skip-Run`，NS／AP／RT／ABC 全部不碰，掃帶跑多久就 SKIP 多久（一輪 25-45 分鐘，遠超 ABC `ss-tok` 約 1 小時的續期窗口，疊加前一次 SKIP 有斷線風險）。**真正的保活機制是 `13c1b` V7-5「關瀏覽器前 NS landing 之外再摸一次 ABC cmspage」**——這條規則跟 V7-1 的 ENEX/ABC 全量掃描開關無關、每一輪收工前都要做（明文禁止輪次內 spawn 保活腳本，只能在掃帶自己的 MCP session 裡 navigate 一次），01:00 跳過全量擷取不影響這一步，本來就會繼續摸。已在 `13c` V7-1 表 01:00 列補註記，避免下一個讀規則的 agent 把「跳過全量」誤讀成「連摸都不摸」。**落地**：已改 `13c` §V5-1／§V7-1 兩張表，01:00 改 ⬜跳過（比照 11:00／20:00 寫法：不查、不寫候選檔、回報寫一行）；04:30 註記窗口變化。**待辦**：觀察 04:30 實際耗時 1-2 輪，樣本不足前先每輪覆核，數據回填本列或另立 E 項。 |

---

## ❌ 明文不做（防止後人重提）

| 項目 | 出處與理由 |
|------|-----------|
| Task 6 三站固定 collector | 省T§六：API 漂移會從「agent 當場繞過」變「無聲失敗」，投報率九項最差 |
| Task 7 LLM 只讀 normalized delta | 依賴 Task 6，一併延後 |
| Task 9 Message Batches | 降 API 計價不降 token，本流程走 subscription CLI 不適用 |
| Haiku 模型分流 | 前綴 64.7k 未砍前，雙模型雙冷啟動不可能划算 |
| Prompt caching 專項 | 降為觀察項，Task 1 已順手記 cache 欄位 |
| ~~D4~~ 無變動快速路徑（diff=0 跳分類／review） | **使用者 2026-09-07 裁定不做**：diff=0 輪極少，改執行行為的風險不值 |
| ~~D10~~ 三站改三子代理並行掃帶 | **使用者 2026-09-07 裁定不做**：評估結論維持單代理，prompt 已明文禁轉包（0811 實錯） |
| ~~D13~~ 每站整併完 context checkpoint | **使用者 2026-09-07 裁定不做**：跨站去重依賴前站記憶；等 T12／A31 砍呼叫後再看 |
| ~~R1／E1 gap-check~~ RT Load More 寫死配方、AP 分頁窗口 gap-check | **使用者 2026-09-07 裁定結案不做**：連兩輪無迴圈，屬偶發漂移 |
| ~~R7~~ src_text 混入中文說明 9 筆清洗（RT1395/3501/3613/3657/4161/4631/4641/4820＋截斷標記） | **使用者 2026-08-18 裁定直接刪除**，原列已從 R 表移除。理由：要回站方重抓原文成本高、素材已老化，只影響事後離線查證依據。防呆已上線（6ecfc17），不會再新增同類。後人不要再立案 |
| ~~R11~~ 0812-2200 的 65 則＋0813-1200 的 80 則缺 src_text | **使用者 2026-08-18 裁定直接刪除**，原列已從 R 表移除。根因早已破案（agent 組批漏欄位，非工具 bug）、防呆已上線（6ecfc17），回補部分先前已由 D8 裁定不補；本列已無殘留待辦。後人不要再立案 |

## ⛔ 不可 merge 的殘留分支（2026-08-18 全分支盤點，使用者要求）

**內容都已經用別的路徑進了 main，分支本身是殘骸；merge 回去不是補齊而是倒退。**
後人看到「有分支沒 merge」不要好心合併，先讀這張表。

| 分支 | 為什麼不可 merge |
|---|---|
| `agent/nhk-side-recording-6digit-tc`（2026-08-02） | P-045 早已在 main，**且已被 P-047 修正／部分作廢**（`00-寫稿通則.md:105,107`、`06-auto-script-sot.md:95` 都明寫「修正 P-045」）。merge 會把作廢的舊規則帶回來 |
| `agent/verified-update-without-footage`（2026-08-02） | P-044 的規則文字早已在 main（`06-auto-script-sot.md:44`）。但這支還帶著 **262 行版的 `scripts/s2_state.py`，main 現在是 1680 行**——merge＝砍掉 1400 行、把狀態檔工具打回 8/02。⚠️ 另有 1 個**從未推上遠端的本機 commit**（`51c8782`），只存在當時那台機器 |
| `agent/rules-v2-sandbox`（2026-07-21，7 commits） | 廢棄的規則去重實驗。main **沒有** `sandbox/`，merge 會憑空加回 20 個檔的舊沙盒（內容是 7/21 版規則，早被推翻） |

📌 同次盤點的其餘結論：**main 本身乾淨**（與 origin/main 零落差；追蹤內容只有 4 個根檔＋目錄，repo 根那堆 `川想擴戰2200 #NN`／`_ns_list_*.txt`／`NE-022SA *` 散落檔**全被 gitignore 擋住**，沒有一個進版控）。`docs/tai-character-convention` 是唯一真的有東西沒進 main 的——臺／台完整規範（對照表＋專有名詞例外＋不溯及既往）當時只活在該分支，main 上僅 `17-網址素材整併.md` 一行摘要，而 17 是講網址素材整併的檔、agent 讀 `00-寫稿通則.md` 根本看不到，**規則與實作脫節了 9 天**；已於本次 cherry-pick 進 main（`fcae283`）並把 17 那行改為指回 00＝唯一來源。另 `docs/learning-0731-俄炸朝彈2200`／`docs/x-material-detection-fix` 遠端已刪、內容全數已合併（0 個未合併 commit），本次一併刪除本機空殼。

⚠️ **流程風險（同日觀察，寫給所有 agent）**：2026-08-18 有多個 session 同時在這個 repo 工作並**反覆切換 HEAD 分支**，一天內至少被切走 3 次。已造成一次實害——main 的 S2 改進 commit 被誤推到 `feat/human-dub-digest` 上（`git push origin main` 推的是沒動過的本機 main＝空推，看起來成功其實什麼都沒推），事後才靠 `git status --short --branch` 發現並 cherry-pick 回 main。**鐵律：每次要寫 repo 前先 `git status --short --branch` 確認 HEAD，不要相信上一次的分支狀態**（全域 CLAUDE.md 早有這條，該次就是沒照做）。

## 實作共同護欄（摘自全流§九，全項目適用）

一次只上一項；歷史 replay→shadow→TestMode→1 正式輪→3 連續正式輪；比 normalized
state mutation 不比文字回覆；不覆寫舊 snapshot；錯誤帶原始訊息不回單一 boolean；
欄位全空必須明確失敗；新路徑跑完一個完整班次＋人工確認前，不刪現行規則與腳本。

---

## 附錄：來源文件 → MASTER 對照（防漏收查核表）

> 下表的「MASTER」欄仍以 ID 代碼表示；每個 ID 現在住在哪個詳情檔，看 ID 開頭字母對應
> `master-detail/{E,R,T,A,D}.md`（例：A2 → `master-detail/A.md`），或直接查上面的索引表。

| 全流程分析 | MASTER | | 省T計畫 | MASTER |
|---|---|---|---|---|
| §4.1 unwrap | A2 | | Task 2 A0-A3 | T1 |
| §4.2 inspect/search/snapshot/compare | A2 | | Task 3 prompt 衝突 | T3 |
| §4.3 show 篩選＋resume --json | A3 | | Task 4 規則分片 | T4 |
| §4.4 排除 Task 工具（A4 實驗） | T2 | | ② batch_prep 路線 | D2→A2 |
| §4.5 遙測細分＋版本 hash | A1 | | Task 5 | 併A2 |
| §4.6 round gate | A3 | | Task 6/7/9、Haiku、caching | ❌不做 |
| §4.7 watchdog 中途死亡 | A5＋D5 | | Task 8 effort | D1 |
| §4.8 schedule check | A4 | | 複核 P0-1~P0-6/P1 | R2~R5/R8~R10 |
| Phase 5 快速路徑/S2b/collector | D4/D6/❌ | | 交接§6-1 RT/§6-2 推播/§6-3 漏收 | R1/歸檔/歸檔 |
| §九 護欄 | 護欄段 | | 交接§6-4 稽核/§6-5 P0 | R3/R2~R10 |
| 全檢 §2b①（五層合一） | A26 刀4 擴充 | | 全檢 §2b②③④ | T13／T8／A3 |
| 全檢 §5（入庫砍呼叫） | T12／A24／T9／A31 | | 全檢 §6（TAG 三層） | A31／A10 P1／D14 |
| 全檢 §7／§8（散落／巨格） | D14→A10 P1／A32 | | 全檢 §9（T/C 拆分） | D15 |
| 全檢 §10／§11（稿單／CTV） | A33／D16／A34 | | 全檢 §3／§4 其餘 | A35／R21／A16／D17 |

## ✅ 已完成歸檔

已搬遷至 [master-detail/archive.md](master-detail/archive.md)（內容逐字保留，未精簡）。
