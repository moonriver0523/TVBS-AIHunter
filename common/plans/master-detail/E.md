# S2 MASTER 詳情檔 — E類（等證據）

> 本檔為 [S2-MASTER-追蹤清單.md](../S2-MASTER-追蹤清單.md) 的詳情內容，內容自原表逐字搬遷，未經摘要或精簡。
> 進度紀錄規則同 MASTER：**只加不改**——更正請追加新行，不要覆寫舊敘事。

## E — 等證據（0812-2200 輪與後續輪次）

| ID | 項目 | 來源 | 狀態 | 要看什麼／驗收 |
|----|------|------|------|----------------|
| E1 | RT Load More 迴圈判定：頁面行為改變 vs 策略漂移 | 交接§6-1 | ⏸半結 | **0812-2200 實測：迴圈消失**（捲動探測 145→11、RT 階段 7.2分/28次，回到正常水位）。傾向策略漂移或短暫頁面狀態，非持續性頁面改版；⚠️ 與 effort medium 同輪上線，歸因不純。再觀察 0030 輪無迴圈即結案；R1 是否仍要把配方寫死當預防，交使用者裁決 → **0813-0100 續驗：RT 僅 13 次/4.7分、scroll 關鍵字 13 次（2000 輪=145），連兩輪無迴圈，✅結案＝策略漂移偶發**。R1 預防性寫死留使用者裁決 → **0820 新增樣本：AP 站首次出現同族「清單漏抓」，非 RT Load More 迴圈本身，但同屬「即時 feed 分頁窗口漏抓一則」的家族**。使用者體檢時發現 0819-0430 輪 AP 收錄清單裡 `AP4679397` 與 `AP4679412` 間跳號，缺了 `AP4679409`；用 Playwright 直接打 AP 官方 API 查證：①`AP4679409` 是真實可用素材（中非共和國礦坑倒塌 UGC 影片，`pubstatus:"usable"`、有完整影片檔）②`transmissionsources` 含 `mimir`、不含 `restoredfromarchive`，完全符合 Latest 清單自己的收錄查詢條件 `type:video AND transmissionsources:mimir AND NOT transmissionsources:restoredfromarchive`，`signals` 也有 `NewsroomReady`——**排除「來源被規則刻意過濾」的可能，純粹是掃帶時清單分頁沒抓到，資料本身一直都在**。**根因假說**：AP Latest 清單是即時新增的 live feed，按 `arrivaldatetime desc` 排序分頁；掃帶輪若用固定 PageSize 分頁、輪詢間隔內又有新素材持續湧入，舊分頁的邊界會被新項目推移，個別項目可能剛好落在兩次分頁之間被跳過——跟 RT Load More 的迴圈成因不同（那是頁面互動行為漂移），但**症狀同屬「live-feed 分頁窗口漏抓」**，值得放在同一個觀察家族。**要看什麼**：目前僅 1 個 AP 樣本、且是使用者主動發現才回頭查證，並非常態巡檢抓到的；累積更多樣本前不動工，但已研擬防堵辦法見下方「候選防堵方案」 → **0820 重大訂正（使用者發現，推翻上面「站方 Page2 bug」的長年判斷）**：舊 recipe（13b/13c/13d 一路沿用）掃的是 `newsroom.ap.org/home` 首頁的 Latest 小工具，**不是真正完整的清單頁**；真正的完整清單在 `newsroom.ap.org/topic?id=116e9ab7aa044476925398d731289267&mediaType=video&navsource=latest&parentlnk=false`（AP「All Latest」topic 頁）。用 Playwright 直接開這個網址、照抄它實際發出的 request body（多了 `Date:"Anytime"`／`digitizationType:"Digitized_NonDigitized"`／`MyPlanSearch:true`／`IsSavedSearch:false` 等舊 recipe 沒有的欄位，`PageSize` 用 `50`）實測 `PageNumber 1→2→3` 三頁連續呼叫：`TotalRows` 穩定約 108260、**每頁與前一頁尾端銜接自然、無跳號、無跳回舊 archive 垃圾資料**，`AP4679409` 正確出現在 Page2、前後接 `4679410`／`4679407`，跟舊漏抓的位置完全吻合。連舊版 recipe 的原始（不完整）body 這次重打 `PageNumber=2` 也拿到正確資料，**懷疑 0803/0804/0814 三次「Page2 回垃圾」的原始判斷，很可能就是當時測試對象是 `/home` 首頁小工具（本身資料範圍/排序邏輯與完整 topic 頁不同），而不是站方真的有分頁 bug**。**這推翻了 13d §7「AP 對帳只信 Page1 的 16 則」規則的前提**，代表過去每輪 AP 只收 16-17 則、以及本輪查到的 `AP4679409` 漏抓，根因很可能是**掃描一直開錯頁面**，不是站方限制也不是無法解的偶發問題。**下一步**：13c/13d 的 AP 清單 recipe 需要改成打 `/topic?id=116e9ab7aa044476925398d731289267` 這個完整清單頁（或直接照抄其 request body），並用 `PageNumber` 正常翻頁；這是可修復、有明確解法的問題，建議另開 R 類項目追蹤實際修 recipe 的動作，此處 E1 僅記錄查證過程與證據 |

**E1 候選防堵方案（0820 研擬，尚未實作，待使用者裁決是否啟動）**：
1. **輕量差異掃描（優先推薦）**：每輪收工後，把當輪新收 AP `editorialid` 由小到大排序，抓「同前綴（如 4679xxx／5467xxx）內差值 >1 的跳號」當可疑缺口候選；不逐一查證，只留一份清單（如 `s2_state.py gap-check --station AP`），累積到一定量或每天巡一次時才真正花 Playwright 呼叫去補查——成本幾乎是 0（純本地數字比對），跟現有「判斷往上游搬、查證是下游補救才做」的省 Token 方向一致（13b §「最省的是根本不需要查證」同一邏輯）。
2. **確認可行的補查手法**：已實測 `POST /v1/nrsearch/search`（非 `/search/topic`，topic 模式會忽略 `SearchText`）可用 editorialid 數字字串直接關鍵字查到單則真實資料，補查缺口時直接用這條路徑，不需要用清單分頁重新爬。
3. **不建議**：不建議把 PageSize 加大或加分頁重疊硬吃掉這類漏抓——目前只有 1 個樣本，不確定發生頻率，加大分頁等於每輪固定多付 token 成本去防一個機率未知的問題，不符合「不做無法量出效益的優化」原則（同 E2 對 effort 的結論）。先用方案 1 的低成本掃描，看實際缺口出現頻率再決定要不要動這一步。
| E2 | effort=medium 實效驗證 | 093c713 | ✅ | **0812-2200(medium) vs 0811-2200(high) 同時段對照：111次/27.0分/29.4M/106.5k vs 117次/29.5分/29.5M/106.5k（收 65 vs 70 則）——各口徑幾乎相同**。與 TODO「量不出差別」結論一致。數據已備，交 D1 裁決 |
| E3 | 鎖檔收工自動清理實戰驗證 | c9614ac | ✅ | 2200 輪 DONE 後鎖檔已不存在，收工推播同輪首次實戰成功（curl 版），移歸檔 |
| E4 | ①b 稽核前置驗收：update-entry 次數 7→1~3 | TODO | 🔶 | 2200 輪 update-entry=**4**（目標 1~3，接近未達）；set-category=8（①目標 1，未達，65 則大輪情有可原）。再看一兩輪趨勢；`s2_state:?`=6 待 A1 認齊後才能斷言。**0813-0100：update-entry=5（仍超標）、set-category=5（收 71 則，較 2200 的 8 略改善）**，①/①b 規則實質未被遵守，繼續觀察。0100 分類健檢發現規則與「邊掃邊分類」流程打架，驗收口徑擬改（見 T7）。**0813-0430/0730 續觀：set-category=3（收158則，佳）/6；update-entry=8/3。0730 收尾又出現 5 次 show 試探＋needs-review 3 次＋ls _待整併 2 次的固定尾巴（T5/T6 同款）** |
| E5 | AP detail 擷取偶發 selector 失效，靠 agent 事後自癒重跑 | 0819-2000 輪體檢 | ⬜列入觀察 | **0819-2000 首次樣本**：20:05 第一次 `browser_evaluate` 抓 AP 詳情頁（`ap_detail_2000.json`）16 筆全部壞資料——`id:"APundefined"`、`cap/script/src` 全空、`sb_count:0`；3 分鐘後 agent 自行判斷不對並重跑，20:08 寫出 `ap_detail_2000b.json`（76KB，內容正常，如 `AP4679330 Top diplomats...`, `sb_count:2`, `has_sot:true`）。根因判斷為 selector/欄位對應一次性失效，非網路問題（同輪其餘 AP 呼叫正常）。**代價**：AP 階段呼叫數 25→50（較 1800 輪翻倍）、耗時 4.1→8.2 分，連帶 `s2_batch_prep.py` +12、`s2_state show` +14（疑為重新核對而多跑），整輪多花約 2 萬 output token。**結果**：最終狀態檔乾淨，AP 本輪新增 15 則、無 `parse_ok=false`、無 `APundefined` 殘留，離開碼=0，`_跳過紀錄.txt` 無痕——屬「靜默自癒但有成本」，不會進正式輪次異常紀錄，只能靠事後翻中間檔案才查得到。**要看什麼**：累積到第 2、3 次真實樣本後，看是否為偶發還是規律性（例如固定在某個時段/某類 AP 條目觸發）；若規律性成立，再議是否加擷取結果檢查（抓到 id 含 `undefined` 或欄位全空時直接觸發重試，不等 agent 自行發現）。目前只有 1 個樣本，不立案動工 |
| E6 | 輪次局部效率下降非則數所致，疑似偶發性行為/效率漂移（RT 反覆縮字為具體表現） | 0819-2200 輪體檢＋使用者追問覆核 | ⬜列入觀察 | **0819-2200 首次樣本**：全輪 41.5 分（今日最長），最初體檢誤判為「167 則新增量帶動負載」；使用者追問「三站則數沒明顯增加，是不是模型偶發性降智」後重查 `_token_metrics.jsonl` 真實數據，**證實三站則數本身沒有增加**（RT 本輪僅 24 則，反而是四輪最少；1600=27、1800=33、2000=27）。但 **RT 呼叫數 47 次／耗時 12.0 分為四輪最高**（1600=25次/7.6分、1800=19次/5.3分、2000=31次/7.4分），每則平均要 2 次呼叫、30 秒，是 1600 的兩倍、1800 的三倍以上；**分類階段 49 次呼叫的每次平均耗時 13.5 秒**，遠高於 1600 的 5.7 秒、2000 的 8.5 秒——同量級呼叫但單次「動作變慢」。**具體行為證據**：RT3623（Meta 訴訟稿）在 `rt_fix_2200`→`rt_fix2_2200`→`rt_fix3_2200` 被連續改 3 次，NS 的 RQ-502WE 在 `ns_fix_2200`→`ns_fix2_2200` 被連續改 2 次，每次都只刪幾個字縮短長度，像是逼近截斷上限才逐步手動裁切，而非一次算好長度寫入——屬於「同一件事重複做」的行為層面低效率，不是資料量撐大的。**判斷**：量不是主因，證據較支持「這輪局部出現偶發性效率下降/行為異常」；是否等同「模型降智」尚無法斷言（未做同條件 A/B 對照），保守定性為與 D9(python -c 行為漂移)、E5(AP 擷取偶發失效) 同一類「偶發性行為漂移」家族，這次表現在 RT/NS 反覆編輯上。**要看什麼**：累積更多樣本判斷是否規律性復發（例如是否與夜間/特定時段、特定 effort 設定相關）；若規律性成立，可考慮寫入前先做長度預檢以避免反覆裁切，但目前僅 1 個樣本，不立案動工 → **第 2、3 樣本（0819-0430、0820-0730 體檢）：NS 站單次呼叫耗時異常**。以歷史基準（多數輪次 NS 平均 5.5～14.9 秒/次）對照，0819-0430 為 29.0 秒/次（29 次/14.0 分）、0820-0730 為 26.1 秒/次（26 次/11.3 分），皆逼近或超過基準兩倍，而 0820-0430 本身也略高（13.6 秒/次）。兩次異常樣本的中間檔案（scratch）皆已被輪次收工自動清理，無法像首樣本一樣抓到逐字裁切的直接證據，僅能以 `_token_metrics.jsonl` 的耗時數字佐證，且兩次都落在 NS 站（非首樣本的 RT/NS 併發）。**要看什麼（更新）**：三個樣本合計對照，0430/夜間相鄰時段出現頻率略高，但尚不足以判定與時段/effort 掛鉤；若第 4 樣本仍集中在 NS 站，可考慮針對 NS 擷取／裁切邏輯加驗證，目前仍維持觀察、不立案動工 |
| E7 | AP 站 0830-1600 登入態掉線事故，事後即時重抓補齊 | 使用者下令補救；本次 agent 執行紀錄（2026-08-30） | ✅ | **事故現象**：0830-1600 該輪 RT／NS 兩站正常完成並留下四份快照＋對帳檔，AP 站僅留 `ap_check.md`——內容是 AP Newsroom 未登入首頁快照（非搜尋結果），代表當輪 Playwright `.playwright-s2-profile-v4` 登入態掉線，完全沒抓到清單；狀態檔僅留 `AP-LOGIN-0830` 一則待人工殼記錄「navigate topic 頁後被導回未登入版、API fetch TypeError」。**補救過程**：①使用者手動在 v4 profile 重新登入 AP Newsroom，agent 用 `browser_navigate` 到 `newsroom.ap.org` 確認直接進 Latest 頁（非登入頁）驗證登入態恢復。②依 13c§2「AP 清單查詢：照抄即用範本（0821 新版）」與 13d§7（R6 0820 訂正），只開 `https://newsroom.ap.org/topic?id=116e9ab7aa044476925398d731289267&mediaType=video&navsource=latest&parentlnk=false`（All Latest topic 頁，非 `/home` 首頁），`POST /v1/nrsearch/search/topic` 打 `PageNumber:1, PageSize:50`（唯一可信主要來源，未動用 Page2+ 候選補充池，避免 13d§7 訂正過的空窗/重疊風險），`browser_evaluate` 一次呼叫、頁面內瘦身後落檔 `ap_list_1600.json`（25,415 bytes，落在 20~30KB 自檢區間，50 則）。③以狀態檔 `_top.window_start`（2026-08-30 13:00 台北）與 `checkpoint`（0830-1600）換算 UTC 05:00–08:00 為窗口，從 50 則中篩出 `ts` 落窗的 20 則；依 13e SNTV 判定與重複規則核對三則利物浦戰報（`AP5468644`／`AP5468645`／`AP5468646` 中的 `AP5468644`／`AP5468645`）與熊貓賽（`AP5468647`）：`AP5468647`（標題含 Vertical，直式版本）與 `AP5468645`（`sntvEditPlay` 版，與 `AP5468644` 的 `SNTVPLAY` 版本 script 逐字相同、同一 Iraola 賽後受訪引言）依規排除，只收 `AP5468644`，窗內實收 18 則。④用 `item/details` API `Promise.all` 一次呼叫補全文，自我檢查無 `APAP` 雙重前綴、無空 `script`、無殼欄位洩漏，落 `ap_detail_1600.json`（20 則含排除的 2 則，供對帳核對用）。⑤18 則寫 `ap_batch_1600.json`（含 `src_text`／`sb_count`／依 13e 格式的三段式 `entry`，BITE 判準以 `sb_count>0` 才標 `(BITE)`、`AP Live Choice` 直播回放一律不標 BITE）並 `add-batch` 入庫、`set-category`／`set-tc`（含機動 T「尼泊爾洪災」加掛，`體育`不佔機動 T 名額）。`AP4681246`／`AP4681224`（熊貓圓圓 22 歲慶生）依「涉臺一律至少 🟡」規則標記；中主題原命名「台灣熊貓慶生」觸發 `s2_state.misplaced_topics()` 的地緣詞（`台灣`→建議歸政治）誤判，改名「圓圓22歲慶生」化解，不影響 TC 標的 `臺灣` 地緣格。⑥落 `_audit_ap_1600.txt`（`CODE|ISO時間` 每行一筆，格式比照當輪 `_audit_ns_1600.txt`），跑 `python scripts/s2_audit.py --mmdd 0830 --rt-list _rt_list_1600.json --ap-list _audit_ap_1600.txt --ns-list _audit_ns_1600.txt`：**RT／NS／AP 三站窗內皆零漏收**，reconcile_log 補齊 0830-1600 三站紀錄；`s2_render.py` 重渲染後品質掃 0 命中、主題重複 0 命中、T/C 覆蓋率 100%。**驗收後仍剩 2 個 🔴**（判定為非問題，未動手改）：一是 AP 窗內「漏收」`AP5468647`／`AP5468645`——即上述③主動排除的 SNTV 直式／重複版本，13e 明文「純粹略過，不是待人工」不必寫 needs-review，`s2_audit.py` 的清單對帳沒有「已知排除」白名單機制才會照樣紅燈，非真漏收；二是 RT 跨日撞號 3 則（`RT7607`／`RT7609`／`RT7651`）——屬 RT 站既有資料、與本次 AP 事故無關，本次未動 RT/NS 任何資料，留給例行輪次判斷是否要 `needs-review`。`AP-LOGIN-0830` 待人工殼已 `needs-review done` 結案（純殼、`done` 後整筆自動移除，不留空紀錄）。**要看什麼**：若之後 AP 登入態掉線再發生，可直接複用本則流程（重登入→topic 頁 Page1 PageSize:50→窗口過濾→SNTV 去重→add-batch/set-category/set-tc→`s2_audit.py` 三站齊對）；`s2_audit.py` 對「已知排除」清單對帳恆常紅燈是否要加白名單機制，可另立 R 類項目評估 |
| E8 | S2 改接 Gemini(claudeg)／Codex(claudex) 代理首次實測 | 使用者 2026-09-08 下令；完整過程見 [跨模型試跑紀錄](../2026-09-08-跨模型(Gemini-Codex)試跑紀錄.md) | ⬜列入觀察 | **做法**：`s2_scan.ps1` 最終呼叫的 `claude @claudeArgs` 包一層對應代理（`claudeg`／`claudex`）的 `ANTHROPIC_*` 環境變數，`--model` 換成代理模型名，其餘（`s2_mcp.json`／`--add-dir`／guard settings／V8 prompt）完全比照正式排程參數，不改任何 S2 腳本或規則本身。每次試跑前備份 `0907-s2-state.json`。**claudeg（Gemini）兩次皆未跑通**：①`gemini-pro` 69 秒即 `429 RESOURCE_EXHAUSTED reason=quota`（Antigravity 帳號憑證冷卻），完全沒進 S2 流程、狀態檔未被觸碰。②使用者指定改用 `gemini-3.8-flash-high[1m]`，`cliproxyapi-gemini\config.yaml` 加入第三條 alias（`name`／`alias` 皆為該字串，直接透傳）＋`claudeg.ps1` 預設值同步改，重啟代理生效後，`server.log` 證實請求確有送達 upstream（`auth_file=antigravity-moonriver0523@gmail.com.json`，proxy 內部把 `[1m]` 後綴剝除、實際送出模型名 `gemini-3.8-flash-high`），但持續收到 `429 quota`；使用者確認整體訂閱配額是滿的，判斷是該模型 ID 本身在 Antigravity 端異常/獨立限流，非帳號用量問題，待核對正確 ID 或狀態改變後重試。兩檔改動皆有 `.bak-*` 備份。**claudex（Codex）第一次跑滿但 0 則寫入**：`checkpoint=0907-1200-claudex試跑`（沿用中文後綴命名），連線／瀏覽／NS→AP→RT 分站判斷全部正常，跑 22.2 分、離開碼=0，但 `add-batch` 因 `s2_state.py` 的 `CHECKPOINT_RE=r"^\d{4}-\d{4}$"`（嚴格 8 位純數字，不接受任何文字後綴）擋下，agent 只留一筆 needs-review 說明無法寫入，實收 0 則——**這是試跑操作者 checkpoint 命名格式的錯，與 Codex 模型能力無關**，狀態檔未被寫壞。順手用 `Get-ScheduledTask -TaskName 'S2掃帶'` 查證當時真實生效排程點（`04:30／07:00／11:00／17:00／20:00／22:00／01:00`），確認 **11:00–17:00 本來就是排程空窗**，兩次試跑皆未搶占或拖累任何正式輪次；`0907-s2-state.json` 在當天 17:00 建檔輪前都是唯一 live state，checkpoint 的 MMDD 標記幾號不影響寫入哪份實體檔。**claudex 第二次改用合規純數字 checkpoint `0908-1200`（順著當時頂層 `0908-1100` 推進）跑通**：離開碼=0、耗時 27.3 分、新增 39 則、`txt=True`、無任何異常標記，`0907晚班交接.txt` 15:10 重渲染（284KB，較試跑前 269KB 符合新增量）。**要看什麼**：①這 39 則尚未與同時段 sonnet 產出做人工品質比對（用字／分類準確度／TC／BITE 判斷），沒有這份比對前只能說「流程通」，不能判斷「品質夠不夠換線」；②Gemini 側等使用者核對模型 ID 或配額狀態改變後才有第二個樣本；③目前僅各 1（Gemini：0通過）／2（Codex：1失敗+1成功）個樣本，不立案動工、不建議據此決定是否真的換線 |

