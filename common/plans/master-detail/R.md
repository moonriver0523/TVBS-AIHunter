# S2 MASTER 詳情檔 — R類（修復）

> 本檔為 [S2-MASTER-追蹤清單.md](../S2-MASTER-追蹤清單.md) 的詳情內容，內容自原表逐字搬遷，未經摘要或精簡。
> 進度紀錄規則同 MASTER：**只加不改**——更正請追加新行，不要覆寫舊敘事。

## R — 修復（品質與可靠性）

| ID | 項目 | 來源 | 優先 | 狀態 | 說明／驗收 |
|----|------|------|-----:|------|------------|
| R1 | RT Load More 迴圈修法：0811「一次抓完」evaluate 配方寫死進 13c §1b＋⛔禁迴圈 | 交接§6-1 | 1 | ⏸等E1 | 每輪省 ~38 次呼叫/6 分。⚠️ 禁令下不准附範本（交接§11-3 教訓） |
| R2 | RT 對帳只用 Edit No 判歷史已收，跨日撞號→靜默漏收（P0-1） | 複核 | 2 | ✅281d380（0813-1600 首次真實命中 RT4607） | 唯一真資料風險（RT3609 實例）。改 Edit No＋日期複合判斷。0812 未踩中但每天在骰 |
| R3 | audit finding fingerprint 去重／紅燈冪等（P0-4） | 複核＋交接§6-4 | 3 | ✅281d380 | PO-11~14WE 已登記 needs-review 仍每輪紅燈重查，1600 輪燒 7.7分/51次。修好順帶消掉目前唯一殘留紅燈 |
| R4 | truncate() 加標記後總長 4014 > 4000（P0-2） | 複核 | 4 | ✅281d380 | 17:05 只改了標記字元，長度溢出沒修→已修：標記算進 limit |
| R5 | audit △ 判準尊重合法 set-mark override（P0-3） | 複核 | 5 | ✅2026-08-13 | 17:05 只修了假警報那一半。修法：`mark` 欄位存在＝set-mark 留痕（s2_state 唯一寫入點已核），有 override 者不再報「判日失敗」；副本合成雙情境測試 PASS（無 override 照抓、有 override 不誤報） |
| R6 | AP 對帳清單抓到 page 2（≥32 則） | 接手查證 | 6 | ✅2026-08-13（13d §7）；**2026-08-14 訂正** | 現況 list=16 恰為 page 1 容量，被擠出 page 1 的素材對帳網接不到（AP5467302 實例）。掃描本身有翻頁（三輪 log 均有 PageNumber=2），弱的是對帳快照。修法走規則層：13d §7 原要求對帳快照 page1+page2 合併（≥32 則，當窗不足 17 則才允許單頁）。屬規則文字，待下輪實戰驗收快照筆數。**0814-1200 體檢訂正：PageNumber=2 回 100+（與 0100 的 117 同形），合併 page2 會把 116 寫進對帳分母。13d §7 已改為「只信 Page1＝16；禁止打 Page2；被擠出的用 term／DOM 定向補，不准 116 假綠燈」**。**0814-0100 實戰：agent 改走 API 一次撈 117 則（超額達標），但落地格式變 tab＋ISO UTC，audit parser 只認 `\|` 分隔→整行當 code、時間空白→117 則全進「窗外」假綠燈（已收 0 竟無紅燈）。main 當場修 parse_list_file（tab 分隔／total 表頭跳過／ISO UTC→本地時區），重跑：AP 已收 58＋前幾天 33＋**窗內未收 23**——全窗對帳首戰就抓到被 page1 快照時代遮住的累積漏收。**後續（0814 上午）：23 之中 1 則是稽核不看日期的誤報（已修 73e172e，窗判斷帶日期）；真漏收 22 則，使用者裁定回補 9 則硬新聞（含哥倫比亞強震搜救）、13 則運動/軟性放掉。sonnet 子代理已回補完成並經 main 獨立驗收：9 則全入庫帶 src_text、分類歸位（哥倫比亞強震那則進機動格）、render 444 則（+9）品質掃 0 命中。⚠️ 流程教訓：main 把「待 0430 回補」寫 MASTER 但排程 agent 不讀 MASTER→訊息斷鏈兩輪，跨輪交辦要走狀態檔（needs-review/alert），不是帳本** → **0820 重大訂正（使用者發現，推翻 0814 的「PageNumber≥2＝站方 bug」判斷）**：0814 判定 Page2 回垃圾，是因為當時（含更早的 0803/0804 原始調查）測試對象是 `newsroom.ap.org/home` 首頁的 Latest 小工具，**不是真正的完整清單頁**。使用者指出真正的完整清單在 `newsroom.ap.org/topic?id=116e9ab7aa044476925398d731289267&mediaType=video&navsource=latest&parentlnk=false`（AP 官方「All Latest」topic 頁）。實測：照抄該頁面實際發出的 request body（`PageSize:50`，含 `Date:"Anytime"`／`digitizationType:"Digitized_NonDigitized"`／`MyPlanSearch:true`／`IsSavedSearch:false` 等舊 recipe 沒有的欄位）連續打 `PageNumber 1→2→3`，`TotalRows` 穩定約 108260、逐頁銜接自然無跳號、無跳回舊 archive 垃圾；連舊 recipe 的簡化 body 這次重打 `PageNumber=2` 也拿到正確資料（`AP4679409` 正確落在 Page2，前後接 `4679410`／`4679407`，跟 E1 記錄的漏抓位置吻合）。**結論：AP 每輪只收 16-17 則、E1 記錄的漏抓，根因很可能是掃描一路開錯頁面（home 小工具而非完整 topic 頁），不是站方限制、也不是不可解的偶發問題**。**下一步（待使用者裁決是否啟動）**：13c/13d 的 AP 清單 recipe 需要改成打 `/topic?id=116e9ab7aa044476925398d731289267` 這個完整清單頁（或照抄其 request body）、用 `PageNumber` 正常翻頁抓多則，13d §7「只信 Page1 的 16 則」規則需要連帶重寫；動規則前建議再多測 1-2 輪（不同時段）交叉驗證 Page2/3 的穩定性，避免單次重測就推翻三次歷史驗證的結論 → **0820 已改13c§2/§0＋13d§7並commit(e45b02e)，下一輪(1600)起生效** → **0820 使用者要求開新網址模擬掃帶再驗一次，發現Page2翻頁沒有一開始想的乾淨，訂正上一版過於樂觀的結論**：實測不論 Page1 的 `PageSize` 是 16／50／100，`PageNumber:2` 一律固定回 100 則、對齊伺服器內部固定 offset（不是接續 Page1）。結果視 `PageSize` 不同分裂成兩種風險：`PageSize`＝16 時 Page1尾／Page2頭時間差20分鐘、50時31分鐘（兩頁之間有素材完全沒被涵蓋到的空窗）；`PageSize`＝100 時 Page1/Page2 反而重疊50則（同一則算兩次）。加上清單本身是即時 live feed，兩次分開呼叫之間還會插入新素材，offset 對齊只會更不穩。**訂正結論**：「改打完整 topic 頁」這件事本身正確、已解決 Page1 只有16-17則的根因；但「PageNumber 翻頁可以直接信任」這個附帶結論收回——**Page1(PageSize:50) 當唯一可信主要來源，Page2+ 只能當候選補充池，須去重＋逐一核實或 needs-review，不能直接當確定則寫入**。已同步訂正 13c§2 與 13d§7（見文件最新版），這次教訓：連續三頁一次性測試看起來乾淨，不代表換個 PageSize 組合／換個時間點重測會一樣乾淨，Page2+ 的不穩定是即時 feed 分頁的結構性風險，不是能一次驗完就結案的問題 → **0820 使用者提議「像 RT 一樣先捲動到底把整頁載完再抓 API」，實測排除此路線**：①先確認這個 topic 頁**不是無限捲動頁**——`window.scrollTo`/`scrollBy` 連續 15 次（含逐格捲動共 12 秒）都沒有觸發任何新的 `search/topic` 呼叫，頁面底部其實是傳統數字分頁按鈕（「第 1 頁，共 1084 頁」）＋「Navigate to next page」，不是 RT 那種 IntersectionObserver 觸發式載入。②改直接點擊真正的「下一頁」按鈕，抓到 AP 官網自己的真實 request：**Page1 用 `PageSize:50`，Page2 按鈕點下去後自動變成 `PageSize:100`**（不是我們手動指定錯的）。用這組「官方原版」Page1(50)+Page2(100) 組合重測，**仍量到 10 分鐘空窗、零重疊**，跟先前 16/50/100 各種組合測到的結果同一形狀（都有落差、從不重疊）。**結論**：問題不在抓取方式（滾動 vs 按鈕 vs 純 API 都是同一條底層 `search/topic` API），而是 AP 官網本身的分頁機制本來就有這個結構性缺口——很可能是 Page1 固定給最新 50 則、但 Page2 並非接續「第 51 則」起算，而是用另一組固定的內部 offset（疑似對應 `PageSize=100` 的計算基準），導致「排名 51-100」這個區間不管用什麼方法都拿不到；這是網站本身用滑鼠正常瀏覽也會踩到的空窗，只是一般人手動看新聞不會發現、也不會在意。**「滾動載入可解決」這個假設已排除，不用再試**；R6 維持 Page1(50) 唯一可信＋Page2+ 候選補充池需 term 核實的最終結論不變 |
| R8 | audit 其餘輸出補完：各清單 shown/total/more（P0-5）＋ --json-report（P0-6) | 複核 | 8 | ✅P0-5（2026-08-13） | §4 已做一處（384d9de）。P0-5 已補齊：more_note() helper 套到 Ⓐ/②/③/④/⑥/⑦ 全部截斷點（生產稽核實測「還有 14 則未列出」8+14=22 對得上）。--json-report（P0-6）仍 ⬜ 留案不動 |
| R9 | update-entry 同步 src_text／footage_type（P1） | 複核 | 9 | ✅6ecfc17；**0818-1000 第三次真實復發** | src_text 已補（--src-text/--src-text-file/批次每筆可帶；{id,src_text} 純回補不動稿）；footage_type 原本就會傳入 apply_update。**0818-1000（使用者要求查浪費，main 查證）**：NS 整批 39 則再度整批漏帶 src_text（`add-batch` 印警告，確認防呆機制正常運作），跟 0812-2200／65 則、0813-1200／80 則同一種失誤第三次出現；AP／RT 兩站這輪正常，僅 NS 中招。防呆只做到「警告＋可回補」，沒有做到「組 batch 當下就擋下漏欄位」，所以每次復發都要付一次現場回補的時間成本——這輪回補（Write ns_fix_1000.json 重寫 27 則措辭＋Write ns_srctext_1000.json 補齊全 39 則 src_text＋兩次 update-entry）從 10:08:37 燒到 10:13:53，約 5.3 分鐘。⚠️ 續觀：若之後幾輪還在復發，代表「事後警告」不夠，該考慮把 src_text 缺欄位做成組 batch 前的硬性檢查（例如 `s2_batch_prep.py` 的 build/compare 流程擋下不完整的 batch，不是等 add-batch 才報） → **0821-0730 第四次真實復發（使用者查成本異常，main 查證）**：這輪 NS 整批 35 則又漏帶 src_text（`ns_batch_0730.json` 35 則欄位為 id/source/checkpoint/status/footage_type/entry，唯獨沒有 src_text），事後另開 `ns_fix_0730.json`（35 則，僅 {id,src_text}）整批回補；AP／RT 這輪正常（`ap_rt_fix_0730.json` 20 則是內容措辭訂正，非缺欄位）。檔案時間戳 `ns_batch_0730.json` 07:37:32 → `ns_fix_0730.json` 07:41:42，回補這段約 4.2 分鐘。這輪整體耗時 29.2 分、output_tokens 131,168（每則 1,822 tokens），明顯高於前後鄰輪（0821-0100 每則 1,242、0821-0430 每則 1,445），成本落差幾乎全部可歸因於這次 NS 整批漏帶 src_text 的回補。**四次全部發生在 NS 站，AP／RT 未曾中招**——值得懷疑是不是 NS 這條 pipeline（`dump`／`build` 兩步驟或人工累積 batch.json 時）結構性比另兩站更容易漏這個欄位，而非單純運氣。仍待裁決：是否該把 `s2_batch_prep.py compare --require src_text` 訂為 NS 站 add-batch 前的硬性檢查步驟（寫進 13c/13d），而不是繼續倚賴事後警告＋人工回補 |
| R10 | BITE 三態 CONFIRMED／NO_BITE／REVIEW_REQUIRED（P1） | 複核 | 10 | ⬜ | **2026-08-22 0430 體檢新證據（使用者要求查 0430/0730/1000 三輪疑似浪費）**：`ns_fix1_0430.json`（23 則）→`ns_fix2_0430.json`（其中 12 則重疊）出現本項機制未落地的具體代價——部分則第一輪先寫「BITE：（暫先標無 BITE，待回頭確認 BITE 內容）」這類明講不確定的暫定狀態，第二輪才回頭補上完整逐字引言或改判定；另有則是第一輪 BITE 引言本身不完整、第二輪才補全整句。這正是 R10 想用「CONFIRMED／NO_BITE／REVIEW_REQUIRED」三態機制取代的行為——目前沒有這個機制，agent 只能用中文備註手動模擬「REVIEW_REQUIRED」狀態，且要回頭補時得把整則 `entry` 重打一次（output token 重複付費）。這輪本可用三態機制第一輪就標記 `REVIEW_REQUIRED` 存底、待稿件/BITE 內容確認後再一次寫定，而不必寫兩次完整 entry。仍未動工，本次僅記錄新的實測代價證據，供將來評估優先度時參考 |
| R12 | truncate 3000 字截斷 SOUNDBITE 段→BITE 無法驗證 | RT9878 實例 | 4.5 | ✅281d380 | RT9878 sb_count 機械數到 9，但 truncate 只取前 3000 字未含逐字引言，agent 只能標無BITE 待人工。修法與 R4 同區：truncate 應保證 SOUNDBITE/SUPERS 段落優先保留，不是傻取前 N 字 |
| R14 | browser_evaluate 大回應落檔＝資料遺失：MCP 回 `[Evaluation result](./檔)` 連結但檔案從未寫出，agent 全機找檔 | 0813-2200健檢 | 2.5 | ✅2026-08-13（13d §6）；**0814-1000 首戰驗收過**：agent 遇到同款連結自述「per V4 §6 資料沒落地」直接分段重抓，前天 13 分鐘的坑這次 ~20 秒繞過 | 0813-2200 NS 站兩踩（ns_probe/ns_full_2200.json）：一次抓 60 則回應過大→MCP 聲稱落檔→實際連自家 output-dir（D:\Downloads\PlaywrightMCP）都沒有→agent 燒約 9＋4 分鐘搜檔（含 `find /` 全磁碟 120s timeout）。該輪 NS 15.8分/46次（正常約3分），整輪 33.7 分。agent 最後自己用分段回傳解掉＝正解。修法：13d 新章節硬規則（見連結視同資料遺失、禁找檔、禁 `find /`、立即分段重抓） |
| R13 | batch 檔寫錯位置→雙重搬運（浪費 ~3 分/輪） | 2200健檢 | 6.5 | ✅281d380 | 2200 輪把 rt/ap batch 先寫 repo 根目錄，再 Read 回來重 Write 到 scratch 目錄（卡點 91s+102s 就在這）。repo 根目錄已累積 9 個各輪殘留 json（0700/0100/1500/2000…）。修法：規則明示 batch 一律直接寫 `scratch-dir` 路徑＋清一次現存殘留。**0813-0100 再犯：ns/ap/rt_new_0813.json 又先落 repo 根（該輪三個 >60s 停頓 345s/136s/148s 全在這組檔的 Read 之後），事後有自清但雙重搬運照舊——已連兩輪，建議優先度上調**。1000 輪第三種變體：ns/ap/rt_list_1000.json 先落 repo 根、就地 python -c 檢查、再 `mv` 進 scratch（比 Read+Write 便宜但同病）。**0813-1200（13d §1 硬規則生效首輪）仍再犯**（detail 檔先落 repo 根再 mv）——規則文字管不住，剩下的硬解是 launcher 把工作目錄設成 scratch-dir（候補案，需裁決，見 D7） |


**R15（2026-08-23 開案）🔴 `common/13` 每輪只載入約一半，大分類清單從沒進過 context**

**發現經過**：A10 T/C 矩陣 v2 熱修計畫的獨立審查子代理提出，主線用獨立腳本掃 transcript 複驗，結論一致且範圍更廣。

**實測**（掃 `~\.claude\projects\G*Claude*2026082*\*.jsonl`，抓 Read 結果最後一個行號前綴）：

| 日期 | 輪次數 | `13` 讀到 | 結果字元數 | `13c` 讀到 |
|---|---|---|---|---|
| 0820 | 9 | 1~522／523 | 30,822／31,042 | 1~298／309（13,198／13,764 字元）|
| 0821 | 3+ | 1~531 | 30,472 | 1~318（14,287 字元）|
| 0823 | 4 | 1~531 | 30,472 | 1~318（14,287 字元）|

- `13` 全檔 **1052 行／53,676 字元** → **每一輪都只載入約 50%**，且**切在表格列中間**，三天內**沒有任何一輪補讀**。
- 截斷點的結果字元數穩定落在 **30,4xx~31,0xx**，強烈指向 **Read 工具結果有約 30,000 字元的上限**（不是 2000 行上限——522 行遠低於它）。
- **被切掉的 445~1052 行包含**：`13:481 ## 已定案的輸出格式`、`13:490 ## 庫存檔格式（三層標記）`、**`13:793 ## 大分類清單（使用者提供的樣板）`**、`13:937 ## 待補`、`13:950 ## 各站已知狀況`。
- ⚠️ **`13c` 也截**（827 行只讀 298~318 行），但結果只有 14k 字元、**未達 30k 上限**，屬**另一個成因**（可能 Read 帶了 limit、或另有補讀未被本次腳本比對到），需另查。

**為什麼危險**

1. **agent 現在是沒讀到大分類清單在做分類的。** A10 全部的分類品質問題（中主題放錯大分類、同事件散落）都要在這個前提下重新評估——可能有一部分根本不是判斷力問題，是規則沒進去。
2. **與 A11-1000 同形狀**：測試環境綠燈、生產環境沒載入，症狀是「功能上線了卻從沒生效」。本 repo 已為這種形狀付過學費（歷史列 2026-08-11、guard hook A11-1000）。
3. **會讓 A10 v2 整個空轉**：v2 計畫（評估書 §12.3）原訂「判準寫進 `common/13`」——附在檔尾＝永遠不載入；插在 522 行以前＝把目前剛好在邊界內的內容擠出去，是主動迴歸。

**建議修法（未裁決）**

- 短期：把「一定要讀到」的內容搬到 `13d`（317 行、實測全載）或獨立小檔，由 `s2_scan_prompt.md` 明列為必讀。
- 中期：`13` 拆檔（對應 T4「規則分片」已列但延後）。
- 驗收方式：**量「規則實際載入到第幾行」**，不要用「agent 有沒有照做」當訊號——後者無法區分「沒載入」與「不遵守」，而 T7／A23 的存在會讓人自動歸因後者。
- 複驗腳本形狀：掃 transcript jsonl → 找 Read 的 tool_result → 取結果文字最後一個 `\n<行號>\t` 前綴 → 比對原檔行數。

---

**R16（2026-08-23 開案）🟠 `.s2-scan.lock` 永久殘留，任何以「鎖檔存在」判斷的閘門都是誤報**

**證據**

- `s2_keepalive.ps1:22` 與 `s2_scan.ps1:86` **共用同一個** `%USERPROFILE%\.s2-scan.lock`。
- `s2_scan.ps1:578` 的 `finally` 有 `Remove-Item $LockFile`；**`s2_keepalive.ps1:229` 的 `finally` 只有 `$lock.Dispose()`，沒有刪檔**。
- 保活每 30 分鐘（:20／:50）跑一次 → 跑完鎖檔就一直躺著。

**實測**（2026-08-23 22:51）：

```
~/.s2-scan.lock            存在，0 bytes，mtime 22:50   ← 保活寫的
_輪次紀錄.txt 最後一行      22:21:37  0823-2200  DONE    ← 輪次 30 分鐘前結束
_NS保活.log 最後一行        22:50:01  OK  NS=OK(3587s)   ← 對上了
```

**影響**：**「鎖檔存在」與「有沒有輪次在跑」零相關。** 任何把它當閘門的程序（含 A10 v2 熱修計畫原本的 §12.4）都是永久誤報，操作者很快會學會忽略它——**比沒有閘門更糟**。

**真正的互斥機制**是 `s2_scan.ps1:302-308` 的 `FileShare::None` 獨佔握把：拿不到就 `Write-Skip` → `exit 0`，**沒有退避重試**（`s2_scan.ps1:144` 的「退避重試三次」註解講的是別的東西，不要誤讀）。

**建議修法**

1. `s2_keepalive.ps1` 的 `finally` 補 `Remove-Item`（獨立小修，低風險）。
2. 要判「現在有沒有輪次在跑」，改用 `s2_watchdog.ps1:220` 已有的 `Test-ScanRunning`（找 `-File …s2_scan.ps1` 行程），或看 `_輪次紀錄.txt` 最後一行是不是 `DONE`。
3. ⚠️ 不論用哪種，都只能回答「**現在**沒在跑」，回答不了「**接下來 3 分鐘**不會有一輪啟動」——改動窗口仍要挑輪次之間。

**R15 2026-08-24 上線與驗收（只加不改）**

**修法**：`13`（1052 行／53,676 字元）切成 `13`＋`13e`＋`13f`；`13c`（827 行／42,998 字元）切成 `13c`＋`13c2`。**內容逐字保留、未改寫任何一條規則**，附逐字節守恆證明（從切出的檔案還原原檔，SHA-256 與備份完全相同：13 = `8274b6cf0659…`、13c = `56e3b760bb58…`）。六份必讀檔估計載入量 11,894~24,801，全部低於 28,000 安全預算。四項配套：①每份檔尾 `RULES-EOF` 護欄線 ②`scripts/s2_rules_check.py` 硬檢查（尺寸／canary／23 條高風險規則，任一不過離開碼 1）③`s2_scan_prompt.md` 要求開工先跑檢查器並回報六個代號 ④一鍵回退 `scripts/s2_rules_rollback.ps1`（備份 `common/_rules_backup_20260824/`＋git tag `rules-pre-slim-20260824`）。commit `cabcc24`（初版兩部）→ `d1f8f88`（補強為三部＋13c 上下）。

**驗收方法**：⛔ **不可拿 `rule_shas` 當證據**——那是 `s2_token_metrics.py` 從檔案系統算雜湊，跟 agent 有沒有讀完全無關（0824 當天差點就這樣誤判）。正確作法是掃 transcript，把每個 `Read` 的 `tool_use` 與它自己的 `tool_result` 用 `tool_use_id` 配對（用「結果裡出現檔名」比對會被檢查器輸出交叉污染，實測會給出完全錯誤的行數）。

**驗收結果（0824-0430 `c42014f0`／0824-0730 `af839f6e`）**：

| 檔 | 讀到 | canary | 補讀次數 |
|---|---|---|---|
| 13 | 338/338 | ✅ | 0 |
| 13e | 464/464 | ✅ | 0 |
| 13f | 256/256 | ✅ | 0 |
| 13c | 422/422 | ✅ | 0 |
| 13c2 | 449/449 | ✅ | 0 |
| 13d | 326/323 | ✅ | 0 |

兩輪都跑了 `s2_rules_check.py`，並輸出 `規則載入：13 ✅／13e ✅／13f ✅／13c ✅／13c2 ✅／13d ✅`。**截斷問題已解決。**

**🔴 但「載入」不等於「遵守」——這是本次最重要的負面發現**：`s2_topic_review.py`（`13c2` §3b 明訂「render 前必做，每輪」）與 `s2_audit.py`（§2a-2「交班前必跑」）在 0430／0730 **仍是 0/2 輪**，與改動前的 0/25 輪完全一樣。這兩條規則現在確定在 context 裡了，agent 依舊沒執行。→ **規則載入是必要條件不是充分條件**；本項只結案「載入」那一半，「遵守」屬 T7／A23 同一家族（規則已改、行為沒跟上），建議另開一列追。對照組：同樣寫在規則裡的 `list-topics` 是 2/2 輪有跑，所以不是全面性不遵守。

**成本**：完整載入規則有代價。表面上 0824 兩輪總成本比同時段基準低 32%，但**那是則數少 40% 的假象**（62／40 則 vs 基準 96/105/96、70/72/73）。以**每則成本**正規化：改動前平均 $0.168 → 改動後 $0.195，**+16%**。這是合理的——以前「省」下來的錢，是因為有一半規則根本沒讀進去。

**R17（2026-08-24 開案）🔴 規則已完整載入，`s2_topic_review.py`／`s2_audit.py` 仍 0 輪執行**

**由 R15 驗收帶出**。R15 修完後，`13c2` 確定完整進了 context（0430／0730 兩輪 transcript 以 `tool_use_id` 配對驗證，449/449 行、canary 有、零補讀），但：

| 工具 | 規則明文 | R15 修復前 | 修復後 |
|---|---|---|---|
| `s2_topic_review.py` | `13c2` §3b「**render 前必做，每輪**」 | 0/25 輪 | **0/2 輪** |
| `s2_audit.py` | `13c2` §2a-2「**交班前必跑**」 | 0/25 輪 | **0/2 輪** |
| `list-topics`（對照組） | `13f` | 23/25 輪 | **2/2 輪** ✅ |

**為什麼重要**：這是「規則載入 ≠ 規則被遵守」的**直接實證**。R15 排除了「沒讀到」這個解釋之後，剩下的就是行為問題。屬 **T7／A23 家族**（T7：`set-category` 規則早改成一站一批，0821-1000 仍打 8 次；A23：執行層硬上限待做）。

**已查證的機制差異**：
- 會跑的那兩支（`s2_validate`、`s2_topic_dedupe`）都是被 `s2_render.py` **自動 import 呼叫**的，不靠 agent 記得。
- 不跑的這兩支**沒有任何自動呼叫者**——`s2_render.py:554` 只是 `print()` 出建議指令，不是執行。
- 對照組 `list-topics` 有跑，所以不是全面性不遵守。差別可能在**觸發時機是否具體**：`list-topics` 綁在「開新中主題前」這個明確動作上；`topic_review`／`audit` 是「每輪／交班前」這種沒有具體觸發點的常規動作。

**候選修法（未裁決）**：
1. **比照 `s2_topic_dedupe`，由 `s2_render.py` 自動 import 呼叫**——最省事，且與現行成功案例一致；⚠️ 要照既有慣例包在 try 裡（`s2_render.py:615`「⛔ 失敗絕不可以影響 txt」）。
2. 收工檢查加硬性閘門（沒跑過就紅燈）。
3. 維持自律，但在遙測記錄「這輪沒跑」，先累積樣本。

**對 A10 v2 的影響**：v2 的整條邏輯是「把 T/C 判準寫進規則檔 → agent 讀到 → agent 照著判」。**本項是該假設的直接反例**，所以 v2 的強制手段要從「規則文字」改成「`set-tc` 寫入端硬性拒絕」，驗收指標也要用行為（`set-tc` 有沒有被呼叫）而不是載入。詳見 [TC 矩陣評估 §14.2](../2026-08-23-S2-TC矩陣render前分類評估.md)。另一個設計啟示：**把 T/C 綁在 agent 已經在做的動作上**（`set-category` 同一批），會比另立「收工前記得跑」的步驟可靠。
