# 0821-1000 體檢：三刀建議（給接手 agent）

- **日期**：2026-08-21
- **觸發**：使用者問當天各輪卡住／浪費 → 點名 1000 有無修補 → 問怎麼修 → 「先 2」（查 D9 漏放）→ 下令把評估＋未做的 1／3 連同案例寫進紀錄，交其他 agent 處理
- **本檔角色**：MASTER A11-1000／A23／A24 的**來源規格**。動手前記本檔＋對應 MASTER 列，**不要再寫 13d 散文**。
- **本 session 已做**：刀 2 查證（transcript 重放）。刀 1／3 **未動工**。刀 2 的 launcher 修補 **未動工**。
- **⛔ 不要做**：把 `src_text` 再砍短（13b 要的是去 tag 的稿件全文，1000 的 3000 字上限是 truncate，不是病；1200 med 157 才偏瘦）；再寫一遍「請一次 set-category」。

---

## 0. 0821-1000 這輪長什麼樣

| 指標 | 值 | 旁輪對照 |
|---|---|---|
| DONE | 10:00–10:28，離開碼 0，+76 則 | 正常收工 |
| 工具呼叫 | **174**（當日最高） | 旁輪約 110–140 |
| NS | 69 次／14.7 分 | 2000 輪 NS 26 次／4.5 分 |
| `s2_batch_prep.py` | 63 | 1200＝22、2000＝23 |
| `python -c:json` | 9 | 應為 0 |
| `python -c:read-state` | 3（實際 transcript 4 次含狀態檔） | 應為 0 |
| `set-category` | 8 | T7 上限＝3 站＋1 次補刀＝4 |
| `list-topics` | 8 | 偏多但成本低於 NS |
| AP 清單 | `ap_list_1000.json` **24KB／50 則** | **不是 A18**（0430/1200 才是 870–930KB） |
| NS batch | 57 則、`src_text` 合計 65k、9 則頂到 3000 | 符合 13b「全文」；浪費在組檔方式不在長度 |

狀態檔在當時的晚班檔 `0820-s2-state.json`（16:00 已歸檔到 `Archive\20260820\`）。scratch：`Archive\20260820\_暫存\`。

transcript：`~\.claude\projects\G---------Claude----------20260820\2bf87ed2-8e58-4c46-917f-b115cf29fa3d.jsonl`  
launcher_flags：`NoBashGuard=False`（hook **應該**開著）。

---

## 刀 2（已查完，待修 launcher）→ MASTER **A11-1000**

### 結論
**不是 regex 漏。hook 整輪沒生效。** 加寬 `s2_bash_guard.py` 的 regex **不准做**。

### 證據
- 14 次 `python -c`，**live deny＝0**。
- 離線把同一批 command 丟進現有 `guard.decide('Bash', cmd)`：**13/14 會 DENY**（9 次碰 raw `.json`＋4 次 command 字串含 `-s2-state.json`）。唯一該放行的是 `import s2_state`（沒碰 json）。
- 鐵證：`python -c "json.load(open(r'…\0820-s2-state.json'))"` 印出 `checkpoint=0821-0730`／後來的 `0821-1000`——是真的跑完，不是 deny 訊息。
- 形狀與 A11（0818-1200 2/2）／A11-補（0819-1200 5/5）同一家族；A11 原敘事「regex 沒問題、平台層看不見」**仍然成立**，1000 補上**可修的 launcher 成因**。

### 最可能根因
D7 把 claude **cwd 設成當班 scratch**（本輪 `G:\…\20260820`）× T1 `--setting-sources project`。  
project settings 會去 **scratch 底下**找 `.claude/settings.json`（沒有）。`--settings scripts/s2_guard_settings.json` 在這個組合下沒載進去。

T1 A3 測過「hook探測=有擋」，**不能當生產證據**：測試 cwd 是 `scripts\_task2_test_out\`，不是 scratch；probe 檔 `probe-s2-state.json` 也可能根本不存在（python 報錯被當成有擋）。**0821-1000 推翻「A3 沒吃掉 --settings」這句在生產 scratch cwd 的適用性。** 請同步看 T1 列。

### 次洞（hook 活了也攔不到）
本輪還 **Write `_build_batch1.py`／`_build_batch_ap.py` 再 `python 檔案.py`** 組 NS／AP batch（各寫 58／15 則）。13d §10 明文禁止這種繞；D9 只攔 `-c`。這就是 57 則 NS 一次組出來、順便把 `batch_prep` 打到 63 的路徑。修 hook **不會**自動消滅這條，要靠刀 3 的 `from-raw`。

### 指定修法（接手就做這個，不要發明）
在 `s2_scan.ps1` 開跑前，把 `scripts/s2_guard_settings.json` 的 hook 區塊 **寫進 scratch 的 `.claude/settings.json`**，讓 `--setting-sources project` 真的載到。  
不要：加寬 regex、關 `--setting-sources project`（除非寫入 scratch 做不到）、改 `s2_bash_guard.py` 判定邏輯。

### 驗收
1. 下一正式輪 transcript：`python -c`＋`.json` 必須出現 deny 訊息（或次數＝0 因為改走標準工具）。
2. 量測 `python -c:json`／`python -c:read-state` 應回 0（偶發 1 次且是 deny 後改寫，可接受）。
3. T1 的 verify prompt 改在**真實 scratch cwd** 重測一次，取代 `_task2_test_out` 的假綠燈。
4. 單元測試：現有 `test_s2_bash_guard.py` 不必因本項而改（regex 沒變）。

---

## 刀 1（未做）→ MASTER **A23**

T7 已於 281d380 把規則改成「一站一批＋收尾 ≤1 次補刀」（13d §2）。**規則在，牙齒不在。** 1000 打了 8 次 `set-category`。後輪 1200＝5、1800＝5、2000＝4，仍踩線或貼線。

### 指定修法
`s2_state.py set-category`：同一個 checkpoint **第 5 次直接拒絕**（3 站＋1 次補刀）。錯誤改 `needs-review add`，不准再補刀。可加 `--force` 給人工，掃帶 agent prompt **不要提 --force**。

### 驗收
- 單元測試：同 checkpoint 第 1–4 次 OK，第 5 次 exit ≠0 且 state 不變。
- 下一正式輪 `tool_calls_by_name['s2_state:set-category']` ≤4。
- 不是 D- 項（T7 已裁決），可直接動工。

---

## 刀 3（未做）→ MASTER **A24**（連 A15／A2）

1000 的 NS 69 次／`batch_prep` 63，主因不是 `src_text` 太長，是：

1. `inspect --ids` 可逗號批次，卻常單 id 連打（A2 子項 0818-1000 已記 19 次單筆；1000 當日同形）。
2. agent 用 Write／臨時 `.py` 把 65k 原文當 output token 重打進 `ns_batch_*.json`（A15 家族）。

### 指定修法（兩段，可拆 PR）

**3a 短（先做）**：`s2_batch_prep.py inspect` 若偵測到「本行程已對同一 raw 檔單 id 呼叫 ≥5 次」，stderr 印一行「改一次 `--ids A,B,C`」，**先警告不擋**（避免誤傷 lengths→全文兩段查詢）。13d §4 補一句指向此提示即可。

**3b 正（A15 的落地）**：新增 `s2_batch_prep.py from-raw <raw> --site ns|ap|rt --out batch.json`  
從 `ns_full`／`ap_full`／`rt_full` **在磁碟上**產出帶好 `id/src_text/footage_type`（AP/RT 加 `sb_count`）的 batch 骨架，agent **只補 `entry`**。禁止為了組骨架再 Write 整包、禁止先落 `_build_batch*.py`。

### 驗收
- 3a：單測＋下一輪 `inspect` 單 id 連打不再出現 10+。
- 3b：NS 站 `add-batch` 前的 batch 檔由 `from-raw` 產生；`python 檔案.py` 組 batch 歸零；A15 的 output token 佔比應下降（對照 `--diff`）。
- 3b 動工前先做一個最小 prototype 確認欄位對得上 `add-batch`（A15 原列「需先確認可行性」——prototype 過了就可以做，不必再等 D-）。

---

## 接手順序（建議）

1. **A11-1000** launcher 寫入 scratch `.claude/settings.json`（刀 2，本 session 已定位）
2. **A23** `set-category` 第 5 次拒絕（刀 1，最小、T7 已准）
3. **A24-3a** inspect 連打提示
4. **A24-3b** `from-raw`（較大，先 prototype）

做完一項就更新本檔「本 session 已做」不適用——改更新 MASTER 該列狀態＋commit hash。本檔敘事只加不改。

## 明確不歸本檔的東西

- A18（AP 清單未瘦身）：1000 沒中。已於 12:54 熱修、1600 驗收過。
- 0821-1600 外殼沒 DONE：另一案（A5 看門狗關著）。不要捆進來。
- `src_text` 長度、分類系統化（A10）、D10 三子代理：都不是 1000 這包。
