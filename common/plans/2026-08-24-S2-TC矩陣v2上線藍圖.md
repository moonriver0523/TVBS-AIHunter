# S2 T/C 矩陣 v2 上線藍圖（熱修版）

**日期**：2026-08-24
**性質**：施工藍圖，**不是動工訊號**。前置閘門（A10 P1 授權）未解除前，本文只是預備。
**上位文件**：[TC矩陣評估](2026-08-23-S2-TC矩陣render前分類評估.md)（§12 原案、§13 M1~M6 修訂、§14 R15 後再修訂）
**本文的角色**：把散在評估書 §12／§13／§14 三層增修裡的步驟收斂成一條可執行的順序，
並補上 2026-08-24 逐一查證程式碼後的錨點。**評估書是「為什麼」，本文是「照什麼順序做」。**

---

## 〇、一句話版本

> 加一個 `set-tc` 寫入子命令（牙齒放寫入端）→ 把儀器補齊（不然驗收看不到東西）→
> 判準寫進 `13f` → render 端 join 已存的 T/C → **一次只接一條產線，先三站**。
> 全程靠「改檔即生效於下一輪」熱修，不重啟任何東西；回退是 `git revert`，同樣挑窗口做。

**總量級**：程式改動 8 處（其中 1 處高風險）、規則檔 1 份、交件格式 1 條產線（只有側錄）。
**增量成本**：`set-tc` 約 4~8 次呼叫／輪 × 約 $0.07 ≈ **$0.28~$0.56／輪**，疊在 R15 後的每則 $0.195 基準線上。

---

## 一、前置閘門（不解除就不能動工）

| # | 閘門 | 現況 | 誰能解 |
|---|---|---|---|
| G1 | **A10 P1 授權**（改 `13`／`s2_state` schema） | ❌ 未授權 | 使用者，且須以裁決形式寫進帳本 A10（帳本規則第 5 條） |
| G2 | **判準寫哪一份**（`13f` vs `13c2`）——`13c2:444` 訂「日後改規則一律改本檔」，但 `13c2:440` 又說「分類規則全部沿用 V1 `13`」 | 🗳 治理衝突未裁決，**但實質只剩一個選項**（見下） | 使用者；建議連同 G1 一起定案 |
| G3 | **T/C 與大／中／小的關係**（取代／並存／輔助視角） | ✅ **2026-08-24 已裁：並存** | — |
| G4 | **v1 是否踩到 2026-08-04「不做任何分類的機械檢查或自動修正」禁令** | ✅ **2026-08-24 已裁：不算** | — |

> 🔴 **G2 錨點更正（2026-08-24 Fable 審查，主線已複驗）**：那兩句治理衝突的原文
> **不在 `13c:11`，而在 `13c2:440`／`13c2:444`**（`13c:11` 附近是「⛔ 只讀上半不算讀完 13c」）。
> 更正後 G2 其實**已無懸念**：所謂「寫 13c」的選項實際指向 **`13c2`**，
> 而本文 §3b 已因載入預算 ⛔ 禁止寫 `13c2`（餘裕僅 3,536）。**G2 實質上只剩 `13f`。**
> 裁決時直接引用這一點即可；順帶在 `13c2:444` 加一行註記說明這個例外，
> 免得下一個 agent 又撞上同一個矛盾。

> **G3＝並存**（2026-08-24 使用者裁決）：T/C 是同一條分類軸的延伸，**大／中／小三層不動、不被取代**。
> 本藍圖六階段全部建立在此前提上，**成立、不需改寫**。
> **G4＝不算**（同日裁決）：v1 全在 render 檢視層，不碰大／中／小、不自動修正既有資料、不寫回狀態檔，
> 不在 0804 禁令射程內。評估 §10.1 原記「這是判讀，不是帳本白紙黑字」——**至此成為白紙黑字**。
>
> ⬜ **G1／G2 仍待裁。G1 未解除前，階段 1／3／5 及任何 `s2_state.py` 改動一律不得動工。**

> G2／G3 是**裁決題不是實作題**。G3 若裁「T/C 取代大分類」，本藍圖整份作廢、要重寫成產線改造計畫。
> 以下所有步驟都預設 G3 裁「**並存**」（T/C 是同一條分類軸的延伸，大／中／小不動）。

---

## 二、動工前的操作閘門（每次動手都要過）

- [ ] **不要用鎖檔判有沒有輪次在跑**（M2）。`.s2-scan.lock` 永久殘留（R16 已修 finally 刪檔，但仍以行程判為準），
      改用 `s2_watchdog.ps1` 的 `Test-ScanRunning`（函式定義在 `:181`），
      或看 `_輪次紀錄.txt` 最後一行是不是 `DONE`。
      ⛔ **只看存在、不要 open 鎖檔**——open 會讓正要啟動的那一輪直接 SKIP。
- [ ] **窗口＝13:30–16:00**（M4）。輪次為 1600／1800／2000／2200／0100／0430／0730／1000／1200，
      每輪 15~33 分；1200 那輪的看門狗代打可能在 12:30~13:30 任一邊界啟動
      （`s2_watchdog.ps1:47,316-334`，`GraceMinutes=30`／`MaxLateness=90`）。
- [ ] 前置檢查 `~\.s2-watchdog-enabled` **不存在**（使用者有「啟動看門狗」的常用口令，狀態會變）。
- [ ] `s2_state.py` 的改動先對**狀態檔的複本**離線試跑，⛔ 不要拿生產狀態檔試。
- [ ] **改完先 commit 再讓下一輪跑**——每輪 metrics 記 `repo.head` 與 `repo.dirty`
      （`s2_token_metrics.py:70-84`，`dirty` 是整個 repo 的布林值、不分路徑），dirty 狀態下出事重現不了。
- [ ] 🔴 **改完跑 `python scripts/test_s2_*.py` 全綠再 commit**（2026-08-24 Fable 審查補）。
      repo 有 **21 支** `test_s2_*.py`，是最便宜的迴歸網。
      「對狀態檔複本離線試跑」只測 `set-tc` 自己，**不測有沒有把既有 22 個子命令弄壞**——
      而 `s2_state.py` 每次呼叫重讀，一個 module-level 錯字就讓下一輪所有 `s2_state` 呼叫全滅。
- [ ] 🔴 **超時協議**（同上）：**工作區就是生產環境**（規則檔輪初現讀、`s2_state.py` 每呼叫重讀），
      改到一半下一輪響了，**未 commit 的半成品照樣會被吃進去**。
      訂死：**窗口收尾前 10 分鐘還沒完成 → `git stash`（或 checkout 還原）讓工作區回乾淨態**，
      下個窗口再繼續。⛔ 不准把半成品留在工作區跨輪。
- [ ] 🔴 **每個窗口以 `Test-ScanRunning` 起手判定，不看時鐘**（同上）。
      帳本 E6 記錄過 41.5 分鐘的輪（0819-2200），按時鐘推算的「輪與輪之間的縫」可能直接撞上還在跑的輪次。

---

## 三、施工順序（六階段，前四階段可在同一個窗口做完）

### 階段 1 — 寫入端：`set-tc`（🔴 最高風險，先做，因為後面全靠它）

`s2_state.py` 目前 **22 個 subparser**（2026-08-24 逐一數過），`set-tc` 是第 23 個。

- [ ] **1a-0. 🔴 T/C 一律存 item 的頂層鍵（如 `tc`），⛔ 絕對不放進 `category` 裡。**
      （2026-08-24 Fable 獨立審查發現，主線已複驗）
      `_set_one_category`（`s2_state.py:961-981`）最後一行是
      **`state["items"][i]["category"] = c`——整包覆寫**，`c` 只重建 `大分類/中主題/小分題` 三鍵。
      G3 裁「並存、T/C 是同一條分類軸的延伸」，實作者很自然會把 T/C 塞進 `category` dict——
      **那之後任何一次 `set-category`／`s2_apply_reclass.py` MOVE 都會無聲抹掉 T/C**，
      而 render 的兜底（沒有就跑 `tag_tc()`）會**掩蓋**這個損失，畫面看起來完全正常。
      頂層鍵則安全：`apply_update`（`:522`）是原地改鍵不重建 dict、
      `cmd_add_batch`（`:434`）撞 id 一律跳過，補稿與批次路徑都不會波及。
- [ ] **1a. 新增 `set-tc` 子命令，⛔ 不是改 `set-category` 的參數。**
      這是讓熱修安全的關鍵：舊輪次不知道有這命令 → 不呼叫 → 那些 item 沒 T/C →
      render 自動退回 `tag_tc()`／「未分類」，**頁面不會壞，只是那天標籤品質差一點**。
      若把 T/C 塞進 `set-category`，改到一半的那一輪會直接炸。
- [ ] **1b. 參數比照 `set-category`**（`s2_state.py:1679-1683`）：
      `--id` ＋ `--tc`，批次用 `--pairs "id=T1,T2/C1,C2;id2=…"`（分隔符優先認分號）。
- [ ] **1c. 牙齒放寫入端**：收到不在字典裡的名字就**拒絕**，比照 `add-side` 沒有大分類就退回 `bad`
      （`s2_state.py:1183`）——本 repo 已驗證的模式。
      ⚠️ **但要整批回報一次、不逐則退回**（M3）：每次拒絕就是一次重試呼叫（≥$0.06），
      逐則退回會把「品質訊號」變成「成本乘數」。
- [ ] **1d. 內建 A23 式硬上限：每 checkpoint 上限 ~~4~~ → 「站數＋2」（初值 6）次呼叫。**
      A23（`set-category` 同 checkpoint 第 5 次硬拒絕）目前仍⬜待做，
      且未來 A23 的上限只會蓋 `set-category`。在一個「已知缺陷就是寫入子指令呼叫次數失控」的系統裡
      再加一個不受管的寫入子指令，是重演已知失效模式。
      🔴 **2026-08-24 修正（Fable 審查）**：原訂 4 次**與本文自己的估算矛盾**。
      checkpoint 就是輪次（`{MMDD}-{HHMM}`），遙測顯示 `set-category` 穩定 4 次／輪、
      0823-2000 有一輪 5 次；若 T/C 綁 `set-category` 同批（3d），**三站＋補刀就頂到 4**，
      階段 5-2／5-3 上線後同一 checkpoint 再加 1~2 次 → **合法呼叫必然撞牆**，
      而 1c 每次拒絕 ≥$0.06，或 agent 放棄 → T/C 靜默缺失。
      **上限訂 6，並在階段 5 每接一條產線時複檢要不要調。**
      ⚠️ **A23 沒有現成實作可抄**：per-checkpoint 計數器**存哪**（state.json 加欄位？記憶體？）
      本身是個設計題，**與 2d 一樣必須在動工前定案**，不能到窗口裡現場設計。
- [ ] **1e. ⛔ 驗證清單不可以再手打一份。** `TC-字典.md` 的名單**已經有第二份拷貝**在
      `build_tc_matrix_0821.py` 的 `T_FIXED`／`C_FIXED`。`set-tc` 再寫死第三份就是三真相源
      （17 檔開頭把「雙真相源」列為具名失效模式）。**改成讀字典檔，或抽成一個共用常數。**

### 階段 2 — 儀器（🔴 不先做，階段 5 的驗收會看不到任何東西）

> 這一階段本身不改變任何行為，但它是 M6「驗收指標必須是行為」的前提。

- [ ] **2a. `s2_token_metrics.py:133-138` 的 `S2_STATE_SUBCOMMANDS` 加 `set-tc`。**
      🔴 **2026-08-24 查證補件**：那個 tuple 目前只有 **20 項**，但 `s2_state.py` 有 **22 個** subparser——
      **`patch-entry` 與 `fix-first-seen` 現在就漏在外面**（既有缺陷，非 v2 造成），
      比不中就落 `s2_state:?`（實測每輪已有 1~7 筆）。順手一起補齊。
      ⚠️ 順序有意義：現有註解記著 `needs-review` 必須排在 `add` 前面（次動詞會搶先比對）。
      `set-tc` 與 `set-top`／`set-topic-order` 無前綴衝突，但插入時請一併複驗。
- [ ] **2b. `classify_phase`（`:209-232`）的「分類」關鍵字清單加 `set-tc`。**
      不加會落「其他」並**繼承前一階段**、污染階段遙測——正是該檔 `:229-232` 記錄過的 0818-0430 事故形狀。
- [ ] **2c. `SHOW_FIELDS`（`s2_state.py:1419-1434`，目前 **14** 個鍵）加 T/C。**
      不加的話 `show --fields` 對不認得的欄位是 `sys.exit(2)` 硬錯（`:1465`），
      而 `s2_bash_guard.py:70-71` 又擋掉 `python -c` 讀狀態檔——
      **agent 沒有任何合規途徑查證自己這輪標了幾則**，只剩 `get --id` 一則一則倒。
      `13c:524-525` 早就預告這個形狀會逼出臨時腳本（A12 那一類）。
- [ ] **2d. 定義「拒絕」落在哪。** 🔴 現在**沒有任何地方會記下來**：
      遙測只記呼叫次數、不記離開碼，整份 `_token_metrics.jsonl` 沒有欄位承載「這次失敗了」。
      選項：寫進 `needs_review`／遙測新增欄位／`set-tc` 自己吐一行進輪次紀錄。**這是設計題，動工前要先定。**

### 階段 3 — 規則檔：判準寫進 `13f`

- [ ] **3a. 判準段落接在 [`13f`](../13f-S2-大分類與各站規則.md) 的「大分類歸位通則」（`:105`）之後**——
      主題最相符（13f 就是大分類清單／歸位通則／`list-topics` 那一份），T/C 是同一條軸的延伸。
- [ ] **3b. `TC-字典.md` 全文貼進去**（1,141 字元，估計載入 1,406，只吃掉 13f 餘裕的 9%）。
      2026-08-24 實測各檔距 28,000 安全預算的餘裕：**13f 16,106** ✅／13d 12,782／13 6,626／
      13c 4,169／13c2 3,536／13e 3,199 ⚠️。
      ⛔ **不要寫進 `13e`／`13c2`**（餘裕只剩約 3,200~3,500），一加就可能推爆預算。
- [ ] **3c. ⛔ 不要往 `13b` 寫任何東西**——`s2_scan_prompt.md:30` 明令「不要再讀 `13b`，
      已改為歷史檔案，讀了會照著錯的做」。往死信檔寫引用＝多一份會發散的拷貝。
- [ ] **3d. 呼叫時機綁在「已經在做的動作」上**：T/C 跟 `set-category` **同一批**下，
      不要另立一個「收工前記得跑」的步驟。
      理由是**成本與對齊**（省一次工具呼叫≈$0.07、天然對齊既有分類批次、
      agent 該則的判斷脈絡還在），不是「agent 不遵守常規動作」。
      🔴 **2026-08-24 更正**：本條原本引用「`list-topics` 執行率 23/25 vs
      `topic_review`／`audit` 0/25」當對照——**該組數字出自已被 R17 撤案的缺陷偵測器**
      （重測為 16/16 輪有跑），**不得再引用**。設計結論不變，但論據換成上面那三個獨立理由。
- [ ] **3e. 🔴 改完立刻跑 `python scripts/s2_rules_check.py`**（`SAFE_BUDGET = 28000`）。
      它擋三種事：某份檔超過預算（會被靜默截斷）、檔尾缺 `RULES-EOF` 護欄線、23 條高風險規則任一不見。
      **離開碼 1 就不要讓下一輪跑。** 加完東西不檢查＝重演 R15。

### 階段 4 — render 端：join 已存的 T/C

🔴 **最容易被漏掉的技術點**：`s2_render_html.collect()` 是**用固定欄位清單**組 row 的
（`:114-115`、`:135-137` 逐鍵列舉），`items` 上新增的 T/C 欄位**不會流到 renderer**；
而 `s2_render_html.py` 暫定不動。所以呈現端必須**自己用 `id` 去 `st["items"]` 撈**。

- [ ] **4a. join 規則**：**有存的用存的 → 沒有的跑 `tag_tc()` → 再沒有就「未分類」**。
      這條 fallback 正是「一條產線一條產線接、不會壞頁面」的技術基礎。
- [ ] **4b. `build_v1.py:53-65` 的 `inspect.getsource()` 字串替換要拆掉。**
      v2 只要動到 `tag_tc()` 就立刻 `SystemExit`。正式化時應**直接改分類器本身讓它回傳「未分類」**。
- [ ] **4c. `build_v1.py:209-213` 的未分類遙測數的是「啟發式」的未分類**——
      `set-tc` 從沒跑過也會回報正常。要改成分開數「已存 T/C」與「啟發式兜底」。
      **順手印出「已存 T/C ≠ `tag_tc()` 輸出」的差集 id 清單**（十行內的事）——
      那是階段 6 的主指標，否則只能用 `show --fields` 撈值再人肉比對。
- [ ] **4d. 🔴 `fold_side()`（`:78-97`）用 `dict(r)` 取第一列**，第 2..N 段的 T/C 直接消失。
      `set-tc` 是 per-item、側錄單元是多 item，**單元內混合狀態會靜默資料損失**。
      側錄的 T/C 以**摺疊後單元**為判斷單位（一單元一組標籤），與計數規則一致。
- [ ] **4e. 收斂條件（§13 對 §三-2 的修訂）**：薄殼**只放呈現層**（資料層一律 import 生產函式），
      且**驗收後矩陣版要變成 `s2_render.py` 每輪吐的那一份、第三條路刪掉**。
      理由是 `s2_render.py:619` 記著 2026-08-11 事故：歷史列只接了 `s2_render_html.py` 獨立入口、
      排程走的 `s2_render.py` 沒接，功能「上線」卻從沒生效——結論是「**兩條路都要接**」。
      再開第三條＝同一失效模式加碼。

### 階段 5 — 分產線導入（一次一條，先三站）

所有外部來源都**不直接寫狀態檔**，一律先落 `_待整併/`、由掃帶輪收工前統一入庫
（`13:237-260`；理由是 `s2_state.py` 沒有檔案鎖，直寫會 lost update，2026-08-10 有實例）。
因此除了側錄以外，**交件格式全部不用改**。

| 順序 | 產線 | 佔比 | 下標點 | 要改什麼 |
|---|---|---|---|---|
| **1** | AP／RT／NS | 61% | **B 整併端** | 只加判準段；agent 本來就逐則判，**零額外讀取**、交件格式零改動 |
| **2** | 韓聯社／CNA／YT（17） | 22% | **B 整併端** | **交件格式零改動**，整併後補 `set-tc` |
| **3** | ABC／ENEX（18） | 獨立流程 | **B 整併端** | **candidate schema 零改動**，`--apply` 後補 `set-tc`。⚠️ 屬 A9，使用者已訂為獨立流程 |
| **4** | CNN／NHK 側錄（14） | 15% | **A 交件端** | 🔴 **唯一要改格式的一條**，見下 |

> 側錄選 A 的理由：`s2_state.py:1183` 已硬性要求大分類（沒有就退回 `bad`），
> 交件端本來就在做歸位判斷，順手多判 T/C 幾乎不加成本；換成整併端判，
> 掃帶輪 agent 要重讀 30+ 個單元的長逐字稿，那才是真的貴。

**階段 5-4（側錄）的遷移設計** —— 四個性質疊起來 ⇒ 上線當天新舊格式必然混在同一個檔：

1. `add-side` **冪等**（`s2_state.py:1186-1188` 已在庫就 `continue`；`common/14:221` 明訂每輪全掃靠它自己略過）
2. `--overwrite` 會一併蓋掉 `raw_entry`（`:1191-1192`）——那是 S2b 存在的目的，**所以補標不能用 `--overwrite`**
3. 候選檔是 **append 模式**（`13:267`、`14:353`），而 `add-side` 每輪重讀整份
4. 舊檔**滯留超過 24 小時**（`_待整併/` 跨天共用，只在 16:00 建檔輪由 `s2_scan.ps1:283` 用**前一班** MMDD 前綴掃走）

- [ ] 缺 T/C 的段落**照樣入庫但列 `uncat`**——本 repo 自己的裁定
      （`s2_platform_merge.py:78-80`：「分類可以事後補，**素材消失補不回來**」）
- [ ] 🔴 **⛔ 不設 `--allow-missing-tc` 旗標**（2026-08-24 Fable 審查修正）。
      原案同時抄了**兩套互斥的哲學**：`common/18:85-103` 是「**讀取端硬 ❌ ＋ 祖父旗標**」，
      `s2_platform_merge.py:78-80` 是「**軟收進 `uncat`**」。兩套疊在一起自相矛盾——
      既然「舊格式永久合法」，硬 ❌ 就得永遠帶著旗標關掉、形同虛設；
      而若照 `uncat` 軟收，旗標根本沒有存在意義。
      **裁定：採軟收 `uncat`、不設旗標**，與「素材消失補不回來」的既有裁定一致。
- [ ] 明寫「**舊格式永久合法**」（照抄 `common/14:459-461`：「⛔ 舊格式仍然合法……**不要回頭去改舊資料**」，0802 踩過）
- [ ] 擴充方式二選一：擬歸位行帶 T/C，或 `add-side --tc`（比照現有 `--homes` 的
      `大分類/中主題[/小分題]=TC,TC;…` 寫法，`s2_state.py:1638-1650`）
- [ ] 🔴 **先修一個現在就存在的靜默失敗**：擬歸位解析（`s2_state.py:1032-1040`）的
      `re.split(r"→|->")` 只取 `segs[0..2]`，**`segs[3]` 以後現在就被靜默丟棄**、沒有 arity 檢查。
      舊格式進新 parser ＝沒 T/C、不進 `bad`、`cmd_add_side` 回傳碼 0。
      T/C 的分隔符也必須加進 `s2_platform_merge.py:110-115` 那種 `;=/` 護欄——
      **`parse_side_txt` 目前完全沒有等價護欄。**

### 階段 6 — 驗收（每輪；M6 重寫版）

⛔ **不可以用「規則有沒有被讀到」當 v2 生效的證據**——那只證明必要條件。

> 🔴 **驗收口徑（2026-08-24 Fable 審查補）**：帳本「實作共同護欄」明訂
> **1 正式輪 → 3 連續正式輪**。**單獨一輪只能宣告「第 1/3 輪查驗通過」，不得宣告驗收完成。**
> 另：`build_v1.py` 目前**還沒掛進 `s2_render.py`**（評估 §10.2 的接法 A/B/C 未定），
> 驗收期間是**人工跑**——這是預期狀態，不是漏掉。

- [ ] **主指標：抽樣比對「已存 T/C ≠ `tag_tc()` 輸出」的差集**——那才是 v2 宣稱的價值。
      （0823 基準線：470 則中啟發式判不出來 143 則＝30%，其中 T 11／C 132。）
- [ ] **次指標：`set-tc` 實際被呼叫了幾次、標了幾則**（`--diff <上一輪> <這一輪>` 比對逐桶呼叫數與 token）
- [ ] **次指標：未分類率**（只反映覆蓋缺口，v1 基準線 C 穩定 18~25%）
- [ ] ❌ **不要用「未分類格子有沒有變少」當主指標**——量錯東西。
      §9.2 的載重論據是「**有信心地答錯**」（`RT7263` 賴清德 823 沒掛臺灣、`SE-001SU` 哥倫比亞高中掛中南美），
      那兩則在 `tag_tc()` 眼中都是**已分類**，agent 改對之後未分類格子**一格都不會少**。
- [ ] ⚠️ **別期待流程遵守度**：T7／A23 顯示 agent 會違反**流程**規則
      （`set-category` 規則早改成一站一批，0821-1000 那輪照打 8 次）。
      封閉字典管得住「叫什麼名字」，管不住「呼叫幾次」——所以 1d 的硬上限不是可選項。

---

## 四、回退

- 規則檔改動：`git revert <sha>` → **下一輪自動生效**，不必重啟排程／launcher／watchdog。
  （R15 是這條路徑的第一次實戰：02:10 commit、04:30 那輪直接生效。**此前提有實證背書。**）
- 🔴 **`s2_state.py` 的回退也要挑窗口**（M5）。它是每次呼叫重新讀檔，
  **輪次進行中 revert 掉 `set-tc`，已經呼叫過的 agent 會拿到 `invalid choice` 而不是靜靜退回兜底**
  ——正是 1a 想避免的形狀，只是從回退方向發生。
- 回退後 T/C 欄位留在狀態檔裡**無害**（render 有就用、沒有就跑 `tag_tc()`）。

---

## 五、紅線（做什麼都不能踩）

1. ⛔ **T/C 絕對不可以印進 `晚班交接.txt`。** `s2_validate.py:54` 的 `LINE_RE` 認不得的行會
   **直接從品質掃與檔頭計數消失**（該檔 `:35-40,67,112` 記錄踩過三次）；
   `s2_p0_history.py:1008-1015` 會 `raise CorpusAnalysisError("仍有未知未辨識行，拒絕輸出統計")`。
2. ⛔ **不要往 `13b` 寫任何東西**（死信檔）。
3. ⛔ **動任何必讀規則檔之後，一律跑 `s2_rules_check.py`**，離開碼 1 就不要讓下一輪跑。
4. ⛔ **不要走「按信心切」的混合路線**（Python 先判、兜底才丟 LLM）——§9.2 已否決，
   攔不到「有信心地答錯」的那 81%。本藍圖是**按來源切**（每則都有人／agent 判），兩者不同。
5. ⛔ **不要開第三條 render 路**（見 4e）。

---

## 六、已知會被低估的三件事

1. **必讀規則檔有六份**（`13`／`13c`／`13c2`／`13d`／`13e`／`13f`），另有 `13b` 是**死信檔**（不讀）。
   判準寫哪一份、其他份怎麼引用，本身是個**決定**（G2）。
2. **側錄／網址的交件者是外部 agent 或人**——`common/14` 原文警告「**上傳側錄的人不一定是規則的作者、
   也不一定懂格式**」。改交件格式的風險不是 token，是**每條產線都要重新驗收，
   而且會有一段時間收到舊格式**。
3. **成本要用「每則」正規化**。0824 兩輪表面總成本比基準低 32%，但那是則數少 40% 造成的假象。
   R15 修復後每則從 $0.168 升到 $0.195（+16%，完整載入規則的代價），v2 的增量疊在這條新基準線上。

---

## 七、順帶列管（非 v2 造成，但會干擾 v2 的驗收）

| # | 事項 | 影響 |
|---|---|---|
| a | `S2_STATE_SUBCOMMANDS` 漏 `patch-entry`／`fix-first-seen` | 這兩支的呼叫現在都落 `s2_state:?`，`--diff` 看不到；階段 2a 順手補 |
| b | `s2_mark_ingested.py:23-24` 宣稱 16:00 輪整批搬走，但 `s2_scan.ps1:283` 的 `-like "$pm*"` 讓 `已入庫_0810-側錄候選.txt` **永遠掃不走** | `_待整併/` 比文件假設的髒，放大階段 5-4 的遷移風險 |
| c | `CNN01`（`source="CNN"`）被 `collect()` 歸成 `wire`，但 txt 檔頭分桶沒算進任何一格 | 矩陣與 txt 差 1 則 |
| d | `find_archive_dates()` 掃不到 `0822-s2-state.json`（資料夾在、檔不在），歷史列**靜默跳過** | 建議加一行診斷 |

---

**下一步**：等 G1~G4 裁決。裁完之後，階段 1~4 可在同一個 13:30–16:00 窗口做完並 commit，
階段 5 從三站開始、一輪一條產線往下接。
