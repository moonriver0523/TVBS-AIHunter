# A26 五層合一（V8）— 取代對照表

> Task 0 產物。這張表是 Task 1 守恆腳本、Task 2 剪貼組稿的輸入。**做完先給使用者看一眼放行再進 Task 2。**

## 0907 重新量測（字元，`len(open(f,encoding='utf-8').read())`，計畫裡的數字已過期）

| 檔 | 計畫舊估 | 0907 現量 | 差異 |
|---|---|---|---|
| `13c` | 21,721 | **22,172** | +451（T13 已搬入） |
| `13c2` | 22,997 | **24,955** | +1,958（T13 已搬入，逼近 25K） |
| `13d` | 20,549 | **20,735** | +186 |
| `13g` | 15,858 | **15,991** | +133 |
| `13h` | 19,332 | **19,332** | 0 |
| **合計** | 100,457 | **103,185** | +2,728 |

T13 收工清單總表現況：已確認在 `13f` 尾（`## 收工清單總表`，2026-09-07 自 prompt 瘦身新增），**不在** `13c2` §6（`13c2` 本來就沒有 §6，T13 落檔時就已改放 13f 尾，見 13f 該節按語「原規劃放 `13c2` §6，因 `13c2` 已逼近 28,000 字元估計載入預算改放本檔尾」）。V8 收工後這節要搬進 `13c3`，`13f` 該處改為一行指路。**此為對照表第 1 筆。**

---

## 一、取代關係（增量檔 § → 被取代的 13c／13c2 §，寫到取代到哪一級）

| # | 增量檔 § | 取代到哪一級 | 被取代的 13c／13c2 內容 | 依據（原文哪一句） | 處置 |
|---|---|---|---|---|---|
| 1 | `13f` 尾「收工清單總表」 | 整節（跨檔搬移，非文字取代） | 無（`13c2` 本來就沒有這節） | `13f` 該節按語：「原規劃放 `13c2` §6，因 `13c2` 已逼近 28,000 字元估計載入預算改放本檔尾」 | **搬進 `13c3`**，`13f` 該處改一行指路（不再放全表） |
| 2 | `13d` §2「`set-category` 改一站一批」 | **節內某一句**（僅 `set-category` 一個動詞，`update-entry`／`add-batch` 明文不受影響） | `13c2` §2 內「⛔ 改狀態檔的子指令，一輪只准呼叫一次」那條 bullet（`set-category`／`update-entry`／`add-batch` 三個一輪各一次） | `13d` §2 自身標題「取代舊『一輪一次』」＋內文「`update-entry`／`add-batch` 仍照 13c §2 原規則，一輪一次（不受本條影響）」 | 該 bullet 拆二：`set-category` 改引 `13d` §2 新規則；`update-entry`／`add-batch` 保留原句 |
| 3 | `13d` §8 上半「開工只精讀 13c＋本檔」 | **整條規則**（整段作廢，不進新檔本文） | 無直接對應 13c/13c2 條文，取代對象其實是 `s2_scan_prompt.md` 舊版「先讀 13 全文」那句 | `13d` §8：「本條蓋過 `s2_scan_prompt.md`『先讀 13 全文』那句」；計畫本身在 Task 0 版面規劃已宣告「V8 後作廢→歸檔」 | 整條**移入歸檔附錄**，不搬進任何新檔本文（prompt 現行版早已不是「讀 13 全文」的形態，T13 後這條規則的前提已不存在） |
| 4 | `13d` §8 下半「NS body 照抄即用」（`POST /api/v3/stories` 具體 body） | **範本內一個佔位符**（13c 現行 NS 節「⛔ token 不離開頁面 JS」小節的 `const body = { /* request body */ };` 佔位） | `13c` ### 3) NS 節「⛔ token 不離開頁面 JS」範本 | `13d` §8：「此處只補 13c 範本裡空白的 `body`」 | 合併時**用 13d §8 的具體 body 內容取代 13c 該處佔位註解**，其餘安全形態說明保留 13c 原文不動 |
| 5 | `13g` V5-0（ENEX 部分） | **節內某一句** | `13c` §0：「⛔ 固定輪就是這三站，不多不少。ENEX／ABC NewsOne 已打通但人工下令才跑、不進固定排程」 | `13g` V5-0 標題自宣「本檔取代 `13c §0` 的『就是這三站』」＋引原文第 23 行 | **已提前執行**：現行 `13c` 該句已用刪除線標示並附「2026-09-07 訂正」按語，V8 只需把刪除線內容搬進歸檔附錄，正文改成訂正後的最終敘述 |
| 6 | `13h` V7-0（ABC 部分） | **節內某一句** | 同上（`13c` §0 同一句的 ABC 部分） | `13h` V7-0 標題自宣「本檔取代四處『ABC 人工下令才跑』」，列出 `13c §0` 為第一處 | 同上，已提前執行；`13g` V5-0 正文內「ABC 維持人工／不要順手掃 ABC」那句也已由 `13g` 自己補刪除線＋「2026-09-07 訂正：ABC 亦已由 13h 取代」——**歸檔時 13g 這句也要一併搬進附錄** |
| 7 | `13g` V5-1「### 順序固定 NS→AP→RT→ENEX」 | **節內一句** | `13c` §0「### ⏱️ 掃描順序固定 **NS → AP → RT**（不可調換）」 | ⚠️ **無明文「取代」關鍵字**，是依循 `13g`／`13h` 前言宣告的「V 版增量只加不改、後讀的蓋前讀的」通例（隱性取代，grep 抓不到，是本表額外找出的一筆） | 三站順序句由五站版本（見下一筆）取代，三／四站中間版**只留最終態**，三站舊句與四站過渡句都搬進歸檔附錄 |
| 8 | `13h` V7-1「### 順序固定 NS→AP→RT→ENEX→ABC」 | **節內一句** | 上一筆的 `13g` V5-1 四站順序句（連鎖取代：13c 三站句 → 13g 四站句 → 13h 五站句） | 同上，隱性通例，非逐字宣告 | 上稿只留 `13h` 這句五站版本 |

## 二、拿不準（「?」，單獨列，不算進上表）

| # | 疑點 | 現況 | 建議 |
|---|---|---|---|
| A | `13g` V5-0／`13h` V7-0 都聲稱「`13e` 與 `13c2` 也各有一處」寫著 ENEX／ABC「人工下令才跑、由人工輪次寫進 `_待整併/`」的複本句，`13g` V5-0 明講「就 ENEX 的部分同樣被本檔取代」 | 在**現行 `13c2` 全文找不到**逐字對應句（已搜尋「人工下令」「固定輪就是」「不進固定」皆無命中）；`13e` line 237 確實還有一句「⚠️ 人工下令才跑、不進固定排程」未刪除線 | ✅ **2026-09-07 team-lead 裁決②**：`13c2` 那句複本視為**已消解**，不再追查、不阻擋 Task 2。`13e` 那句因全域限制「13／13e／13f 三份不動」本次仍不處理，留給另案（`13g`/`13h` 都寫了「本檔取代」但 13e 正文從未真的改，是遺留的已知矛盾，非本次範圍） |
| B | `13d` §7（AP 完整 topic 頁）與現行 `13c` `### 2) AP` 一節高度重疊 | 現行 `13c` AP 節**已經**是 0820/0821 訂正後的最終版本（topic 頁網址、`PageSize:50`、fetch＋瘦身範本全部都在），並且**已經**明文寫「細節與正確用法見 `13d` §7」——換句話說 `13d` §7 描述的修正**早就被直接改寫進 `13c` 正文**，不是等 V8 才要做的取代。`13d` §7 剩下的獨有內容只有「對帳快照」那條 bullet（Page1／Page2 標記可信度分開陳述） | ✅ **2026-09-07 team-lead 裁決③執行結果**：`13c`（上）AP 節保留現行版本**完全不動**；`13d` §7 全段（含對帳快照那句）**整段搬進歸檔附錄**（`13c-V8-已取代條文.md`），不重複複製。同一裁決也套用到 `13d` §8（NS body 配方）——13c 現行「⛔ token 不離開頁面 JS」小節的 body 仍是佔位註解 `{ /* request body */ }`，具體 body 範例整段在歸檔檔案裡，**已知殘留缺口**：兩者未合併成一份完整範本，留待下次整併 |

## 三、純新增（沒有取代關係，直接搬移的節）

- `13d`：§1（scratch 路徑）、§3（`--compact`）、§4（`inspect`/`unwrap`）、§5（`src_text` 必帶）、§6（大回應落檔）、§9（素材行不加「摘要：」）、§10（工具層防護）、§11（hook 補洞）
- `13g`：V5-2（ENEX API 直查）、V5-3（整併路徑）、V5-4（素材行/分類/檔頭）、V5-5（對帳與收工）——V5-6（上線前檢查表）**不是純新增**，是要**歸檔**（給執行切換的人看，非掃帶 agent 必讀）
- `13h`：V7-2（ABC 清單直查）、V7-3（整併路徑）、V7-4（素材行/分類/檔頭）、V7-5（對帳與收工）——V7-6 同上**歸檔**
- `13c2` 附「分檔前 13c 的原始檔頭」（2026-08-24 存查）→ **歸檔**（進 `13c-V8-已取代條文.md`，不進 `13c3` 正文）
- 五份原檔各自的檔頭「沿革/增補」敘事區塊（如 `13d` 開頭 8/13～8/31b 的變更記錄、`13g`/`13h` 開頭的「V5/V7 增量，疊在…之上」說明）→ **這些是非空行，守恆腳本會算，不能丟**，全部搬進歸檔附錄（新檔本文只留計畫規定的 5 行新檔頭，不含這些舊敘事）

## 四、四份新檔估字元 vs 28,000 上限

用「現量原檔對應段落字元數直接相加」粗估（未扣除重複／取代後刪掉的舊文，實際 Task 2 剪貼後應更小；但也未含歸檔附錄，那是另一份檔案不佔這個預算）：

| 新檔 | 粗估（現量相加） | 對 28,000 | 備註 |
|---|---|---|---|
| `13c`-上 | 現行 13c 全文 22,172 ＋ 13d §6/7/8 共 5,061 ＋ 13g V5-1（1,472）＋ 13h V7-1（1,910）＝ **約 30,615** | ⚠️ **粗估已超標** | 這是「原封不動疊加」的上限值；扣除表一第 2/7 項會被取代刪掉的舊句（13c §0 三站句已是刪除線、13d §7 大段已被 13c 正文吸收不必重抄）後應會降下來，但**沒把握降到 28,000 以下，Task 2 剪貼後務必立刻量一次**，超過就照計畫拆檔 |
| `13c1`-中（ENEX＋ABC） | `13g` V5-2..V5-5 共 12,337 ＋ `13h` V7-2..V7-5 共 15,202 ＝ **27,539**（未含 5 行新檔頭＋`RULES-EOF` 尾） | 🔴 **確定會超標** | 加上檔頭尾（約 300～400 字元）幾乎篤定跨過 28,000。**計畫自己的但書「超過就再拆 ENEX/ABC 兩檔」已經觸發**，Task 2 建議直接拆成 `13c1`（ENEX，約 12,700）／`13c1b`（ABC，約 15,600），兩份都在安全範圍 |
| `13c2`-下（狀態檔與指令） | `13c2` §1b/2/2a/2a-2/2b/2d 部分＋ `13d` §1/2/3/4/5/9/10/11（不含已析出的 §6/7/8） | 未逐字加總，但 `13c2` 現量已 24,955，扣掉要搬去 `13c3` 的 §3/3a/3b/4/4b/4c/5/5a/5c 後剩餘量會明顯降，再疊加 `13d` 八節 | 估計仍在安全範圍，Task 2 剪貼後量一次確認 |
| `13c3`-下（收工與防卡） | `13c2` §3/3a/3b/4/4b/4c/5/5a/5c ＋ `13f` 尾收工清單總表 | 未逐字加總 | 計畫估 ~18K，量級上看合理 |

**結論：`13c1` 幾乎確定要拆成兩檔（ENEX／ABC）；`13c`-上也有超標風險，Task 2 剪貼完成後兩者都要立刻重新量測，不能只信這裡的粗估。**

---

## 給使用者看的 3 筆重點（放行判斷用）

1. **`13c1` 確定超標**：ENEX＋ABC 合併粗估 27,539 字元（未含檔頭尾），計畫自己的但書已觸發，建議直接拆成 `13c1`（ENEX）／`13c1b`（ABC）兩檔，不要等 Task 2 做完才發現。
2. **拿不準項 A**：`13g`/`13h` 都聲稱「`13c2` 也有一處」ENEX/ABC 人工下令的複本句，但現行 `13c2` 全文找不到——可能早被移除，也可能是舊文件的失真描述，需要人工核實或授權「視為已消解、不追」。
3. **`13d` §7（AP topic 頁）其實早就被直接寫進現行 `13c` 正文**，不是等 V8 才要做的取代——這代表五層疊加的實況比計畫假設的更亂：至少有一處「增量檔」的內容已經被直接改寫回了被疊加的下層檔案，取代關係不是單向的。Task 2 剪貼粒度要注意這種「已提前完成」的案例，不要對著已經合併過的文字再做一次機械複製造成重複。

---

## 五、Task 2-7 執行結果（2026-09-07，team-lead 放行後）

裁決①②③已套用（見上方裁決①拆兩檔、裁決②表二 A 項、裁決③表二 B 項）。實際剪貼時
因字元預算又做了兩處與原計畫版面不同的調整，記錄如下：

- **`13d` §6（大回應落檔）** 原計畫排進 `13c`（上），因 `13c`（上）疊上 V5-1／V7-1
  兩節後逼近 28,000（估計載入量，含 Read 工具行號前綴開銷），改放 `13c2`（下）。
- **`13d` §10（工具層防護）／§11（hook 補洞）** 原計畫排進 `13c2`（下），因 `13c2`
  疊上 §6 後也逼近預算，改放 `13c3`（下）——與 `13c3` 自己的 §5 防卡設計同主題，
  相鄰閱讀動線更合理。
- 最終六檔實際字元數：`13c`（上）25,222／`13c1`（ENEX）12,696／`13c1b`（ABC）
  15,559／`13c2`（下）24,382／`13c3`（下）16,805／`13c-V8-已取代條文`（歸檔）
  12,476。`s2_rules_check.py` 的「估計載入量」（含行號開銷）顯示 `13c`（上）
  27,847，距 28,000 只剩 153 餘裕，**未來再加規則要優先考慮移出，不要塞進這一檔**。
- 守恆腳本最終版採「結構行兩邊豁免」設計（標題／`<!--BODY-->`／`<!--ENDBODY-->`／
  `RULES-EOF`／純 `---` 分隔線，兩邊都不計入比對），結果**缺 0 行（硬性通過）／
  多 32 行**（21 行是六份新檔各自的新導覽頭，11 行是搬自 `13f` 尾且更新過內部
  引用的收工清單總表——`13f` 本身不在五份守恆集合內，這 11 行對五份原檔而言
  100% 是新增，非缺陷）。
- 已知殘留缺口（不影響硬性通過項，明確記錄留待下次整併，見表二 B 項裁決③執行結果）：
  `13c`（上）NS 節「⛔ token 不離開頁面 JS」小節的 body 仍是佔位註解，真正的具體
  body 範例整段在 `13c-V8-已取代條文.md` 的 `13d` §8 段落，未合併成一份完整範本。
