# T8 過期描述清理 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans。純文字修訂，無腳本邏輯改動。

**Goal:** 清掉三處會被每輪讀到、且已被實務推翻的敘述：`13` 的 12 輪排程表與 16:00 建檔輪、prompt「rules_check 只查六份」、`s2_scan_prompt_v7.md` 快照與 `s2_v7_switch.ps1` 橫幅仍寫 09:00；並補完 13h V7-6「切換後才做」剩下的文字項。

**Architecture:** 只改文字；排程時刻改成「指路現查」而不是再寫一張會過期的表。

**Tech Stack:** Markdown、PowerShell（只改字串）、`s2_rules_check.py`。

## Global Constraints

- 只在改規則空窗（11:00 輪收工後～17:00 前）動 `common/13*`。
- 逐字保留被取代的段落：在同檔加一行「（2026-09-07 訂正：…）」說明，不刪歷史句，改成 `~~刪除線~~` 或搬到該檔尾的「已作廢」小節——擇一，整份計畫用同一種。
- 不動 `13b`（退役檔）。

---

### Task 1: `13` 排程表與 16:00 建檔輪

**Files:**
- Modify: `common/13-S2-定時掃帶.md`（§「每日固定排程」約 L119–150；L125、L167、L272 的 16:00）

- [ ] **Step 1: 定位**
  ```bash
  grep -nE "16:00|每日固定排程|標記邊界" common/13-S2-定時掃帶.md
  ```
  Expected: 至少命中 L119 附近的表、L125（建檔輪 16:00）、L167（`△` 16:00～23:00 五輪）、L272（`New-ShiftState`）。

- [ ] **Step 2: 排程表改成指路**
  把 12 列時刻表整段用 `~~` 劃掉（或搬到檔尾「已作廢」），在原位加：
  > **2026-09-07 訂正**：排程時刻**以現查為準**——`python scripts/s2_schedule_check.py`，或 `Get-ScheduledTask S2掃帶`。ENEX／ABC 哪幾輪掃看 `13g` V5-1／`13h` V7-1。0907 現查七輪：17:00／20:00／22:00／01:00／04:30／07:00／11:00。**本檔不再維護時刻表**（三次改版都漂掉了）。
  保留「標記邊界」段（`mark_for()` 判準仍有效），但 `△` 的說明由「16:00～23:00 五輪」改成「17:00～22:00（含）」。

- [ ] **Step 3: 16:00 → 17:00**
  L125、L272 的「16:00 建檔輪」改 17:00，各加「（2026-09-07 訂正，13c2 §5a 早已是 17:00）」。

- [ ] **Step 4: 驗證**
  ```bash
  grep -nE "16:00" common/13-S2-定時掃帶.md
  python scripts/s2_rules_check.py; echo exit=$?
  ```
  Expected: `16:00` 只剩劃掉／已作廢段落與歷史敘述（如「2026-08-11 16:00 那輪起上線」）；rules_check exit=0。

### Task 2: prompt「只查六份」段

**Files:**
- Modify: `scripts/s2_scan_prompt.md`

- [ ] **Step 1: 定位並刪除**
  ```bash
  grep -n "只檢查前六份" scripts/s2_scan_prompt.md
  ```
  刪掉整段「⚠️ `s2_rules_check.py` 目前只檢查前六份…沒看到就用 `offset` 往下補讀」（`s2_rules_check.py` 0907 實跑已列八份）。「規則載入：13 ✅／…／13h ✅」那一行**保留**。

- [ ] **Step 2: 驗證**
  ```bash
  grep -c "只檢查前六份" scripts/s2_scan_prompt.md
  python scripts/s2_rules_check.py | tail -3
  ```
  Expected: 0；rules_check 尾行列出八個代號。

### Task 3: v7 快照與 switch 橫幅

**Files:**
- Modify: `scripts/s2_scan_prompt_v7.md`（整檔以 live 覆蓋）
- Modify: `scripts/s2_v7_switch.ps1`（L106–107 橫幅、L158–159 收尾提示）

- [ ] **Step 1: 快照重取**
  ```bash
  cp scripts/s2_scan_prompt.md scripts/s2_scan_prompt_v7.md
  diff scripts/s2_scan_prompt.md scripts/s2_scan_prompt_v7.md && echo SAME
  ```
  Expected: `SAME`。檔尾兩個 `FORK-BASE` 註解隨 live 一起帶著，不必改。

- [ ] **Step 2: 橫幅改字**
  L106 → `ENEX 輪次（13g V5-1）：01:00／04:30／07:00／17:00／22:00`
  L107 → `ABC  輪次（13h V7-1）：同 ENEX 五輪（2026-09-05 使用者裁示，09:00 已刪）`
  L158–159 的「s2_rules_check 加 13g＋13h」已完成 → 刪那一句，其餘保留。
  ⚠️ `.ps1` 含中文，存檔**必須 UTF-8 with BOM**（`feedback_ps1_utf8_bom_encoding`）。

- [ ] **Step 3: 驗證**
  ```bash
  head -c 3 scripts/s2_v7_switch.ps1 | od -c | head -1     # 應見 357 273 277（BOM）
  MSYS_NO_PATHCONV=1 pwsh -NoProfile -File scripts/s2_v7_switch.ps1
  ```
  Expected: 狀態列全 OK、橫幅無 09:00。

### Task 4: 13h V7-6「切換後才做」殘餘文字項

**Files:**
- Modify: `common/13c-S2-定時掃帶-v3省token.md`（L23）
- Modify: `common/13g-S2-定時掃帶-v5-四站.md`（L34–35、L445、L454 EOF 註解）
- Modify: `common/13e-S2-素材行與分類規則.md`／`13c2`（若仍有「ENEX／ABC 人工下令才跑」句；0907 grep 13e L235 只是來源表，可不動）

- [ ] **Step 1: 13c L23**
  「⛔ 固定輪就是這三站…人工下令才跑」→ 劃掉，加「（2026-09-07 訂正：ENEX 自 V5、ABC 自 V7 皆進固定輪，見 `13g` V5-1／`13h` V7-1）」。

- [ ] **Step 2: 13g**
  L35「ABC 的部分全部維持原樣」→ 劃掉，加「（2026-09-07 訂正：ABC 亦已由 `13h` 取代）」；L445 待辦句同樣標已完成；**L454 `RULES-EOF 13g` 那行只改括號內說明文字**，`RULES-EOF 13g` 三個 token 不動（`s2_rules_check.py` 靠它）。

- [ ] **Step 3: 驗證**
  ```bash
  grep -nE "人工下令才跑|維持原樣" common/13c-S2-定時掃帶-v3省token.md common/13g-S2-定時掃帶-v5-四站.md
  python scripts/s2_rules_check.py; echo exit=$?
  ```
  Expected: 命中處都在劃掉／訂正句內；exit=0。

### Task 5: MASTER 回寫與 commit

- [ ] **Step 1: A9／T8 索引列**
  MASTER `T8` 一句話現況改：「✅2026-09-07 `<hash>`：13 排程表改指路＋16:00→17:00；prompt 刪『只查六份』；v7 快照重取＋switch 橫幅；13c/13g 人工下令句作廢」。`A9` 現況末尾加「13h V7-6 文字收尾已於 T8 完成」。

- [ ] **Step 2: commit（只加這些檔）**
  ```bash
  git add common/13-S2-定時掃帶.md common/13c-S2-定時掃帶-v3省token.md common/13g-S2-定時掃帶-v5-四站.md scripts/s2_scan_prompt.md scripts/s2_scan_prompt_v7.md scripts/s2_v7_switch.ps1 common/plans/S2-MASTER-追蹤清單.md
  git diff --cached --stat
  git commit -m "全檢/T8：過期敘述清理（排程表指路、16:00→17:00、prompt 只查六份、v7 快照、13c/13g 人工句）"
  git push origin main
  ```
  Expected: stat 只列上述檔；push 成功。

- [ ] **Step 3: 下一個正式輪觀察**
  看該輪 transcript 的「規則載入」回報行仍為八個 ✅，無新增 `needs-review`。
