# D15 T/C 字典拆分（T＋軍事國防、C＋英國） — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans。使用者 2026-09-07 裁決：**只加這兩格**，社會／政治不拆。

**Goal:** `TC-字典.md` 新增 T「軍事國防」與 C「英國」；`13f` 兩張表同步；矩陣顯示順序補格；舊資料不回改。

**Architecture:** `s2_state._load_tc_dict()` 直接解析 `TC-字典.md` 的表格，字典改了 `set-tc` 立即認得；顯示端 `prototype/build_tc_matrix_0821.py` 有 T/C 顯示順序與 emoji 表，要補；`normalize_c()` 的三種形狀：英國走**①專項獨佔**（掛英國就不掛歐洲，比照以色列／伊朗），**不進 `C_IMPLIES`**。

**Tech Stack:** Markdown、Python。

## Global Constraints

- 只在改規則空窗改 `13f`；`TC-字典.md` 與 `13f` 表**同一個 commit**。
- 舊狀態檔不回改、不補標。
- ⛔ 不動 `normalize_t`／`normalize_c` 的既有映射。

---

### Task 1: 字典與規則表

**Files:**
- Modify: `common/plans/a10-p0-data/TC-字典.md`
- Modify: `common/13f-S2-大分類與各站規則.md`（T 表 L~172、C 表 L~230）

- [ ] **Step 1: T 表加一列**（放在「地緣衝突」之後）
  `| 軍事國防 | 非戰爭的軍事新聞：演習、閱兵、軍購軍售、國防預算、部隊部署、軍事科技展示 | 進行中的熱戰／軍事對峙→烏俄、美伊、地緣衝突 |`
- [ ] **Step 2: C 表加一列**（放在「歐洲」之前）
  `| 英國 | 英國（含北愛）；**掛英國就不掛歐洲**（專項獨佔，同以色列／伊朗） |`
  歐洲那列改成「歐盟、瑞士、歐陸（不含烏克蘭、英國）」。
- [ ] **Step 3: 字典下方「三種形狀」①專項獨佔的例子加「英國」**；T 表下方註記「11 類→12 類（2026-09-07 使用者裁決 D15）」。
- [ ] **Step 4: `13f` 兩張表逐字同步**（13f 的表與字典內容相同、只是多前言），並在 T／C 表標題旁加「（2026-09-07 加軍事國防／英國）」。
- [ ] **Step 5: 驗證解析**
  ```bash
  python -X utf8 -c "import sys;sys.path.insert(0,'scripts');import s2_state as S;T,C=S._load_tc_dict();print(len(T),T);print(len(C),C)"
  python scripts/s2_rules_check.py; echo exit=$?
  ```
  Expected: T 12 個含 `軍事國防`、C 22 個含 `英國`；exit=0。

### Task 2: 矩陣顯示順序

**Files:**
- Modify: `scripts/prototype/build_tc_matrix_0821.py`（T／C 顯示順序、emoji 表）
- Read: `scripts/s2_render_matrix.py`（`_FALLBACK_PATCHES` 只 patch 兜底分支，不受影響）

- [ ] **Step 1**: `grep -nE "T_ORDER|C_ORDER|EMOJI|烏俄|歐洲" scripts/prototype/build_tc_matrix_0821.py` 找到順序表；`軍事國防` 插在「地緣衝突」後、`英國` 插在「歐洲」前，各配一個 emoji（🪖／🇬🇧）。
- [ ] **Step 2**: `tag_tc()` 關鍵詞兜底**可選**補：`英國|倫敦|首相`→英國（歐洲兜底維持）；`軍演|閱兵|軍購|國防部`→軍事國防。⚠️ 兜底只影響「沒下 T/C」的則，A31 之前不必求全。
- [ ] **Step 3: 渲染驗證**
  ```bash
  python -X utf8 scripts/s2_render.py --file "G:/我的雲端硬碟/Claude共用/自動掃帶系統/0906-s2-state.json" 2>&1 | tail -5
  ```
  Expected: 無 KeyError；矩陣頁多出兩欄（0 則亦顯示或按既有規則隱藏，照 BASE 現行行為）。

### Task 3: 測試與 commit

- [ ] **Step 1**: 臨時狀態檔測 `set-tc --pairs "X=軍事國防/英國"` → OK；`"X=政治/英國,歐洲"` → OK 但印提醒「英國專項獨佔，歐洲多餘」（若 Task 2 沒做提醒，這一條列為已知限制，不阻斷）。
- [ ] **Step 2**: `ls scripts/test_s2_*.py | xargs -I{} python -X utf8 {}` 無新增失敗。
- [ ] **Step 3**:
  ```bash
  git add common/plans/a10-p0-data/TC-字典.md common/13f-S2-大分類與各站規則.md scripts/prototype/build_tc_matrix_0821.py common/plans/S2-MASTER-追蹤清單.md
  git commit -m "全檢/D15：T 加軍事國防、C 加英國（專項獨佔）；13f 表同步；矩陣顯示順序"
  git push origin main
  ```
  MASTER `D15` 現況加「✅落地 `<hash>`」。
- [ ] **Step 4: 下一輪驗收**：transcript 中 `set-tc` 對英國／軍事題材的素材有下新格；`tc_rejected` 無「不在字典」的新格名稱。
