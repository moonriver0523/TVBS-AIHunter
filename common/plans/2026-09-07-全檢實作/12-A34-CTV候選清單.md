# A34 CTV 候選清單 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:test-driven-development。獨立、唯讀；A33 稿單提案會吃它的 json，所以先做。

**Goal:** `scripts/s2_ctv_candidates.py --file <狀態檔> [--out <dir>]`：用狀態檔既有欄位機械篩出「可做 CTV」的 NS 素材，輸出 `{MMDD}-CTV候選.txt`（給使用者勾選）＋同名 `.json`（給 A33）。

**Architecture:** 純讀取。判準（全檢 §11）：`source=="NS"` ∧ `script_status=="has_script"` ∧ `len(src_text)>=500` ∧ `fields.no_bite==False` ∧ `60s<=duration<=210s` ∧ notes 無限制字（`限用|限非|僅授權|不得|禁止|embargo`）。排序：🔴>🟡>🔖>其餘，涉臺（`tc.C` 含臺灣）前置，🟤 沉底。

**Tech Stack:** Python；`s2_render.load_state`、`s2_parse` 欄位。

---

### Task 1: 失敗測試

**Files:**
- Create: `scripts/test_s2_ctv_candidates.py`

- [ ] 合成 6 則：①合格（🟡、2 BITE、01:32）②AP 來源→排除 ③NS 無 BITE→排除 ④NS 04:10→排除 ⑤NS notes「限用社群」→排除 ⑥NS 合格＋🔴＋C 臺灣→應排第一。
  斷言：json 兩則、順序 ⑥①；txt 每行格式 `序｜id｜標記｜題目(notes 首項)｜時長｜BITE 數｜限制｜C`。
- [ ] 跑 → FAIL。

### Task 2: 實作

**Files:**
- Create: `scripts/s2_ctv_candidates.py`

- [ ] `is_candidate(it) -> (bool, reason)`；`rank_key(it)`；`main()` 印摘要「NS N 則 → 候選 M 則（排除：無 BITE a／時長 b／限制 c／稿未全 d）」，寫 txt＋json 到 `--out`（預設狀態檔同目錄 `_稿單提案/`，不存在就建）。
- [ ] 時長解析：`fields.duration` 為 `MM:SS`；解析失敗視為不合格並計入「時長」排除。
- [ ] 測試 PASS。

### Task 3: 真實資料校準

- [ ] `python -X utf8 scripts/s2_ctv_candidates.py --file "<0906 狀態檔>"` → 預期候選 ≤14；人工抽 3 則對照 `cnn/01` 寫稿條件（有 ≡Q 全文、有 SOT）；若明顯漏掉合格則（例如 `src_text` 500 門檻太高），調門檻並記在檔頭常數註解。
- [ ] 全套測試 PASS。

### Task 4: 上線與驗收

- [ ] commit：`全檢/A34：CTV 候選清單（機械篩＋排序）`；push；MASTER `A34` 現況更新。
- [ ] 使用者用一週：每天看 `{MMDD}-CTV候選.txt`，回報「該列沒列／不該列有列」各幾則 → 調門檻；A33 上線後改由 A33 的排程一起產出。
