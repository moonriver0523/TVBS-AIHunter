# 側錄初處理工作台 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在掐BITE 工作台同一網站加「側錄初處理」分頁：不跑 ASR，用靜音＋音量＋下標 OCR 切段，人核對後剪成獨立檔，TC 加檔名 6 碼 offset。

**Architecture:** 新邏輯全在 `scripts/side_precut.py`（可單測、ffmpeg／OCR 可注入）。`bite_workbench.py` 只加背景 job 路由；`bite_workbench.html` 同一檔加分頁殼與版面 B。旁路 `{stem} PRECUT.json`。掐BITE 轉錄／翻譯／SB 路徑零改動。

**Tech Stack:** Python 3.11（Hermes venv）、unittest、ffmpeg、RapidOCR onnx、既有 FastAPI／uvicorn、`sb_format.fmt_tc`／`offset_from_filename`、`scan_dir`。

**規格：** `common/plans/2026-08-28-側錄初處理工作台-design.md`

## Global Constraints

- 不跑 Whisper／任何 ASR、不跑全片 scene_changes、不呼叫視覺 LLM。
- 不改 `scripts/sb_format.py`、不改 `common/07-bite-assistant.md`。
- Offset 用 `Material.offset`（`scan_dir` 的 `SIDE_PREFIX_RE` 路徑）。禁止對完整檔名 `CNN 075658 ….mp4` 只呼叫 `offset_from_filename`（會得到 0）。
- `t0`／`t1` 永遠是檔內相對秒；顯示／檔名的 6 碼母帶 TC 才加 offset。
- `kind` 五值：`ad` 廣告、`anchor` 主播、`reporter` 記者、`interview` 訪問、`other` 其他。
- 輸出：`{dir}/{stem} 初處理\{來源} {tc0}-{tc1} {類型中文} {主題}.mp4`；廣告預設不輸出。
- 預設 ffmpeg `-c copy`；`accurate=True` 才重編碼。
- 測試：`python scripts/test_side_precut.py`（假資料）。瀏覽器 v1 只必過 `D:\TVBS\CNN\CNN 075658 0817 增強藥專題.mp4`。
- Python 就是 Hermes venv 的 `python`。ffmpeg 已在 PATH。
- 每個 task 結束才 commit；不要把 `D:\TVBS\CNN` 影片加進 git。

---

## File map

| 檔案 | 職責 |
|---|---|
| `scripts/side_precut.py` | 切段、標類型、快取、組 ffmpeg 剪檔命令、檔名 |
| `scripts/test_side_precut.py` | 上述機械行為（unittest，無真實 500MB） |
| `scripts/bite_workbench.py` | `/api/precut*` 路由＋背景 job；`scan_dir`／`/media` 不動語意 |
| `scripts/assets/bite_workbench.html` | 頁頂分頁＋側錄初處理版面 B |
| `scripts/test_bite_workbench.py` | 只加「scan_dir 不被這次改壞」若有碰到；否則不動 |
| `scripts/sb_format.py` | 禁止修改 |

---

### Task 1: 資料結構、母帶 TC、輸出檔名

**Files:**
- Create: `scripts/side_precut.py`
- Test: `scripts/test_side_precut.py`

**Interfaces:**
- Consumes: `sb_format.fmt_tc`, `sb_format.offset_from_filename`（只對純 6 碼字串）
- Produces:
  - `KINDS: dict[str, str]`
  - `@dataclass PrecutSeg` with `id, t0, t1, kind, topic, ocr, selected`
  - `master_tc(t: float, offset_sec: int) -> str`
  - `seg_to_dict(seg, offset_sec) -> dict`（含 `tc0`/`tc1`）
  - `sanitize_filename_part(s: str) -> str`
  - `export_filename(source, seg, offset_sec) -> str`
  - `export_dir(video_path: str) -> str`

- [ ] **Step 1: Write the failing test**

Create `scripts/test_side_precut.py`:

```python
# -*- coding: utf-8 -*-
import os
import sys
import tempfile
import unittest

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
import side_precut as P  # noqa: E402
import bite_workbench as W  # noqa: E402


class TestKinds(unittest.TestCase):
    def test_五類中英對照齊(self):
        self.assertEqual(P.KINDS, {
            "ad": "廣告", "anchor": "主播", "reporter": "記者",
            "interview": "訪問", "other": "其他",
        })


class TestMasterTc(unittest.TestCase):
    def test_檔內秒加offset成六碼(self):
        off = 7 * 3600 + 56 * 60 + 58  # 075658
        self.assertEqual(P.master_tc(0, off), "075658")
        self.assertEqual(P.master_tc(66, off), "075804")  # +1:06


class TestScanDirOffsetNotNaive(unittest.TestCase):
    """規格硬規定：不可對完整『CNN 075658 …』檔名只呼叫 offset_from_filename。"""
    def test_scan_dir給正確offset(self):
        d = tempfile.mkdtemp()
        self.addCleanup(lambda: __import__("shutil").rmtree(d, ignore_errors=True))
        open(os.path.join(d, "CNN 075658 0817 增強藥專題.mp4"), "wb").close()
        m = W.scan_dir(d)[0]
        self.assertEqual(m.offset, 7 * 3600 + 56 * 60 + 58)
        self.assertEqual(m.source, "CNN")


class TestExportName(unittest.TestCase):
    def test_檔名含來源母帶TC類型主題(self):
        seg = P.PrecutSeg(id="s1", t0=66.0, t1=120.0, kind="anchor", topic="增強藥")
        off = 7 * 3600 + 56 * 60 + 58
        self.assertEqual(
            P.export_filename("CNN", seg, off),
            "CNN 075804-075858 主播 增強藥.mp4",
        )

    def test_主題空白不留多餘空白(self):
        seg = P.PrecutSeg(id="s1", t0=0, t1=10, kind="ad", topic="")
        name = P.export_filename("CNN", seg, 0)
        self.assertEqual(name, "CNN 000000-000010 廣告.mp4")
        self.assertNotIn("  .", name)

    def test_檔名非法字元清掉(self):
        self.assertEqual(P.sanitize_filename_part('A/B:C*?"<>|x'), "ABC x")

    def test_輸出資料夾是檔名加初處理(self):
        p = r"D:\TVBS\CNN\CNN 075658 0817 增強藥專題.mp4"
        self.assertEqual(
            P.export_dir(p),
            r"D:\TVBS\CNN\CNN 075658 0817 增強藥專題 初處理",
        )


if __name__ == "__main__":
    unittest.main(verbosity=2)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python scripts/test_side_precut.py`

Expected: `ModuleNotFoundError: No module named 'side_precut'`

- [ ] **Step 3: Write minimal implementation**

Create `scripts/side_precut.py`:

```python
# -*- coding: utf-8 -*-
"""側錄初處理：靜音切段＋字卡 OCR＋剪檔。規格見
`common/plans/2026-08-28-側錄初處理工作台-design.md`。"""
from __future__ import annotations

import os
import re
from dataclasses import asdict, dataclass, field

import sb_format as SBF

KINDS = {
    "ad": "廣告",
    "anchor": "主播",
    "reporter": "記者",
    "interview": "訪問",
    "other": "其他",
}

ILLEGAL_FS = re.compile(r'[\\/:*?"<>|]+')


@dataclass
class PrecutSeg:
    id: str
    t0: float
    t1: float
    kind: str = "other"
    topic: str = ""
    ocr: str = ""
    selected: bool = True


def master_tc(t: float, offset_sec: int) -> str:
    return SBF.fmt_tc(t + offset_sec, 6)


def seg_to_dict(seg: PrecutSeg, offset_sec: int) -> dict:
    d = asdict(seg)
    d["tc0"] = master_tc(seg.t0, offset_sec)
    d["tc1"] = master_tc(seg.t1, offset_sec)
    d["duration"] = round(seg.t1 - seg.t0, 2)
    return d


def sanitize_filename_part(s: str) -> str:
    s = ILLEGAL_FS.sub("", s)
    return re.sub(r"\s+", " ", s).strip()


def export_filename(source: str, seg: PrecutSeg, offset_sec: int) -> str:
    src = sanitize_filename_part(source or "SIDE")
    kind_zh = KINDS.get(seg.kind, seg.kind)
    topic = sanitize_filename_part(seg.topic)
    tc = f"{master_tc(seg.t0, offset_sec)}-{master_tc(seg.t1, offset_sec)}"
    parts = [src, tc, kind_zh]
    if topic:
        parts.append(topic)
    return " ".join(parts) + ".mp4"


def export_dir(video_path: str) -> str:
    parent, name = os.path.split(video_path)
    stem, _ = os.path.splitext(name)
    return os.path.join(parent, stem + " 初處理")
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python scripts/test_side_precut.py`

Expected: 全部 PASS。若 `sanitize` 測例因你清掉非法字後空格不同而失敗，把測例期望改成實作的單一空格規則（測例已要求 `A/B:C*?"<>|x` → `ABC x`：非法字刪除、殘空白壓縮）。

- [ ] **Step 5: Commit**

```bash
git add scripts/side_precut.py scripts/test_side_precut.py
git commit -m "S7 側錄初處理：段資料、母帶 TC、輸出檔名"
```

---

### Task 2: 靜音骨架與氣口合併

**Files:**
- Modify: `scripts/side_precut.py`
- Test: `scripts/test_side_precut.py`

**Interfaces:**
- Consumes: `PrecutSeg`
- Produces:
  - `BREATH_MAX = 1.5`（秒，短於這個的靜音併回）
  - `parse_silencedetect(stderr: str) -> list[tuple[float, float]]`  # (silence_start, silence_end)
  - `speech_blocks(duration: float, silences: list[tuple[float,float]], breath_max: float = BREATH_MAX) -> list[tuple[float,float]]`

- [ ] **Step 1: Write the failing test**

Append to `scripts/test_side_precut.py`:

```python
FFMPEG_SILENCE = """
[silencedetect @ 000] silence_start: 10.0
[silencedetect @ 000] silence_end: 10.8 | silence_duration: 0.8
[silencedetect @ 000] silence_start: 40.0
[silencedetect @ 000] silence_end: 45.0 | silence_duration: 5.0
[silencedetect @ 000] silence_start: 90.0
[silencedetect @ 000] silence_end: 91.2 | silence_duration: 1.2
"""


class TestSilence(unittest.TestCase):
    def test_parse兩端時間(self):
        sil = P.parse_silencedetect(FFMPEG_SILENCE)
        self.assertEqual(sil, [(10.0, 10.8), (40.0, 45.0), (90.0, 91.2)])

    def test_短氣口合併長靜音切開(self):
        # duration=100；0.8s 與 1.2s 氣口併掉，5s 切開
        blocks = P.speech_blocks(100.0, [(10.0, 10.8), (40.0, 45.0), (90.0, 91.2)])
        self.assertEqual(len(blocks), 2)
        self.assertAlmostEqual(blocks[0][0], 0.0)
        self.assertAlmostEqual(blocks[0][1], 40.0)
        self.assertAlmostEqual(blocks[1][0], 45.0)
        self.assertAlmostEqual(blocks[1][1], 100.0)

    def test_沒有靜音就是整段(self):
        self.assertEqual(P.speech_blocks(50.0, []), [(0.0, 50.0)])
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python scripts/test_side_precut.py -k TestSilence`

Expected: FAIL `AttributeError: parse_silencedetect`

- [ ] **Step 3: Write minimal implementation**

Append to `scripts/side_precut.py`:

```python
BREATH_MAX = 1.5
SILENCE_START_RE = re.compile(r"silence_start:\s*([0-9.]+)")
SILENCE_END_RE = re.compile(r"silence_end:\s*([0-9.]+)")


def parse_silencedetect(stderr: str) -> list[tuple[float, float]]:
    starts, ends = [], []
    for line in stderr.splitlines():
        m = SILENCE_START_RE.search(line)
        if m:
            starts.append(float(m.group(1)))
            continue
        m = SILENCE_END_RE.search(line)
        if m:
            ends.append(float(m.group(1)))
    out = []
    for i, s in enumerate(starts):
        e = ends[i] if i < len(ends) else s
        out.append((s, e))
    return out


def speech_blocks(
    duration: float,
    silences: list[tuple[float, float]],
    breath_max: float = BREATH_MAX,
) -> list[tuple[float, float]]:
    if duration <= 0:
        return []
    long_sil = [(s, e) for s, e in silences if (e - s) >= breath_max]
    if not long_sil:
        return [(0.0, duration)]
    blocks = []
    t = 0.0
    for s, e in long_sil:
        if s > t:
            blocks.append((t, s))
        t = max(t, e)
    if t < duration:
        blocks.append((t, duration))
    return [(a, b) for a, b in blocks if b - a >= 0.2]
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python scripts/test_side_precut.py -k TestSilence`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add scripts/side_precut.py scripts/test_side_precut.py
git commit -m "S7 側錄初處理：靜音骨架與氣口合併"
```

---

### Task 3: 類型啟發式＋相鄰主題合併

**Files:**
- Modify: `scripts/side_precut.py`
- Test: `scripts/test_side_precut.py`

**Interfaces:**
- Produces:
  - `RMS_AD = 0.25`（相對 peak 的門檻，可實調常數，測例鎖這個值）
  - `classify_kind(*, rms: float, ocr: str, duration: float, near_black: bool) -> str`
  - `merge_topic_runs(segs: list[PrecutSeg]) -> list[PrecutSeg]`（相鄰非廣告、topic 正規化後相同 → 合併；id 重編 `s1…`）
  - `norm_topic(s: str) -> str`

- [ ] **Step 1: Write the failing test**

```python
class TestClassify(unittest.TestCase):
    def test_無字高音量偏廣告(self):
        self.assertEqual(
            P.classify_kind(rms=0.4, ocr="", duration=20, near_black=False),
            "ad",
        )

    def test_無字但兩頭黑也偏廣告(self):
        self.assertEqual(
            P.classify_kind(rms=0.05, ocr="", duration=25, near_black=True),
            "ad",
        )

    def test_有字卡預設主播(self):
        self.assertEqual(
            P.classify_kind(rms=0.1, ocr="BREAKING NEWS", duration=40, near_black=False),
            "anchor",
        )

    def test_記者關鍵字(self):
        self.assertEqual(
            P.classify_kind(rms=0.1, ocr="CNN's Jane Doe", duration=30, near_black=False),
            "reporter",
        )

    def test_其餘無字是other(self):
        self.assertEqual(
            P.classify_kind(rms=0.05, ocr="", duration=8, near_black=False),
            "other",
        )


class TestMergeTopics(unittest.TestCase):
    def test_相鄰同主題非廣告合併(self):
        segs = [
            P.PrecutSeg("s1", 0, 10, "anchor", topic="增強藥", ocr="增強藥"),
            P.PrecutSeg("s2", 10, 20, "anchor", topic="增強藥", ocr="增強藥"),
            P.PrecutSeg("s3", 20, 30, "ad", topic=""),
        ]
        out = P.merge_topic_runs(segs)
        self.assertEqual(len(out), 2)
        self.assertEqual((out[0].t0, out[0].t1, out[0].kind), (0, 20, "anchor"))
        self.assertEqual(out[1].kind, "ad")

    def test_廣告不與兩邊合併(self):
        segs = [
            P.PrecutSeg("a", 0, 10, "anchor", topic="A"),
            P.PrecutSeg("b", 10, 20, "ad", topic="A"),
            P.PrecutSeg("c", 20, 30, "anchor", topic="A"),
        ]
        self.assertEqual(len(P.merge_topic_runs(segs)), 3)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python scripts/test_side_precut.py -k TestClassify`

Expected: FAIL `classify_kind` 不存在

- [ ] **Step 3: Write minimal implementation**

```python
RMS_AD = 0.25
REPORTER_RE = re.compile(r"\bCNN'?s\b|\breport(er|ing)\b|特派|記者", re.I)
INTERVIEW_RE = re.compile(r"\b(analyst|professor|minister|official)\b|訪問|專家", re.I)


def norm_topic(s: str) -> str:
    return re.sub(r"\s+", " ", (s or "")).strip().lower()


def classify_kind(*, rms: float, ocr: str, duration: float, near_black: bool) -> str:
    text = (ocr or "").strip()
    if not text and duration >= 8 and (rms >= RMS_AD or near_black):
        return "ad"
    if INTERVIEW_RE.search(text):
        return "interview"
    if REPORTER_RE.search(text):
        return "reporter"
    if text:
        return "anchor"
    return "other"


def merge_topic_runs(segs: list[PrecutSeg]) -> list[PrecutSeg]:
    if not segs:
        return []
    out = [PrecutSeg(**{**asdict(segs[0])})]
    for s in segs[1:]:
        prev = out[-1]
        same = (
            prev.kind != "ad"
            and s.kind != "ad"
            and prev.kind == s.kind
            and norm_topic(prev.topic) != ""
            and norm_topic(prev.topic) == norm_topic(s.topic)
        )
        if same:
            prev.t1 = s.t1
            continue
        out.append(PrecutSeg(**{**asdict(s)}))
    for i, s in enumerate(out, 1):
        s.id = f"s{i}"
        s.selected = s.kind != "ad"
    return out
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python scripts/test_side_precut.py`

Expected: 全綠

- [ ] **Step 5: Commit**

```bash
git add scripts/side_precut.py scripts/test_side_precut.py
git commit -m "S7 側錄初處理：類型啟發式與主題合併"
```

---

### Task 4: 快取 JSON、分析管線（ffmpeg／OCR 可注入）、剪檔命令

**Files:**
- Modify: `scripts/side_precut.py`
- Test: `scripts/test_side_precut.py`

**Interfaces:**
- Produces:
  - `cache_path(video_path) -> str`  → `{stem} PRECUT.json`
  - `save_cache(video_path, offset_sec, segs, mtime) -> str`
  - `load_cache(video_path) -> dict | None`
  - `cache_stale(video_path, cache: dict) -> bool`
  - `waveform_points(rms_series: list[float], buckets: int = 800) -> list[float]`
  - `analyze(video_path, *, offset_sec, duration, run_silence, rms_of, ocr_of, black_of, on_phase=None) -> dict`
  - `cut_cmd(video_path, seg, dest, *, accurate: bool) -> list[str]`

`run_silence(path) -> str` 回 ffmpeg stderr。  
`rms_of(t0,t1) -> float`、`ocr_of(t_mid) -> str`、`black_of(t0,t1) -> bool` 測試注入。

- [ ] **Step 1: Write the failing test**

```python
class TestCacheAndAnalyze(unittest.TestCase):
    def setUp(self):
        self.d = tempfile.mkdtemp()
        self.addCleanup(lambda: __import__("shutil").rmtree(self.d, ignore_errors=True))
        self.vid = os.path.join(self.d, "CNN 075658 0817 增強藥專題.mp4")
        open(self.vid, "wb").close()

    def test_旁路檔名(self):
        self.assertTrue(P.cache_path(self.vid).endswith("CNN 075658 0817 增強藥專題 PRECUT.json"))

    def test_存讀往返(self):
        segs = [P.PrecutSeg("s1", 0, 10, "anchor", topic="X")]
        P.save_cache(self.vid, 28618, segs, mtime=1)
        data = P.load_cache(self.vid)
        self.assertEqual(data["offset_sec"], 28618)
        self.assertEqual(data["segments"][0]["topic"], "X")
        self.assertEqual(data["segments"][0]["tc0"], "075658")

    def test_mtime更新算stale(self):
        P.save_cache(self.vid, 0, [], mtime=1)
        self.assertTrue(P.cache_stale(self.vid, {"mtime": 1, "segments": []}))
        # 檔案 mtime 一定 ≥1；明確用比較：cache mtime 小於真實 mtime → stale
        cache = P.load_cache(self.vid)
        cache["mtime"] = 0
        self.assertTrue(P.cache_stale(self.vid, cache))

    def test_分析用注入假靜音與OCR(self):
        stderr = (
            "[silencedetect] silence_start: 20.0\n"
            "[silencedetect] silence_end: 28.0 | silence_duration: 8.0\n"
        )
        def ocr_of(t):
            return "" if t >= 24 else "BREAKING"
        out = P.analyze(
            self.vid, offset_sec=28618, duration=60.0,
            run_silence=lambda p: stderr,
            rms_of=lambda a, b: 0.4 if a >= 20 else 0.05,
            ocr_of=ocr_of,
            black_of=lambda a, b: False,
        )
        kinds = [s["kind"] for s in out["segments"]]
        self.assertIn("anchor", kinds)
        self.assertIn("ad", kinds)
        self.assertEqual(out["offset_sec"], 28618)

    def test_copy與精準命令(self):
        seg = P.PrecutSeg("s1", 10.0, 20.0, "anchor")
        copy = P.cut_cmd("in.mp4", seg, "out.mp4", accurate=False)
        self.assertIn("-c", copy)
        self.assertIn("copy", copy)
        acc = P.cut_cmd("in.mp4", seg, "out.mp4", accurate=True)
        self.assertTrue(any(x in acc for x in ("libx264", "crf")))
        self.assertNotIn("copy", acc)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python scripts/test_side_precut.py -k TestCacheAndAnalyze`

Expected: FAIL 缺函式

- [ ] **Step 3: Write minimal implementation**

```python
import json
import time as _time


def cache_path(video_path: str) -> str:
    stem, _ = os.path.splitext(video_path)
    return stem + " PRECUT.json"


def save_cache(video_path: str, offset_sec: int, segs: list[PrecutSeg], mtime: float | None = None) -> str:
    path = cache_path(video_path)
    if mtime is None:
        mtime = os.path.getmtime(video_path)
    payload = {
        "source": os.path.basename(video_path),
        "offset_sec": offset_sec,
        "mtime": mtime,
        "segments": [seg_to_dict(s, offset_sec) for s in segs],
    }
    with open(path, "w", encoding="utf-8") as f:
        json.dump(payload, f, ensure_ascii=False, indent=2)
    return path


def load_cache(video_path: str) -> dict | None:
    path = cache_path(video_path)
    if not os.path.isfile(path):
        return None
    with open(path, encoding="utf-8") as f:
        return json.load(f)


def cache_stale(video_path: str, cache: dict) -> bool:
    try:
        return float(cache.get("mtime") or 0) < os.path.getmtime(video_path) - 0.01
    except OSError:
        return True


def dicts_to_segs(rows: list[dict]) -> list[PrecutSeg]:
    out = []
    for i, r in enumerate(rows, 1):
        out.append(PrecutSeg(
            id=r.get("id") or f"s{i}",
            t0=float(r["t0"]), t1=float(r["t1"]),
            kind=r.get("kind") or "other",
            topic=r.get("topic") or "",
            ocr=r.get("ocr") or "",
            selected=bool(r["selected"]) if "selected" in r else (r.get("kind") != "ad"),
        ))
    return out


def waveform_points(rms_series: list[float], buckets: int = 800) -> list[float]:
    if not rms_series:
        return [0.0] * buckets
    n = len(rms_series)
    out = []
    for i in range(buckets):
        a = int(i * n / buckets)
        b = max(a + 1, int((i + 1) * n / buckets))
        out.append(max(rms_series[a:b]))
    return out


def analyze(video_path: str, *, offset_sec: int, duration: float,
            run_silence, rms_of, ocr_of, black_of, on_phase=None) -> dict:
    def phase(msg: str):
        if on_phase:
            on_phase(msg)
    phase("靜音偵測")
    sil = parse_silencedetect(run_silence(video_path))
    blocks = speech_blocks(duration, sil)
    segs: list[PrecutSeg] = []
    for i, (a, b) in enumerate(blocks, 1):
        phase(f"標段 {i}/{len(blocks)}")
        ocr = ocr_of((a + b) / 2) or ""
        kind = classify_kind(
            rms=float(rms_of(a, b)), ocr=ocr, duration=b - a,
            near_black=bool(black_of(a, b)),
        )
        segs.append(PrecutSeg(
            id=f"s{i}", t0=a, t1=b, kind=kind,
            topic=ocr.replace("\n", " ").strip(), ocr=ocr,
            selected=kind != "ad",
        ))
    segs = merge_topic_runs(segs)
    mtime = os.path.getmtime(video_path) if os.path.isfile(video_path) else 0
    save_cache(video_path, offset_sec, segs, mtime=mtime)
    return {
        "source": os.path.basename(video_path),
        "offset_sec": offset_sec,
        "mtime": mtime,
        "stale": False,
        "segments": [seg_to_dict(s, offset_sec) for s in segs],
    }


def cut_cmd(video_path: str, seg: PrecutSeg, dest: str, *, accurate: bool) -> list[str]:
    ss = f"{seg.t0:.3f}"
    to = f"{seg.t1:.3f}"
    if accurate:
        return [
            "ffmpeg", "-y", "-ss", ss, "-to", to, "-i", video_path,
            "-c:v", "libx264", "-preset", "veryfast", "-crf", "18",
            "-c:a", "aac", dest,
        ]
    return [
        "ffmpeg", "-y", "-i", video_path, "-ss", ss, "-to", to,
        "-c", "copy", "-avoid_negative_ts", "make_zero", dest,
    ]
```

注意：copy 模式 `-ss` 放 `-i` 之後較準（仍吸關鍵影格）；精準模式 `-ss` 放 `-i` 之前加速 decode。測例只檢查有無 `copy`／`libx264`。

- [ ] **Step 4: Run test to verify it passes**

Run: `python scripts/test_side_precut.py`

Expected: 全綠。`test_mtime更新算stale` 若剛寫入的檔 mtime 等於 cache 而不 stale，以「把 cache mtime 改 0」那支為準。

- [ ] **Step 5: Commit**

```bash
git add scripts/side_precut.py scripts/test_side_precut.py
git commit -m "S7 側錄初處理：PRECUT 快取、可注入分析、剪檔命令"
```

---

### Task 5: 真實 ffmpeg／RapidOCR 適配器

**Files:**
- Modify: `scripts/side_precut.py`
- Test: `scripts/test_side_precut.py`（只測「沒裝 RapidOCR 時 `require_ocr()` 丟什麼」；不在 CI 跑 65 分鐘片）

**Interfaces:**
- Produces:
  - `OCR_IMPORT_HINT = "pip install rapidocr-onnxruntime"`
  - `require_ocr() -> None`  （沒裝 raise `FileNotFoundError`，訊息含 HINT）
  - `ffprobe_duration(path) -> float`
  - `run_silence_ffmpeg(path) -> str`
  - `rms_ffmpeg(path, t0, t1) -> float`
  - `black_ffmpeg(path, t0, t1) -> bool`
  - `ocr_at(path, t_mid) -> str`  （抽幀、裁下方 28%、RapidOCR；OpenCC 有就 s2twp）
  - `analyze_file(path, offset_sec, on_phase=None) -> dict`  （串上述真實適配器）
  - `export_segs(video_path, segs, *, mode, accurate, source, offset_sec, run=subprocess.run) -> list[str]`

- [ ] **Step 1: Write the failing test**

```python
class TestAdapters(unittest.TestCase):
    def test_沒裝OCR時require會說明怎麼裝(self):
        # 用注入：把 _ocr_mod 設成 None
        old = getattr(P, "_ocr_mod", "missing")
        P._ocr_mod = None
        self.addCleanup(lambda: setattr(P, "_ocr_mod", old) if old != "missing" else None)
        with self.assertRaises(FileNotFoundError) as ctx:
            P.require_ocr()
        self.assertIn("rapidocr-onnxruntime", str(ctx.exception))

    def test_export非廣告略過ad(self):
        calls = []
        def fake_run(cmd, **kw):
            calls.append(cmd)
            class R:
                returncode = 0
                stderr = ""
            return R()
        d = tempfile.mkdtemp()
        self.addCleanup(lambda: __import__("shutil").rmtree(d, ignore_errors=True))
        vid = os.path.join(d, "CNN 075658 x.mp4")
        open(vid, "wb").close()
        segs = [
            P.PrecutSeg("s1", 0, 10, "ad", selected=False),
            P.PrecutSeg("s2", 10, 20, "anchor", topic="主題", selected=True),
        ]
        paths = P.export_segs(
            vid, segs, mode="non_ad", accurate=False,
            source="CNN", offset_sec=0, run=fake_run,
        )
        self.assertEqual(len(paths), 1)
        self.assertTrue(paths[0].endswith("CNN 000010-000020 主播 主題.mp4"))
        self.assertEqual(len(calls), 1)

    def test_export只出勾選(self):
        calls = []
        def fake_run(cmd, **kw):
            calls.append(cmd)
            class R:
                returncode = 0
            return R()
        d = tempfile.mkdtemp()
        self.addCleanup(lambda: __import__("shutil").rmtree(d, ignore_errors=True))
        vid = os.path.join(d, "a.mp4")
        open(vid, "wb").close()
        segs = [
            P.PrecutSeg("s1", 0, 5, "anchor", selected=False),
            P.PrecutSeg("s2", 5, 9, "ad", selected=True),
        ]
        paths = P.export_segs(
            vid, segs, mode="selected", accurate=True,
            source="CNN", offset_sec=0, run=fake_run,
        )
        self.assertEqual(len(paths), 1)
        self.assertIn("廣告", paths[0])
        self.assertIn("libx264", calls[0])
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python scripts/test_side_precut.py -k TestAdapters`

Expected: FAIL 缺 `require_ocr`／`export_segs`

- [ ] **Step 3: Write adapters**

```python
import subprocess
import tempfile
import shutil

_ocr_mod = "unset"
OCR_IMPORT_HINT = "pip install rapidocr-onnxruntime"
CROP_BOTTOM = 0.28  # CNN 720x480 下方約 28%


def require_ocr() -> None:
    global _ocr_mod
    if _ocr_mod is None:
        raise FileNotFoundError(
            f"沒有 RapidOCR，無法分析字卡。安裝：{OCR_IMPORT_HINT}"
        )
    if _ocr_mod == "unset":
        try:
            from rapidocr_onnxruntime import RapidOCR  # type: ignore
            _ocr_mod = RapidOCR
        except ImportError:
            _ocr_mod = None
            raise FileNotFoundError(
                f"沒有 RapidOCR，無法分析字卡。安裝：{OCR_IMPORT_HINT}"
            )


def _run(cmd: list[str], timeout: int = 3600) -> subprocess.CompletedProcess:
    return subprocess.run(
        cmd, capture_output=True, text=True, timeout=timeout,
        encoding="utf-8", errors="replace",
    )


def ffprobe_duration(path: str) -> float:
    r = _run([
        "ffprobe", "-v", "error", "-show_entries", "format=duration",
        "-of", "default=nk=1:nw=1", path,
    ])
    if r.returncode:
        raise RuntimeError(r.stderr.strip() or "ffprobe 失敗")
    return float(r.stdout.strip())


def run_silence_ffmpeg(path: str) -> str:
    # 閾值可實調；規格允許不寫進規則文件
    r = _run([
        "ffmpeg", "-hide_banner", "-nostats", "-i", path,
        "-af", "silencedetect=noise=-30dB:d=0.6", "-f", "null", "-",
    ])
    return (r.stderr or "") + (r.stdout or "")


def rms_ffmpeg(path: str, t0: float, t1: float) -> float:
    dur = max(0.05, t1 - t0)
    r = _run([
        "ffmpeg", "-hide_banner", "-nostats",
        "-ss", f"{t0:.3f}", "-t", f"{dur:.3f}", "-i", path,
        "-af", "volumedetect", "-f", "null", "-",
    ])
    m = re.search(r"max_volume:\s*([-\d.]+)\s*dB", r.stderr or "")
    if not m:
        return 0.0
    db = float(m.group(1))
    # 0 dB → 1.0；-20 dB → ~0.1。廣告啟發式用相對值。
    return max(0.0, min(1.0, 10 ** (db / 20)))


def black_ffmpeg(path: str, t0: float, t1: float) -> bool:
    dur = max(0.05, min(2.0, t1 - t0))
    r = _run([
        "ffmpeg", "-hide_banner", "-nostats",
        "-ss", f"{t0:.3f}", "-t", f"{dur:.3f}", "-i", path,
        "-vf", "blackdetect=d=0.2:pix_th=0.10", "-f", "null", "-",
    ])
    return "black_start" in (r.stderr or "")


def _maybe_opencc(text: str) -> str:
    try:
        import opencc  # type: ignore
        return opencc.OpenCC("s2twp").convert(text)
    except Exception:
        return text


def ocr_at(path: str, t_mid: float) -> str:
    require_ocr()
    tmp = tempfile.mkdtemp(prefix="precut_")
    try:
        png = os.path.join(tmp, "f.png")
        r = _run([
            "ffmpeg", "-y", "-hide_banner", "-loglevel", "error",
            "-ss", f"{t_mid:.3f}", "-i", path, "-frames:v", "1",
            "-vf", f"crop=iw:ih*{CROP_BOTTOM}:0:ih*(1-{CROP_BOTTOM})",
            png,
        ])
        if r.returncode or not os.path.isfile(png):
            return ""
        engine = _ocr_mod()
        result, _ = engine(png)
        if not result:
            return ""
        lines = [row[1] for row in result if len(row) > 1 and row[1]]
        return _maybe_opencc(" ".join(lines).strip())
    finally:
        shutil.rmtree(tmp, ignore_errors=True)


def analyze_file(path: str, offset_sec: int, on_phase=None) -> dict:
    require_ocr()
    duration = ffprobe_duration(path)
    return analyze(
        path, offset_sec=offset_sec, duration=duration,
        run_silence=run_silence_ffmpeg,
        rms_of=lambda a, b: rms_ffmpeg(path, a, b),
        ocr_of=lambda t: ocr_at(path, t),
        black_of=lambda a, b: black_ffmpeg(path, a, b),
        on_phase=on_phase,
    )


def segs_overlap(segs: list[PrecutSeg]) -> bool:
    ordered = sorted(segs, key=lambda s: s.t0)
    for a, b in zip(ordered, ordered[1:]):
        if b.t0 < a.t1 - 0.05:
            return True
    return False


def export_segs(video_path, segs, *, mode, accurate, source, offset_sec, run=subprocess.run) -> list[str]:
    chosen = []
    for s in segs:
        if mode == "non_ad" and s.kind == "ad":
            continue
        if mode == "selected" and not s.selected:
            continue
        chosen.append(s)
    dest_dir = export_dir(video_path)
    os.makedirs(dest_dir, exist_ok=True)
    paths = []
    for s in chosen:
        dest = os.path.join(dest_dir, export_filename(source, s, offset_sec))
        cmd = cut_cmd(video_path, s, dest, accurate=accurate)
        r = run(cmd, capture_output=True, text=True)
        if getattr(r, "returncode", 0):
            err = (getattr(r, "stderr", None) or "")[-400:]
            raise RuntimeError(f"ffmpeg 剪檔失敗：{err}")
        paths.append(dest)
    return paths
```

然後在工作台的 Hermes venv 裝一次（實作 task 時執行，不要把 venv  commmit）：

```bash
python -m pip install rapidocr-onnxruntime
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python scripts/test_side_precut.py`

Expected: 全綠

- [ ] **Step 5: Commit**

```bash
git add scripts/side_precut.py scripts/test_side_precut.py
git commit -m "S7 側錄初處理：ffmpeg／RapidOCR 適配器與匯出"
```

---

### Task 6: FastAPI 路由（背景分析＋匯出＋存檔）

**Files:**
- Modify: `scripts/bite_workbench.py`（只加 import 與路由，**不要**改 `transcribe`／`translate`／`api_sb` 本體）
- Test: `scripts/test_side_precut.py` 可加「從 dict 組 segs」已在 Task 4；路由用 FastAPI TestClient 若專案沒在用 TestClient，**不要新加 starlette 測試依賴**。改測純函式 `export_segs`／`analyze` 已足夠。本 task 用手動 curl 驗（步驟 4）。

**Interfaces:**
- `POST /api/precut` `{name, force?: bool}` → `{state: running|done}`；沒 OCR → 400／404，detail 含 pip 指令；running 重複 → 409
- `GET /api/precut/status?name=` → `{state, phase, elapsed?, stale?, segments?, offset_sec?, error?}`
- `POST /api/precut/save` `{name, segments: [...]}` 寫回 PRECUT.json
- `POST /api/precut/export` `{name, mode: non_ad|selected, accurate?: bool, segments?: [...]}`
- `GET /api/precut/export/status?name=`

- [ ] **Step 1: 在 `build_app` 加 job 與路由**

檔頭附近：

```python
import side_precut as SP  # noqa: E402
```

在 `jobs: dict = {}` 之後加入（同一 `build_app` 內，複製轉錄 job 風格）：

```python
    def _precut_run(m: Material):
        job = jobs[f"precut:{m.name}"]
        try:
            data = SP.analyze_file(
                m.path, m.offset,
                on_phase=lambda p: job.update(phase=p),
            )
            job.update(state="done", phase="完成", **data)
        except Exception as e:
            job.update(state="error", error=str(e))

    @app.post("/api/precut")
    def api_precut(payload: dict):
        m = get_material(payload["name"])
        force = bool(payload.get("force"))
        try:
            SP.require_ocr()
        except FileNotFoundError as e:
            raise HTTPException(400, str(e))
        key = f"precut:{m.name}"
        if jobs.get(key, {}).get("state") == "running":
            raise HTTPException(409, "這支素材的初處理分析已在跑，別重複啟動")
        cached = SP.load_cache(m.path)
        if cached and not force:
            stale = SP.cache_stale(m.path, cached)
            jobs[key] = {"state": "done", "phase": "快取", "stale": stale, **cached}
            return {"state": "done", "stale": stale}
        jobs[key] = {"state": "running", "phase": "啟動中", "started": time.time()}
        threading.Thread(target=_precut_run, args=(m,), daemon=True).start()
        return {"state": "running"}

    @app.get("/api/precut/status")
    def api_precut_status(name: str):
        job = jobs.get(f"precut:{name}")
        if not job:
            m = get_material(name)
            cached = SP.load_cache(m.path)
            if not cached:
                return {"state": "none"}
            return {"state": "done", "phase": "快取",
                    "stale": SP.cache_stale(m.path, cached), **cached}
        out = dict(job)
        if "started" in out:
            out["elapsed"] = round(time.time() - out.pop("started"), 1)
        return out

    @app.post("/api/precut/save")
    def api_precut_save(payload: dict):
        m = get_material(payload["name"])
        segs = SP.dicts_to_segs(payload.get("segments") or [])
        SP.save_cache(m.path, m.offset, segs)
        overlap = SP.segs_overlap(segs)
        return {"ok": True, "overlap": overlap,
                "segments": [SP.seg_to_dict(s, m.offset) for s in segs]}

    def _export_run(m: Material, segs, mode, accurate):
        job = jobs[f"export:{m.name}"]
        try:
            paths = SP.export_segs(
                m.path, segs, mode=mode, accurate=accurate,
                source=m.source or "SIDE", offset_sec=m.offset,
            )
            job.update(state="done", phase="完成", files=paths,
                       dir=SP.export_dir(m.path))
        except Exception as e:
            job.update(state="error", error=str(e))

    @app.post("/api/precut/export")
    def api_precut_export(payload: dict):
        m = get_material(payload["name"])
        mode = payload.get("mode") or "non_ad"
        if mode not in ("non_ad", "selected"):
            raise HTTPException(400, "mode 只能是 non_ad 或 selected")
        accurate = bool(payload.get("accurate"))
        rows = payload.get("segments")
        if rows is None:
            cached = SP.load_cache(m.path) or {}
            rows = cached.get("segments") or []
        segs = SP.dicts_to_segs(rows)
        SP.save_cache(m.path, m.offset, segs)  # 匯出前先落地人改的
        key = f"export:{m.name}"
        if jobs.get(key, {}).get("state") == "running":
            raise HTTPException(409, "這支素材正在剪檔")
        overlap = SP.segs_overlap(segs)
        jobs[key] = {"state": "running", "phase": "剪檔", "started": time.time(),
                     "overlap": overlap}
        threading.Thread(
            target=_export_run, args=(m, segs, mode, accurate), daemon=True
        ).start()
        return {"state": "running", "overlap": overlap}

    @app.get("/api/precut/export/status")
    def api_precut_export_status(name: str):
        job = jobs.get(f"export:{name}")
        if not job:
            return {"state": "none"}
        out = dict(job)
        if "started" in out:
            out["elapsed"] = round(time.time() - out.pop("started"), 1)
        return out
```

`app = FastAPI(title="掐BITE 工作台")` 可改成 `title="TVBS 工作台"`——**不要改**，避免無關 diff。HTML 分頁自己寫標題。

- [ ] **Step 2: 迴歸舊測試**

Run: `python scripts/test_bite_workbench.py` 與 `python scripts/test_side_precut.py`

Expected: 全綠。若 import 循環（`bite_workbench` import `side_precut` import `bite_workbench`），`side_precut` **不准** import `bite_workbench`。

- [ ] **Step 3: Commit**

```bash
git add scripts/bite_workbench.py
git commit -m "S7 側錄初處理：分析／存檔／匯出 API"
```

---

### Task 7: 前端分頁＋版面 B

**Files:**
- Modify: `scripts/assets/bite_workbench.html`

**Interfaces:**
- Consumes: 上列 `/api/precut*`、既有 `/api/materials`、`/media`
- Produces: 可切換的兩個分頁；初處理為滿寬波形＋列表

- [ ] **Step 1: header 加分頁，兩套 main**

把 `<header>` 改成（保留 `#dir` `#load` `#status`）：

```html
<header>
  <nav id="tabs">
    <button type="button" id="tabBtnBite" class="tab on">掐BITE</button>
    <button type="button" id="tabBtnPrecut" class="tab">側錄初處理</button>
  </nav>
  <input type="text" id="dir" size="52" placeholder="素材資料夾路徑">
  <button id="load">讀取</button>
  <span id="status"></span>
</header>
```

現有 `<main>` 加 `id="viewBite"`。在它後面加：

```html
<main id="viewPrecut" hidden>
  <section id="pcMats"></section>
  <section id="pcMain">
    <div id="pcTop">
      <video id="pcPlayer" controls preload="metadata"></video>
      <div id="pcForm">
        <div class="note" id="pcNote">不跑 ASR。分析用靜音＋下標 OCR，類型請人改。</div>
        <label>類型</label>
        <select id="pcKind">
          <option value="ad">廣告</option>
          <option value="anchor">主播</option>
          <option value="reporter">記者</option>
          <option value="interview">訪問</option>
          <option value="other">其他</option>
        </select>
        <label>主題（字卡，可改）</label>
        <input type="text" id="pcTopic" style="width:100%">
        <div class="row">
          <div><label>檔內起（秒）</label><input type="text" id="pcT0"></div>
          <div><label>檔內訖（秒）</label><input type="text" id="pcT1"></div>
        </div>
        <div id="pcTc" class="note"></div>
        <label><input type="checkbox" id="pcAccurate">精準重編（慢，切點對齊秒）</label>
        <div class="row" style="margin-top:8px">
          <button id="pcAnalyze">分析</button>
          <button class="primary" id="pcExportAll">一鍵出全部非廣告</button>
          <button id="pcExportSel">只出勾選</button>
          <button id="pcSave">存回 PRECUT.json</button>
        </div>
      </div>
    </div>
    <canvas id="pcWave" height="72"></canvas>
    <div id="pcSegs"></div>
  </section>
</main>
```

CSS 追加（沿用既有變數）：

```css
#tabs { display:flex; gap:0; }
.tab { border-radius:6px 6px 0 0; }
.tab.on { background:var(--accent); color:#08121f; font-weight:600; }
#viewPrecut { display:grid; grid-template-columns:260px 1fr; height:calc(100vh - 53px); }
#viewPrecut[hidden] { display:none !important; }
#viewBite[hidden] { display:none !important; }
#pcTop { display:grid; grid-template-columns:1.2fr 1fr; gap:8px; }
#pcPlayer { width:100%; max-height:280px; background:#000; border-radius:8px; }
#pcWave { width:100%; display:block; background:#0e1013; border:1px solid var(--line); border-radius:6px; margin:8px 0; cursor:pointer; }
.pcseg { display:flex; gap:8px; align-items:flex-start; border:1px solid var(--line); border-radius:8px; padding:8px; margin-bottom:6px; cursor:pointer; }
.pcseg.on { border-color:var(--accent); background:#182430; }
.pcseg.ad { border-color:#5a2a2a; }
```

- [ ] **Step 2: JS — 分頁切換、分析輪詢、波形、匯出**

在既有 script 末段、綁定 `load` 之後追加。重點行為（完整寫進 HTML，不要「類似 Task N」）：

```javascript
let PC_MAT = null, PC_SEGS = [], PC_SEL = -1, pcSeek = null, pcTimer = null, pcExTimer = null;
const KIND_ZH = {ad:'廣告', anchor:'主播', reporter:'記者', interview:'訪問', other:'其他'};
const KIND_COLOR = {ad:'#f2555a', anchor:'#4da3ff', reporter:'#f0b429', interview:'#4cc38a', other:'#9aa2b1'};

function showTab(which) {
  const bite = which === 'bite';
  $('viewBite').hidden = !bite;
  $('viewPrecut').hidden = bite;
  $('tabBtnBite').classList.toggle('on', bite);
  $('tabBtnPrecut').classList.toggle('on', !bite);
}
$('tabBtnBite').onclick = () => showTab('bite');
$('tabBtnPrecut').onclick = () => showTab('precut');

function pcFmt(tc) { return tc ? tc.slice(0,2)+':'+tc.slice(2,4)+':'+tc.slice(4,6) : ''; }

function renderPcMats(list) {
  $('pcMats').innerHTML = list.map((m,i) =>
    `<div class="mat" data-i="${i}"><div class="n">${m.name}</div>
     <span class="tag">${m.kind}</span>
     ${m.offset ? `<span class="tag has">offset ${m.offset}</span>` : `<span class="tag warn">無 6 碼 TC</span>`}
    </div>`).join('');
  $('pcMats').querySelectorAll('.mat').forEach(el => el.onclick = () => pcPick(list[+el.dataset.i]));
}

const _renderMaterials = renderMaterials;
renderMaterials = function(list) {
  _renderMaterials(list);
  renderPcMats(list);
};

function pcPick(m) {
  PC_MAT = m; PC_SEGS = []; PC_SEL = -1;
  $('pcPlayer').src = '/media?name=' + encodeURIComponent(m.name);
  $('pcNote').textContent = m.offset ? ('母帶 offset +' + m.offset + ' 秒') : '檔名沒 6 碼 TC，offset＝0';
  pcLoadStatus(false);
}

async function pcLoadStatus(startIfNone) {
  if (!PC_MAT) return;
  const j = await api('/api/precut/status?name=' + encodeURIComponent(PC_MAT.name));
  if (j.state === 'done' && j.segments) {
    PC_SEGS = j.segments;
    if (j.stale) status('原片比快取新，建議重跑分析', false);
    renderPcSegs(); drawWave();
    return;
  }
  if (startIfNone && j.state === 'none') pcAnalyze();
}

async function pcAnalyze() {
  if (!PC_MAT) return;
  status('分析中…');
  try {
    await api('/api/precut', {method:'POST', headers:{'Content-Type':'application/json'},
      body: JSON.stringify({name: PC_MAT.name, force: true})});
  } catch (e) { status(e.message, true); return; }
  clearInterval(pcTimer);
  pcTimer = setInterval(async () => {
    const j = await api('/api/precut/status?name=' + encodeURIComponent(PC_MAT.name));
    status((j.phase || j.state) + (j.elapsed ? ' ' + j.elapsed + 's' : ''));
    if (j.state === 'done') {
      clearInterval(pcTimer); PC_SEGS = j.segments || []; renderPcSegs(); drawWave();
      status('分析完成 ' + PC_SEGS.length + ' 段');
    }
    if (j.state === 'error') { clearInterval(pcTimer); status(j.error, true); }
  }, 800);
}
$('pcAnalyze').onclick = pcAnalyze;

function renderPcSegs() {
  $('pcSegs').innerHTML = PC_SEGS.map((s,i) =>
    `<div class="pcseg ${s.kind}${i===PC_SEL?' on':''}" data-i="${i}">
      <input type="checkbox" ${s.selected!==false?'checked':''} data-chk="${i}">
      <div>
        <div class="meta">${KIND_ZH[s.kind]||s.kind} · ${pcFmt(s.tc0)}–${pcFmt(s.tc1)} · ${s.duration}s</div>
        <div>${s.topic || '（無字卡）'}</div>
      </div>
    </div>`).join('');
  $('pcSegs').querySelectorAll('.pcseg').forEach(el => el.onclick = ev => {
    if (ev.target.dataset.chk != null) return;
    pcSelect(+el.dataset.i);
  });
  $('pcSegs').querySelectorAll('[data-chk]').forEach(el => el.onchange = ev => {
    PC_SEGS[+el.dataset.chk].selected = el.checked;
    ev.stopPropagation();
  });
}

function pcSelect(i) {
  PC_SEL = i;
  const s = PC_SEGS[i]; if (!s) return;
  renderPcSegs();
  $('pcKind').value = s.kind;
  $('pcTopic').value = s.topic || '';
  $('pcT0').value = s.t0;
  $('pcT1').value = s.t1;
  $('pcTc').textContent = '母帶 ' + pcFmt(s.tc0) + '–' + pcFmt(s.tc1);
  const v = $('pcPlayer');
  const go = () => { v.currentTime = s.t0; };
  if (v.readyState >= 1) go(); else { pcSeek = s.t0; v.onloadedmetadata = () => { if (pcSeek!=null) v.currentTime = pcSeek; }; }
}
$('pcKind').onchange = () => { if (PC_SEL>=0) { PC_SEGS[PC_SEL].kind = $('pcKind').value; renderPcSegs(); drawWave(); } };
$('pcTopic').oninput = () => { if (PC_SEL>=0) PC_SEGS[PC_SEL].topic = $('pcTopic').value; };
function pcRetime() {
  if (PC_SEL<0) return;
  PC_SEGS[PC_SEL].t0 = parseFloat($('pcT0').value);
  PC_SEGS[PC_SEL].t1 = parseFloat($('pcT1').value);
}
$('pcT0').onchange = pcRetime; $('pcT1').onchange = pcRetime;

function drawWave() {
  const c = $('pcWave'); const w = c.parentElement.clientWidth || 800;
  c.width = w; const h = c.height; const ctx = c.getContext('2d');
  ctx.fillStyle = '#0e1013'; ctx.fillRect(0,0,w,h);
  if (!PC_SEGS.length) return;
  const T = Math.max(...PC_SEGS.map(s => s.t1), 1);
  PC_SEGS.forEach(s => {
    const x = s.t0 / T * w, ww = Math.max(2, (s.t1-s.t0)/T*w);
    ctx.fillStyle = KIND_COLOR[s.kind] || '#9aa2b1';
    ctx.globalAlpha = 0.45; ctx.fillRect(x, 8, ww, h-16); ctx.globalAlpha = 1;
  });
}
$('pcWave').onclick = ev => {
  if (!PC_SEGS.length) return;
  const T = Math.max(...PC_SEGS.map(s => s.t1), 1);
  const t = ev.offsetX / $('pcWave').width * T;
  const i = PC_SEGS.findIndex(s => t >= s.t0 && t < s.t1);
  if (i>=0) pcSelect(i);
};

async function pcSave() {
  if (!PC_MAT) return;
  const j = await api('/api/precut/save', {method:'POST', headers:{'Content-Type':'application/json'},
    body: JSON.stringify({name: PC_MAT.name, segments: PC_SEGS})});
  PC_SEGS = j.segments; renderPcSegs();
  status(j.overlap ? '已存（警告：段落邊界重疊）' : '已存 PRECUT.json', j.overlap);
}
$('pcSave').onclick = () => pcSave().catch(e => status(e.message, true));

async function pcExport(mode) {
  if (!PC_MAT) return;
  status('剪檔中…');
  const j = await api('/api/precut/export', {method:'POST', headers:{'Content-Type':'application/json'},
    body: JSON.stringify({name: PC_MAT.name, mode, accurate: $('pcAccurate').checked, segments: PC_SEGS})});
  if (j.overlap) status('邊界重疊，仍繼續剪…');
  clearInterval(pcExTimer);
  pcExTimer = setInterval(async () => {
    const s = await api('/api/precut/export/status?name=' + encodeURIComponent(PC_MAT.name));
    if (s.state === 'done') {
      clearInterval(pcExTimer);
      status('剪完 ' + (s.files||[]).length + ' 檔 → ' + (s.dir||''));
    }
    if (s.state === 'error') { clearInterval(pcExTimer); status(s.error, true); }
  }, 800);
}
$('pcExportAll').onclick = () => pcExport('non_ad').catch(e => status(e.message, true));
$('pcExportSel').onclick = () => pcExport('selected').catch(e => status(e.message, true));
```

`showTab('bite')` 預設。

- [ ] **Step 3: 靜態檢查**

確認 HTML 沒有第二份 SB 排版邏輯、沒有 API key、沒有 `whisper` 呼叫。

- [ ] **Step 4: Commit**

```bash
git add scripts/assets/bite_workbench.html
git commit -m "S7 側錄初處理：分頁與版面 B 前端"
```

---

### Task 8: 本機裝 RapidOCR＋瀏覽器驗收（075658）

**Files:** 不改規格外的檔。可把實測紀錄補進 design 文末「實測」小節（選擇性，一行即可）。

- [ ] **Step 1: 安裝 OCR**

Run: `python -c "import rapidocr_onnxruntime"`  
若失敗：`python -m pip install rapidocr-onnxruntime` 再測一次。

- [ ] **Step 2: 單元測試全綠**

Run:

```
python scripts/test_side_precut.py
python scripts/test_bite_workbench.py
python scripts/test_sb_format.py
```

Expected: 全 PASS。`sb_format` 必須零 diff。

- [ ] **Step 3: 啟動**

```
python scripts/bite_workbench.py --dir "D:\TVBS\CNN" --port 8765
```

瀏覽器開 http://127.0.0.1:8765

- [ ] **Step 4: 驗收清單（curl 不算）**

用 `CNN 075658 0817 增強藥專題.mp4`：

1. 分頁「側錄初處理」看得到四支素材；075658 的 offset tag 不是「無 6 碼」。
2. 按分析（第一次約數分鐘）。console 除 favicon 外無紅錯。
3. 有多段色塊；點段影片跳到該 `t0`。
4. 改一個類型／主題 → 存回 → 重新選素材仍在。
5. 「一鍵出全部非廣告」→ 資料夾 `D:\TVBS\CNN\CNN 075658 0817 增強藥專題 初處理\` 出現 mp4。
6. 抽一檔名核對：6 碼 = 檔內秒 + 07:56:58（例檔內 66 秒 → `075804`）。
7. 掐BITE 分頁仍能列素材、沒被這次改壞。

未過：修對應 task，不要用「先交再補」。

- [ ] **Step 5: Commit 實測註記（若有改常數如 -30dB）**

只 commit 因實調改的閾值與一行實測紀錄。沒改就不必空 commit。

```bash
git add scripts/side_precut.py common/plans/2026-08-28-側錄初處理工作台-design.md
git commit -m "S7 側錄初處理：依 075658 實調靜音閾值"
```

---

## Spec coverage（self-review）

| 規格節 | Task |
|---|---|
| 同一網站新分頁、掐BITE 不動 | 6, 7 |
| 不跑 ASR、靜音＋OCR | 2, 3, 5 |
| RapidOCR 未裝 4xx＋pip 提示 | 5, 6 |
| `CNN 075658` offset 經 scan_dir | 1（迴歸）、6 |
| PRECUT.json 旁路、mtime stale | 4, 6 |
| 版面 B 滿寬波形 | 7 |
| 一鍵非廣告／只出勾選 | 5, 6, 7 |
| copy vs 精準重編 | 4, 5, 7 |
| 輸出路徑與檔名 | 1, 5 |
| 重疊警告不阻擋 | 5 `segs_overlap`、6、7 |
| 同名覆蓋不刪其他檔 | `export_segs` 只寫自己的 dest |
| v1 瀏覽器 075658 | 8 |
| 不改 07、不改 sb_format | Global + Task 6 迴歸 |

## Placeholder scan

無 TBD／「實作細節另補」。閾值 `-30dB`／`0.6s`／`RMS_AD=0.25`／`CROP_BOTTOM=0.28` 寫死在程式當可實調常數，Task 8 可改。
