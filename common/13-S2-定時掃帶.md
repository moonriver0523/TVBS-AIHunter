# S2 定時掃帶（通訊社網站輪詢）

> 🧪 **V2 測試中（2026-08-01）**：省 token 執行版見 [`13b-S2-定時掃帶-v2省token.md`](13b-S2-定時掃帶-v2省token.md)，使用者指令明確說「用 V2」時才走；預設仍走本版。內容格式規則兩版共用、以本檔為準。

> **狀態：規格草案，決策已定、尚未實作。** 2026-07-30 與使用者逐題確認的結果。
> 定位見 [`12-子系統地圖.md`](12-子系統地圖.md)：S2 抓素材 → 寫進 **S3 庫存**（Dashboard 建好前用純文字清單暫代）→ AI 依議題整理成 **S4 rundown 稿單** → **S4b 分派給記者** → 編號／下載／寫稿。**S2 是打通整條主線的阻塞點。**
> S2 只涵蓋通訊社網站素材；側錄逐字那一半屬 **S2b**（[`14-S2b`](14-S2b-側錄轉譯摘要.md)：**現行版＝使用者貼上人工篩選內容，已可用**；agent 自主抓取版仍未實作）。

## 已定案的六項決策

| # | 項目 | 決定 |
|---|---|---|
| 1 | 監控範圍 | **AP／Reuters Connect／CNN Newsource 三邊都做**。RT 已知較貴且不穩（SPA 卡舊快取、清單只能截圖讀），設計上要允許 **RT 單獨失敗而不拖垮另外兩邊** |
| 2 | 輪詢／整併節奏 | ⚠️ **2026-07-30 補完**：**人工指令與排程都支援**。排程**暫定 5 個檢查點：16:00／18:00／20:00／22:00／23:00**，23:00 為**晚班定版**（收班、產出定版晚班交接）；16:00–22:00 這 4 個是**中繼整併**（供隨時查看，非最終版）。使用者隨時可額外手動觸發（急件不必等下一個檢查點）。仍是暫定，實測後可調整間隔或增減檢查點。<br>⚠️ **2026-08-02 新增隔夜續掃 4 個檢查點：01:00／04:40／06:40／08:40**——23:00 定版**不是結束**，同一份檔案繼續掃到白天，新素材加隔夜標記，見下方「隔夜續掃」 |
| 3 | 狀態記錄 | **獨立 JSON 狀態檔**，每筆素材存七個欄位，**批次摘要直接當欄位存在 JSON 裡，不另開片段檔**。完整欄位設計見下方「狀態 JSON 欄位設計」 |
| 4 | 稿未到的素材 | **先進庫存、就地標記**：依 shotlist 寫一句摘要並標 `(稿未到)`，正常歸入分類；稿到後回頭換成完整摘要。理由：大新聞的畫面晚班會想馬上知道，不該因為稿未到就看不見。**後補節奏**：平時批次擷取若再碰到同則且稿已到 → 覆寫 `raw_entry`＋`pending`→`has_script`；**最終整併晚班交接時，必須清查 pending／「稿未到」並回站補齊**；**定版／收班整併還必須掃格式異常、明顯可疑、缺漏，有則回外電網站核對後修**（見下「最終整併：稿未到清查＋品質掃」）。⚠️ **2026-08-03 訂正（WP1 上線後）：中繼整併（16/18/20/22）也要主動全量重查 pending**，走 `13b` §1a API 批次重查（NS 整批一次、RT／AP 一次 `Promise.all` 打 N 則），不是舊時代逐則點頁面那種成本——**查完照規則直接覆寫，不必詢問使用者**；pending 為 0 就跳過，不必空跑。舊的「不強制、只能順手補」是 UI 逐則點擊時代的顧慮，API 批次重查上線後已不成立 |
| 5 | 庫存 txt 維護 | ⚠️ **2026-07-30 再訂正——批次分立，5 個檢查點整併**（使用者確認：實務上晚班本來就是這樣做，每批次掃帶的摘要先分立存放，收班前才整併成一份晚班交接）。**不是每輪重寫、也不是每輪跑三方比對**——每輪只把新找到的素材寫進 JSON，不碰、不重讀先前批次；三層分類與人工編輯的三方比對，只在**整併時**（16/18/20/22 中繼、23:00 最終，見決策 2）做，且靠 JSON 的 `compiled` 欄位做**增量整併**（只處理上次整併後的新增／變動，不是每次全量重算）。細節見下方「批次分立與整併」與「狀態 JSON 欄位設計」 |
| 6 | 大分類歸屬 | **照使用者的樣板**（見下節，順序照抄不重排）。**同時滿足兩格時一律放最前面那格**（2026-08-03 訂案，見「大分類歸位通則」）。不在清單上的歸「話題」；最上面的**機動格由使用者當天決定**，agent 不自行開 |

## 批次分立與整併（2026-07-30 使用者訂正，取代先前「每輪重寫」設計）

**核心原則：每輪只寫新東西，不碰累積的彙整結果；分類彙整只在整併時做一次。** 這是使用者確認的實務做法——晚班本來就是每批掃帶各自產出摘要、下班前才整併成一份晚班交接，不是邊掃邊維護一份持續更新的總表。

### 兩種節奏，職責不同

| | 批次擷取（每個檢查點都做） | 整併（同一個檢查點，緊接批次擷取之後） |
|---|---|---|
| 觸發 | 決策 2：排程 16/18/20/22/23 或人工加開 | 同一次觸發裡，擷取完接著跑；23:00 額外標記為**最終整併** |
| 做什麼 | 找新素材、寫三段式內容，**存進 JSON 的欄位**，不產生任何 txt；若掃到**已在庫且 `pending`** 的同則且稿已到 → 覆寫 `raw_entry` | 讀 JSON 裡**尚未整併**的項目，判斷大分類／中主題歸屬、處理同組相鄰，套三方比對保留人工編輯，寫出 `{MMDD}晚班交接.txt`。**最終整併另加「稿未到清查＋品質掃」**（下節） |
| 要不要判斷力 | 不用——是否已見過純靠代碼比對 JSON | **要**——「這則歸哪個中主題、要不要開新大分類」需要語意判斷；最終清查時還要判斷稿是否已到、摘要怎麼補、格式／可疑是否回站 |
| 觸碰範圍 | 只有這次新找到的素材（＋順手補到的 pending） | **增量**：上次整併後的新增／變動；**最終整併額外**：(1) 全部仍 `pending`／`(稿未到)`；(2) **品質掃命中的可疑／缺漏列**（見下） |
| 頻率 | 5 次（16/18/20/22/23） | 5 次；**每輪都做 pending 清查**（2026-08-03 起，見決策 4），23:00 為最終版（另加品質掃） |

⚠️ **2026-08-03（WP1）起，走 V2 執行層時本節的「三方比對／增量整併」已不適用**：晚班交接 txt 改由 `scripts/s2_render.py` 從狀態檔**全量渲染**，狀態檔是唯一真相源、人工不改 txt，`.snapshot.txt` 與 `diff3` 一併廢除，`compiled` 增量欄位也廢除。格式規則（三層骨架、素材行寫法、標記）仍以本檔為準，執行流程見 [`13b`](13b-S2-定時掃帶-v2省token.md) §2／§3。

**批次擷取不需要三方比對**：每次只是把新素材寫進 JSON 的欄位，不去改動任何已整併過的 txt 內容，沒有「舊版被覆蓋」的風險。三方比對只在**整併**時用——若某次整併後、下次整併前，有人工編輯過上一版 txt，下次整併要靠「比對現行 txt vs 上次機器版快照」保留那些編輯。

**大新聞即時可見（承接決策 4）不受影響**：決策 4 要的是「稿未到也要能看到」，這靠的是**JSON 裡的批次內容立即存在**（不必等整併），跟「有沒有分類彙整成正式三層庫存 txt」是兩回事。急件可以直接查 JSON，不用等下一個整併檢查點——或直接手動加開一次整併。

### 最終整併：稿未到清查＋品質掃（2026-07-31 但書，同日晚補品質掃）

**觸發**：**最終整併**（排程 23:00 定版，或使用者明確下令「最終整併／定版晚班交接／收班整併」）。中繼整併**不強制**跑全量清查／全量品質掃。

**目的**：
1. 避免定版晚班交接仍殘留「稿其實已到、庫存還寫稿未到」（實務：RT2333）。
2. 避免定版帶著**格式異常、明顯可疑、明顯缺漏**出廠——**先掃可疑，有命中才回站**，不是 100 則全回站（省 token／時間）。

**步驟**（在一般增量整併**之前或緊接其後、寫出定版 txt 之前**完成）：

#### A. 稿未到／pending 清查

1. **列清單**：狀態 JSON 全部 `script_status = pending`，**並** txt 字串掃 `稿未到`（雙保險）。
2. **逐則回查**（2026-08-03 起**優先走 `13b` §1a API 批次重查**——NS 一次查詢可重判整批、RT 用 Promise.all 打 /api/item；API 不可用才退回開詳情頁），看是否已有完整 SCRIPT／STORYLINE（非僅 shotlist／佔位）。0803 實例：52 則 NS pending 用 API 一次重判完，舊做法逐則點 Preview 約需 130 次呼叫。
3. **稿已到**：覆寫 `raw_entry` 為完整三段式（含畫面／BITE 或無BITE、**講者**、時長若可見）；備註**去掉**「稿未到」；`script_status → has_script`；整併**沿用既有 `category`**。
4. **稿仍未到**：維持 `pending` 與 `(稿未到)`，**不要**刪列。
5. **回報**：「pending 清查：N 已補／M 仍稿未到／（代碼）」。

> ⚠️ **「有稿」判準（2026-08-03 使用者訂案，兩點都是為了讓 pending 不再無限堆積）**
>
> **(1) 判準看內容不看標記字串**：有 `--LEAD IN--`／`--VO SCRIPT--` 當然算；**沒有這兩個標記、但有完整敘事段落**（成句的稿件文字，如 CNN 的 `CORRESPONDING SCRIPT` 區塊）**一樣算稿已到**。實例：0803 `WE-014SU` 有完整敘事卻缺標記字樣，嚴格抓字串會誤判成稿未到。
>
> **(2) 「本來就不會有旁白稿」的素材＝`has_script`，不是 `pending`**：只有 `--SUPERS--`／拍攝資訊的原始素材——國會聽證會 ISO／CLEAN AIR 長帶、政論節目裸 SOT 引言、字卡——**這類天生沒有通訊社旁白稿，等再久也不會有**。一律標 `has_script`，並在**第一括號備註寫明素材形態**（`ISO長帶`／`裸SOT`／`CLEAN AIR` 等），**不要**標 `(稿未到)`。
>
> ⚠️ **NS 兩類素材不採納，列表判讀階段直接排除（2026-08-03 使用者訂正，判準 08-03 晚實測確認為機械欄位）**：**不寫入 `add-batch`、不進狀態檔、不建 `needs-review` 殼**，掃到直接略過。
>
> **(a) 純音軌 —— `footageType === "AUDIO TRACK"`**：沒有獨立畫面內容，只有記者各地播出署名、頻道標籤這類配套聲軌（實例：`PY-04MO`「(TAGS) PACIFIC NORTHWEST INFERNO (4AMET)」、`PY-02SU`）。⚠️ 跟上一條「有畫面但沒稿」的 ISO／裸 SOT／CLEAN AIR **不是同一件事**：那些**有可用畫面**只是沒有旁白稿，音軌是**連畫面都沒有**，兩者判準不同，不要混為一談。
>
> **(b) 初稿佔位公告 —— `footageType === "GRAPHIC"` 且 script 含 `THIS IS NOT THE FINAL SCRIPT`**：這**不是一支可用的新聞帶**，是站方發的佔位公告，內文會明講最終版在哪一則、幾點更新。實例 `PRE-400aET`「PRELIM SCRIPT & EMBARGO INFO FOR 400amET PKG」，script 開頭固定寫 `***THIS IS A PRELIMINARY SCRIPT FOR THE 4:00amET PKG*** / THIS IS NOT THE FINAL SCRIPT. / ***THE FINAL EMBARGOES AND SCRIPT WILL BE IN ITEM PY-03MO AT 4:00aET***`。
> - **代碼形態**：這類 id 常是 `PRE-` 開頭（如 `PRE-400aET`），但**不要只靠代碼判**——以 `footageType` ＋ script 字樣為準。
> - ⭐ **它點名的最終版 ID 要記下來**（上例的 `PY-03MO`）：那才是真正要收的素材，下一輪掃到時正常收錄。可寫進當輪回報，不必進狀態檔。
>
> 錯誤示範（0803 實錯，NS pending 一度堆到 59 則的主因）：把 26 則聽證會長帶與裸 SOT 全標 `pending`，每輪新增十幾則、永遠沒有一則能被清掉——因為它們等的那份稿根本不存在。
>
> ⚠️ **AP 初稿 `++PRELIMINARY SCRIPT++` ＝ 素材照收、標 `pending`，下一輪回頭補正式稿（2026-08-04 使用者訂案）**
>
> **跟上面 NS 的 (b) 佔位公告完全不同，不要混為一談**：NS 那種是「站方發的公告」，本身**不是新聞帶**、要整則排除；AP 這種是**真的素材**（實例 `AP4676355` 伊朗外交部簡報，有畫面也有 SOUNDBITE），只是稿子還是初稿版、之後會出正式稿。
>
> - **判準**：AP 詳情的 `script`（`script.nitf`）**開頭**寫 `++PRELIMINARY SCRIPT ++`。⚠️ AP **沒有**對應的機械欄位可判（不像 NS 有 `footageType`），只能掃這個字樣——所以**只認開頭**，不要對全文 `find`，內文順帶提到不算。
> - **處置**：素材照常寫進 `add-batch`，`script_status` 標 **`pending`**、第一括號備註加 **`(初稿)`**。內容照初稿版正常寫三段式（有 BITE 就寫 BITE，不要因為是初稿就留白）。
> - **下一輪 pending 清查時回頭重查**：正式稿到了就照 A 節第 3 步覆寫 `raw_entry`、拿掉 `(初稿)`、轉 `has_script`。
> - **不會無限堆積**：正式稿一定會出，跟「等一份根本不存在的稿」是兩回事，不違反上面那條錯誤示範的精神。若連續數輪都沒等到，照一般 pending 處理（維持 `pending`、不刪列）。

#### B. 格式異常／明顯可疑／缺漏掃 → 有則回站補

**先機器＋規則掃整份定版 txt（與必要時 JSON `raw_entry`）**，只對**命中列**回外電網站核對並修 `raw_entry`＋txt。中繼整併不強制；使用者單獨下令「抽查／掃可疑」時可只做 B 而不做 A。

| 類型 | 怎麼抓（例） | 命中後 |
|---|---|---|
| **格式異常** | `(BITE)` 與 `無BITE` 矛盾；有 `▎BITE：` 無 `(BITE)` 或相反；缺 `▎畫面：`；結尾既非 `無BITE` 也非 `BITE：`；第一備註寫了 `BITE`；第二括號不是 `(BITE)` 卻重複備註內容；備註仍寫 `FILE`／`檔案`（應 `資料畫面`）；素材行出現 `GMT`；**來源／格式／狀態標在半形備註與摘要全形 `（）` 重標**（`SNTV`／`CCTV`／`資料畫面`／`直式`／`稿未到`＋`script待補` 等）；素材行出現 **`（列表摘要）`** 等操作備註 | 回站對 shotlist／script 改對格式與內容；重標則刪摘要側重複、必要時併限制語進第一備註 |
| **明顯缺漏** | `▎BITE：「…」` **無講者**（違反講者規則）；摘要／畫面寫「待補／依shotlist／模板先上／early script」卻**已** `has_script`（應完整）；`片未到` 但稿已齊時可保留片況標記、內容仍應完整 | 回站補講者／完整摘要畫面 BITE |
| **明顯可疑** | 數字／單位／開收盤用語前後矛盾；同一代碼 txt 與 JSON 不一致；同一代碼重複列；摘要與代碼來源常識嚴重不符（例：標無BITE 但摘要大段引用受訪） | 回站核原文後再改；**不確定就不改**，回報「待人工」 |
| **非缺漏** | 行尾無 `▎MM:SS`（**允許省略**，不溯及）；空大分類標題保留（樣板要求） | **不**當錯誤、不為此回站 |

**回站修完**：同步 JSON `raw_entry`（與必要時 `script_status`）；品質修過的列若已 `compiled`，視為 `raw_entry` 變動，整併沿用 `category` 重排該則。

**回報**：「品質掃：X 則命中／Y 則已回站修正／Z 則仍待人工（代碼＋原因）」。

**不做的事**：

- **不**為品質掃去**無差別**重掃當日全部已 `has_script` 的素材（只掃命中／可疑）。
- 品質掃（B）仍**不強制**中繼輪全量做——只有 pending 清查（A）2026-08-03 起改為中繼輪也要主動全量做（見決策 4）。
- 不回溯改更早日期的舊晚班交接檔。
- 不把「可選省略」（如時長）當錯誤。

## 隔夜續掃：接續晚班交接掃到白天（2026-08-02 使用者定案；2026-08-04 固定排程定案）

23:00 晚班定版**不是收工**——同一份 `{MMDD}晚班交接.txt` 繼續往隔天白天掃，讓早班一開檔就看得到整夜進的素材。

### 每日固定排程（2026-08-04 定案，原則性、使用者可當天隨時特例調整）

⚠️ **這是本節第三次訂正邊界**：2026-08-03 晚一度把邊界改成 22:00／05:00，同日深夜又發現「下一個整點換算」的錯誤規則，2026-08-04 使用者給出完整固定排程後**才真正定案**，把邊界**改回 23:00／07:00**（原始版本），差別只在於現在有明確的固定時刻表，且正式收 `◆`（日班）這第四個標記。以下是唯一權威版本，之前的討論過程不必再回頭看。

| 檢查點 | 標記 | 班別 |
|---|---|---|
| 16:00 | `△` | 晚班第一輪，開啟當晚 `{MMDD}晚班交接.txt`，掃 13:00–16:00。⭐ **建檔輪有七項專屬動作**（`window_start`、機動格、前一天歸檔…），見 [`13b` §5a](13b-S2-定時掃帶-v2省token.md#5a-每天第一輪建檔輪的額外動作2026-08-04-補) |
| 18:00 | `△` | 晚班 |
| 20:00 | `△` | 晚班 |
| 22:00 | `△` | 晚班 |
| 23:00 | `△` | 晚班下班前最後一輪 |
| 01:00 | `▲` | 無人值守 |
| 04:30 | `▲` | 無人值守 |
| 07:00 | `■` | 晨班 |
| 08:00 | `■` | 晨班 |
| 10:00 | `◆` | 早班 |
| 12:00 | `◆` | 早班 |
| 13:00 | `◆` | 早班 |

**標記邊界**（`mark_for()` 判準，只認「實際開掃時間」，不做任何「下一個整點」換算——這條 2026-08-03 深夜訂正過的原則維持不變）：16:00–23:00（含）＝`△`；隔天 07:00 前＝`▲`；07:00–09:00＝`■`；09:00–14:00＝`◆`。

**時刻是原則不是鐵律**：使用者可以當天隨時因應實際狀況加開、跳過、或調整某一輪的時間，不需要每次都回來改這份表；`mark_for()` 判斷的是**素材實際被收進來那一刻的時鐘時間**，不是排程表上該輪「應該」是幾點。

> 🎫 **排程間隔全部超過 1 小時，所以 NS 需要「保活」（2026-08-04 訂案）**：NS（CNN Newsource）的登入態是**1 小時滑動時效**，沒有任何長效 cookie 撐著（AP 有 7 天、RT 有 23 小時，NS 一個都沒有），過期就只能人工重登。而上表每一段間隔都 > 1 小時（23:00→01:00 隔 2hr、01:00→04:30 隔 3.5hr、13:00→隔天 16:00 隔 3hr），等於每輪開工 NS 幾乎必定已過期。
> **做法**：掃帶 agent 在兩輪之間的等待期，距上次接觸 NS 接近 **58 分鐘**時，順手開一次 NS 頁面續期（1–2 次工具呼叫，不查清單不打 API）。
> ⏱️ **配套：每輪掃描順序固定 `NS → AP → RT`（2026-08-04 使用者訂案）**。NS 排第一站，「上次接觸 NS」就等於整點開工那一刻，續命時刻用整點推算即可；NS 若排最後，實際打開的時間會隨當天素材量浮動（16:05 或 16:40 都可能），基準點每輪都在飄、算不準。
> ⛔ **必須由掃帶 agent 自己做，不可另開獨立 agent／排程**——Playwright 只有一個 persistent profile，另開行程會跟掃帶互鎖（0803 曾害 RT 空窗八小時）。詳細機制、實測證據與失敗處置見 [`13b`](13b-S2-定時掃帶-v2省token.md) §1a-3「NS 登入態」與「NS 保活」兩節。

每輪一樣是「批次擷取 → 整併寫檔」，做法與晚班的中繼整併相同（見上節），**不另開檔、不另開狀態 JSON**，續用當晚那一份。

⚠️ **檔名不換日**：01:00 之後掃到的素材仍寫進**當晚**那份 `{MMDD}晚班交接.txt`（MMDD＝晚班起始日）。8/2 晚班開的 `0802晚班交接.txt`，8/3 凌晨到早上的續掃全部續寫進去，**不要**在 8/3 另開 `0803晚班交接.txt`。13:00 那輪之後到下一個 16:00 之間沒有排程，是刻意的空檔。

### 素材編號前的時段標記

**每一則素材的代碼前都要加一個全形符號＋空格**，讓編輯一眼分出哪些是新的、也方便快速瀏覽時定位素材：

```
△ RT2650 (23:00 前晚班既有素材) ▎…
▲ RT2681 (希臘野火 空拍) ▎摘要…▎畫面：…▎無BITE。
▲ 🔴 IN-23SU (希臘直升機相撞) ▎…　←重大素材：時段標記後、代碼前加 🔴
■ AP1234567 (東京地鐵事故) (BITE) ▎摘要…▎畫面：…▎BITE：…▎01:12
◆ RT2699 (國會聽證追蹤) ▎摘要…▎畫面：…▎無BITE。
△ CNN 16:14:30　（主播）
△ NHK 16:53:50-16:56:38　（記者）
```

| 符號 | 時段 | 意義 |
|---|---|---|
| `△` | 23:00（含）前 | 晚班既有，16:00～23:00 五輪都算晚班有人值班（23:00 是下班前最後一輪） |
| `▲` | 23:00–07:00 | 無人值守新增（01:00、04:30 兩輪） |
| `■` | 07:00–09:00 | 晨班新增（07:00、08:00 兩輪） |
| `◆` | 09:00–14:00 | 早班新增（10:00、12:00、13:00 三輪） |

> ⚠️ **每則都要有標記，包含 `△`（不用「無標記」表示既有素材）**：三種以上標記齊全、格式統一，快速瀏覽時每一行起手都一樣寬，好找素材。**側錄行（CNN／NHK）同樣要標**，寫在來源之前：`△ CNN 16:14:30　（主播）`。
>
> 🔄 **`■` 原本是 `●`，2026-08-04 使用者訂正改用方塊**（理由：`●` 與其他圓形標記——`🔴` 重大、`🟤` 已做過——擠在同一個行首位置容易看混）。
> **舊資料不回頭改**（使用者訂案）：0803 以前的晚班交接整份仍是 `●`，維持原樣不重新渲染。
> ⚠️ **所以腳本的解析端必須永遠認得 `●`**——`s2_validate.MARK_RE`／`s2_render.MARK_RE` 都保留了它，`MARKS` 也留著 `●=07:00–09:00 新增（舊符號）` 的圖例對照。**這是長期相容分支，不是過渡碼，不要當成殘留清掉**：拿掉的話舊檔那些 `●` 行會 `LINE_RE` 不 match，從品質掃與檔頭統計裡**靜默消失**（不報錯、只是數字少算，`△` 上線時已經踩過一次）。實測 `Archive/20260803/0803晚班交接.txt` 的 56 行 `●` 仍正常計入 362 則統計、品質掃 0 命中。

- ⚠️ **符號歸屬看「這則是哪一輪掃進來的」，不是看素材的上站時間**。同一輪掃進來的素材標記一致，不必逐則去查發稿時間。
- ⚠️ **標記一律看實際開掃時間，不做「下一個整點」換算**——交件時間可以算下一個整點方便排程回報，但那跟標記是兩件事，不連動。
- 標記**只加在素材行行首**，加在代碼之前、空一格。三段式其餘結構完全不變。
- 側錄段落（[`14-S2b`](14-S2b-側錄轉譯摘要.md)）若在隔夜時段補進來，比照加同一組標記。⚠️ **但隔夜輪次現階段不跑側錄篩選**（2026-08-02 定案：23:00 之後不會有新側錄，跑了空轉）——標記規則先寫著當**預留接口**，日後要開啟時流程與格式都不必再改，見 `14-S2b` 的「時段範圍」。
- `scripts/s2_validate.py` 已支援：品質掃與檔頭統計都會先剝掉標記（`△▲■◆`）再比對，標記不影響任何既有檢查；另外會檢查「用了標記但檔頭沒圖例」。**檔頭「隔夜續掃 N 則」只計 `▲`／`■`／`◆`，`△` 是基底不計入。**

## 狀態 JSON 欄位設計（2026-07-30 定案）

每則素材一筆，欄位分兩組：**批次擷取寫的**（找到當下就填）與**整併寫的**（分類彙整時才填）。

| 欄位 | 誰寫 | 內容 | 用途 |
|---|---|---|---|
| `id` | 批次擷取 | 素材代碼（`RT0595`）或側錄識別（`CNN 151328`） | 唯一鍵，判斷「是否已見過」的依據 |
| `source` | 批次擷取 | **封閉字彙，只能用這六個**：`AP`／`RT`／`NS`／`YT`／`SIDE_CNN`／`SIDE_NHK`。⚠️ **不得自創同義值**（2026-08-02 訂正，0802 實錯：同一個 Newsource 同時出現 `NS` 與 `CNN_newsource` 兩種寫法，per-source 統計與差集全亂）。`NS`＝CNN Newsource，與檔頭統計用的縮寫一致 | 追溯來源、統計用 |
| `first_seen_checkpoint` | 批次擷取 | 首次抓到的檢查點，**格式統一為 `{MMDD}-{HHMM}`**（`0802-1600`）。⚠️ 2026-08-02 實錯：同一份檔案混用 `16:00`／`r1-0802-1400-1500`／`19:00-21:00` 三種寫法，無法比對排序 | 追溯素材何時進系統 |
| `last_checked_checkpoint` | 批次擷取（每次重新確認都更新） | 最近一次確認仍存在的檢查點 | 判斷素材是否過期／該不該繼續追蹤 |
| `script_status` | 批次擷取 | `has_script` ／ `pending`（對應決策 4 的「有稿／稿未到」） | 整併時判斷要不要標 `(稿未到)` |
| `raw_entry` | 批次擷取（`pending` 轉 `has_script` 時**覆寫**） | 三段式內容本身（代碼＋備註＋摘要＋畫面＋BITE），批次擷取當下就寫好。**含行首的時段標記（`△▲■`）與重大標記（`🔴`）**（2026-08-03 訂案：與 txt 行完全一致，整併照抄不再加工；`s2_validate` 比對時會自行剝掉標記） | 整併時的**唯一輸入來源**——整併不重新生成內容，只做分類與排版 |
| `compiled` | 整併寫入 | `null`（未整併）／上次整併的檢查點時間 | **增量整併的依據**：整併時只挑 `compiled` 為 `null`，或 `raw_entry` 在 `compiled` 之後又被改過（`script_status` 從 pending 轉 has_script）的項目來處理，其餘直接沿用上一版 txt 裡已經排好的內容，不重新判斷分類 |
| `category` | 整併寫入 | `{大分類, 中主題}` | 一旦分類過就記住，避免下次整併重新判斷同一則該歸哪裡；也是「同組相鄰」排版時的依據 |

⚠️ **`items` 只放素材，不准塞待辦**（2026-08-02 訂正）：0802 實錯——`items` 裡出現 `SCRIPT-TODO`、`SCRIPT-TODO-2`、`CATEGORY-FIX-1`、`FORMAT-FIX-1`、`RT-MISC-20-30`、`AP-MISC-20-30`、`CEUTA-DEATH-TOLL-UPDATE` 等 8 筆**假素材**（拿 items 當備忘錄用），害 per-source 統計出現 `?` 分類、`pending` 數字灌水。每一筆 `id` 都必須是**真實素材代碼**；agent 自己的待辦寫在回報訊息裡，不進狀態檔。

**這組欄位怎麼讓整併變成增量而不是全量**：整併時，一則素材只會落在三種狀態之一——

1. **全新**（`compiled` 為 `null`）→ 要判斷分類、寫進新的 `category`，成本全付
2. **稿到了但已整併過**（`raw_entry` 在上次 `compiled` 之後被覆寫，通常是 pending→has_script）→ **沿用既有 `category`**，只重新排版這一則的內容，不必重新判斷分類
3. **沒變化**（`compiled` 已存在且 `raw_entry` 沒變）→ **直接照抄上一版 txt 裡的內容**，完全不進 LLM

只有第 1、2 類真的要花 token；第 3 類（多數素材，一旦分類過就不會再變）幾乎零成本。這是「增量整併」在數字上比「每次整併都全量重讀」省的地方。

> ⚠️ **這組欄位不能直接拿去當 Dashboard／Aigent-Fable 的 schema**（2026-07-30 使用者問過，答案是不能）。原因：
> - `raw_entry` 是**預先渲染好的顯示字串**（三段式黏在一起），Dashboard 要的是拆開的結構化欄位（標題／摘要／畫面描述／BITE 各自獨立）才能篩選查詢——這正是 Aigent-Fable 藍圖的 `ai_enrichments`／`asset_bites` 已經做對的事，我這裡是把它們黏回去，是退步不是延伸。
> - `compiled` 欄位的存在理由是「沒有即時資料庫」；有了 Dashboard，查詢即時發生，這個欄位跟整個增量整併機制應該直接消失，不是被繼承。
> - `id` 把來源與代碼黏成一個字串，藍圖是分開的 `source_id`／`source_ref`。
> - **唯一值得回饋的是 `category`（大分類／中主題）**——藍圖的 `assets` 表目前完全沒有主題分組概念，這是這輪設計挖出來、藍圖之後可能該補的東西，方向是反過來的。
>
> 這份 JSON 從一開始就是**用完即丟的過渡工具**（見 `common/12`：「Dashboard 建好前用純文字清單暫代」），不是 Dashboard schema 的草案，不要在它身上做超出「撐到 Dashboard 蓋好」以外的擴充規劃。

### Token 成本（2026-07-30，一晚 60–80 則量級，抓量級用非精確值）

以 70 則、16/18/20/22/23 共 5 個檢查點（每點約新增 14 則）試算：

| 項目 | 成本 |
|---|---|
| A. 展開 70 則詳情頁（每則 ~2,500 input） | input 175,000 |
| B. 5 個檢查點 × 3 站輪詢空轉 | input 42,500 |
| **整併×5（增量，只處理當次新增）** | input 6,300／output 6,300 |
| **合計** | input 223,800／output 16,800，加權當量 **≈ 30.8 萬** |

對照四種設計的成本量級：

| 設計 | 加權當量 |
|---|---|
| 每輪 LLM 重寫整份（最初誤判，20 輪） | ≈ 79 萬 |
| 每輪腳本渲染（機械式、無判斷力，20 輪） | ≈ 39.75 萬 |
| 批次分立、20 輪擷取＋整併 1 次全量 | ≈ 43.5 萬 |
| **5 檢查點、增量整併**（現行定案） | **≈ 30.8 萬** |

比最初設計省逾六成。兩個因素疊加：**檢查點從 20 降到 5**（B 項直接砍到 1/4）＋**整併走增量**（多數素材分類過一次就不再重付判斷成本）。若全量重算（每次整併都重讀全部 70 則），5 檢查點版本也還是約 38 萬，比 20 輪整併 1 次略省——可見**降低檢查點頻率**本身就是最大的省法，增量整併是疊加的第二層優化。

## 輸出位置與檔名

- **資料夾**：`Claude共用/自動掃帶系統/`
  - Windows：`G:\我的雲端硬碟\Claude共用\自動掃帶系統\`
  - Mac：`~/Library/CloudStorage/GoogleDrive-moonriver0523@gmail.com/我的雲端硬碟/Claude共用/自動掃帶系統/`

> ⚠️ **技術調查記錄一律直接進 repo，不要落在這個 Drive 資料夾（2026-08-03 深夜訂正）**：卡點報告、API 逆向工程過程、探索性實驗結果這類**有長期參考價值、會被規則文件引用**的東西，寫進 `common/investigation-logs/`（見該資料夾 `README.md`），不要先存 Drive 再回頭搬——2026-08-03 那批（三站 API 破解總表、NS 掃帶卡點報告等）就是先存 Drive、被 `TODO.md`／`13b` 用絕對路徑引用，差點在整理暫存檔時被一併清掉才發現問題。這個 Drive 資料夾**只放**：每日暫存檔（見下方「每日暫存檔歸檔」）與當日正式庫存（`{MMDD}-s2-state.json`、`{MMDD}晚班交接.txt`）。

### 每日暫存檔歸檔（2026-08-03 深夜訂正，取代先前「暫定」狀態）

批次擷取階段產生的暫存檔（`_ap_*.json`／`_rt_*.json`／`_mk_*.py`／`{MMDD}-batch-*.json` 等，凡是底線開頭或帶時間戳的中間檔）**一律直接寫進當晚班次的日期子資料夾** `{YYYYMMDD}/`（例：`20260803/`，8 碼、資料夾不存在就自動建立），**不要**先寫在 `自動掃帶系統/` 這層再回頭搬。

**強制執行方式（2026-08-04 上線，不是靠 agent 自己記得）**：每輪開工第一步（`resume` 之後）跑 `python scripts/s2_state.py scratch-dir --mmdd {晚班起始日MMDD}`，印出並自動建立這個資料夾，本輪所有暫存檔都寫進這個路徑。指令與時機見 `13b`「防卡設計」第 1 條。

- **日期用「晚班起始日」，不是實際掃帶當下的日曆日**——跟「檔名不換日」同一個道理：8/2 晚班開的班次，8/3 凌晨到早上四輪的暫存檔一樣進 `20260802/`（若晚班是 8/3 開的則進 `20260803/`），不要因為凌晨後日曆日換了就開新資料夾。
- **正式庫存檔（`{MMDD}-s2-state.json`、`{MMDD}晚班交接.txt`、`.prev.txt`）維持留在 `自動掃帶系統/` 這層，不搬進日期子資料夾**——現行所有腳本的 `DEFAULT_FILE` 與文件裡的路徑範例都假設它們在這層，搬了要同步改一大批地方，先不動；日期子資料夾只收暫存檔。
- 已經產生的舊暫存檔不必回溯搬，下一輪開工起套用即可。
- 💾 **`batch.json` 每筆要留 `src_text`＝瘦身後的稿件原文（2026-08-04 訂案）**：原本只存 `entry`（消化完的中文三段式成品），原文用完就丟——0804 回頭查 BITE 誤判時，**RT 4 則、AP 9 則已捲出站方清單範圍、永遠查不回來**。存了原文就能離線核對誤判、限制語、時長、講者，不必重開瀏覽器也不受站方清單長度限制。成本一晚約 1–2MB，隨日期資料夾隔天可清。細節與欄位寫法見 [`13b`](13b-S2-定時掃帶-v2省token.md) §2「批次規則」。
- **正式庫存檔名**：`{MMDD}晚班交接.txt`（4 碼日期，例：`0730晚班交接.txt`）——**由整併產生**，批次擷取階段不寫這個檔。
- **批次摘要不另開片段檔**（2026-07-30 已解）：批次擷取的內容直接寫進狀態 JSON 的 `raw_entry` 欄位（見「狀態 JSON 欄位設計」），整併時從 JSON 讀取尚未整併的項目即可，不需要額外的中間檔案。
- 這是 **S3 庫存**檔，[`S2b 側錄轉譯摘要`](14-S2b-側錄轉譯摘要.md) 寫進**同一個檔案**，不另開。
- 與 [`S4 稿單`](11-wire-rundown-organizer.md) 的 `{SLUG} 稿單.txt`（放 `Claude共用` 根目錄）是**不同東西**，別混。
- 狀態 JSON 放這個資料夾（`{MMDD}-s2-state.json`）。~~上一次整併的機器版快照~~ **2026-08-03 起不再產生**（render 全量渲染，見上方 WP1 註記）。
- 存檔後確認本機同步資料夾內檔案已出現、無 `.tmp`／`.crdownload` 殘留即視為完成，不等雲端二次確認（同 [`01-shared-folders.md`](01-shared-folders.md)）。

> ⚠️ 該資料夾裡有使用者放的**大分類樣板**，是大分類清單的權威來源——**實作前必須先讀它**，不要用本文件下方那份提案清單。

## 晚班交接檔頭（2026-08-02 訂正為三行整併版；2026-08-03 加第 5 行重大提醒）

**`{MMDD}晚班交接.txt` 最上方（第一個 `======` 之前）必須有三行**，讓編輯不用逐行數就知道這是哪天、掃了多久、掃了多少：

```
0802 晚班交接
時間窗：2026-08-02 14:00–15:00（約1hrs）
收錄外電共9則（AP 3則／RT 4則／NS 2則）
```

| 行 | 內容 |
|---|---|
| 1 | `{MMDD} 晚班交接`——標題 |
| 2 | `時間窗：{YYYY-MM-DD} {起}–{訖}（約Xhrs）`——起訖之間用**全形連接號 `–`**；時數取整數小時 |
| 3 | `收錄外電共{N}則（AP X則／RT X則／NS X則[／其他 X則]）[；隔夜續掃 ▲ X則／■ X則／◆ X則]`——`NS` 指 CNN Newsource；非 AP／RT／NS 的**通訊社／網址素材**（YouTube 等）併入**其他**，其他為 0 時該段省略。⚠️ **CNN／NHK 側錄段落完全不計入**，見下。隔夜續掃那半段只列出實際有的標記（0 則的標記不印） |
| 4 | `標記：△=23:00 前既有　▲=23:00–07:00 新增　■=07:00–09:00 新增　◆=09:00–14:00 新增`——素材行的時段符號對照圖例，中間用**全形空格**分隔。只要檔案裡出現任何 `△`／`▲`／`■`／`◆`，這行就**必須有**（2026-08-03 起素材一律帶標記，所以這行實務上一定有），見「隔夜續掃」節 |
| 5 | **（僅有重大素材時才有）** `🔴 重大：{標記}{代碼} {一句話}`——**重大提醒行**，放在檔頭**最後**、每則一行、**最多 3 行**。見下節 |

隔夜續掃＋重大提醒的完整版（2026-08-02 新增第 4 行／2026-08-03 新增第 5 行／2026-08-04 補 `◆`）：

```
0802 晚班交接
時間窗：2026-08-02 14:00–2026-08-03 09:00（約19hrs）
收錄外電共58則（AP 12則／RT 30則／NS 6則／其他 10則）；隔夜續掃 ▲ 9則／■ 4則／◆ 3則
標記：△=23:00 前既有　▲=23:00–07:00 新增　■=07:00–09:00 新增　◆=09:00–14:00 新增
🔴 重大：▲ IN-23SU 希臘消防直升機撲滅野火時空中相撞，有事發瞬間畫面；1人傷、第二架機組下落未明
```

- 三行（隔夜續掃後四行、再加重大提醒最多七行）**連續不空行**，最後一行與第一個大分類之間空一行。
- **跨夜的時間窗要寫兩個日期**（`2026-08-02 14:00–2026-08-03 09:00`），時數照實算（跨午夜要加 24 小時），不要只寫 `14:00–09:00` 讓人以為是負的。
- ⚠️ **2026-08-02 起廢除舊版兩行式**（`已完成掃帶時段 …` ＋ `收錄外電共 …`，另加獨立的標題／時間窗區塊）——資訊重複又分散，一律改用上面三行。
- **時區備註不寫進檔頭**（舊版曾附「台北時間；CNN Newsource 列表 GMT 已換算 +8」）。時間窗一律台北時間，GMT 換算是掃帶 agent 的內部邏輯，見下方「GMT 只給掃帶 AGENT 用，不進素材行」。
- 計數以**整份定版庫存的素材行數**為準（同一則只算一次）；中繼整併比照辦理，反映**累計到當次**的時段與則數。
- ⚠️ **CNN／NHK 側錄段落（S2b）一律不計入檔頭則數**（2026-08-02 訂正，原本併入「其他」是錯的）：一段連線常被切成十幾個 6 碼 TC，計進去會把「收錄外電共X則」灌爆、檔頭失去一眼掃視的價值。檔頭統計的是**通訊社素材則數**，側錄是另一條料源，**完全不納入**（連「其他」也不算）。側錄格式見 [`14-S2b-側錄轉譯摘要.md`](14-S2b-側錄轉譯摘要.md)。
- 每次整併（含中繼）都要**重新算過、覆寫**這三行。可用 `python scripts/s2_validate.py stats {MMDD}晚班交接.txt --window "14:00 - 15:00"` 直接產出（日期由檔名 MMDD 推得）。

### 重大提醒行（2026-08-03 使用者訂案）

**問題**：一份近百則的晚班交接裡，重大突發跟例行素材長得一模一樣，都是三段式的一行，**編輯得自己掃完整份才會發現今天有什麼非處理不可的東西**。0802 深夜 `IN-23SU`（希臘消防直升機撲滅野火時空中相撞、有事發瞬間畫面）就是實例——素材已在庫，但沒有任何機制讓它跳出來。

**做法**：檔頭最後補**重大提醒行**，一則一行：

```
🔴 重大：▲ IN-23SU 希臘消防直升機撲滅野火時空中相撞，有事發瞬間畫面；1人傷、第二架機組下落未明
```

| 項目 | 規則 |
|---|---|
| **位置** | 檔頭**最後**（圖例行之後、第一個 `======` 之前）。落到大分類區塊裡＝錯，`check` 會抓 |
| **格式** | `🔴 重大：{標記}{代碼} {一句話}`——`🔴` 半形空格接 `重大：`；`{標記}` 沿用素材行的 `△`／`▲`／`■`；`{代碼}` 必須是**正文真的存在的素材代碼**（`check` 會比對，找不到＝命中） |
| **一句話怎麼寫** | 事件 ＋ **為什麼重大／畫面亮點**，約 40 字以內。不是摘要的複製貼上——摘要講「發生什麼」，這裡要講「**為什麼你現在就該看這則**」（獨家事發畫面／死傷規模／台灣關聯…） |
| **上限 3 行** | 全都標重大＝都不重大。同時有 4 件以上時只留當班最該先處理的 3 件，其餘靠正文分類 |
| **禁止** | 操作備註（`待查`／`待人工`／`未開詳情`）；只有敘述沒有代碼；同一事件標兩行（取最完整的那則代碼） |
| **正文也要標**（2026-08-03 訂案） | 檔頭點名的那則，**正文素材行也要帶 `🔴`**——位置在**時段標記之後、代碼之前**：`▲ 🔴 IN-23SU (…) ▎…`。行首對齊不變（時段標記仍固定第一位）。檔頭有、正文沒有＝漏，`check` 會抓 |
| **正文 🔴 標過就永久保留**（2026-08-03 訂正） | **檔頭的重大提醒行每輪汰換**（新的大事進來、舊的退場，上限 3 行）；**但正文素材行的 `🔴` 一旦標了就留著，之後每一輪都不准拿掉**。所以「正文有 `🔴`、檔頭已經沒有它」是**正常狀態不是錯**，`check` 不會抓這個方向 |

> **為什麼正文也要標**：檔頭只告訴編輯「今天有這幾則重大」，但一份近百則的交接檔裡，編輯還是得自己往下找那一行在哪。正文帶 `🔴` 就能直接掃到。
> **為什麼標過不拿掉**：檔頭是「當下最該處理的前 3 件」，會一直換；正文的 `🔴` 是「這則素材曾被判定重大」的永久註記——凌晨標的重大素材，早班翻整份檔時還是該一眼看到，不能因為檔頭換人了就消失。
> ⚠️ 側錄行（CNN／NHK）不標 `🔴`——重大提醒只針對通訊社素材行。

### 🟤 已播標記：本台已經做過的素材（2026-08-04 使用者訂案）

**語意**：這則新聞**本台已經做過了**。⚠️ **但素材仍照常摘要入庫、留在庫存裡**——後續發展可能還要再做，`🟤` 只是把優先度降下來，讓編輯一眼略過已處理的，**不是「不要收」、也不是「可以刪」**。

| 項目 | 規則 |
|---|---|
| **符號** | `🟤`（U+1F7E4 棕圓，2026-08-04 使用者選定）。與 `🔴` 同一系列、寬度一致，色調沉、視覺上比 `🔴` 弱，符合「已處理、優先度降低」的語意。⚠️ **一度用過 `⚪` 白圓，實際畫面上辨識度不佳已換掉**；也不要改用空心圓 `○`／`◯`——圓形符號容易與其他標記混淆（`●` 就是因此在 2026-08-04 被換成 `■` 的） |
| **位置** | **時段標記之後、`🔴` 之後、代碼之前**：`▲ 🔴 🟤 IN-23SU (…) ▎…`。兩者**可以並存**（重大素材本台已做過，但還有後續） |
| **誰來標** | ⛔ **只有使用者下令才標，agent 不得自行判斷**——外電網站不會知道 TVBS 播了什麼，這是純人工資訊。性質同 `set-alert`（重大提醒行） |
| **怎麼標** | `python scripts/s2_state.py set-aired --ids RT2995,AP4676455`（取消用 `--clear`）。寫進該則的 `aired` 欄位，**不動 `raw_entry`** |
| **圖例** | 當份有用到才在檔頭圖例行印 `🟤=已做過`，沒用到不印。⚠️ 措辭刻意精簡（2026-08-04 使用者訂正，原為「本台已做過（仍可做後續）」）——檔頭圖例是一眼掃過的東西，四個字就夠；「仍可做後續」屬規則語意，寫在這份文件裡即可，不必每天佔一行 |
| **會不會被清掉** | 同 `🔴`，標了就留著，除非使用者明講要取消 |

> **為什麼還要入庫**：晚班交接是給編輯看的**庫存**，不是待辦清單。已經做過的新聞常常會有後續（傷亡數字更新、官方回應、追蹤報導），素材留著才調得到；直接不收等於後面要重找一次。
> ⚠️ 側錄行（CNN／NHK）同樣不標 `🟤`——標記只針對通訊社素材行。

**什麼算重大**（符合任一即可，拿不準就**標**——漏標的代價遠大於多標）：

1. **重大傷亡／災難／攻擊事件**，且**我們手上有第一手事發畫面或關鍵現場**。
2. **國際級突發**的當日首出素材：戰事開打／停火、元首級人事變動、重大政經衝擊。
3. **與台灣直接相關**的突發（台灣人傷亡、兩岸軍事動態、台廠重大衝擊）。
4. **使用者當場指名**的素材——不必自行判斷，直接標。

**什麼時候撤掉**（每次整併重評，不是標了就長在那）——⚠️ **以下三條只講「檔頭那行」；正文素材行的 `🔴` 標過就永久保留，任何一輪都不准拿掉**（2026-08-03 訂正）：

- 事件已成當日主線、正文已有完整一組素材並各自分好類 → 撤掉，改由分類本身承擔。
- 續報已取代原素材（有更完整的一版）→ 換代碼，不要兩行並存。
- 隔日換檔（新的 `{MMDD}晚班交接.txt`）→ 不繼承，重新評。

**腳本支援**（`scripts/s2_validate.py`）：

```
# 新增／取代重大提醒（可重複給多個 --alert，最多 3 則）
python scripts/s2_validate.py stats {MMDD}晚班交接.txt --window "…" \
    --alert "▲ IN-23SU 希臘消防直升機空中相撞，有事發瞬間畫面；1人傷、第二架機組下落未明"

# 撤掉全部重大提醒
python scripts/s2_validate.py stats {MMDD}晚班交接.txt --window "…" --clear-alerts
```

- ⚠️ **不給 `--alert` 時，`stats` 會自動沿用檔內既有的重大行**。因為檔頭每輪整併都要重算覆寫，若不沿用，一次例行 `stats` 就會把上一輪標好的重大行洗掉——這是設計上刻意的保險，不要改成每次重生。
- `check` 會抓五種錯：代碼在正文找不到、沒寫代碼、超過 3 行、行落在檔頭以外，**以及檔頭點名了但正文那行沒標 `🔴`**。⚠️ **反向（正文有 `🔴`、檔頭沒有）不算錯**——那是檔頭汰換後的正常狀態。

## 已定案的輸出格式（先前確認，不再重決）

- 三層骨架：**大分類 → 中主題 → 小分題**（素材行掛在小分題底下，不算一層）——標記寫法見下節
- **中主題要實際寫出來**（2026-07-30 使用者訂正）。範例檔裡偶爾缺中主題，那是人工省略的瑕疵、不是規範——產出時一律要有
- 中主題**怎麼分**尚未定案，**暫定由 AI 依素材內容判斷**（分軸不固定：國別／面向／階段皆可，不套固定字庫）。見 `TODO.md`
- **同組必須相鄰**；歸錯位要歸位（範例① 天氣段「西人難過」即為歸錯的實例）
- **摘要階段不掐 BITE**（指不做逐字＋精確 TC）——那是下游 S7 的事；`BITE：` 段寫的是濃縮引言，不衝突
- 大分類通常固定，隨時事機動增減

## 庫存檔格式（三層標記，2026-07-30 定案）

| 層級 | 寫法 | 說明 |
|---|---|---|
| 大分類 | `======{名稱}======` | 前後各 6 個等號，**整份樣板順序全列**（**無素材的大分類也要留空殼**，見「新開／整併晚班交接」），不重排、不省略 |
| **中主題** | **`【{名稱}】`** | 全形方括號包圍，**一律要寫**，分軸由 AI 依內容判斷 |
| 小分題 | `{名稱}`（裸行） | **通常 4 個全形字元**；可夾雜數字或英文符號，但**不可整行都是數字或英文**；**相似素材應共用同一小分題**（見下） |
| 素材行 | `{代碼} ({備註}) [(BITE)] {摘要}▎畫面：…▎BITE：…` | 掛在小分題底下，三段式，詳見下方「素材行的寫法」 |

**空行／分隔規則**（2026-07-31 使用者訂正；同日再補「小分題 `+` 分隔」）：

| 位置 | 規則 |
|---|---|
| 大分類 `======…======` **與** 其下第一個中主題 `【…】` 之間 | ✅ 空一行 |
| **中主題與中主題** 之間（同一大分類內換下一組 `【…】`） | ✅ 空一行 |
| 中主題 `【…】` **與** 小分題 之間 | ❌ 不空行（緊貼） |
| 小分題 **與** 素材行 之間 | ❌ 不空行（緊貼） |
| 同一小分題下的**素材行與素材行**之間 | ❌ 不空行（緊貼連排） |
| **小分題與下一個小分題**之間（同一中主題內換下一組裸行標題） | ✅ **獨立一行只寫 `+`**（半形加號、前後不空行），**不要用空行**隔小分題 |
| 不同大分類之間（上一區塊末與下一 `======…======`） | ✅ 可空一行（大分類本身已是強分隔） |

錯誤範例（舊寫法，不要再產出）：

```
【休達移民】

岸際聚集

RT2333 …
```

```
【休達移民】
岸際動態
RT2333 …
回流折返
RT2302 …
```

（小分題之間用空行、或不加 `+`，都難掃。）

正確範例：

```
======社會======

【休達移民】
岸際動態
RT2333 (摩洛哥岸邊 海巡 稿未到) ▎…▎畫面：…▎無BITE。
RT2302 (回流 圍籬破口) (BITE) ▎…▎畫面：…▎BITE：移民「…」
+
政要表態
RT2360 (英相 限英國) (BITE) ▎…▎畫面：…▎BITE：英國首相「…」

【希臘命案】
箱屍命案
RT2334 ▎…
```

**小分題的長度與字元限制**（2026-07-30 使用者補充，並以範例①② 實測校準）：

- **通常 4 個全形寬**。實測：範例① 84.5%（49/58）、範例② 77.8%（56/72）是 4 字；扣掉混進統計的雜訊（`+` 分隔符、講者行如 `川普：`／`柏林市長：`、逐字內文句如 `目前仍有待觀察。`）後，真正的小分題幾乎全是 4 字。範例③ 有 3 字（`屋子倒`）與較長的（`空拍橋斷、煙囪倒塌`），所以是「通常」而非硬性上限。
- **算的是全形寬度，不是字元數**——英數佔半形，兩個才吃掉一格。實測命中 `陸AI廚師`、`陸AI機人`：5 個字元，但正好 4 個全形寬。這正是「可以夾雜數字或英文」成立的原因。
- **不可整行都是數字或英文**。
- ⚠️ **最後這條不只是風格，是解析的保命條款**：小分題是**裸行、沒有任何標記**，唯一能跟素材行區分的依據就是「它不長得像素材代碼」。若允許整行數字或英文，就會跟 `RT1396`、`AP4675135`、`IN-02TU`、`CNN 151328` 這些代碼行撞在一起，機器分不出這行是標題還是素材。

**小分題要夠粗、不要一則一個**（2026-07-31 使用者訂正）：

- **相似／同事件／同面向的素材，應掛在同一個小分題底下**（同一小分題下可連排多則素材行，中間不空行）。
- ❌ 不要為每一則素材各開一個幾乎同義的小分題（例如「岸際聚集」「移民回流」「圍籬破口」其實都可併成「岸際動態」或「休達移民」下的一個粗分題）。
- ✅ 只有**事件階段、地點、主角或新聞面向明顯不同**時才另開小分題。
- 整併時若發現既有庫存小分題過碎，**優先併入相近既有小分題**，不要再往下切。

**素材分成四大種類**（2026-07-30 使用者訂定的分類軸）：

| # | 種類 | 涵蓋 | 寫法狀態 |
|---|---|---|---|
| 1 | **側錄** | CNN／NHK（日後可能更多台） | ✅ 已定案，見 [`14-S2b`](14-S2b-側錄轉譯摘要.md)：`{來源} {6碼}` ＋大段摘要 |
| 2 | **外電網站** | AP／RT／**CNN Newsource**／ENEX／ABC | 🔨 AP／RT／Newsource 討論中（見下方「素材行的寫法」）；**ENEX 與 ABC 暫不處理**，見 `TODO.md` |
| 3 | **YouTube 頻道** | 韓聯社／CNA／CNN 官方頻道等 | ⬜ 未討論，見 `TODO.md` |
| 4 | **其他網址素材** | 烏俄等戰事的社群／OSINT 來源，名單見 [`11-wire-rundown-organizer.md`](11-wire-rundown-organizer.md) 模式 C | ⬜ 未討論，見 `TODO.md` |

> ⚠️ **CNN 同時出現在第 1 類與第 2 類**，兩者是不同東西、代碼形式也不同：
> - **CNN 側錄**（第 1 類）＝ 自家錄下的 CNN 訊號，代碼是 `CNN {6碼TC}`，逐字稿在「掃帶歐印萬」資料夾
> - **CNN Newsource**（第 2 類）＝ 官方供稿平台，代碼是 `{2字母}-{數字}{星期兩碼}`（`IN-07SU`），可下載母帶與 script
>
> 判斷依據看代碼形式，不要看到 CNN 就歸同一類。

**第 2 類的代碼規則**（2026-07-30 使用者訂定）：

| 來源 | 代碼形式 | 說明 |
|---|---|---|
| Reuters | `RT` ＋ 4 碼數字 | **`RTV` 就是 `RT`，一律統一寫成 `RT`**，不保留 RTV 寫法 |
| AP（原生） | `AP` ＋ 7 碼數字 | 實測 43 次 |
| AP（cctv） | `APcctv` ＋ 6 碼數字 | **要收**——AP 站內 CCTV 供稿的編碼；**免費可用**（見下） |
| RT（CCTV＋等） | 仍是 `RT` ＋ 4 碼 | Source／稿內標 **CCTV／CCTV+／China Central Television** 時**要收**，備註標 `(CCTV)`；**不是**另開代碼前綴 |
| CNN Newsource | `{2 字母}-{2~3 數字}{星期兩碼}` | 前綴不固定，見 [`05-material-numbering.md`](05-material-numbering.md) |

> 🚫 **其他前綴／後綴的 AP 付費 stringer／UGC 供稿一律排除，掃帶階段就不錄**（`vrb017589`／`stltd029775`／`un010491` 這類代碼）。理由：通常要額外付費扣點數（`10cr`／`15cr`）。
>
> ✅ **CCTV／APcctv 必收（2026-07-31 使用者訂正）**：**免費可用，掃帶不可整批略過。**
> - **AP**：代碼 **`APcctv`＋6 碼**，或 Metadata `Provided by`／Source 為 CCTV 相關 → **照收**，備註可標 `(CCTV)`（代碼已是 `APcctv` 時備註可省略重複字樣，保留畫面精華詞即可）。
> - **RT**：Details／稿頭 **SOURCE: China Central Television (CCTV)**、`CCTV+`、slug／headline 含 `CCTV+` → 代碼仍 **`RT####`**，備註**必含** `(CCTV)`；限制常見 `No access Chinese mainland` → 備註可並排 `(限非陸媒)` 或寫進限制語感，**不要**因此整則丟棄。
> - ❌ **禁止**把「CCTV／CCTV+／APcctv／CNS」與「付費 stringer」混為一談、寫進「已排除：…付費」。  
> - ❌ **禁止**只因 Edit No. 落在 `56xx`、列表有 **Paid Content** 徽章、或 slug 含 China，就當付費不錄——**CCTV 在本帳可用範圍內是免費素材**。  
> - 真正要排除的仍是：要**另扣點**的 stringer 前綴（`vrb`／`stltd`／`un`…）、以及規則另訂的 UGC（例 RT 特定 UGC 條）。  
> - **CNS（China News Service）**：與 CCTV **不同源**；若詳情下載區為 **FREE TO ME／Included** 或帳號可免點下載 → **可收**，備註標 `(CNS)`；若明確需另購點數再排除。不可再寫「CNS／CCTV+ 一律付費排除」。
>
> ✅ **SNTV 例外（2026-07-31 使用者訂正；同日晚再修識別）**：**可以收**，備註**必須標 `(SNTV)`**。**SNTV＝AP 體育新聞供稿**，易辨特徵：
> 1. **編號一定是 `AP5` 開頭**（實測 `546xxxx` 段，寫成 `AP5466549`），**不是 `AP4`（`467xxxx` 等 AP 原生／一般新聞）**。
> 2. 主題是**體育**（足籃球、FIFA、WRC、NBA…）；**中東／社會／天氣等非體育＋`AP4…` 一律不是 SNTV**。
> 3. 清單常見 `Source: SNTV`／slug 含 `SNTV`／`sntvEditPlay`／`SNTVPLAY`；詳情 `Provided by: SNTV`（頁尾常註 not produced/verified by AP）。
>
> ❌ **禁止**用 `AP4…` 當 SNTV 範例或備註：`AP4675888`、`AP4675927` 等**都不是** SNTV。  
> ✅ 正確範例：`AP5466549 (SNTV 因凡提諾摩洛哥)`／`AP5466546 (SNTV 詹姆斯轉隊)`。
>
> ⚠️ **SNTV 同主題重複（任何原因）只收一則（2026-08-03 使用者訂案，同日補充「不限直橫式」）**：SNTV 清單常見同一事件被**不同編號重複收錄**，不限於直式／橫式差異——實例：`AP5466712`／`AP5466713` 兩則標題、摘要、BITE 引言**完全相同**（謝芭蓮卡受訪同一段話），純粹是清單重複列出。**只留一則，其餘不收**：
> - 判準＝**標題與描述內容高度相似或完全相同**（同一事件、同一受訪片段、同一引言），不限於畫面比例差異。
> - **兩則內容完全相同時**：任選一則收錄即可（無先後可比時取編號較小者，避免任意猶豫）。
> - 兩則若有實質差異（不同角度、不同受訪片段、不同時間點）就不算重複，**都收**，不要因為同事件同主題就臆測合併。
>
> ⚠️ **同主題直式／橫式重複只收橫式（2026-08-03 使用者訂案）**：上面那條「內容完全相同」之外，SNTV 清單同一體育主題有時會**同時供**直式（手機直式拍攝／社群比例）與橫式兩種版本，**內容不同、只有拍攝比例不同**，各自是**不同的 `AP5` 代碼**。電視新聞用橫式，**直式版本一律放棄，只收橫式**。
> - **判準是標題／描述，不是機械欄位**：AP 清單目前沒有結構化的 Aspect Ratio 欄位可讀，靠標題或縮圖看出是否為直式拍攝（常見寫法含 `(Vertical)`／`Portrait`／直式構圖描述），既有備註標 `直式` 就是這樣判來的。
> - **配對依據＝同一事件／同一時間點的標題高度相似**（人物、地點、賽事、時間點幾乎一致，只差鏡頭比例），不是同運動項目就算重複。
> - **兩版都在清單裡時**：只把橫式那則收進 `add-batch`；直式那則**不收錄**，不必特別記備註或 `needs-review`（純粹略過，不是待人工）。
> - **只有直式、沒有橫式版本時**：照現行規則收錄，備註標 `(直式)`（見下方「備註↔摘要」節），不因為是直式就整則丟棄——這條只處理「兩版並存」的情形。
> - 不確定是否為同一主題的重複配對（例如標題相似但看不出是否同一事件）→ 兩則都收，不要臆測合併／捨棄。
>
> ⚠️ **列表文字黏標（2026-07-31 實錯）**：`get_page_text` 會把**下一則**體育卡上的獨立標籤 `SNTV` 黏在**上一則** `AP4` 編號後面，形如 `… 4675927 | 01:40 | SNTV | …5466549…`。**不可**因此把 `AP4` 判成 SNTV；SNTV 旗標只認**本則**的 `Source:`／`Provided by:`／**本則 ID 是否 `5` 開頭**，且優先用結構化卡片欄位，不要對扁平字串做「ID 後第一個 SNTV 字樣」歸屬。
>
> ⚠️ 付費 stringer 排除比 [`11-wire-rundown-organizer.md`](11-wire-rundown-organizer.md) 現行規則**更嚴格**——那邊寫的是「先不收進稿單，只在對話中列出候選讓使用者決定」。差別在於：`common/11` 是人工發動的補充素材搜集（S4c），使用者當下可以決定要不要花點數；而 **S2 掃帶是自動的，不該把要另扣點的 stringer 掃進庫存**。**CCTV／APcctv／（免點的）CNS、SNTV 都不在排除列。**

**素材行的寫法**（2026-07-30 使用者訂定；**2026-07-31 再訂**備註禁重複 BITE、FILE→資料畫面、Newsource PKG/DONUT 長片標 SOT、**備註禁內容重複第二括號**、**`BITE：` 必寫講者**、**行尾時長 `▎MM:SS`**；**2026-08-02 再訂**摘要前補 `▎` 標記、**摘要硬上限150字**）：

```
{代碼} ({備註}) [(BITE)] ▎{摘要}▎畫面：{逐項}▎BITE：{講者}「{濃縮引言}」[▎MM:SS]
{代碼} ({備註}) ▎{摘要}▎畫面：{逐項}▎無BITE。[▎MM:SS]
```

⚠️ **摘要段開頭也要有 `▎`（2026-08-02 使用者訂正）**：原格式摘要緊接在備註後面沒有分隔符，跟其他段（`畫面：`／`BITE：`）不一致，機器判讀也不好切。**摘要前補一個 `▎`**，讓四段（摘要／畫面／BITE／時長）分隔符統一。

實例（使用者 2026-07-30 提供；備註已依 7/31 規則不重複寫 BITE；BITE 段已依同日晚規則補講者；時長段示意 7/31 定案格式；**摘要前 `▎` 依 8/2 規則補上**）：

```
RT0595 (滅餘火 毀損對比 畫面好) (BITE) ▎法國萊日－卡普費雷野火，前職消呂多維克·波萊特與家人徹夜護鄰，與晚到消防車合力救4戶。▎畫面：滅餘火、拖車與屋全毀、灑水器、為鄰園噴水、防泥炭、毀損對比。▎BITE：呂多維克·波萊特「我和兒子徹夜守住，拒撤離，終救四戶」▎03:42

RT0681 (空拍濃煙 民宅烈焰) ▎馬德里郊外多處山火延燒，西班牙首度因野火全國緊急；近2萬人撤離，逾2000人10架機阻擋火場匯合。▎畫面：空拍濃煙、民宅旁烈焰；消防員、消防车與直升機待命；民眾翻圍籬佈水線撲救後院與起火車。▎無BITE。▎02:15
```

**各段的分工**

| 段 | 內容 | 規則 |
|---|---|---|
| 第一段 | `{代碼} ({備註}) [(BITE)] {摘要}` | 備註放**最精華的一兩個詞**供一眼掃視；摘要一句話講完事件，**硬上限150字**（含標點，不含代碼/備註/畫面/BITE段），WRAP／多國整理包等結構性長文**不例外**——超過就分成多句或拆進畫面段，不要整段塞進摘要 |
| `畫面：` | 逐項列出畫面內容 | 頓號分隔、同群用分號、句號收尾。這裡放**完整清單**，與備註的「精華詞」分工 |
| `BITE：` | 引言 | **必寫講者**＋**濃縮版**（不是逐字）；無引言時整段寫 `無BITE。` |
| `MM:SS`（可省略） | 素材時長 | 放在**整行最後**（`BITE：`／`無BITE。` **之後**）；**有就寫、沒有就整段省略**（不要寫 `▎?`／`▎00:00` 佔位） |

**分隔符用全形直線 `▎`，不加前後空格。**
- ❌ 不用 `///`——會跟多來源代碼的 ` / ` 混淆，人工手打也容易打成 `//` 或 ` / / `。
- ✅ `▎` 在 39,851 字的既有語料裡零出現，不與 `/`（多來源代碼）、`+`（**小分題**分隔）、`：`（段落標籤與地點前綴）衝突。

**`(BITE)` 第二括號只在「有 BITE」時出現**（使用者 2026-07-30 訂定）。無 BITE 時不標，靠尾端的 `無BITE。` 表達即可。用途是讓編輯只看行首前半段就知道這支有沒有引言，不必把整行約 90 字讀完。

⚠️ **代碼後半形括號的職能**（2026-07-31 使用者訂正；同日晚補「備註↔摘要勿重標」）：
1. **第一個 `({備註})`**：畫面精華／限制／種類／來源標（`SNTV`／`CCTV`／`資料畫面`／`稿未到`…）——**一個括號寫完**。
2. **第二個括號**：有引言時**只允許** `(BITE)`；無引言時**不要**第二括號。
3. ❌ **禁止**第二括號再寫與第一括號**內容重複或同義**的詞（含整詞重出）。  
   ❌ `RT2245 (商場清運 稿未到) (稿未到) …`  
   ✅ `RT2245 (商場清運 稿未到) …`  
   ❌ `RT2333 (摩洛哥岸邊 海巡 稿未到) (稿未到) …`  
   ✅ `RT2333 (摩洛哥岸邊 海巡 稿未到) …`  
4. `稿未到`／`稿簡`／限制語等若已寫進第一備註，**不要**再開第二括號重標；需要補狀態時**併進第一備註**（例 `(… 稿簡 稿未到)`），不要變成兩個 `()`。
5. ⚠️ **備註已寫的來源／格式／狀態標，摘要與 `BITE：` 段不要再用全形 `（）` 重標**（2026-07-31 晚清查「素材摘要()註記重複」後補漏）：  
   - **來源／格式標**（只寫進半形備註）：`SNTV`、`CCTV`／`CGTN`、`資料畫面`、`整理包`、`直式`、`PKG`／`DONUT`、`SOT`、`第三方`、`限日本`／`限英國`／`限非陸媒`／`限澳`／`限秘魯`…  
   - **狀態標**（只寫進半形備註）：`稿未到`／`稿簡`。摘要**不要**再寫 `（完整script待補）`／`（script待補）`／`（early／待完整稿）`；`BITE：` 引號後也**不要**再掛 early／待補括號。  
   - **操作備註**（給 agent 自己看的）：`（列表摘要）`／`（詳情頁待補）`／`（急用請開Shotlist）` **禁止寫進晚班交接素材行**（半形或全形都不行）。列表級收錄就直接寫一句事件摘要；不夠再回站補，不要用括號對編輯喊話。  
   - **全形 `（）` 只留給摘要內文**（譯名原文、單位換算、人名括註等），**不是**第二套備註欄。  
   - ❌ `AP5466554 (SNTV 卡塞米羅 邁阿密 直式) (BITE) …（SNTV 直式）。`  
   - ✅ `AP5466554 (SNTV 卡塞米羅 邁阿密 直式) (BITE) 卡塞米羅加盟國際邁阿密…`  
   - ❌ `RT2359 (… 稿未到) (BITE) …睡覺（完整script待補）。`  
   - ✅ `RT2359 (… 稿未到) (BITE) …睡覺。`（稿未到只在備註）  
   - ❌ `RT5617 (CCTV 毛寧談日情報局) …行事（限非陸媒）。`（限制應併進備註）  
   - ✅ `RT5617 (CCTV 毛寧談日情報局 限非陸媒) …行事。`  
   - **允許**：備註是短標籤、摘要用**完整句子**敘事時，自然出現同一專名（如備註 `(挪威足總警告)`、摘要 `挪威足總警告 FIFA…`）——這是可讀性，**不是**重標。禁止的是「同一套來源／格式／狀態標再抄一次」。  
   - **允許**：`畫面：` 段描述鏡頭內容時寫「資料畫面／空拍／檔案訓練…」等**畫面描述詞**（與備註標記職能不同）。

⚠️ **第一個 `({備註})` 禁止再寫 `BITE`**（2026-07-31 使用者訂正）：有引言時**只**用第二括號 `(BITE)` 與尾端 `▎BITE：…` 表達；備註裡再寫 `BITE` 是重複、一律刪掉。  
❌ `RT2244 (官房長官 BITE 限日本) (BITE) …`  
✅ `RT2244 (官房長官 限日本) (BITE) …`

⚠️ **`BITE：` 段必寫講者**（2026-07-31 使用者訂正）：格式 **`▎BITE：{講者}「{濃縮引言}」`**（講者與左引號之間**不**加冒號；多講者可並排，如 `盧比歐「…」、川普「…」`）。編輯必須一眼知道**誰說的**，不能只剩引號內文。  
❌ `▎BITE：「情況正在好轉，但請大家不要鬆懈」`  
✅ `▎BITE：西班牙首相桑切斯「情況正在好轉，但請大家不要鬆懈」`  
✅ `▎BITE：盧比歐「…」、川普「…」`  
- 講者用**短稱**即可（官銜＋姓／常見譯名：`川普`／`普欽`／`官房長官木原稔`／`毛寧`／`居民`／`消防隊員`…）；匿名受訪可寫角色（`移民`／`災民`／`店家`）。  
- 同一則多則引言：`講者A「…」、講者B「…」`（同一人多句可 `講者「句一…句二…」` 或分兩段濃縮）。  
- **不可**只有「這句在講什麼」卻**沒有是誰講**。

⚠️ **`BITE：` 段是濃縮不是逐字**——像 `呂多維克·波萊特「我和兒子徹夜守住，拒撤離，終救四戶」` 這樣，讓編輯知道**誰**、**這句在講什麼、值不值得用**。**不可直接拿去當 SB**；逐字原話與精確 TC 是下游 [`07-bite-assistant.md`](07-bite-assistant.md)（S7）的事。

> 📌 **這是新版三段式，取代範例①② 的舊寫法**（舊的只有「代碼＋一句話＋句尾 BITE」）。舊範例仍可參考排版與分類，但素材行以本節為準。

**細部規則**

- **代碼後接一個半形空格**。實測範例有 26/101 則代碼與摘要黏在一起（`IN-09TU中國航班…`），那是瑕疵，一律補空格。
- **多來源用 ` / ` 相接**，斜線**前後各一個半形空格**（`AP4675107 / IN-02TU`）。實測範例寫成 `AP4675107/IN-02TU` 沒有空格，同樣是瑕疵。
- **備註緊接在代碼後面、摘要之前**，用**半形括號 `()`**。實測範例多數把備註放在最後，只有 `RT0957 (炸瞬 無聲) 監視器畫面…` 這種放在前面——**以放前面為準**，因為編輯掃清單時先看到「這支畫面值不值得看」才決定要不要讀摘要。
- 備註要**非常簡短**，涵蓋四件事：**畫面重點／使用限制／畫面種類／畫面好不好**。電視新聞最重視畫面吸不吸引人，全靜態、沒有吸睛畫面就不是好素材，這點要讓編輯一眼看到。
  - 畫面種類實例：`(空拍圖)`／`(氣象圖)`／`(棚內解說)`／`(記者STAND)`／`(有google街景前後對比)`／`(共4張照片)`／`(有多張圖)`／`(白天)`／**`(資料畫面)`**
  - 使用限制實例：`(標來源)`／`(180天下架)`／`(可上網不下架)`
  - 畫面／聲音重點實例：`(炸瞬 無聲)`／`(畫面好!!)`
  - **資料畫面（舊稱 FILE／檔案）**（2026-07-31 使用者訂正）：通訊社介面的 **FILE／「檔案」**（庫存／重發／非當日新攝畫面）**一律改注記 `資料畫面`**，**禁止**在備註再寫 `FILE`、`檔案`、`File`。  
    ❌ `(大衛營 內閣會 FILE)`／`(特斯拉 上海廠 檔案)`  
    ✅ `(大衛營 內閣會 資料畫面)`／`(特斯拉 上海廠 資料畫面)`  
    摘要或 `畫面：` 段若描述畫面上的歷史影像，可用中性敘述，但**備註標記統一用「資料畫面」**。
  - **整理包（RT 介面 WRAP）**（2026-07-31 使用者訂正）：Reuters Connect 標題／類型標 **WRAP**＝把同事件多支 edit／相關畫面**彙編成一支**的**整理包**（常含既有 edit 編號、未必有全新畫面）。**備註第一括號必標 `整理包`**（可並排主題／限制／稿未到，如 `(整理包 休達回流 稿未到)`）。  
    - ❌ 只在摘要開頭寫 `WRAP：…`、備註卻寫 `(休達WRAP …)` 而不標整理包  
    - ✅ `RT2295 (整理包 休達回流 稿未到) 西國稱…`  
    - 摘要**不要**再以 `WRAP：` 當前綴；主題與事件一句講完即可。`畫面：` 可註「彙編自 7/30–31 相關 edit」之類。  
    - WRAP 仍可能 `SCRIPT TO FOLLOW`／無新素材 → 照決策 4 標 `(稿未到)` 並保留列，**不**因是整理包就刪。
  - **供稿來源標註：SNTV（2026-07-31）**：**僅** `AP5…`（體育）且本則 `Source:`／`Provided by: SNTV` → 備註**必含** `(SNTV)`（可並排，如 `(SNTV 詹姆斯轉隊)`）。**`AP4…` 禁止標 SNTV**。SNTV **可收、不排除**。列表扁平文字易把下一則 SNTV 徽章黏到上一則——見上文「列表文字黏標」。
  - **供稿來源標註：CCTV／APcctv（2026-07-31）**：**免費、必收**。RT 備註**必含** `(CCTV)`（例 `RT5617 (CCTV 毛寧談日情報局) (BITE) …`）；AP 用代碼 `APcctv######` 或備註 `(CCTV)`。見上文代碼表。
  - **CNN Newsource：PKG／DONUT 長片標 SOT**（2026-07-31 使用者訂定）：詳情 **Footage Type** 為 **`PKG`** 或 **`DONUT`**，且**時長超過 1 分鐘** → 備註**必含** `SOT`（可並排其他詞，如 `(地方 海灘 SOT)`、`(第三方 睡眠危機 SOT)`）。  
    - ≤1 分鐘的 PKG／DONUT **不**標 SOT（短包／短甜甜圈當 VO 用即可）。  
    - 非 PKG／DONUT（如 raw VO、STILL、TAGS-only）**不**因有旁白就亂標 SOT。  
    - 此 `SOT` 寫在**第一個備註括號**內，與第二括號的 `(BITE)` **不同職能**：SOT＝整包可用成音／成稿感；BITE＝有可掐引言。兩者可並存：`(地方 SOT) (BITE) …`。
  - **禁止把 Newsource 上架 GMT／台北換算寫進備註或摘要**（2026-07-31 使用者訂正）：GMT **只**用於掃帶 agent 篩時間窗；素材行不寫 `GMT xx:xx`、`台北xx:xx` 這類時刻。見「各站時間／時區 → GMT 只給掃帶 AGENT 用」。
  - ⚠️ 上列**不是封閉清單**，只是目前從三份範例歸納到的。正在蒐集歷史稿單做語料分析以求完整，見 `TODO.md`。
- **行尾時長 `▎MM:SS`**（2026-07-31 使用者定案）：
  - **位置**：整行**最後一段**——接在 `▎BITE：…` 或 `▎無BITE。` **之後**，再起一段 `▎MM:SS`（`MM` 可為一位或多位分鐘，`SS` 兩位秒；例 `▎0:45`、`▎02:35`、`▎75:10`）。
  - **有則寫、缺則省略**：列表／詳情看得到 Duration 就抄；看不到、稿未到、只寫列表摘要來不及開詳情 → **整段不寫**（不要 `▎?`、不要空白 `▎`）。
  - **不溯及既往**：**本規則生效後新掃入的素材**才必遵守；**不**要求回填已在庫存／`raw_entry` 裡的舊則。
  - **與 `SOT` 並存**：Newsource PKG／DONUT **>1 分**仍依上文在備註標 `SOT`；有完整 `MM:SS` **不**取代 `SOT` 關鍵字（編輯掃備註仍要一眼看到成音包）。
  - **資料來源**：AP／RT／CNN 列表或詳情的 Duration 欄位皆可；優先用通訊社標示時長，**不要**為了填時長去本機下載量片。
  - ❌ `…▎BITE：…` 中間插入時長、或寫進備註 `(02:35)`  
  - ✅ `…▎BITE：講者「…」▎02:35`  
  - ✅ `…▎無BITE。▎01:08`  
  - ✅ `…▎無BITE。`（時長未知時到此結束）
- **括號的半形／全形有分工，不可混用**（實測 109 個括號零反例）：
  - **半形 `()` ＝ 備註標記**（給編輯看的畫面／限制資訊）
  - **全形 `（）` ＝ 摘要內文**（譯名原文對照如 `（Patriot）`、單位換算如 `（約攝氏43度）`，依 [`00-寫稿通則.md`](00-寫稿通則.md)）
  - 這不只是排版偏好——半形是機器判斷「這是備註」的唯一依據。

完整長相：

```
======天氣======

【西班牙山火】
西國緊急
RT0681 (空拍濃煙 民宅烈焰) ▎馬德里郊外多處山火延燒，西班牙首度因野火全國緊急；近2萬人撤離，逾2000人10架機阻擋火場匯合。▎畫面：空拍濃煙、民宅旁烈焰；消防員、消防车與直升機待命；民眾翻圍籬佈水線撲救後院與起火車。▎無BITE。▎02:15
+
西相講火
RT1396 (標來源) (BITE) ▎西班牙首相桑切斯表示山火情況正在好轉，敦促民眾保持謹慎。▎畫面：首相受訪、災區勘查。▎BITE：西班牙首相桑切斯「情況正在好轉，但請大家不要鬆懈」▎01:20

【法國山火】
法人護鄰
RT0595 (滅餘火 毀損對比 畫面好) (BITE) ▎法國萊日－卡普費雷野火，前職消呂多維克·波萊特與家人徹夜護鄰，與晚到消防车合力救4戶。▎畫面：滅餘火、拖車與屋全毀、灑水器、為鄰園噴水、防泥炭、毀損對比。▎BITE：呂多維克·波萊特「我和兒子徹夜守住，拒撤離，終救四戶」▎03:42
```

（上例已依 2026-07-31：中主題之間空行；**小分題之間用獨立一行 `+`**；同小分題下素材緊貼；**行尾可有 `▎MM:SS`**。）

側錄段落（[S2b](14-S2b-側錄轉譯摘要.md)）同樣掛在小分題底下，只是素材行換成「來源＋6 碼」開頭的大段摘要：

```
======美伊======

【外交斡旋】
談判有望
CNN 151328
CNN記者 Eleni Giokos
外交途徑仍有希望，阿曼、巴基斯坦、卡達都在試圖促成突破…
```

> **為什麼中主題要加 `【】`**：大分類有 `======` 可辨識，但中主題與小分題原本都是裸行，**機器無法分辨哪一層**（人工寫時靠語感，自動產出與回讀都不行）。加全形方括號後三層各有依據，且不影響閱讀。範例①② 沒有這個標記，是因為當時中主題本來就常被省略。

## 大分類清單（使用者提供的樣板，2026-07-30）

**權威來源，順序照抄，不要自行重排或增刪。** 兩處各存一份，內容相同：

- repo：[`wire-rundown-cases/2026-07-30-大分類樣板.txt`](wire-rundown-cases/2026-07-30-大分類樣板.txt)
- Drive：`Claude共用/自動掃帶系統/`**`晚班交接大分類樣版.txt`**

> ✅ **2026-07-30 晚使用者已處理 Drive 正本**：舊檔（缺財經／娛樂、第 5 格仍是「以巴」）已清除，現在資料夾內就是正確版 `晚班交接大分類樣版.txt`（含財經／娛樂、美伊／中東切法）。**讀 Drive 就讀這個檔名**，不必再找帶日期後綴的副本。

```
======(時效性特殊 例如"熊本地震")======
======大陸======
======關稅======
======美伊======
======中東======
======烏俄======
======美國======
======政治======
======財經======
======社會======
======天氣======
======體育======
======科技======
======娛樂======
======話題======
```

> **財經／娛樂是 2026-07-30 依使用者指示補上的**（原樣板沒有）。插入位置參照範例①②——兩份範例都是「財經」在政治與社會之間、「娛樂」在科技與話題之間。**Drive 正本已同步含這兩格**（同日晚使用者確認）。

### 新開／整併晚班交接：必須整份套樣板（2026-07-31 使用者訂正）

1. **新開** `{MMDD}晚班交接.txt`（當日第一份庫存）時：**先複製整份大分類樣板**再寫 header、再注入素材——**不可**只輸出「今天有稿的大分類」。
2. **沒有素材的大分類也要保留標題列**（`======大陸======` 等），底下可為空（僅保留樣板慣用的空行結構即可）。
3. **增量整併**時同樣維持完整 15 格順序：漏掉的大分類要**補回空殼**，已有內容的只在對應格內增刪中主題／小分題／素材，**不重排大分類順序**。
4. 歸類時對照樣板名稱：例如俄烏戰事／俄羅斯社會影響 → **`======烏俄======`**，不要塞進「話題」只因當日先前沒開該格。
5. ❌ 錯誤：只有「中東、美國、社會…」等有料區塊。✅ 正確：時效 → 大陸 → 關稅 → 美伊 → 中東 → **烏俄** → 美國 → … → 科技 → 娛樂 → 話題（中間空區也在）。

五個要點：

1. **分隔符是 `======{名稱}======`**（前後各 6 個等號，同一行）。範例①② 裡看到的 `\====伊朗=====` 是 Google Docs 匯出的跳脫殘留，**以本樣板為準**。
2. **第一格是機動格**，⚠️ **2026-08-03 使用者訂正：機動格放什麼由使用者在當天「晚班交接」開啟時決定**（0802 決定的是「熊本地震」），**agent 不自行判斷要不要開、也不自行命名**。使用者沒指定就維持樣板原樣的空機動格。所以「開新分類」不是在清單中間插一格，而是使用者啟用最上面這一格。
3. **「美伊」與「中東」的分界**（2026-07-30 定義，2026-08-03 使用者細化為主體歸屬制，見下方「大分類歸位通則」）。
4. **未歸類的進「話題」**（清單最後一格）——但前提是**樣板上已有的格（如烏俄）必須先用對**，不能因為懶得開空格就把該進烏俄的丟話題。
5. **整份樣板必現**（本節上一小節，2026-07-31）：空分類也要放。

### 大分類歸位通則（2026-08-03 使用者訂案）

> 🔴 **本節部分內容已知有誤，全盤檢討中（2026-08-04 使用者立案）——照做前先讀這段。**
>
> 使用者 2026-08-04 定調：**`財經（股市）`／`娛樂`／`體育` 是「題材格」，優先級高於地緣格**，理由是**這些主題非常明確、要獨立放**，不能照「越前面越優先」硬套。所以：
> - ✅ **美股歸「財經」是對的**——下方表格「美股 → 美國」那列**是錯的**，不要照做。
> - ✅ 美國體育歸「體育」、好萊塢歸「娛樂」同理。
> - ⚠️ **例外**：財經若涉及**川普政策**（例如關稅）→ 歸 **`關稅`** 或 **`美國`**，不留財經。
>
> 「越前面越優先」這條線性通則描述不了實務（0803 定版 435 則實測，依字面執行會把體育 5 則、娛樂 2 則、財經 5 則、話題 4 則全部判成歸錯，但那些其實都是正確做法）。**層級模型正在重新定義**，見 [`TODO.md`](../TODO.md)「全盤檢討分類問題」節。在那之前：**地緣格之間**的判定（下方主體歸屬制）仍然有效照用，**題材格 vs 地緣格**則依上面三條澄清處理。

**核心規則：清單越前面＝優先層級越高。一個素材同時滿足兩個大分類時，一律放最前面那一個。**

0803 實錯兩例（就是這條規則要防的）：
- ❌ 中國經濟相關 → 丟「財經」　✅ 應歸 **「大陸」**（大陸在財經之前）
- ❌ 美國政治 → 丟「政治」　　　✅ 應歸 **「美國」**（美國在政治之前）

**三個地緣格用「主體歸屬制」判定**（2026-08-03 使用者定義，先判主體再套優先序）：

| 大分類 | 收什麼 |
|---|---|
| **美伊** | 美國與伊朗之間的一切，**以及所有跟伊朗相關的事務**（伊朗國內、伊朗與他國、涉伊制裁／核談判／代理衝突中的伊朗角色） |
| **中東** | **除伊朗外**的所有中東事務——以色列、巴勒斯坦（加薩）、黎巴嫩、敘利亞、海灣國家、埃及等 |
| **烏俄** | 烏克蘭、俄羅斯，**以及美國或他國與烏俄相關者**（軍援、和談、制裁、能源戰） |

- 以色列與伊朗直接衝突 → **涉伊朗，歸「美伊」**（不是中東）。
- 加薩／以巴、黎巴嫩真主黨（未直接涉伊朗時）→ **中東**。
- 歐美對烏俄的外交政治動作 → **烏俄**（烏俄在政治之前）。

**其他高風險重疊配對，一律照「越前面越優先」機械處理**：

| 素材 | ❌ 常見誤放 | ✅ 正確 | 理由 |
|---|---|---|---|
| 中國經濟／中國科技／中共人事／中國社會案件 | 財經／科技／政治／社會 | **大陸** | 大陸最前 |
| ~~美國政治／美股／美國槍案／美國野火~~ ⚠️ **美股那項已作廢** | 政治／財經／社會／天氣 | **美國**（美股除外） | 🔴 **2026-08-04 訂正：美股歸「財經」，不歸美國**——財經是題材格、優先級更高（見本節開頭）。同理美國體育→體育、好萊塢→娛樂。**例外**：涉川普政策（如關稅）→ 關稅／美國。其餘（美國政治／槍案／野火）維持歸美國 |
| 美中貿易關稅、關稅衝擊 | 財經／美國／大陸 | **關稅** | 關稅在三者之前 |
| 颱風／野火／洪水**造成傷亡** | 天氣 | **社會** | 社會在天氣之前；純氣象（預報、高溫紀錄、無傷亡現象）才留天氣 |
| 軟性趣味、迷因、奇聞 | 娛樂／社會 | **話題** | 話題是最後收容格，前面都不符才進 |

> ⚠️ **天氣那列最容易直覺出錯**：社會排在天氣**之前**，所以「有傷亡的天氣災害」依通則歸**社會**，不要因為「這是颱風／野火」就直覺丟天氣。⚠️ 但**地緣格優先於一切**：希臘野火傷亡→仍是社會；**美國**野火傷亡→歸「美國」（美國更前面）。

**整併時發現歸錯**：照 4b「分類修正」規則搬到正確大分類（`set-category` 更新、舊位置的行移除），不要兩邊各留一半。

### 與範例①② 的差異（只登記，不自行調和）

樣板比範例①② 精簡，且切法不同：

- **樣板有、範例沒有**：關稅
- **樣板沒有、範例有**：以黎、伊波拉（財經、娛樂已於 2026-07-30 補回，中東亦已改回，見上）
- **改過名的**：範例的「伊朗」→ 樣板「美伊」；範例的「美國政治」拆成「美國」與「政治」兩格

範例①② 是 7 月中的實際產出，樣板是使用者現在給的版本，**以樣板為準**。「以黎」被併進「中東」（美伊以外的中東總匯），是切法改變不是遺漏；「伊波拉」屬機動題，本來就該用最上面那格。

## 待補（實作前要解決）

1. ~~輪詢間隔~~ **已解（2026-07-30）**：暫定 5 個檢查點 16:00／18:00／20:00／22:00／23:00（23:00 最終整併），人工指令隨時可加開。**估算已完成**（見「Token 成本」，5 檢查點＋增量整併加權當量 ≈30.8 萬），仍是估算不是實測，實作後要回頭校準
2. ~~整併觸發時機~~ **已解（2026-07-30）**：人工指令與排程都支援，排程即上述 5 個檢查點
3. ~~批次摘要片段的存放方式~~ **已解（2026-07-30）**：不另開片段檔，直接寫進狀態 JSON 的 `raw_entry` 欄位，見「狀態 JSON 欄位設計」
4. ~~大分類清單定案~~ **已解（2026-07-30）**：樣板取得、補上財經／娛樂、以巴改中東；repo 與 Drive 兩份皆已更新。~~刪舊 Drive 檔~~ **同日晚使用者已處理**，Drive 正本檔名回到 `晚班交接大分類樣版.txt`
5. **格式前置**（2026-07-30 處理中）：
   - ✅ 三層骨架定案、中主題確認要實際寫出來、`wire-rundown-cases/README.md` 的「兩層」與「①②③ 結構完全不同」錯誤描述已訂正、分隔符確認為 `======{名稱}======`
   - ✅ 「第四種輸入模式」的錯誤框法已訂正（`common/11` 第 7 行末＋`TODO.md`）：A／B／C 的共同點是**使用者主動提供**，而定時監控產出的是 **AI 自己生成的整理清單**（庫存），性質不同；S2 產出 S3 庫存。（同日再訂正一次：`common/11` 也不是 S4 決策層，而是 **S4c 補充素材搜集**——人工發動、寫稿期間缺素材才啟動；真正的 S4 尚未建構。`common/11` 已把「先查既有庫存」訂為**最優先取材路徑**，見該文件「取材優先序」）
   - ✅ 中主題與小分題的標記方式已定：中主題用 `【名稱】`、小分題維持裸行，見上方「庫存檔格式」。**格式前置全數清空，不再阻塞 S2**
6. **狀態檔保留期**：位置已定（與庫存 txt 同資料夾），但舊素材多久移出未定（避免無限增長與重複摘要）；跨機器共用與否也還沒談
7. ~~S2b 介面的素材識別約定~~ **已解**：[`14-S2b`](14-S2b-側錄轉譯摘要.md) 訂為「來源＋6 碼」（`CNN 151328`／`NHK 222009`），與通訊社代碼不會撞號，兩者可安全混排在同一份庫存

## 各站已知狀況（2026-07-28 三輪實測，細節見 `TODO.md`）

| 站 | 清單抓取 | 稿件完整度判斷 | 已知雷 |
|---|---|---|---|
| **AP** | `get_page_text` 一次穩定拿到；走 `/home` 的 Latest 分頁（空白關鍵字查詢網址會回空白頁） | 上架即有完整 STORYLINE | 清單即時更新會被插隊；彈窗 Close 偶爾點一次沒關乾淨；**SNTV＝體育且編號 `AP5…`（非 `AP4`）**，可收必標 `(SNTV)`；扁平列表文字會把下一則 SNTV 徽章黏到上一則 ID 後——**勿誤標** |
| **CNN Newsource** | 清單可正常截圖讀取 | **有 Script 欄位 ≠ 有正文**，要看有沒有 `--LEAD IN--`／`--VO SCRIPT--`；列尾 `≡🔍` 圖示比「Has Script」篩選器好用（該篩選器點了沒反應）。⚠️ **這只是判斷 `script_status`，不是收錄門檻**——見下方「NS 收錄門檻」 | modal 關閉後畫面會跳回頂端輪播；**清單時間是 GMT+0，不是台北**（見下） |
| **RT** | **清單 `get_page_text` 只抓得到單一卡片，只能靠截圖讀** | 會出現 `SHOTLIST ONLY / TEMPLATE ONLY, COMPLETE SCRIPT TO FOLLOW` | SPA 卡舊快取；**詳情頁用 Previous／Next（‹ ›）連讀**（見下） |

**共同**：清單都是即時更新、新項目不一定加在最上面，**一律用素材編號集合做差集**，不能只看頂端有沒有變。

#### ⛔ NS 收錄門檻：沒稿也要收（2026-08-02 訂正，0802 實錯）

**`≡🔍`／`--LEAD IN--`／`--VO SCRIPT--` 只用來決定 `script_status` 要填 `has_script` 還是 `pending`，不是「要不要收這則」的門檻。**

0802 實錯：整晚 NS 只收到 4 則（14:00輪2／16:00輪1／17:00輪1／之後兩輪掛零），而且**全部 `has_script`、pending 掛零**；同期 RT 收 31 則、有 2 則 pending。Newsource 是穩定上稿的站，連續 4 小時 0 則不合理——研判是把「有沒有正文」當成收錄條件，沒稿的整批跳過沒進庫存。

正確做法（決策 4 本來就這樣寫，NS 沒有例外）：

1. **窗內出現的 NS 素材一律進庫存**，不論有沒有正文。
2. 沒正文的：依 Footage Type／標題／可見資訊寫一句摘要，標 `(稿未到)`、`script_status = pending`，正常歸類。
3. 稿到了再回頭覆寫 `raw_entry`、轉 `has_script`（最終整併的 pending 清查會掃）。

#### 每輪必做的 per-source 數量回報（2026-08-02 新增）

每個檢查點回報時**逐站列出本輪新收幾則**：`本輪新增：AP 4／RT 8／NS 0／YT 3`。

⚠️ **某站掛零時不可靜默略過**，必須加一句佐證：「NS 0 則——已換算 GMT 窗 `09:00–11:00 GMT` 並下捲到窗底，該區間確實無新素材」。三站同時只有一站長時間掛零，通常是**時區換算錯**或**收錄門檻用錯**（上面兩個坑），不是真的沒素材。

### 各站時間／時區（2026-07-31 訂正，必讀）

S2 時間窗一律以**台北時間（+08）**陳述與整併，但**各站介面顯示時區不同**——比對前必須先換算，**不可把畫面數字當本地時間**：

| 站 | 介面時間標記 | 換算成台北（+08） |
|---|---|---|
| **CNN Newsource** | **`GMT` / GMT+0**（例：`31 Jul 26 12:22 GMT`） | **+8 小時**（12:22 GMT → 台北 20:22） |
| **AP Newsroom** | 列表常顯示本地相對時間／站內時鐘（依帳號設定） | 以詳情 Arrival／Creation 為準，**不要假設＝GMT** |
| **Reuters Connect** | Details 常見 `Friday, July 31st, 2026, 19:15`（實務上與台北掃帶窗對得上；若見 `GMT`／`UTC` 字樣再另換） | 見標示；有時區字樣就換算 |

**CNN 換算口訣（不可再犯）**：

- 台北 **19:30–20:30** ⇔ Newsource **11:30–12:30 GMT**
- 台北 **15:13–19:30** ⇔ Newsource **07:13–11:30 GMT**
- 台北 **17:22–19:30** ⇔ Newsource **09:22–11:30 GMT**

❌ **禁止**：看到 Newsource `12:xx` 就當成台北中午、或以為「列表很新所以掃完了」卻其實只掃到 GMT 上午／只抓了最新可見 4–5 則。  
✅ **正確**：先把目標台北窗換成 GMT 窗 → 列表**漸進下捲**收集該 GMT 區間全部 ID → 再與狀態 JSON 差集。

> 2026-07-31 實錯：掃「至 20:30」時未把 Newsource 當 GMT 處理，導致窗內 CNN 幾乎漏掃；已補規則，之後每次 S2 必換算。

#### GMT 只給掃帶 AGENT 用，不進素材行（2026-07-31 使用者訂正）

| 層 | 要不要寫 GMT／上架時刻 |
|---|---|
| **掃帶篩窗／checkpoint／狀態判斷** | **要**（純內部 +8，agent 自己心算／記在狀態 JSON） |
| **素材行 `({備註})`／摘要／`畫面：`／`BITE：`** | **不要** |
| **晚班讀庫存、下游編號／下載／寫稿** | **不需要**靠素材行裡的 GMT |

- GMT 換算是 **S2 agent 進不進本輪時間窗**的內部邏輯，**不是**給編輯看的內容欄位。
- ⚠️ **檔頭也不寫**（2026-08-03 使用者訂正，取代本節原寫的「可寫在檔頭備註」）：舊版曾允許在檔頭附「台北時間；CNN Newsource 列表 GMT 已換算 +8」，現已廢除。檔頭時間窗一律台北時間、不加任何時區備註，與上方「檔頭」節第 4 點一致。
- AP／RT 素材行本來就不寫上架時刻；Newsource **同樣不寫**。
- ❌ `PO-20FR (…；GMT 12:02＝台北20:02) …`／`JL-15FR (…；GMT 11:23) …`
- ✅ `PO-20FR (大衛營 內閣會 資料畫面) …`／`JL-15FR (ICE槍擊 目擊 CLEAN 直式 SOT) …`
- 時間窗以**檔頭「時間窗：」／狀態 JSON** 為準，不必每則重複。

### RT 詳情頁 Previous／Next（‹ ›）連讀（2026-07-31 實測補強；同日晚再升格為鐵律）

S2 或任何需**連掃多則 RT 素材**時，**固定只走這條路徑**（2026-07-31 晚實作連卡兩次後定稿——**不要再發明清單捲動／Load more 路線**）：

#### 標準 5 步（口訣：**列表最新 → 點進去 → Next 往早**）

1. **開 Video 全列表**：My Subscription **ON**、`https://www.reutersconnect.com/all?media-types=vid`（Newest First）。  
   - 等列表渲染完；需要刷新 a11y 時才 screenshot／讀 text。  
   - **讀清單頂端 1 屏即可**（確認最新 Edit No.／時間）——**不要**為了「多抓幾則」去狂按 **LOAD MORE**（見下方禁則）。
2. **點清單最上方（時間最新）那則卡片**進詳情（保留結果集）。進詳情後 URL 宜呈  
   `all?id=tag:reuters.com,…&media-types=vid`（或同等「仍掛在列表結果集」的形態）。
3. **在詳情頁用 Previous／Next（‹ ›）連讀**：aria-label `Previous`／`Next`。  
   - **Next（›）＝往時間更早**；**Previous（‹）＝往更新**。  
   - **反覆 Next** 收集到時間窗下沿（低於截止就停）。  
   - **不要**每則回清單重點下一則。
4. **每則**讀 Edit No.→`RT{4碼}`、Duration、Restrictions／SOURCE（CCTV？）、script 狀態（COMPLETE SCRIPT／TO FOLLOW）、shotlist／BITE；與狀態 JSON **差集**——新則寫 `raw_entry`，已見過只更新 `last_checked_checkpoint`（稿到可覆寫 pending）。
5. **停條件**：Details 時間**低於本輪截止**（或已連續撞上大量已在庫 ID 且時間出窗）→ 停。不要為了湊數再 LOAD MORE。

#### 禁則（卡過兩次的坑——違者重來）

| ❌ 禁止 | 為什麼 | ✅ 改做 |
|---|---|---|
| 在列表狂按 **LOAD MORE**／深捲「多撈舊稿」當主掃法 | 結果集被拉成**隔日舊帶**，頂端最新窗消失，整輪白做 | 頂端 1 屏確認最新 → **立刻點最新進詳情** → Next 往早 |
| `navigate` 到純 **`/detail?id=…`** 或 `search=all:####` 再想 ‹ › | **沒有前頁結果集 → 沒有 Next／Previous** | 一定從 `all?media-types=vid` **點卡片**進 |
| 把 ‹ › 當瀏覽器分頁切換、或清單頁翻頁 | 鍵只在**素材詳情**內有效 | 確認頁上有 `Back to results`／詳情欄再按 Next |
| 只看列表淺色就當「掃完時間窗」 | 淺色＝本 session 看過，**≠** 時間窗已覆蓋 | 以 **Details 時間戳**＋ JSON 差集為準 |
| 最新則用搜尋 Edit No. 單獨開、再手動拼下一則 | 斷結果集、又慢又易漏 | 永遠 **列表最新點進 + Next 鏈** |

#### 檢查清單（每次 RT 連掃開工前默念）

- [ ] My Subscription ON、Video、Newest First  
- [ ] **沒有**先 LOAD MORE  
- [ ] 點的是**清單最上方最新則**（不是隨機舊卡）  
- [ ] 詳情有 **Next**，URL 仍帶結果集  
- [ ] 只往 **Next／更早** 收到截止  
- [ ] 差集靠 **Edit No.**，不靠「列表好像沒新的」

列表**淺色＝已看過**（可當提示，仍以 JSON 為準）。  
單筆關鍵字／Edit No. 搜尋仍走 [`reuters/01-search-workflow.md`](../reuters/01-search-workflow.md)；**S2 時間窗連掃一律本節 5 步**。
