# 歐印萬掃帶（監控／指定翻譯大段／雙語TC稿）

> **狀態：已在用（2026-07 起實測多次）。**
> 定位見 [`12-子系統地圖.md`](12-子系統地圖.md)：與 [`S2b 側錄轉譯摘要`](14-S2b-側錄轉譯摘要.md) 同樣以「掃帶歐印萬」資料夾為素材來源，但**產物粒度不同**——S2b 產出濃縮摘要、直接寫進共用 S3 庫存（`{MMDD}晚班交接.txt`）；本規則產出**完整逐字/翻譯全文**，存成獨立 txt、放在來源音檔旁邊。兩者目前各自獨立運作，本規則的產物可作為 S2b 未來摘要時的上游全文來源，但尚未串接。

## 這個子功能在做什麼

針對 [`01-shared-folders.md`](01-shared-folders.md) 所述「掃帶歐印萬」資料夾裡的側錄音檔（CNN／NHK 等側錄訊號，檔名如 `{記錄人}{來源} {6碼TC}.wav`），提供兩種觸發方式、兩種輸出格式，把整支音檔轉成帶正確母帶 TC 的繁體中文文字稿，存在原始音檔旁邊。

## 觸發方式

1. **指定檔案**：使用者報出檔名（或檔名關鍵字，例如「許岱軒CNN 042918」），並指定格式（「大段」「雙語」或兩者都要）——直接處理該檔。
2. **監控模式**：使用者要求「監控歐印萬」或詢問「資料夾有新的需要翻譯嗎」：
   - **一次性詢問**：列出資料夾差集（比對已有輸出 vs 沒有），回報並視需要處理。
   - **排程監控**：用 `CronCreate` 排一個週期性 job（例如每 15 分鐘，避開整點/半點，見 CronCreate 工具說明的 jitter 建議），prompt 內容重複「列資料夾差集 → 有新檔案就用預設格式處理 → 無新檔案只回一行」的邏輯。
   - ⚠️ **CronCreate 排程是 session-only**：7 天自動失效，session 結束也會消失，**不會跨 session 保留**。要在新 session 恢復監控，需重新呼叫一次 CronCreate（可用下方「重啟監控用 Prompt」）。

## 「已處理」的判斷依據

來源音檔（`.wav`/`.mxf`/`.mp4`）旁邊只要存在**任一種**輸出檔就視為已處理，跳過：
- `{檔名}_TC中文大段翻譯.txt`
- `{檔名}_雙語逐字稿.txt`

⚠️ **處理前務必先檢查是否已有公司自動化系統產生的既有逐字稿**（[`01-shared-folders.md`](01-shared-folders.md) 提到的 `<檔名>.txt` / `<檔名>(雙語對照版).txt`）——那是不同的檔名慣例，若存在且精確度足夠，優先直接引用，不要重新對整支素材跑 `video_analyze`。

（`{檔名}_原始逐字稿.txt`——見下方「原始逐字稿備份」——不算在判斷依據內，單獨存在不代表已處理。）

## 原始逐字稿備份（2026-08-01 起）

**每次處理新檔案時，除了主要輸出格式外，一律額外存一份 `{檔名}_原始逐字稿.txt`**，內容是逐句原文（未翻譯、未濃縮），保留原始時間戳（相對於音檔起點，**不加 TC 偏移**）。

目的：避免日後想補做另一種格式（例如已有大段翻譯，之後想另外做雙語逐字稿）時，需要重新對整支音檔轉錄一次。只要原始逐字稿還在，之後就能直接讀檔改寫，不必再轉錄。

### 本機 ASR 產生原始逐字稿（2026-09-01 起，取代 video_analyze）

**這份逐字稿一律優先用 `scripts/oyinwan_asr_draft.py` 在本機產生，不再對整支音檔跑 `video_analyze`。**

```
python scripts/oyinwan_asr_draft.py "<來源media路徑>" -o "<輸出目錄，預設同來源資料夾>"
```

- 底層是本機 whisper.cpp（large-v3-turbo + Silero VAD，與 `scripts/bite_workbench.py` 共用同一顆模型，GPU 加速），輸出格式已直接對齊上方規格，可原封不動存成 `{檔名}_原始逐字稿.txt`，不需要再手動排版。
- **省下的是「聽音檔」這段 token**：Agent 之後只需讀這份文字草稿做 TC 大段翻譯（敘事整併、講者角色判斷、專名訂正、150字摘要），不必再讓 LLM 直接處理音訊。
- ⚠️ **本機 ASR 產出的英文原文未經校對**，人名/專有名詞偶有誤聽（例如曾把 `Manasarovar` 聽成 `Mansur over`），一樣照既有規則在 TC 大段翻譯階段查證訂正、於備註標註「※ASR原聽為…查證後訂正」。
- ⚠️ **不要**用本機 NLLB 翻譯模型直接產生 TC 大段翻譯的中文正文——實測逐句直譯常斷裂、遇到片語會出現 `<unk>` 失敗（見 2026-09-01 測試案例），品質不足以直接交付，中文正文仍由 Agent 讀英文草稿撰寫。
- 若 `whisper-cli`／模型檔不存在或 ffmpeg 轉檔失敗，腳本會拋錯，此時退回原本的 `video_analyze` 流程。

```
原始逐字稿(未翻譯/未濃縮)— {檔名}
來源檔案:{檔名}.wav(時長 {mm:ss})
TC 起始偏移:{HH:MM:SS}(依檔名前6碼 {6碼} 換算)
備註:逐句ASR原文,保留原始時間戳(未加TC偏移),供日後製作雙語逐字稿等用途參考。

[mm:ss–mm:ss] {原文}
...（逐句）
```

## 監控範圍的重要限制：只認「監控啟動後新增」的檔案

**排程監控啟動當下，先列一次資料夾現況作為基準線。之後每輪只處理「基準線之後才出現、且尚未有輸出檔」的來源檔案。**

資料夾裡經常存在**監控啟動前就已經沒有輸出檔的舊素材**（例如橫跨數週的舊新聞：世足、伊朗、關稅等舊題材）。這些不是「剛掉進來的新檔案」，**預設一律不自動處理**，除非使用者明確指名要補做。原因：
- 這些舊檔通常代表已經過時、或已透過其他管道處理掉的素材，不代表遺漏。
- 貿然對十幾支舊檔跑完整 `video_analyze` 屬於高 token 成本、且不可逆（一次消耗大量時間與資源）的動作，屬於**要先問使用者**的範疇，不可自行判斷「反正沒輸出檔就是新的」就整批處理。

（2026-07-31 實測案例：啟動監控時發現 18 個橫跨 6/5～7/28 的舊檔沒有輸出檔，經 `AskUserQuestion` 確認後，使用者選擇「只處理往後新增的檔案」，18 個舊檔維持排除狀態。）

## TC 偏移換算

依 [`02-tc-offset-filename.md`](02-tc-offset-filename.md)：檔名前 6 碼 = `HH:MM:SS`，是這支片段在母帶時間軸上的起始時間碼，內部由 0 起算的所有時間戳都要加上這個 offset 才是輸出用的時間碼。

### ⛔ 這條**只適用「自己跑 `video_analyze` 轉錄」的流程**（2026-08-09 使用者定案）

公司自動化系統產出的 `{基名}.wav.txt`（見上方「已處理」判斷依據那條 ⚠️）裡的
TC **已經是母帶絕對時間，偏移在上游就加過了**。下游（S2b 入庫、掐 BITE）
**直接沿用，不准再加一次**。

實測佐證（2026-08-09，三支互相獨立）：

| 檔名前 6 碼 | 檔內第一個 TC |
|---|---|
| `150102` 伊朗 | `15:01:02` |
| `150648` 西班牙移民危機 | `15:06:48` |
| `151018` 烏俄 古巴SOT | `15:10:18` |

再加一次會把 `15:14:39` 變成 `30:29:18`——**超過 24 小時、一眼就看得出是錯的**，
但如果只錯幾分鐘（例如來源檔改成從 0 起算）就完全看不出來。所以**判準不是
「看起來合不合理」，而是看檔案來源**：公司系統產的 `.wav.txt` 一律當絕對時間。

> 現況補記（2026-08-09 清點）：資料夾裡 `_TC中文大段翻譯.txt` 的**檔數是 0**，
> 實際存在的全是公司系統的 `{基名}.wav.txt` 與 `{基名}.wav(雙語對照版).txt`。
> 也就是說**本檔描述的自行轉錄流程目前沒有在跑**，偏移換算那條實務上用不到。

## 兩種輸出格式

### 1. 雙語逐字稿（`{檔名}_雙語逐字稿.txt`）

逐句／逐段中英對照，**中文翻譯在上、原文在下**（依 [`04-bilingual-subtitle-qa.md`](04-bilingual-subtitle-qa.md) 與台灣慣例：中文先）。段落長度依語音停頓自然切分，不強行切成固定長度。

> **格式來源（2026-08-01 使用者定案）**：**檔頭欄位沿用本規則既有寫法**；正文採 Archive 既有雙語文本的做法並**統一飄動處**。Archive 的 12 份 `(雙語對照版).txt` 在講者位置、段落分隔、TC 寫法、括號全半形上並不一致，以下為定版，**不再照抄個別舊檔的變體**。

```
CNN 逐字稿(雙語)— {記錄人}{來源} {6碼}(若接續前一檔案:「接續 {前一檔名}」)
來源檔案:{檔名}.wav(時長 {mm:ss})
TC 起始偏移:{HH:MM:SS}(依檔名前6碼 {6碼} 換算;若接續前檔會註明銜接關係)

### 雙語對照 ###

HH:MM:SS-HH:MM:SS
（{角色} {姓名}） {中文翻譯}
（{原文姓名}） {原文（英文/日文等）}

HH:MM:SS-HH:MM:SS
（{角色} {姓名}） {中文翻譯}
（{原文姓名}） {原文}

...（逐段）

──────────
備註:
1. 主題摘要...
2. 內文人名/專有名詞不確定時的音譯備註...（⚠️ 只限內文，講者標記一律英文原名、不寫暫譯）
```

**各部分規則**

| 部分 | 規則 |
|---|---|
| **檔頭三行** | 沿用本規則既有欄位。接續前檔時標題註明「接續 {前一檔名}」 |
| **`### 雙語對照 ###`** | 檔頭之後、正文之前，獨立一行。（Archive 12 份全部都有，保留） |
| **段落結構** | **三行一組**：TC 行 → 中文行 → 原文行。**TC 獨立一行**（不與講者同行、**不加括號**、不加粗體）；講者標記寫在**中文行與原文行各自的開頭** |
| **講者標記** | 中文行用 `（{角色} {姓名}）`，角色同大段版（新聞內容限 `主播`／`記者`／`受訪者`／`專家`；`旁白`／`廣告配音`／`群眾` 僅限廣告墊檔等非新聞段落，見大段版說明）；原文行用 `（{原文姓名}）`，**一律全形括號**（Archive 有半形 `(Narrator)` 變體，不採用）。無姓名者原文行寫角色英文（`（Crowd）`／`（Narrator）`／`（Ad voice）`）。<br>⚠️ **`{姓名}` 一樣照大段版「SUPER 姓名語言」寫英文原名**（2026-08-02 補）——中文行是「角色中文＋姓名英文」（`（記者 Helena Lins）`），不是把名字音譯成中文。 |
| **段落分隔** | 組與組之間**只空一行**。**不要** `---` 水平線（Archive 部分舊檔有，不採用——長帶掃起來太吵） |
| **專名夾原文** | 同大段版：中文行的人名／機構／地名首次出現夾全形括號原文，單位依 [`00-寫稿通則.md`](00-寫稿通則.md) 換算。原文行維持原話，不加註 |
| **廣告／墊檔照收** | 同大段版，保留並標角色 |
| **不要的東西** | ❌ 開頭贅述前言（「以下是融合『中文文稿』與『原文文稿』的…」）；❌ `**粗體TC**`、`**文稿**` 等 markdown 殘留；❌ 只有起點沒有終點的 TC（`(04:56:58)`） |
| **與大段版的差異** | 雙語版**沒有**開頭重點摘要、**沒有**檔尾時間軸（那兩項是大段版專有）；改為檔尾 `──────────` 備註區，列主題摘要與**內文**音譯不確定處（講者標記不列，一律英文原名） |

### 2. TC中文大段翻譯（`{檔名}_TC中文大段翻譯.txt`）

**純中文、大段整理翻譯（非逐句），不附原文**。把多個 ASR 段落合併成較大的敘事段落，濃縮但保留官方公布的具體數字/地名/機構名/引述時間等事實細節（例如死傷人數、震度、機構名稱）。⚠️ **合併僅限同一講者內部**：講者角色一旦有轉折（主播→記者→專家→受訪者等）就必須切成新段落，不可為了敘事連貫把不同講者的話合併進同一段（2026-08-02 訂正，見下方表格）。**這是監控模式下沒有特別指定格式時的預設格式**（雙語逐字稿需使用者另外明確要求）。

> **格式來源（2026-08-01 使用者定案）**：**檔頭欄位沿用本規則既有的五行**（保留 TC 偏移與接續關係，供追溯換算）；**其餘一律改採 Archive 既有大段文本的做法**——開頭重點摘要、`#####` 分隔、TC 與內文同行並標講者、專名首次出現夾原文、檔尾時間軸索引、廣告／墊檔照收。參考樣本見「掃帶歐印萬」資料夾的 `Archive\` 內公司系統產出的 `<檔名>.txt`（非雙語對照版）。

```
{來源} 逐字稿(TC中文大段翻譯)— {檔名}(若接續前一檔案:「接續 {前一檔名}」)
來源檔案:{檔名}.wav(時長 {mm:ss})
TC 起始偏移:{HH:MM:SS}(依檔名前6碼 {6碼} 換算;若接續前檔會註明銜接關係)
內容:{內容摘要一行}
備註:大段整理翻譯(非逐句),僅列中文,不附原文。

{一段話,150字以內,扼要點出這支帶子在講什麼}

#####

HH:MM:SS-HH:MM:SS
（{角色} {姓名}） {中文敘事段落}

HH:MM:SS-HH:MM:SS
（{角色} {姓名}） {中文敘事段落}

...（逐段）

時間軸
- HH:MM:SS-HH:MM:SS （{角色} {姓名}）
- HH:MM:SS-HH:MM:SS （{角色} {姓名}）
...
```

**各部分規則**

| 部分 | 規則 |
|---|---|
| **檔頭五行** | 沿用本規則既有欄位，不變。接續前檔時標題註明「接續 {前一檔名}」（見下方「同一新聞事件的多檔接續」） |
| **重點摘要（2026-08-31 使用者訂正：簡化）** | 檔頭之後、`#####` 之前，**一段文字、總長150字以內**，不再用 `- ` 條列多點。原因：下方 TC 逐段正文本身就是完整的大段翻譯內容，這裡不需要再重複一次精細摘要，只需最扼要點出這支帶子在講什麼，供快速判斷是否要往下細看。<br>❌ 舊版「短帶4–5條、長帶3條」的條列式重點摘要**已停用**，不要再照抄舊檔案的寫法。 |
| **`#####` 分隔** | 重點摘要與正文之間，**獨立一行五個半形井號**。（Archive 短帶用 `### 中文 ###`／`### 原文 ###`，那是雙語檔的分法；大段檔只有中文，一律用 `#####`） |
| **段落行** | **兩行一組**（2026-08-01 使用者訂正，與雙語版一致）：**TC 獨立一行** → 下一行 `（{角色} {姓名}） {內文}`。TC 用半形連字號相接，**不加括號**、不加粗體。組與組之間空一行 |
| **講者轉折強制分段（2026-08-02 使用者訂正）** | 合併 ASR 段落成大段敘事時，**只要講者身分／角色有轉折就必須另起新段落**（新 TC），即使話題連貫不斷也不能跨講者合併成一段。例如同一則新聞裡「主播 → 記者 → 專家 → 受訪者（SOT）」這種轉場，每次角色切換都是一個分段點。判斷依據＝**講者變了就切**，不是「主題變了才切」。<br>⚠️ **切段是在產生大段翻譯的當下就做完，不是事後補救**：合併 ASR 段落時手上就有逐句時間戳，切點的起始 TC 直接取**該講者第一句話的 ASR 時間戳**＋檔名 offset 換算，前一段的結束 TC 同步收在切點。**這個階段不存在「沒有 TC 可用」的情況**（時間戳同時也會留在 `{檔名}_原始逐字稿.txt` 備份裡）。<br>❌ **不准用「第二個全形括號嵌在內文中間」代替分段**——`（記者 X） …內容…（受訪者 Y） …內容…` 這種寫法是錯的（實例：0802 的 171030／180008／181056）。下游（S2b／掐 BITE）**不負責回頭拆**，看到沒切好只會照搬並回報。<br>❌ **更嚴重的變體：受訪者整段被吞掉、連 SUPER 都沒有**（0802 `140849` 實錯）——專訪段落把受訪者的話改寫成間接引述（「某某表示……」）塞進 `（記者）` 或 `（主播）` 的段落裡，受訪者**從頭到尾沒有自己的 `（受訪者 X）` 標記與 TC**。訪談一問一答**必須切成獨立段落**：提問方一段（`（主播）`／`（記者）`）、回答方一段（`（受訪者 X）`／`（專家 X）`），受訪者的話**維持第一人稱直述**，不要改寫成第三人稱轉述——下游掐 BITE 要的是原話。 |
| **講者標記** | **每段必標**，全形括號寫在**內文行開頭**（不與 TC 同行）。無姓名或群體聲音只寫角色。<br>⚠️ **角色分兩類（2026-08-02 使用者訂正）**：<br>・**新聞內容只能是** `主播`／`記者`／`受訪者`／`專家`——**新聞段落標成 `旁白` 一律是轉錄誤判，必須改正**（實例：CNN 側錄主播口播被誤標旁白）。<br>・`旁白`／`廣告配音`／`群眾` **僅限**整節帶裡的廣告、節目預告、片頭片尾、現場群體聲等**非新聞內容**。<br>⚠️ **記者 vs 受訪者易混淆，是常見誤判（2026-08-02 訂正，實例：CNN 記者被誤標成受訪者）**：判斷依據看**身分**而非語氣——CNN／NHK 自家編制內的記者做連線／分析／追蹤報導，一律標 `記者`；被記者或主播訪問的**外部人士**（官員、學者、目擊者、當事人等第三方）才標 `受訪者`。同一人若在片段中身分不變，全程標記要一致，不要中途切換。 |
| **SUPER 姓名語言（2026-08-02 使用者訂正）** | 姓名**預設寫英文原名**，例如 `（記者 Amanda Davis）`；**只有知名人物**（有通用中文譯名、台灣媒體慣用中文稱呼者，例如政要、明星、常上新聞的官員）才寫中文，且比照台灣慣用譯名（見 [`00-寫稿通則.md`](00-寫稿通則.md) 川普／普欽等既有慣例）。不確定算不算「知名」時，優先用英文原名，不要自己音譯。此規則**取代**先前「有姓名就寫『記者 阿曼達·戴維斯』」的中文音譯範例。<br>❌ **實錯反例（0802 `140849` 檔）**：ASR 給的是 `Helena Lins`，卻寫成 `（記者 海倫娜·林斯）`，並在檔頭備註加註「記者『Helena Lins』暫譯『海倫娜·林斯』」。**知道英文原名就直接用**——`（記者 Helena Lins）`，備註**不必**寫暫譯（音譯備註只適用內文專名，見下「專名夾原文」）。 |
| **專名夾原文** | 人名、機構、地名、專有名詞**第一次出現**時用全形括號夾原文：`費蘭·托雷斯（Ferran Torres）`、`美國中央司令部（U.S. Central Command，CENTCOM）`、`霍爾木茲海峽（Strait of Hormuz）`。同一檔內重複出現不必再夾。單位依 [`00-寫稿通則.md`](00-寫稿通則.md) 換算並夾註：`50至80英里（約80至129公里）`。<br>⚠️ **這條只管「內文」，不管「講者標記」**（2026-08-02 補：實錯根源）——講者標記走上一條「SUPER 姓名語言」，**直接寫英文原名、不音譯、不夾註**。內文提到同一個人時才照本條寫成 `中文（原文）`。 |
| **廣告／墊檔照收** | 整節帶裡的廣告配音、節目預告、跑馬旁白、片頭片尾**保留不省略**，標明角色（`（廣告配音）`／`（旁白）`）。這些是判斷節目段落邊界的依據，刪掉反而難對位 |
| **檔尾時間軸** | 正文之後空一行寫 `時間軸`，其下用 `- HH:MM:SS-HH:MM:SS （{角色} {姓名}）` 逐段列出，**與正文段落一一對應**、順序相同。供快速定位、掐 BITE 時對 TC 用 |

## 同一新聞事件的多檔接續

若新檔案的人名/來源前綴與資料夾內**時間上緊接的前一個檔案**相同，且內容明顯是同一則新聞事件的延續片段（例如熊本地震從第一則緊急快報一路到後續傷亡更新），則：
- 標題註明「接續 `{前一檔名}`」。
- **人名/專有名詞跨檔案維持一致**——若使用者曾針對某個名字給過修正（例如熊本地震專家「酒井慎一」曾被 ASR 誤聽為「坂井真一」，經使用者訂正後，後續所有檔案一律用訂正後的名字）。
- 重複播報的內容（NHK 常見同一則公告播報 2–3 次）只翻譯一次，並註記「此段訊息重複播報」，不逐字重譯。

## 新人名/新來源出現時

若監控中發現的新檔案，其記錄人姓名是資料夾裡**從未出現過**的新名字，處理前先用 `AskUserQuestion` 確認要用哪種格式（大段／雙語／兩者）。**若使用者未回應或直接略過（dismissed）該問題，不處理這批檔案，回報略過並等待下一步指示——不可自行套用預設格式繼續處理。**

## 重啟監控用 Prompt（開新 session 時貼上）

CronCreate 排程不會跨 session 保留，開新 session 要重新監控時，貼下面這段（依需求調整資料夾路徑與週期）：

> ⛔ **每輪開工前先 `git pull` 並重讀本檔**（2026-08-02 訂正，實錯根源）：這段 prompt 把格式規則**複寫**在裡面，排程一旦建立就凍結在建立當下的版本，**規則之後改了它不會知道**。0802 實例：SUPER 英文原名規則 19:53 進 repo，21:17 產出的檔案仍照舊音譯成「海倫娜·林斯」，因為排程是 19:53 之前建的。<br>**下面的規則複寫只是摘要，權威永遠是 `common/15-歐印萬掃帶.md` 本文**；兩者不一致時以本文為準。

```
監控資料夾：G:\我的雲端硬碟\Autopilot\(掃帶歐印萬) 檔名取TC起頭 6位數\

步驟：
0. 每輪先 cd E:\GitHub\TVBS-AIHunter && git pull，重讀 common/15-歐印萬掃帶.md。
   本 prompt 內的格式描述只是摘要，與該檔不一致時一律以該檔為準。
1. 用 PowerShell 列出該資料夾所有 .wav（也留意 .mxf/.mp4）檔案，作為基準線。
2. 對每個來源音檔，檢查是否已存在對應的 "{檔名}_TC中文大段翻譯.txt" 或 "{檔名}_雙語逐字稿.txt"（只要任一種存在就算已處理過，跳過）。
3. 找出「基準線之後才新增、且尚未有任何輸出檔」的來源音檔，視為新檔案。基準線當下就存在但一直沒有輸出檔的舊素材，預設不處理。
4. 若沒有新檔案：只需簡短回覆一行「本次監控無新檔案」，不要做其他事。
5. 若有新檔案：用預設格式「TC中文大段翻譯」（純中文、大段整理翻譯、非逐句、不附原文；格式規則見 common/15-歐印萬掃帶.md）處理：
   a. 用 `python scripts/oyinwan_asr_draft.py "<來源路徑>"` 在本機跑 whisper.cpp 取得逐字稿（存成 `{檔名}_原始逐字稿.txt`），失敗才退回 video_analyze。
   b. 依檔名前6碼計算 TC 偏移，將每段時間戳加上此偏移。
   c. 若與時間上緊接的前一個檔案屬同一新聞事件延續，標題註明「接續 {前一檔名}」，人名/專有名詞跨檔案保持一致。
   d. 依格式寫檔：檔頭五行（來源檔案／TC偏移／內容／備註）→ 開頭重點摘要（一段話,150字以內,不條列,只扼要點出帶子在講什麼——下方TC正文才是完整內容）→ 獨立一行 "#####" → 正文每段兩行一組：第一行只寫 TC「HH:MM:SS-HH:MM:SS」（不加括號），第二行寫「（角色 姓名） 內文」（新聞內容角色限主播/記者/受訪者/專家，記者=CNN自家連線記者、受訪者=被訪問的外部人士，兩者易混不要弄反；旁白/廣告配音/群眾僅限廣告墊檔等非新聞段落，每段必標；**姓名一律寫英文原名如「記者 Helena Lins」，不准音譯成中文、不准寫「暫譯」**，只有政要明星等知名人物才用台灣慣用中文譯名），**講者角色一有轉折就另起新段落，不可跨講者合併；專訪一問一答必須提問方一段、回答方一段，受訪者要有自己的（受訪者 X）標記與 TC，話維持第一人稱直述不改寫成轉述**，組間空一行 → 專名首次出現夾原文、單位換算 → 檔尾寫 "時間軸" 並用 "- TC （角色 姓名）" 逐段對應列出。廣告配音與墊檔旁白保留不省略。
   e. 寫成 "{檔名}_TC中文大段翻譯.txt"，存在同一資料夾。
   f. 簡短回覆已處理哪些檔案、TC 範圍、內容摘要重點。

注意：預設格式一律用「TC中文大段翻譯」（不要雙語），除非使用者另外明確要求雙語。若新檔案來自從未出現過的全新人名，先用 AskUserQuestion 確認格式，使用者不回應/略過就不處理該批次並回報略過。用 CronCreate 排成每 15 分鐘一次的排程（避開整點/半點分鐘）。
```

## 與 S2b 的關係（避免混淆）

| | 本規則（歐印萬掃帶） | [`S2b`](14-S2b-側錄轉譯摘要.md) |
|---|---|---|
| 產物 | 完整逐字稿/翻譯全文 | 濃縮摘要（每段2–6句） |
| 存放位置 | 「掃帶歐印萬」資料夾，緊鄰來源音檔 | 共用 `Claude共用/自動掃帶系統/{MMDD}晚班交接.txt`，與通訊社素材混排 |
| 觸發方式 | 使用者指定檔案，或排程監控（session-only） | 目前是臨時方案：agent 整理庫存時順手去資料夾撈 |
| 用途 | 供記者/使用者直接查全文、掐 BITE 取逐字引言 | 供晚班快速掃過今日全部素材、決定要不要開稿 |

兩者目前各自獨立運作，未來若要串接（例如 S2b 直接讀取本規則產出的全文檔做摘要，而不是重新讀原始逐字稿），需另外設計，尚未定案。
